# Mission Control — Session Handoff (for a fresh agent)

_Last updated: 2026-07-07 (post MCA-83 **W5** — ask-mode, PR TBD; per-task `work_mode` execute|ask, single-turn answer posted to the thread — **closes epic MCA-83 Work surface**. Prior: W4 task watchdogs PR #168; MCA-85 **D2** llms.txt + `7ei-mc onboard` — epic MCA-85 DX complete). Paste the "Kickoff prompt" below into a new session; the rest of this doc is the detail it should verify._

## Kickoff prompt

You're taking over the **7Ei Mission Control App** — an AI-agent "virtual office" control plane (our flagship). Read `HANDOFF.md`, `STATUS.md`, `CLAUDE.md` (layered — subsystem guides load on demand), `GO-LIVE.md`, and `docs/DESIGN_SYSTEM.md` (v2 — binding design rules incl. the red-green-colorblind constraint). Run the verification commands in HANDOFF.md and flag divergence. Current work: four epics are filed and sequenced — MCA-82 Theme (T1 ✅, T2 glass chrome ✅, T3 hex sweep + contrast audit ✅ — **epic complete**), MCA-83 Work surface/failure UX (W1 recovery cards ✅, W2 blocker chips + next-up + sub-task cost rollups ✅, W3 thread w/ wake-on-comment ✅, W4 task watchdogs ✅, W5 ask-mode ✅ — **epic complete**), MCA-84 Visibility (V1 heartbeat 24h timeline ✅, V2 tri-state approvals + inbox retry rows + read receipts ✅, V3 per-wake preflight cap + cheap-model config validation ✅ — **epic V complete**), MCA-85 DX (D1 openapi+CLI ✅; **auth-scoping hardening ✅ PR #165** — D1's openapi.json revealed jira/gmail/comms/notifications/memory/webhooks/usage/skills were registered PUBLIC; moved behind Clerk, split the two webhook-receiver files, added `auth-scoping.test.ts` lock-down; **D2 llms.txt + `7ei-mc onboard` ✅ — epic MCA-85 DX complete**; **per-org webhook signing ✅ PR #166**), **MCA-83 W4 task watchdogs ✅ (PR #168); W5 ask-mode ✅ (PR TBD) — epic MCA-83 Work surface COMPLETE**. Plans: repo `docs/DESIGN_SYSTEM.md` + vault `01-Projects/Paperclip-Gap-Analysis-v2-2026-07-06.md`. Ship per convention (one PR per story, tests+evals+build green, squash --admin, STATUS.md bump, Jira transition). All four gap-bridge epics (MCA-82 Theme, MCA-83 Work surface, MCA-84 Visibility, MCA-85 DX) are now complete — next work is open: pick from R4 vault RAG (Turso native vectors, backend-only), R6 Sync-Registry automation, MCA-81 follow-ups, or the `app/` (Expo) archive decision, unless I direct otherwise. Ask before anything touching the live OpenClaw adapter, credentials, or vendor consoles.

## Repo + stack

- **Path:** `/Users/artutito/Developer/7Ei-Mission_Control_App` (GitHub `Arturito7ei/7Ei-Mission_Control_App`).
- **backend/** — Fastify + Drizzle + Turso/libSQL, deployed on Fly app `7ei-backend` (region fra).
- **web/** — Next.js 15 on Vercel (`app.7ei.ai`), Clerk auth on a **dev** instance.
- **adapters/** — external BYO-agent runtime (`openclaw/`, `mac-mini/`, `presets/`).
- **cli/** — `7ei-mc` zero-dep Node CLI over the agent API. **evals/** — orchestration eval harness.

## State (verify each)

- Paperclip-parity gap-bridge: **5/5 phases shipped** — epics MCA-47 (execution core: atomic checkout, run telemetry, deps, overspend), MCA-52 (adapter http-executor + presets + CLI), MCA-56 (attachments/work-products, ticket timeline, workspace preview URLs), MCA-60 (execution policies + config rollback + per-agent permissions + HMAC run-tokens + plugin jobs), MCA-65 (orchestration evals + PWA + self-host Docker).
- UI epic **MCA-69 complete** (MCA-70/71/72/73): design tokens (`web/app/dashboard/tokens.ts`), task drawer, governance panel, a11y/responsive.
- Go-live hardening (**PR #147**): OpenClaw adapter (`adapters/openclaw/mc_adapter.py`) reads `MC_LLM_API_KEY` from the encrypted secret store at boot (no plaintext key on disk); `adapters/mac-mini/setup.sh` one-command installer; `GO-LIVE.md` runbook.
- Hardened adapter deployed to the laptop's live `~/.openclaw/mc-adapter/` on 2026-07-02 (backup: `mc_adapter.py.bak-*`). **Still pending:** key rotation (then strip `MC_LLM_API_KEY` from `mc.env`) and the move to the Mac mini — see GO-LIVE.md §3–4.
- Shared-memory sprint (2026-07-02 evening, proposal: vault `02-Architecture/Shared-Memory-Upgrade-2026-07-02.md`): **MCA-74** all.ts split into domain route modules + `services/push.ts` (PR #148); **MCA-75** memory bus — session-summary endpoint, org+agent long-term vault memory injected into `buildSystemPrompt()`, nightly KV export (PR #149); **MCA-76** weekly consolidation routine, Sun ≥04:00 UTC (PR #150). Backend now 448 tests. Vault has per-agent namespaces `Memory/agents/<slug>/`. **All memory-bus features dormant until `GITHUB_VAULT_TOKEN` is set** (GO-LIVE §3b).
- CLAUDE.md is layered: slim root + `backend/`, `web/`, `adapters/` files (keep root <70 lines; state belongs in STATUS.md, not CLAUDE.md).
- **7Ei_OS PR #3 open** (protocols: MC as primary coordination + memory bus) — human review required; after merge, re-sync the vault `Protocols/7Ei_OS/` mirror.
- UI overhaul epic MCA-78 COMPLETE (2026-07-03/06): MCA-77 connectors replace-token/pills, MCA-79 foundation (density/text/space tokens, `dashboard/ui.tsx` primitives, `web/lib/api.ts` client), MCA-80 Cockpit split (129-line root + 13 `cockpit/` components), MCA-81 KPI strip + skeletons + consolidated Google card + gear settings (backend `GOOGLE_CONNECTOR_CONFIG` config-as-secret + GET/PUT `/connectors/:id/config`). Backend 460 tests.
- Paperclip gap analysis v2 + live-instance teardown (2026-07-06): vault `01-Projects/Paperclip-Gap-Analysis-v2-2026-07-06.md`. Their strength = failure/recovery UX; their Memory is still roadmap (we're live — press with R4). Epics filed: MCA-83 W, MCA-84 V, MCA-85 D.
- 7Ei theme (design system v2, `docs/DESIGN_SYSTEM.md`): **MCA-86 T1 shipped** — light+dark CSS-variable themes, ThemeProvider + toggle, colorblind-safe statuses (⚠ user is red-green colorblind: red never a CTA, always iconed; active=purple), official 7-hexagon mark top-left (`web/public/7ei-mark.svg`). **T2 glass chrome shipped** — `.mc-glass` utility (translucent `--glass` + 16px blur, solid `--s1` `@supports` fallback) on sidebar/TaskDrawer/all cockpit modals; new `web/app/dashboard/CommandPalette.tsx` (⌘K, keyboard-first, grouped Navigate+Theme commands, built to be extended by Epic V); tokenized `--shadow-modal`/`--shadow-drawer`. **W1 recovery cards shipped** (PR TBD) — `services/recovery.ts` `buildRecovery()` + `GET /api/tasks/:id/recovery`, `system_notice` comments on failure (new `task_comments.kind`), `web/app/dashboard/RecoveryCard.tsx` at the top of TaskDrawer (red left border + ⚠, owner/source-run/evidence/next-action, Retry-run + Add-a-note, open-until-decision), TaskBoard ⚠ on failed/blocked. **W2 work surface shipped** — new `services/worksurface.ts` (`nextUp`/`readyQueue` + `rollupCost`, pure/tested); `buildRecovery()` emits reasoned `blockers` chips (upstream title+status, open-first); `/subtasks`→`{ subtasks, rollup }`, `/cockpit`→`nextUp`; RecoveryCard blocker chips, TaskDrawer sub-task cost roll-up + header total, TaskBoard "Next up" banner + head-card flag. Backend 482 tests. **T3 hex sweep shipped** (PR TBD) — closes epic MCA-82 (theme): no raw hex remains in dashboard components (T1 already swept cockpit `shared.tsx`/`status.ts`); dropped dead `--7ei-*` brand aliases from `globals.css`; scripted **WCAG AA contrast audit both modes** — `--muted` fixed (light `#8a8a88`→`#6b6b6b`, dark `#555555`→`#7e7e7e`; was 2.4–3.5:1, now ≥4.5:1 on `--s0`/`--s1`), `--accent`-on-dark documented as a kept AA-large brand item; **PWA `theme-color` now theme-aware** (pre-paint script + ThemeProvider track `--s0` per resolved theme, new `themeSurface()`; manifest `#0a0a0a`→`#070707`). **V1 heartbeat 24h timeline shipped** (Epic MCA-84, PR TBD) — `services/timeline.ts` pure helpers (`runActivity`/`taskActivity`/`mergeActivity`/`buildHeartbeatTimeline`) project run telemetry (external `agent_runs`) merged with task timing (internal `durationMs`/`completedAt`) into per-agent 24h lanes as start/width %; new `GET /api/orgs/:orgId/timeline`; web `cockpit/TimelineSection.tsx` (lanes with heartbeat icon + run/active/spend, axis ticks + gridlines, status-coloured blocks, striped=ongoing shape cue, colour→status legend) after Agent fleet; `orphaned`→failed alias. Backend 496 tests. **W3 thread + wake-on-comment shipped** (Epic MCA-83, PR TBD) — commenting on an idle ticket picks the work back up (Paperclip parity). New `services/thread.ts` pure helpers (`decideWake` — a *user* comment on a task with an agent + no in-flight run wakes it; `isWakeableStatus` pending/assigned/blocked/failed/done — `done` reopens, `in_progress` excluded so an internal agent mid-run isn't double-fired; `threadHistory` projects the prior thread into the executor's `conversationHistory`; `buildWakeInput`). No new store: thread IS `task_comments`, wake reuses `executeAgentTask`. `POST …/comments` decides+fires the wake (durable "Agent woken" system-notice + thread replay), returns `{ comment, woke, wakeReason }` (optional `wake` flag overrides); `GET …/comments` enriches agent authors with name+emoji. Web TaskDrawer: `Comment & wake` composer + hint when wakeable, accent-tinted woke confirmation, agent comments render `emoji name`. Backend 506 tests. **V2 tri-state approvals + inbox retry rows + read receipts shipped** (Epic MCA-84, PR TBD) — approvals go binary→tri-state (new `services/approvals.ts` `decideApproval` + `approval_requests.decision_note`; `revision_requested` requires a reviewer note = the revision loop; `/api/approvals/:id/decide` takes `{decision,note}`; InboxSection inline "↩ Request changes" composer). Inbox retry rows: `buildInbox` marks failed tasks `retryable` + carries truncated `output` as inline `error` (executor catch now persists failure text as output), failed rows render error line + "↻ Retry" (reuses `POST /api/tasks/:id/execute`). Board read receipts: `services/receipts.ts` (`taskActivityAt`/`unreadTaskIds`) + `task_reads` table + `POST /api/tasks/:id/read`; `/cockpit` tags each task `unread`; cockpit TaskBoard cards now click-to-open (page drawer) with a "● new" chip cleared on open. Backend 521 tests. **V3 per-wake preflight cap + cheap-model config validation shipped** (Epic MCA-84, PR TBD — closes epic V, Paperclip G9) — new `services/preflight.ts` pure helpers (`estimateWakeCost`/`preflightWake`/`parseCapUsd`/`validateModelConfig`/`validateRoster`). **Preflight cap**: the internal executor bounds a wake's worst-case cost (input at input-rate + full `max_tokens` at output-rate) and, if it exceeds `deployConfig.maxCostPerWakeUsd` (org-wide; optional `maxCostPerWakeUsd:<agentId>` override; null/≤0 = no cap), parks the task `blocked`/`needs_attention` with a reason + durable `system_notice` comment and returns provider `preflight` — before any RAG/Drive/memory fetch so a capped wake is free. Orthogonal to `budget.ts` cumulative scoped budgets. Unpriced models under a cap are allowed-but-flagged `unbounded` (never block a working custom/local runtime). **Model validation**: audits each agent — unpriced (no `COST_RATES` entry) or output rate above the cheap threshold ($5/M output) flagged with reasons. New `GET/PUT /api/orgs/:orgId/preflight`. Web `cockpit/PreflightSection.tsx` after Budgets (editable cap, accent CTA never red; ✓/⚠ per-agent audit rows, colorblind-safe). Backend 540 tests. **D1 self-describing API shipped** (Epic MCA-85, PR TBD — Paperclip G6) — the API describes itself so agents self-configure instead of drifting against `docs/API.md`. New `services/openapi.ts` pure helpers: `buildOpenApiSpec()` (valid, deterministic **OpenAPI 3.1**), `zodToOpenApiSchema()` (the Zod subset our validators use; unknown → `{}`, never throws), `openApiPath`/`pathParameters`/`tagForPath`/`defaultSummary`. **Route table auto-collected from Fastify's `onRoute` hook** (in `index.ts`) → every registered path is in the spec, cannot drift; auth tagged by three composable hooks — baseline root (`none`, fires for all) + secured-scope (`clerk`) + a new wrapper scope round the agent API (`agentToken`), rank makes it order-independent, HEAD/OPTIONS filtered. Request bodies+summaries from the real Zod validators via `documentEndpoint()` (routes push schema into the service — respects the routes→services import rule), wired across the agent surface (`documentAgentApi()`). Public `GET /api/openapi.json`; CLI `7ei-mc openapi` (token-free — `mc.mjs` only demands `MC_AGENT_TOKEN` for non-public cmds). Live: 158 paths / 26 groups. Backend 555 tests. **D2 shipped (PR TBD — closes epic MCA-85 DX)** — new `services/llms-txt.ts` `buildLlmsTxt()` (pure, PUBLIC_URL-interpolated, trailing-slash-safe) served public as `text/markdown` at `GET /llms.txt` + static mirror `web/public/llms.txt` (resolves at 7ei.ai/llms.txt on Vercel); per [llmstxt.org](https://llmstxt.org) shape, points at D1's `/api/openapi.json` as source of truth. `npx @7ei/mc onboard` wraps the org+external-agent+token mint via new pure planner `cli/onboard.mjs` (`parseFlags`/`parseOnboard`/`orgRequest`/`agentRequest`) + runner in `mc.mjs`; both mint endpoints are Clerk-secured so onboard uses the operator's Clerk session JWT (`MC_CLERK_TOKEN`), `--org-name` creates a new org (auto-hires Arturito) or `--org <id>` targets an existing one, agent flags `--name/--role/--runtime/--provider/--model`, `--dry-run` previews. Backend 570 tests (+4), CLI 7 tests (+5). **Also shipped: per-org webhook signing (PR #166)** — `services/webhook-auth.ts` deterministic HMAC-per-org shared secret verifying the Telegram (`secret_token` header) + Jira (`?secret=` query) receivers, gated on `WEBHOOK_SIGNING_SECRET` (enforced when set, open in dev), 566→ backend tests. **W4 task watchdogs shipped** (Epic MCA-83, PR TBD — Paperclip G2) — declarative per-task checks evaluated on the scheduler tick, edge-triggered notices posted in-thread ("stop babysitting long runs"). New `services/watchdogs.ts` pure helpers (`parseWatchdogSpec`/`describeWatchdog`/`evaluateWatchdog`/`watchdogTransition`) + DB sweep `runWatchdogSweep()` (mirrors `runHeartbeatSweep`) wired into the 1-min scheduler tick. Four kinds: `runtime` (running run older than N min), `cost` (task cost over $X), `no_activity` (idle over N min, never on `done`), `status` (reaches blocked/failed/done/in_progress). Edge-triggered via `task_watchdogs.state` (ok|triggered) → a `system_notice` fires only on a flip (⚠ triggered / ✓ cleared), so an over-threshold task posts once not sixty times; the notice feeds the W1 recovery card for free. `no_activity`'s activity signal excludes `system_notice` comments so it can't flap on its own post. New `task_watchdogs` table; routes `GET/POST /api/tasks/:taskId/watchdogs` + `PATCH/DELETE /api/watchdogs/:id` (Clerk-secured). Web TaskDrawer Watchdogs section (✓/⚠ colorblind-safe, add/remove). Backend 580 tests (+10). **W5 ask-mode shipped** (Epic MCA-83, PR TBD — closes epic MCA-83 Work surface) — a task carries `work_mode` (`execute` = full loop | `ask` = a question). An ask is a single LLM turn: no workspace/checkout, no RAG/Drive/goal context, no `[REMEMBER]`/`[WEBHOOK]`/`[DELEGATE]` side-effects — the answer posts straight to the thread as an agent-authored `answer` comment + mirrors to `task.output`. New pure `services/askmode.ts` (`normalizeWorkMode`/`isAskMode` — unknown ⇒ execute; `buildAskSystemPrompt` — lean identity+org+memory prompt that omits the execute-loop directives so it never advertises tools an ask can't run + is honest about the one-reply constraint; `ASK_ANSWER_KIND='answer'`). `executeAgentTask` routes ask tasks to the new **`answerAskTask`** lean path (keeps governance/scoped-budget/preflight + daily-budget/slot guards, drops the workspace setup); W3 wake-on-comment re-enters through it, so an answered ask becomes a continuable Q&A thread. Idempotent `ALTER TABLE tasks ADD COLUMN work_mode` (default `execute`); task-create takes `workMode`; new **`POST /api/agents/:agentId/ask`** (Clerk-secured) creates+fires an ask task → `{ taskId }`. Web: Cockpit Agent-fleet cards get a 💬 Ask inline composer (Enter/Escape, opens the drawer); TaskDrawer badges an ask ticket (💬 Ask accent pill). Backend 588 tests (+8). **Epic MCA-83 Work surface COMPLETE.** NEXT: open — R4 vault RAG / R6 Sync-Registry / MCA-81 follow-ups / `app/` archive decision.
- Onboarding system live in 7Ei_OS (`onboarding/`, PRs #3+#4 merged); OpenClaw first graduate; memory bus verified E2E in prod on rotated credentials.
- Other candidates: R4 vault RAG (Turso native vectors — backend-only, parallelizable), R6 Sync-Registry automation, MCA-81 follow-ups (Google userinfo email at OAuth callback; executor driveScope), archive `app/` (Expo) decision.

## Conventions

One PR per story, squash-merged with `--admin` (auto-deploys Fly + Vercel). Idempotent ALTER migrations in `backend/src/db/setup.ts`. Pure-helper services with `node --test`. Boot test `backend/src/tests/boot.test.ts` guards route collisions. Keep `STATUS.md` current each shipped story; mirror milestones to the vault.

## Shared memory / vault

Canonical Obsidian vault: `/Users/artutito/7Ei-MC_TARCO` (repo `Arturito7ei/7Ei-MC_TARCO`, content under `vault/`). Latest status doc: `vault/07-Agents/STATUS-Mission-Control-2026-07-02.md`. Connectors: Jira via Atlassian Rovo OAuth (cloudId `5dadc567-085a-4cd8-99a3-c0bd9886fee9`, projects MCA + OS), Slack, Gmail.

## Open items — user-only console actions (see GO-LIVE.md)

1. Clerk **production** instance → `pk_live`/`sk_live` on Vercel.
2. Google consent-screen sensitive scopes (Gmail/Calendar) + add test user.
3. Rotate NVIDIA key → set as `MC_LLM_API_KEY` secret; rotate vault GitHub PAT.
4. Run `adapters/mac-mini/setup.sh` on the Mac mini; unload the laptop's launchd service.

## Verify before trusting the above

```bash
cd /Users/artutito/Developer/7Ei-Mission_Control_App
git log --oneline -15
cd backend && npm test && npm run evals
cd ../web && npm run build
curl -s https://7ei-backend.fly.dev/api/health   # expect 200, db: connected
```
