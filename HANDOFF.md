# Mission Control — Session Handoff (for a fresh agent)

_Last updated: 2026-07-15 — **Epic H · H6 LOOPBACK AUTH BUILT** — the packaged profile now enforces a **real single-operator loopback identity** (Clerk swapped for `loopbackAuth` on `127.0.0.1`; per-install `SECRETS_ENC_KEY`/`RUN_TOKEN_SECRET`/`MC_LOOPBACK_SESSION_SECRET` generated into the macOS Keychain; **fail-closed-on-default-key** boot guard) that gates the **same** membership/owner-checked write routes; the packaged dashboard is reachable as the operator. Hosted Clerk path **byte-identical + green** (1277 tests · 11/11 · typecheck clean · no standalone). **STOPPED for the independent H6 audit** (see the "Epic H — H6" block below). Prior: **Epic H · H1 packaging BUILD PIPELINE BUILT** — the desktop backend is now a **compiled esbuild bundle** (no dev toolchain); `npm run dist:mac` yields a reproducible release-quality **UNSIGNED** `.app` (463 MB, was 619) + `.dmg` (157 MB, was 195) that boots the packaged mesh from the built app; the **sign/notarize surface is wired but inert** behind a one-env-flip (Apple-account-gated). **STOPPED for the independent H1 audit** (see the "Epic H — H1" block below). Prior: **Epic H · H0 packaging spike BUILT** — an Electron shell (`apps/desktop/`) boots the packaged/loopback mesh, verified from the built unsigned `.app`. Prior: **Epic ONB COMPLETE** — invite-based agent onboarding is shipped, audited, and now fully documented (ONB7: `docs/RUNBOOK-agent-onboarding.md`, a consolidated `GO-LIVE.md`, `docs/SECURITY-posture.md`). The inverted token lifecycle (self-describe → human approves → claim once) is real end to end; ONB5 is deferred (gateway/webhook only). See the "Epic ONB — complete" block below. Prior: **Epic AG COMPLETE (AG1–AG7)** — the Agents experience: a Staff card grid + a six-tab agent detail page (Dashboard · Instructions · Skills · Configuration · Runs · Budget), incl. the net-new managed instructions bundle and avatar upload. See the "Epic AG — complete" block below. Prior: **Epic CC COMPLETE (CC1–CC6)** — Claude Code is a first-class office engineering agent (`docs/DESIGN-claude-code-agent.md`, PLAN §0). Host adapter + machine_exec approval bridge + secure-by-default registration + UI/CLI wiring + command denylist + autonomous-exec mode (OFF by default, fail-closed behind two guards + the denylist). Nothing runs un-approved on the host. See the "Epic CC — complete" block below. Earlier context (Paperclip-gap-bridge v2 epics + Arturita epics) is preserved further down._

> **Epic H — H6 loopback auth built (2026-07-15).** H6 closes the H0/H1 **temporary auth bypass**: the packaged profile now enforces an **authenticated single local operator**, not an open instance. It is **additive + profile-branched** — hosted Clerk is byte-identical — and satisfies the four **AUDIT-H1** requirements. **(1) Dual auth, profile-selected:** `backend/src/index.ts` installs `profile === 'packaged' ? loopbackAuth : clerkAuth` on the **same** secured scope; both attach the identical `req.auth.userId`, so `requireOrgMembership`/`requireOrgRole`/audit/telemetry run **unchanged** — H6 swaps the identity source, keeps the gate. **(2) The identity (H-Q6, OS-user-bound):** the Electron shell generates `MC_LOOPBACK_SESSION_SECRET` into the **macOS login Keychain** (`apps/desktop/src/keychain.cjs`) and injects it as the `Authorization` bearer on every window→backend request (`main.cjs` `onBeforeSendHeaders`, **never in page JS**); `middleware/loopback-auth.ts` constant-time-compares it → the single **`local-operator`**, seeded as **owner** of the local org (`services/loopback-identity.ts`). A request without the session **401s**. **(3) Per-install keys + fail-closed (AUDIT-H1 #1/#2/#4):** three **distinct** per-install keys into the Keychain (the H0/H1 throwaway default is gone); `services/secret-keys.ts` `assertSecretKeysSafe()` **refuses to boot** packaged on any missing/default/reused key (no-op on hosted). **(4) Usable dashboard:** the desktop web build is `NEXT_PUBLIC_MC_PACKAGED=1` so the dashboard renders as the operator (the H0 `/dashboard` gap). **Tests +14 → 1277** (`loopback-auth`/`secret-keys.test.ts`, incl. a real-DB secured-scope pass/401). **Honest flags:** the full built-app round-trip (Keychain under a signed `.app`, injected-header flow in a live BrowserWindow) is **verification-gated** — same non-interactive limit as H1's sign/notarize; the keychain helper (live `security` round-trip), the backend auth + fail-closed logic (real-DB tests), and the baked web flag were verified here. Unsigned-app Keychain ACLs stabilise on the H1 signing run. No TCC wizard (H2)/auto-update (H3)/config-seeding (H4). **Where to look:** `backend/src/{middleware/loopback-auth.ts, services/{secret-keys,loopback-identity}.ts, index.ts}`, `apps/desktop/src/{keychain,main}.cjs`, `web/app/dashboard/page.tsx`, `docs/DESIGN-packaging.md` §17, PLAN §0 (H6). **STOPPED for the H6 audit.** Prior H1 detail follows.
>
> **Epic H — H1 build pipeline built (2026-07-15).** H1 hardened the H0 spike into a **production-grade build pipeline** — everything **up to** the Apple-account-gated sign/notarize run, confined to **`apps/desktop/`** (hosted build byte-identical, verified). **(1) Compiled backend:** `scripts/build-desktop.mjs` bundles `backend/src/index.ts` with **esbuild** → one tree-shaken ESM `index.js` (5.1 MB) with pure-JS deps inlined; only **`@libsql/client`** + **`officeparser`** (native/wasm) stay external, re-installed **pinned** as a pruned prod `node_modules`. `main.cjs` forks the **compiled `index.js`** in packaged mode (dev still uses `tsx`). The built `.app` ships **no `tsx`/`typescript`/`drizzle-kit`/TS source**. **(2) Sizes:** `.app` 619→**463 MB**, `.dmg` 195→**157 MB** (dev toolchain gone; the remaining backend weight is officeparser's OCR/PDF native stack, a real runtime dep). **(3) Boots from the built app:** `/api/health` `{status:ok, db:connected}` on `127.0.0.1:8787` (fresh `mc.db`, migrations on boot), web 200 on `:8788`; `npm run dist:mac` reproducible with correct appId/productName/**version (0.1.0)**/icon. **(4) Signing WIRED but INERT:** `hardenedRuntime: true` + minimal `build/entitlements.mac.plist` + self-skipping `afterSign: scripts/notarize.cjs`; `CSC_IDENTITY_AUTO_DISCOVERY=false` forces the deterministic unsigned build. **The flip** = supply cert + notarytool creds, drop one env guard → signed+notarized+stapled (`GO-LIVE.md` §17). **Still the temporary auth bypass** (no Clerk keys; throwaway `SECRETS_ENC_KEY`) — **NOT security-complete until H6.** No TCC wizard/auto-update/config-seeding/loopback-auth built; Tray deferred. Hosted verified: **1263 tests · 11/11 evals · typecheck clean · hosted web build emits no standalone.** **STOPPED for the H1 audit.** Where to look: `apps/desktop/`, `docs/DESIGN-packaging.md` §16, PLAN §0 (H1). Prior spike detail follows.
>
> **Epic H — H0 spike built (2026-07-15).** The **packaging** de-risk spike is built in **`apps/desktop/`** (the seed for H1, not throwaway) and verified **from the built `.app`**. An Electron shell supervises the **packaged/loopback mesh as one app**: it forks the **Fastify backend** (`MC_DEPLOYMENT_PROFILE=packaged`, `DATABASE_URL=file:…/mc.db`, migrations on boot, `127.0.0.1:8787`) and the **Next.js standalone UI** (`127.0.0.1:8788`) as children of Electron's own Node, then opens a BrowserWindow once `/api/health` is green. Produces an **unsigned** `.app` + `.dmg` via `electron-builder`; runs with **no dev toolchain** (`tsx` + backend `node_modules` incl. the libSQL native addon + the Next standalone ship in `Contents/Resources/`). **Decisions confirmed for H1:** Electron (no Tauri bake-off) · Node-child via `tsx` (H1 → compiled/pruned bundle) · web = standalone-as-child (`DESIGN-packaging.md` §2.3 a) · pure local libSQL file · signing is a later **config addition** (H1/Apple-account-gated). **Key finding:** `@clerk/fastify`'s `clerkPlugin` 500s *every* request without a publishable key → gated on `CLERK_PUBLISHABLE_KEY` (the packaged profile is an auth **bypass**, NOT security-complete until **H6**). **Minimal prod edits (additive, hosted no-op):** `backend/src/index.ts` (clerk gate + `HOST` bind), `web/next.config.ts` (`output:standalone` behind `DESKTOP_BUILD=1`). **Hosted verified untouched:** 1263 backend tests · 11/11 evals · hosted web build emits no standalone. **Dev/build:** `cd apps/desktop && npm install`, then `npm run desktop` / `npm run pack:mac` / `npm run dist:mac`. **Where to look:** `apps/desktop/` (+ its README), `docs/DESIGN-packaging.md` §15 (spike results), PLAN §0 (H0). **Stopped before H1** per the spike scope. Details in STATUS.md.

> **Epic AG — complete (2026-07-14).** The **Agents experience**: a **Staff card grid** (avatar · name · handle · colorblind-safe status dot · Tasks Active / Token Cost today / Last active) and a **six-tab agent detail page** (`#agents/<id>/<tab>`) — **Dashboard · Instructions · Skills · Configuration · Runs · Budget**. **All seven stories shipped + merged:** AG1 shell + routing ([#227](https://github.com/Arturito7ei/7Ei-Mission_Control_App/pull/227)) · AG2 Dashboard + agent-overview/runs endpoints + per-task token split ([#228](https://github.com/Arturito7ei/7Ei-Mission_Control_App/pull/228)) · AG3 Instructions + the **managed instructions bundle** ([#229](https://github.com/Arturito7ei/7Ei-Mission_Control_App/pull/229)) · AG4 Skills + the first uninstall path ([#230](https://github.com/Arturito7ei/7Ei-Mission_Control_App/pull/230)) · AG5 Configuration + **avatar upload** ([#231](https://github.com/Arturito7ei/7Ei-Mission_Control_App/pull/231)) · AG6 Runs + Budget ([#232](https://github.com/Arturito7ei/7Ei-Mission_Control_App/pull/232)) · AG7 **Staff grid** ([#233](https://github.com/Arturito7ei/7Ei-Mission_Control_App/pull/233)). **Reuse first** — runs/tasks/costs/skills/adapter/budget/heartbeat/org-chart were already there; only three things were net-new: `agent_files` (the instructions bundle — the gap doc's #1 config gap), `agents.avatar_url` (a capped 256KB data URI; SVG refused; browser downscales first), and the per-task token split that makes the Costs strip real. **Hard rules held:** every migration is idempotent + nullable with **nothing backfilled**, so existing agents are unchanged — the bundle only feeds the prompt for files the operator **saved**; write paths are owner-gated, field-allowlisted and snapshotted (the legacy unvalidated `PATCH /api/agents/:id` was not extended); a Reports-to loop is refused; the OpenClaw and Claude Code adapters were never touched. **Where to look:** `web/app/dashboard/agent/` (StaffGrid + the six tabs), `backend/src/routes/agent-detail.ts`, `backend/src/services/{agent-overview,agent-files,agent-skills,agent-config,agent-avatar,agent-budget,staff-grid}.ts`. **Follow-ups (optional):** "Run Heartbeat" on the header runs the **org sweep** (there is no per-agent wake endpoint); the avatar data URI rides the agents JSON — swap the column for a blob-store URL if avatars ever get large or numerous; the Instructions bundle feeds the execute path's system prompt but not the lean ask-mode prompt (deliberate). Invariant at epic close: **1029 backend tests · 11/11 evals · web build · 135 web tests**. Details per story in STATUS.md + PLAN §0 (AG1–AG7).

> **Epic CC — complete (2026-07-13).** Claude Code is a `runtime:'claude_code'` external fleet executor. **All six stories shipped + merged to main:** CC1 host adapter (#221) · CC2 machine_exec approval bridge (#222) · CC3 secure-by-default registration (#223) · CC4 UI/CLI wiring (#224) · CC5 command denylist (#225) · CC6 autonomous-exec (OFF by default) + go-live docs ([#226](https://github.com/Arturito7ei/7Ei-Mission_Control_App/pull/226)). **What's live vs off:** propose-and-approve is live (assign a task → the agent runs a headless `claude -p` in plan mode → proposes; shell commands surface as A2 `machine_exec` approvals with verbatim argv + step-up). **Autonomous host exec is OFF by default** — fail-closed behind `CC_AUTONOMOUS=1` + `CC_AUTONOMOUS_CONFIRM=1` + the CC5 denylist; even then every command passes the denylist. **Hard rules held:** the live OpenClaw adapter was never touched; the CC agent defaults to `low_trust_review` + an explicit capability list; every run traverses the 9-gate chain; nothing runs un-approved. **To hire one:** Cockpit → Add agent → 🤖 Claude Code (or `npx @7ei/mc onboard --runtime claude_code`), then `adapters/claude-code/setup.sh` on the host (see GO-LIVE.md item 6). **Follow-ups (optional):** the guard hook gates `Bash` only — file edits happen in the `cc/` worktree (reviewable as a diff); extending the guard to gate out-of-worktree Write/Edit is a possible hardening. Invariant at epic close: **934 backend tests · 11/11 evals · web build · 112 web tests · 48 adapter tests**. Details per story in STATUS.md + PLAN §0 (CC1–CC6).

> **Epic ONB — complete (2026-07-15).** **Invite-based agent onboarding** — an external agent onboards *itself* from one pasted prompt: it reads a per-invite document, picks its runtime, probes for a reachable base URL, submits a **join request** describing its own capabilities, **waits for a human board approval**, and only then **claims its API key, once**. The load-bearing move is the **inverted token lifecycle** — self-describe first, human approves second, credential minted and claimed last, by the party that uses it, never through the operator's clipboard. **All MVP stories shipped + independently audited:** ONB1 spine + adapter registry ([#244](https://github.com/Arturito7ei/7Ei-Mission_Control_App/pull/244)) · ONB2 onboarding doc + prompt ([#246](https://github.com/Arturito7ei/7Ei-Mission_Control_App/pull/246)) · ONB3 join + board-approval gate ([#250](https://github.com/Arturito7ei/7Ei-Mission_Control_App/pull/250)) + H-1 owner-gate fix ([#252](https://github.com/Arturito7ei/7Ei-Mission_Control_App/pull/252)) · ONB4 one-time claim ([#253](https://github.com/Arturito7ei/7Ei-Mission_Control_App/pull/253)) · ONB6 create-invite UI + prompt + CLI ([#255](https://github.com/Arturito7ei/7Ei-Mission_Control_App/pull/255)) · audit-trail enablement ([#257](https://github.com/Arturito7ei/7Ei-Mission_Control_App/pull/257)/[#259](https://github.com/Arturito7ei/7Ei-Mission_Control_App/pull/259)) · ONB7 docs/go-live (this PR). **Four invariants held** (test-locked): public join OFF by default (profile-derived) · invites single-use by default · every invite-agent `low_trust_review` regardless of runtime · the raw claimed token never reaches an operator UI/log. **ONB5 explicitly DEFERRED** — optional, gateway/webhook-only; the four *available* runtimes (`openclaw_local`, `claude_code`, `cursor`, `openai_generic`) need no server-side reachability check, so deferring it ships nothing insecure. **Today's posture:** hosted prod answers join/claim/doc with a flat 404 until `MC_ENABLE_REMOTE_ONBOARDING` is set — the safe default. **Where to look:** `docs/RUNBOOK-agent-onboarding.md` (operator guide), `docs/SECURITY-posture.md` (the model), `GO-LIVE.md` (the switches), `backend/src/services/adapter-registry.ts` (runtime source of truth), PLAN §0 (ONB1–ONB7). Invariant: **1263 backend tests · 11/11 evals · typecheck clean**.

> **Current story (2026-07-15) — MEMBERSHIP HARDENING, HIGH-1 CLOSED: the ~25 top-level RECORD routes are now gated too. The gate is truly surface-wide, fail-closed. STOPPED for the re-audit.**
> The independent audit of the R-4 fix (`docs/AUDIT-MCA-membership-review.md`, verdict NEEDS-FIXES) proved it was **not** surface-wide: `resolveRequestOrg` knew only `:orgId`/`:agentId`/`:taskId`, so ~25 top-level record routes whose org lived in another param (`:projectId`, `:goalId`, `:itemId`, `:skillId`, and the generic `:id` for secrets/budgets/plugins/workspaces/attachments/watchdogs/scheduled/webhooks/policies/revisions) resolved `scoped:false` → the gate stood down → a non-member could read/delete another org's secret, confidential knowledge doc, project, policy, webhook, agent revision. Now closed.
> **URL-PREFIX → owning-table derivation.** `resolveRequestOrg` gained `RECORD_ORG_ROUTES` (`middleware/rbac.ts`): 14 route-prefix→table entries deriving the org from the record (generic `:id` disambiguated by route PREFIX, not param name — `req.routeOptions.url`). Each **fails closed** (missing/foreign record → 403), same contract as the `:agentId`/`:taskId` tail. `:orgId`/`:agentId`/`:taskId` precedence unchanged; backward compatible (no `routeUrl` → prefix tier skipped).
> **FLAGGED + handled — global vs per-org skills.** `skills.orgId` is nullable: a `null` row is a shared **global-library** skill → the gate **stands down** (readable, not a tenant record); a per-org custom skill (`orgId != null`) **is** gated; a missing skill fails closed. Global-library collection routes (`GET/POST /api/skills`, `sync`, `obsidian-sync`) are on the leak-guard EXEMPT allowlist.
> **Widened leak-guard — can't reopen.** `membership-scoping.test.ts` replaced the `/api/orgs/:orgId/*` filter with an **allowlist-negation sweep over ALL secured routes**: each must resolve an org OR be on a short justified EXEMPT allowlist (16 routes); a new secured route resolving `scoped:false` unlisted FAILS the test. Self-test proves teeth; a behavioural sweep 403s a non-member at every non-exempt route; targeted tests drive a non-member at each real ORG record → 403 and a member → non-403. The boot now also registers **`webhookRoutes`** — it was **MISSING** from the original boot (the blind spot that hid the webhook leak). Red-proof: disabling the fix reds the sweep + record proofs.
> **Where to look:** `backend/src/middleware/rbac.ts` (`RECORD_ORG_ROUTES` + `resolveRequestOrg`), `backend/src/tests/membership-scoping.test.ts` (the widened guard + EXEMPT allowlist), `backend/src/tests/rbac-membership.test.ts` (prefix-derivation units), `docs/AUDIT-MCA-membership.md` (prefix→table map + flags).
> **Invariant: 1263 backend tests (+10) · 11/11 evals · typecheck clean.** **Untouched: adapters, telemetry, `allowShell`, the agent-token API, the public onboarding surface, the deployment posture.**
> **Flags for the re-auditor:** (1) `skills.orgId` nullable — global skills stand down (fail-open-by-design, shared library) while per-org skills are gated; verify that's the intended read. (2) The pre-existing fact that `GET /api/skills` and `POST /api/skills` are all-org / global-library writes is unchanged and EXEMPT (a shared-library design choice, not a tenant leak) — flagged, not "fixed". (3) `DELETE|POST /api/notifications/register` take a `userId` from the BODY with no self-check (device/user push tokens, in-memory) — org-agnostic, EXEMPT, pre-existing, out of this fix's scope. (4) L-2 object-scoping (a member reading a foreign record by id within their own org path) is a separate object-authz concern, still out of scope — unchanged.
>
> **Prior story (2026-07-15) — MULTI-TENANT MEMBERSHIP HARDENING: org membership enforced SURFACE-WIDE (the R-4 fix). STOPPED for the independent audit.**
> The cross-tenant gap the ONB2/ONB3 audits kept surfacing: `/api/orgs/:orgId/*` was Clerk-authed but NOT membership-checked — only ~35 of ~159 org routes ran `requireOrgRole`, so any logged-in user could act on any org by swapping `:orgId`. Closed with ONE mechanism.
> **ONE scope-level gate.** `requireOrgMembership` (`middleware/rbac.ts`) is a `preHandler` on the whole Clerk-secured scope (`index.ts`, right after the Clerk hook). It enforces baseline `member` for the org each request targets — from `:orgId`, or derived from the `:agentId`/`:taskId` **record** where the path has no org id (`resolveRequestOrg`). Reuses the single `enforceOrgRole` core; **fails closed** (id→no row → 403, never skip); skips `OPTIONS`. Scope-level ⇒ a new org route is covered on registration; owner gates still layer on top.
> **Grandfathered — operator never locked out.** `enforceOrgRole` now honours `organisations.ownerId` as an implicit owner when there's no `org_members` row (nothing backfilled pre-membership orgs). Existing owners keep full access, zero migration; only genuine non-members newly 403.
> **In-handler fixes (`multi-org.ts`):** `GET /api/users/:userId/orgs` → self-only; `transfer`/`clone` → also membership-check the TARGET org (gate covered the source).
> **Exempt (precise + tested):** agent-token API `/api/agent/*` (token auth, separate scope), public onboarding (invite/join/claim), `arturita/panic` (in-handler session token), `auth/google` (OAuth, pre-existing public), health/ready/openapi. None changed.
> **Guard:** `backend/src/tests/membership-scoping.test.ts` (behavioural — sweeps EVERY `/api/orgs/:orgId/*` route, >80, non-member→403; member/owner positives; record-derived; grandfather; agent-API unaffected) + `rbac-membership.test.ts` (unit). `auth-scoping.test.ts` boot now mirrors `index.ts`.
> **Where to look:** `backend/src/middleware/rbac.ts` (the mechanism), `backend/src/index.ts` (the one hook), `backend/src/routes/multi-org.ts`, `docs/AUDIT-MCA-membership.md` (full route inventory + flagged edge cases).
> **Invariant: 1253 backend tests (+18) · 11/11 evals · typecheck clean.** **Untouched: adapters, telemetry, `allowShell`, the live adapter, the deployment posture.**
> **Flags for the auditor:** (1) The gate derives org from `:agentId`/`:taskId` via one extra DB read on those bare routes (handlers load the record anyway); the `/api/orgs/:orgId/*` bulk pays no extra read. (2) `POST /api/approvals/:id/decide` is record-derived via `:id` and **self-enforces** membership + a type-mapped role in-handler (ONB3-H1) — the scope gate deliberately stands down for it (no matching param), preserving its owner-for-minting logic; covered by `onb3-approval-gate.test.ts`. (3) `transfer`/`clone` target-org check is the conservative read (blocks injecting an agent into an org you don't belong to); a super-admin "move any agent anywhere" model is intentionally NOT introduced. (4) Grandfathering leans on `organisations.ownerId` being the durable ownership truth — so no path may let a non-owner set it: org-create sets it to the caller, and `PATCH /api/orgs/:orgId` (unvalidated body) now **strips `ownerId`/`id`** so a member can't rewrite themselves to owner. Ownership transfer, if ever wanted, is a dedicated (owner-gated) route, not this general edit.
>
> **Prior story (2026-07-15) — Epic ONB / audit M5 RESOLVED: the SHELL-EXECUTION DEFAULT is OFF for new agents; existing agents (incl. the live OpenClaw ops agent) GRANDFATHERED. STOPPED for the independent audit.**
> The operator took the M5 product call: align the shell default to OFF without breaking the live ops agent. The ONB1 audit flagged the drift — server registry `allowShell: false` vs the wizard's paste-block emitting `MC_ALLOW_SHELL=1`, so every UI-onboarded OpenClaw agent got shell-on. Now both default OFF.
> **Enforcement is CLIENT-ONLY (traced end to end).** `MC_ALLOW_SHELL` → local `ALLOW_SHELL` at `adapters/openclaw/mc_adapter.py:31`, gating `shell_execute` (:105) + the `llm_execute` tool loop (:175). **No server/registry code gates shell or `machine_exec` from an agent's stored `allowShell`** — the registry field is only the declared `agentDefaultsPayload` default (validated + stored, never consulted to block). The wizard run-block is the only lever, so flipping it **cannot retroactively disable a running agent.**
> **New agents ship shell-OFF.** `web/lib/adapterProfile.ts`: openclaw no longer hardcodes `MC_ALLOW_SHELL=1`; `mcEnv`/`runBlock` append an explicit `MC_ALLOW_SHELL=0` for shell-capable runtimes (openclaw, custom), with an opt-in (`{ allowShell: true }` → `=1`) shown as an **advanced, off-by-default checkbox** in the Add-Agent wizard + Hire dialog. Cursor/Claude Code never emit the flag (unchanged).
> **Existing agents GRANDFATHERED — automatic + zero-touch.** The live OpenClaw agent runs from its own `mc.env` on its host (`~/.openclaw/mc-adapter/`), which the wizard never rewrites; the adapter reads the flag from that local env; there is no server gate to flip. Its `MC_ALLOW_SHELL=1` is untouched → it keeps working. **No server gate was touched; the live adapter install was not touched.**
> **Registry ↔ wizard reconciled + test-locked.** `web/lib/adapterProfile.test.ts` (13, +6): new agent's run-block does NOT enable shell · explicit opt-in does · non-shell runtimes never emit the flag · wizard default agrees with registry `allowShell:false` (backend side locked at `adapter-registry.test.ts:65`).
> **Invariant: 1235 backend tests (unchanged — no backend edit) · 11/11 evals · web build clean · adapterProfile suite 13 (+6).** **Untouched (operator/product calls): the live adapter, telemetry hook, the deployment posture, any server-side shell gate.**
> **Flags for the auditor:** (1) The fix is purely the *client* default + a UI opt-in + docs — deliberately **no server gate added**, because a registry-driven server gate on `allowShell` *would* be the thing that could block the live agent; adding one is a separate, riskier decision left for the operator. (2) Grandfathering relies on the client-only enforcement model — verify there is genuinely no server consumer of stored `allowShell` (grep: only the registry field declaration + the adapter's local env-read exist). (3) `custom` was already shell-off (it never emitted the flag); it now emits an explicit `=0` and is opt-in-capable — a consistency improvement, not a behavior change for existing custom agents.

> **Prior story (2026-07-15) — Epic ONB / audit H-1 SHIPPED: the AUDIT TRAIL is LIVE for sensitive writes, with 90-day retention. STOPPED for the independent audit.**
> The operator took the H-1 decision (`GO-LIVE.md` §7 option **b**): enable the previously-dead audit subsystem, conservatively — record sensitive writes + the onboarding/invite/join/approval surfaces only, skip the read-only GET flood, with retention. This turns on writes to the hosted Turso `audit_logs` table, behind every prerequisite the ONB2 audits demanded (query routes owner-gated · `sanitizeBody` recurses over the registry secret detector · path/telemetry redaction — all already on main).
> **HOISTED so it fires.** The hook was a no-op because it lived inside an encapsulated `app.register()` child (audit H-1: zero rows for any sibling). It is now `auditLogPlugin(app)` on the **root instance** in `src/index.ts`, before any `register()`, mirroring the correct `onRoute` hook — so its `onResponse` fires for every route. `backend/src/tests/audit-onb-enable.test.ts` **proves it records for a sibling route** (the exact thing the ONB2 audit proved it did NOT).
> **SCOPED — no GET flood.** Pure tested `shouldAudit(method, path)` (`middleware/audit-log.ts`): every mutating method (POST/PUT/PATCH/DELETE) **plus** `/agent-invites`·`/agent-join-requests`·`/approvals` (incl. security-relevant GET reads of the token-addressed doc); the dashboard-poll GET flood is skipped. The insert is fire-and-forget (`dbSink` `.catch()`), so it can never add latency to or fail the request it records.
> **RETENTION.** `services/audit-retention.ts` prunes rows older than `MC_AUDIT_RETENTION_DAYS` (default **90**; junk/0/negative safe-defaults to 90 so a typo can't wipe the table) on a daily scheduler tick (~03:00 UTC). Cutoff math + env parse + daily gate are pure/tested; a `created_at` index (`setup.ts`) serves both the prune and the query `ORDER BY`.
> **REDACTION re-proven THROUGH the live hook.** End-to-end: a real join POST carrying a nested `apiKey` + a registry-declared `webhookAuthHeader` bearer + an `mci_inv_` token in the path → the persisted row has the **path redacted to `:token`** and **no secret/token anywhere** (`buildAuditRow` → `redactPath` + recursive registry-driven `sanitizeBody`).
> **Reads stay gated.** `GET /api/orgs/:orgId/{audit-log,traces}` remain Clerk + `requireOrgRole('owner')` in the secured scope (from #248) — a test boots both query routes and asserts 401 without a session. Enabling writes did not open reads.
> **Tripwire `[ONB2-H1]` REWORKED, not deleted.** It used to fail if the hook was hoisted (it held the hook shut). It now guards the **safety envelope**: permits the hoisted enablement, requires telemetry to stay off, and fails if the hook is enabled without the redaction/sanitize/gating prerequisites (behavioral checks on `buildAuditRow` + `shouldAudit`).
> **Telemetry left OFF (separate concern).** `telemetryPlugin` stays an encapsulated no-op — it is an in-memory span ring buffer (no Turso writes, bounded), and its `/traces` under-reports until `llm.call` spans carry an org id. Enabling it is its own operator call.
> **Invariant: 1232 backend tests (+11) · 11/11 evals · typecheck clean · web build clean.** **Untouched (operator/product calls): `allowShell`/`MC_ALLOW_SHELL`, telemetry hook, the live adapter, the deployment posture.**
> **Flags for the auditor:** (1) **Turso cost** — one fire-and-forget INSERT per SENSITIVE request; writes are mostly low-volume (onboarding/approval) + the app's mutating traffic, and the GET flood is excluded, so this is not per-poll; verify the scope is what the operator intended and that the fire-and-forget `.catch()` is acceptable (a failed insert is logged and dropped, never surfaced). (2) The prune runs **lazily on the scheduler tick**, not as a DB job — same pattern as the existing KV-export/consolidation sweeps; verify once-per-day gating. (3) `orgId` on a row comes from `req.params.orgId`, so writes on non-`:orgId` paths (e.g. `/api/agent-invites/:token/join`) record `orgId: null` — correct (the org isn't known at that point) but means those rows won't appear in an org-scoped audit query; noted, not a leak. (4) Telemetry deliberately NOT enabled — confirm that matches the operator's scope.
>
> **Prior story (2026-07-15) — Epic ONB / ONB6 SHIPPED: the CREATE-INVITE UI + copy-able onboarding prompt + CLI. Onboarding is usable end-to-end. STOPPED for the independent ONB6 audit.**
> The operator-facing half of the epic — the acceptance test for the whole thing: **onboard an external agent by pasting one prompt.** **No backend change** — the ONB1–ONB4 contract was already complete; ONB6 surfaces it.
> **Create-invite dialog (Cockpit → "✉ Invite an agent", `web/app/dashboard/cockpit/InviteAgentDialog.tsx`).** Owner-gated (backend `requireOrgRole('owner')`). Pick the allowed runtime(s), single-use (default) / bounded multi-use, a TTL; on create the dialog shows **exactly once** the invite token + the **copy-able onboarding prompt** (`buildOnboardingPrompt`) with copy buttons + the doc URL, and states plainly they are shown once and unrecoverable. Honest posture note when hosted-join is closed (which it is in prod — `MC_ENABLE_REMOTE_ONBOARDING` unset).
> **The raw CLAIMED agent key is NEVER shown** — the dialog reveals the invite token + the prompt, never a claimed agent token (no code path to one). `operatorCanSeeClaimedKey` stays `false`; the claimed key is minted only when the joining agent claims it and only the claimer sees it.
> **The adapter picker renders FROM the server registry** (`GET /api/adapters` → `pickableAdapters` = `invitable && available`, never `internal`), NOT client-side `adapterProfile.ts` — this closes the ONB1 audit's "second client-side source of truth" clause **for the invite flow**. **Residual, honest:** `adapterProfile.ts` still generates the *manual* Add-Agent/Hire `mc.env` run-block client-side (a different concern — run-block env, not the `agentDefaultsPayload` contract), so the manual path's 4-runtime list is not yet registry-driven. Noted for the auditor + ONB7.
> **Invite list + revoke; join requests in the shipped Inbox.** Active invites list with **colorblind-safe** status pills (icon+label+tone) + owner-gated **Revoke** (reuses the ONB1 list/revoke routes). Pending **join requests** now surface in `InboxSection` as distinct `🤝 Agent wants to join` cards that render their machine-generated self-declared/unverified warnings — decided through the **ONB3-H1-hardened** owner-gated `applyJoinDecision` (`POST /api/approvals/:id/decide`). Reuse, no forked UI.
> **CLI (`cli/invite.mjs` → `cli/mc.mjs`).** `7ei-mc invite create --org <id> …` (Clerk-authed operator; prints the invite token + the pastable onboarding prompt; **never a claimed agent token**) and `7ei-mc onboard --invite <token> …` — the **agent-side** client (join → poll the claim until a human approves → claim once → write a **chmod-600 `mc.env`** with only `MC_BASE_URL`/`MC_AGENT_TOKEN`/`MC_WORKDIR` + flags, **never an LLM key**; the token is written to the file and **never printed**). Legacy `onboard` (Clerk-authed operator mint) is unchanged.
> **Where to look:** `web/lib/invites.logic.ts` (pure — `pickableAdapters`/`inviteStatusChip`/`buildCreateInviteBody`/`joinRequestChip`; 5 web tests), `web/app/dashboard/cockpit/InviteAgentDialog.tsx`, `web/app/dashboard/CockpitPanel.tsx` (the toolbar button), `web/app/dashboard/cockpit/InboxSection.tsx` (the 🤝 card), `cli/invite.mjs` (pure planners; 8 CLI tests), `cli/mc.mjs` (the two runners).
> **Invariant: 1221 backend tests (unchanged — no backend edit) · 11/11 evals · web build clean + 186 web tests (+5) · 15 CLI tests (+8).** **Untouched (operator/product calls): the audit/telemetry hooks — still a no-op — `allowShell`/`MC_ALLOW_SHELL`, the deployment posture, the live adapter.**
> **Flags for the ONB6 auditor:** (1) **residual client duplication** — `adapterProfile.ts` (the manual Add-Agent/Hire run-block) is still a second client-side description of the runtime list; the *invite* flow is registry-driven, the *manual* flow is not. Verify this is acceptable to leave to ONB7, or fold `adapterProfile.ts` onto `/api/adapters`. (2) The dialog is shown to all operators and the **backend** owner-gates create/list/revoke (a non-owner gets a 403 surfaced as an error) — same pattern as Add-Agent/Hire; confirm that is the intended gating rather than hiding the button. (3) The agent-side `onboard --invite` claim-poll uses the flat-404 as the "still pending" signal (there is no public request-status endpoint by design); confirm the poll/`--max-wait` behaviour is acceptable. (4) In hosted prod the join/claim are 404 (posture closed), so the agent-side flow cannot be exercised live today — it degrades honestly ("join not open / claim not open"); it is unit-tested at the planner level only.
>
> **Prior story (2026-07-15) — Epic ONB / ONB4 SHIPPED: the ONE-TIME KEY CLAIM. The onboarding CORE (ONB1–ONB4) is complete.**
> The last piece of the inverted token lifecycle: an approved agent finally gets a credential — minted only after a human approves, claimed exactly once by the party that will use it, never through an operator's clipboard or a log.
> **The claim secret is minted at JOIN, hash-only.** A `mcc_` secret (256-bit, `backend/src/services/claim.ts`) is generated when the agent submits its join request, stored as a **sha256 hash** with a TTL clamped to **≤ the invite's expiry**, and returned to the agent **exactly once** in the join response. Only the hash is in the DB (the `arturita_bindings` pattern). New nullable columns on `agent_join_requests`: `claim_secret_hash`, `claim_secret_expires_at`, `claimed_at` (idempotent ALTERs + fresh CREATE; nothing backfilled — a pre-ONB4 row has a null hash and simply cannot be claimed).
> **The claim is fail-closed with NO oracle.** Public `POST /api/agent-join-requests/:id/claim-api-key` `{claimSecret}`. **Every failure is one identical flat 404** — unknown request · not approved · **missing agent row** · wrong secret · expired · already claimed · lost race. This is a **deliberate deviation from design §3.6** (which specified distinct 403/409/410) to match the epic's no-oracle posture — **flagged for the auditor** below. The secret is matched with a **constant-time compare** (`hashesEqual`/`timingSafeEqual`, the `arturita_bindings` helper), never a SQL `=` or Node `===`.
> **Agent-must-exist honoured (ONB3 auditor #3).** The claim re-reads the agent row and fails closed if it is absent or already credentialed — it never mints against `status='approved'` alone, which also neutralises M-1's ghost-agent shape (an "approved" request whose agent insert failed cannot be claimed against).
> **Single-use = two atomic CAS.** The claim consume is one conditional UPDATE (`claimed_at IS NULL` is the compare-and-set; `claimed_at` set + secret hash **cleared** in the same statement); the `mca_` token is then minted under a **second CAS** (`api_token_hash IS NULL`) and stored **hash-only** on the agent. `services/claim.ts` owns both statements — like `invite-consume.ts`, neither may be re-implemented at a route. A **concurrency test** drives two simultaneous claims and asserts exactly one 200 + one token hash.
> **The raw token appears once, to the claimer, nowhere else.** Never persisted plaintext, never logged (`mcc_`/`mca_` redacted by `log-redaction.ts` — which already reserved `mcc_`), never in an operator UI (`operatorCanSeeClaimedKey` literal `false`). The claimed agent stays `low_trust_review` (A2 gate still applies); `claude_code` stays plan-mode.
> **Posture + guard honest.** `TOKEN_CLAIM_IMPLEMENTED` flips **true**; the landmine guard (`auth-scoping.test.ts`) now asserts the claim route exists **iff** the constant, **both directions**. Exposure follows the deployment profile exactly like the join — hosted prod answers a flat 404 without `MC_ENABLE_REMOTE_ONBOARDING`. `perIpRateLimit()` wired onto the claim (the fixed socket/`Fly-Client-IP` keying, ONB3 M-2). The join response now carries the `mcc_` secret, so ONB3 tests that asserted "no secret in the join response" were updated to ONB4 reality.
> **Where to look:** `backend/src/services/claim.ts` (the two CAS statements + the fail-closed chain), `backend/src/routes/agent-invites.ts` (the join mints the secret; the new claim route), `backend/src/tests/onb4-claim.test.ts` (12 driven tests — the fail-closed matrix + concurrency + hash-only + posture gating + redaction).
> **Invariant: 1221 backend tests (+12 ONB4) · 11/11 evals · web build clean.** **Untouched (operator/product calls): the audit/telemetry hooks — still a no-op — and `allowShell`/`MC_ALLOW_SHELL`; the live adapter.**
> **Flags for the ONB4 auditor:** (1) the **flat-404-for-every-failure** decision is a deliberate deviation from design §3.6's distinct codes — verify it is the intended no-oracle posture and that the onboarding doc's failure-state text (now "one flat 404") is honest. (2) M-1's "make it a real transaction" recommendation is **NOT** done — the claim uses two sequential CAS statements with a compensation on the (near-impossible) mint failure, not a DB transaction, because `db.transaction()` opens a second connection the `:memory:` harness cannot follow (per the ONB3 audit); the two-CAS design is single-use-correct without it, but a real transaction remains the end state. (3) M-3's schema-level `CHECK` on `secrets.scope` is **NOT** done (a CHECK on a live Turso table is a table-rebuild — unsafe as an additive migration); the code allow-list (`AGENT_RESOLVABLE_SCOPES`) shipped in ONB3 remains the boundary. (4) verify the claim-secret TTL clamp (`≤ invite.expiresAt`) and that the `mcc_` secret in the **join response body** is never logged (Fastify does not log bodies; `redactTokensInText` covers it if it ever lands in a log string).
>
> **Prior story (2026-07-15) — Epic ONB / ONB3 audit H-1 CLOSED: the board-approval gate is owner-gated on BOTH doors. The ONB4 blocker is cleared — ONB4 MAY NOW MERGE once this is on main.**
> The independent ONB3 audit (`docs/AUDIT-ONB3.md`) proved one HIGH: the generic `POST /api/approvals/:id/decide` route — the door the **shipped Inbox card** actually calls, wired into `applyJoinDecision` — had **no membership and no role check**. An authenticated user with **no `org_members` row** for the org could approve a join and **mint an agent** (200), while the dedicated owner route refused the same caller (403). The route has no `:orgId` path param, so `requireOrgRole` no-ops on it (the R-4 trap) and the MCA-85 leak guard couldn't see it. This was the blocker that had to land **before ONB4** converts "approved" into "claimable credential".
> **The fix, per the operator-approved policy:** membership is now **always required**, and the role is **data-driven from the approval type**. New pure helper `backend/src/services/approval-authz.ts` — `requiredRoleForApproval({ type, actionType })`: **owner** for the agent-minting types (`agent_join_request`, `agent_create`, and a `low_trust_review` wrapping one), **member** for every other well-formed type (approval types are open-ended — an agent's plan emits `[APPROVAL: <type>]` — so everyday cards keep their member reach and are **not** over-restricted), **owner (fail closed)** for an absent/malformed type. A new minting card type is one array entry from being gated.
> **One enforcement path, both doors:** new `enforceOrgRole()` in `middleware/rbac.ts` is the single membership+role check (`requireOrgRole` now delegates to it). Unlike the preHandler it **never skips on a missing org** — the decide route derives `orgId` **from the approval row** (not the path) and enforces the mapped role **before any decision logic runs**.
> **Proven, driven, fails-against-main:** `backend/src/tests/onb3-approval-gate.test.ts` (9 cases through the real route + real gate + real in-memory DB; identities owner/member/outsider): non-member → 403 on any decide (join + generic card, **0 agents minted**); member → 403 on `agent_join_request`/`agent_create` but **allowed** on a lower-stakes card; owner → allowed on all. Each security case fails against the pre-fix `main`. `auth-scoping.test.ts` `[ONB3-H1]` adds the route to the leak-guard net by name.
> **Invariant: 1209 backend tests (+10) · 11/11 evals · typecheck clean.** **Untouched (operator/product calls): the audit/telemetry hooks — still a no-op — and `allowShell`/`MC_ALLOW_SHELL`.** **Residual for the ONB3 auditor to re-verify:** the type→role map matches the operator's intended minting set and doesn't under-gate a money-moving type (today `wallet_tx` sits at member + its existing A2 step-up); and the pre-existing cross-tenant gap (`/api/orgs/:orgId/*` Clerk-authed but not membership-checked) is **still open, its own PR**.
> **Next: ONB4** (the one-time claim) may now proceed to merge. Carry in: ONB3 mints **no claim secret at all**, so ONB4 owns the whole credential lifecycle end to end.

> **Prior story (2026-07-14) — Epic ONB / ONB3 shipped: the JOIN REQUEST + the BOARD-APPROVAL GATE. (H-1 from its audit is now closed — see the current-story block above.)**
> An external agent can now **describe itself** to Mission Control over a public, unauthenticated endpoint, and a **human decides** — and **no token is minted anywhere in ONB3**. Public `POST /api/agent-invites/:token/join` creates **no agent and no credential**: it writes an `agent_join_requests` row (`pending_approval`) plus a card in the **shipped** tri-state approvals queue, and answers `{ requestId, status, claimPath }` — never a token, never a claim secret. Proven against a **real in-memory libSQL database**, not a mock: after a join, the `agents` table is empty.
> **The body has NO free-text field** (Zod `.strict()`, so an unknown key is refused rather than ignored): `agentName` is charset-restricted (it is rendered on a human's approval card — audit R8), `capabilities` is an **allow-listed enum array** (never prose; never empty, because `permissions: []` is *allow-all* in `governance2`), `adapterType` is a registry key, and `agentDefaultsPayload` is validated field-by-field by the registry. That is the ONB2 re-audit's carried caveat honoured literally.
> **ONB1 audit H1 is CLOSED:** the single-use consume is one **atomic conditional UPDATE** (`used_count = used_count + 1` in SQL; `used_count < max_uses` in the WHERE clause; every precondition re-asserted with the write; `rowsAffected !== 1` → the identical flat 404; the join row written **only after** the consume wins). `consumeUsePatch` is **deleted** — one consume path, and it is the safe one (`services/invite-consume.ts`). The **TOCTOU test** fails against a read-then-write consume and passes against the CAS (verified by mutation), and a multi-use invite admits exactly `maxUses`.
> **The gate:** owner-gated list/approve/reject **and** the existing Cockpit Inbox card both run **one** decision path (`applyJoinDecision`), so an approved card cannot exist without the contained agent being created; a double-approve is a **409**, never a second agent. **Approve →** the agent is created in **`low_trust_review` regardless of runtime** (invariant #3, read from `INVITE_AGENTS_ALWAYS_LOW_TRUST`) with an explicit capability list, an explicit boundary, and **`api_token_hash = NULL`**. **Reject →** nothing minted, and the agent's supplied secrets are **deleted**. Declared secrets go to the **encrypted store**, parked pre-approval in an **inert scope** (`join_request`) that no agent can resolve.
> **Rate limit:** `perIpRateLimit()` (zero call-sites since it was written) is wired onto the join at 10/min and is now a **checked hardening control** — the re-audit's M-3 condition for ever enabling remote onboarding in prod.
> **Posture, honestly:** `PUBLIC_JOIN_IMPLEMENTED` → **true** (the surface exists); new `TOKEN_CLAIM_IMPLEMENTED` → **false** (the claim does not, and `operatorCanSeeClaimedKey` stays literal `false`). `publicJoinEnabled` still requires the profile + the operator enable, so **hosted production answers the join route with a flat 404 today**. The ONB1 landmine guard got *stricter*: the join route must exist iff its constant says so, and **no claim route may exist in any scope** while `TOKEN_CLAIM_IMPLEMENTED` is false.
> **Invariant: 1195 backend tests (+28) · 11/11 evals · web build clean.** **Untouched on purpose (operator/product calls): the audit + telemetry hooks — still a no-op — and `allowShell`/`MC_ALLOW_SHELL`.**
> **Next: the independent ONB3 audit**, then ONB4 (the one-time claim). Carry into the audit: ONB3 mints **no claim secret at all**, so ONB4 owns the whole credential lifecycle end to end.

> **Epic ONB / pre-ONB3 SECURITY HARDENING — shipped (2026-07-14); superseded by ONB3 above.** #248 closed the three ONB2-audit findings that had to land **before** ONB3 wires the join route (`docs/AUDIT-ONB2.md`). **H-2 — the audit surface is no longer public:** `GET /api/orgs/:orgId/audit-log` and `GET /api/traces` were registered *inside* the hook plugins, which `src/index.ts` registers in the **public** block — so an unauthenticated caller who knew an `orgId` could read that org's audit log, and anyone could read real telemetry spans. Both query routes are split out of their plugins (`auditLogQueryRoutes`, `telemetryQueryRoutes`) and registered in the **secured** scope; the audit log is additionally `requireOrgRole('owner')`. **The guard gap is closed too:** `bootLikeIndex()` (the MCA-85 public-leak net) never registered `auditLogPlugin`/`telemetryPlugin`, which is precisely why it missed this — both plugins are now booted there, so no plugin-registered route can hide from the guard again. **H-3 — `sanitizeBody` recurses:** it only redacted *top-level* secret-shaped keys and copied object values in whole, so a secret nested in `agentDefaultsPayload` (which ONB2's own onboarding doc **tells** agents to send) would have hit `audit_logs.metadata` in plaintext the moment ONB3 lands. It now walks objects + arrays (depth-capped at 8), applies the same key test at every depth, and runs every surviving string through `redactTokensInText` — so `NIT-1`'s never-called helper is finally wired. **M-1 — the telemetry span URL is redacted** with the same `redactPath` helper (`http.url` / `http.route` / span name), so "one helper, every sink" is true. **The audit trail is now safe-to-enable-by-construction — but it is NOT enabled.** ⚠️ **H-1 remains open, deliberately, and is an OPERATOR DECISION:** both hooks are still encapsulated siblings and still fire for nothing, so `audit_logs` still records nothing. **Enabling it (step 4)** = hoist the hooks (`fastify-plugin`, or add at the root before any `register()`) — which turns on **one Turso `INSERT` per HTTP request, forever**. Do not do it without: (a) a **retention policy**, and (b) a **`SENSITIVE_METHODS`-only filter** (skip the read-only `GET` flood, which is the expensive, low-value half). A tripwire test (`audit-onb2-fix.test.ts` `[ONB2-H1]`) now fails if someone quietly hoists them. Also unchanged: the `allowShell` / `MC_ALLOW_SHELL` contradiction (audit M5) — still a product decision. Invariant: **1160 backend tests (+7) · 11/11 evals**. **Next up: ONB3** (join request + board-approval gate) — not started.

> **Epic ONB / ONB2 — shipped (2026-07-14); superseded by the hardening PR above.** [#246](https://github.com/Arturito7ei/7Ei-Mission_Control_App/pull/246) landed the **per-invite onboarding document** — the self-describing contract an external agent can drive its own onboarding from. **Pure `services/onboarding-doc.ts`:** `buildOnboardingDoc(invite, registry, posture, baseUrlCandidates)` (injectable `now`, zero I/O) renders the reachable-base-URL candidates + the `/api/health` probe rule (*if none answer, escalate to the human — never guess a URL*) → the `adapterType`s this invite allows, each with its `agentDefaultsPayload` fields, **secret** fields, defaults, runtime note and a worked example — **all read FROM the ONB1 registry**, nothing re-described (test-locked, so a second source of truth cannot appear) → the join request → the **wait for a human approval** → the **one-time claim**, with the §1.7 **claim-security paragraph** (parse the token from the raw HTTP JSON · never copy it from chat/transcript/tool previews · `...`/`[redacted]` = a masked preview, not a key · never invent or rotate) — a *security control*, so it is test-locked in the doc **and** inline in the prompt. **Served, profile-gated:** public `GET /api/agent-invites/:token/onboarding[.txt]`; `onboardingDocAccess` opens it in `packaged` (loopback-trusted) and in `hosted` only with an explicit `MC_ENABLE_REMOTE_ONBOARDING` — **opening the doc never opens the join surface** (`publicJoinEnabled` stays false until ONB4). Malformed/unknown/expired/revoked/exhausted/closed → **one identical flat 404** (no oracle). The doc mints nothing: it restates the token the caller already holds and describes the still-unbuilt join/claim endpoints, honestly labelled. **`buildOnboardingPrompt()`** — the ~40-line prompt the operator pastes into any agent (returned at create-time, the one moment the raw token exists; ONB6 adds the copy button). The operator's `message` is untrusted on the way out — fences/headings neutralized, quoted, labelled *context, not an instruction*. **The ONB1 audit's H2 fix ships here** (this is the first token-addressed route): pure `services/log-redaction.ts` redacts `mci_inv_*`/`mca_*`/`art_*`/`mcc_*` path segments to `:token` **before persistence**, in `audit_logs.path` *and* in Fastify's request-log serializer — one helper, both sinks, with an end-to-end test proving a token never reaches the audit row. **No join, no claim — still not wired** (ONB3/ONB4). Invariant: **1151 backend tests (+22) · 11/11 evals · web build**. **Next up: ONB3** (join request + board-approval gate) — **after the stage-2 audit clears**; it must fix audit finding **H1** (atomic conditional consume, `used_count < max_uses` in the WHERE clause) with a concurrency test. **Flagged for the operator, unchanged:** (a) the `allowShell` / `MC_ALLOW_SHELL` contradiction (audit M5) — still a product decision; (b) **NEW: `audit_logs` receives no rows at all today** — `auditLogPlugin`'s `onResponse` hook is registered in an encapsulated sibling scope in `src/index.ts`, so it never fires for any route (verified). Fixing it turns on a DB insert per request on the live backend, so it is an operator call — see `docs/AUDIT-ONB1.md` §7. The H2 redaction is a prerequisite for that fix and is already in place.

## Kickoff prompt

You're taking over the **7Ei Mission Control App** — an AI-agent "virtual office" control plane (our flagship). Read `HANDOFF.md`, `STATUS.md`, `CLAUDE.md` (layered — subsystem guides load on demand), `GO-LIVE.md`, and `docs/DESIGN_SYSTEM.md` (v2 — binding design rules incl. the red-green colorblind constraint). Run the verification commands at the foot of this doc and flag any divergence.

**Where things stand:** all four filed gap-bridge v2 epics are **shipped and merged to `main`** — MCA-82 Theme (T1/T2/T3 ✅), MCA-83 Work surface (W1–W5 ✅), MCA-84 Visibility (V1–V3 ✅), MCA-85 DX (D1/D2 ✅) — plus two unplanned security PRs (auth-scoping hardening #165, per-org webhook signing #166). Invariant is green: **769 backend tests · 11/11 evals · web build green · 15 web tests**. Backend healthy at v1.3.0.

> **Latest session (2026-07-08) — Epic P / P0b: promote Cockpit sections to first-class Paperclip areas (main green: web build · 80 web tests):** completes Epic-P story **P0** (the Paperclip IA re-fold). `CockpitPanel` gained an `only` **section filter** — the same composition root (one data load, existing section components) renders either the full **Operations** stack or a **single focused area**. Eight sections are promoted to their own nav areas: **Inbox · Activity** (Overview), **Goals · Org** (Workspace), **Workspaces** (Operate), **Budgets** (Delivery), **Plugins · Secrets** (Company) — **reuse, no rebuild**. Approvals stay in Inbox + Governance (no duplicate queue). Nav model gained a `section` kind + `isSection`/`navSectionKey` (9 web tests). **Current story / next up:** the remaining Epic-P *gaps* the placeholders point at (Routines web surface, Pipelines, Review-Queue page, Artifacts, adapter registry, Members/grants ledger) — see `docs/GAP-paperclip-config.md`. PLAN §0 **P0** `done` · REQUIREMENTS **FR-P0c** · STATUS · `docs/IA-paperclip-mapping.md`.

> **Prior session (2026-07-08) — Epic P / P0a: Paperclip IA re-fold, nav shell (main green: web build · 79 web tests):** reorganised the web dashboard nav to **mirror Paperclip's information architecture** (`docs/TRD-paperclip.md` §3; mapping in `docs/IA-paperclip-mapping.md`). The flat 14-item sidebar → a **folded, grouped, collapsible rail** in Paperclip's group order (**Overview · Workspace · Operate · Delivery · Company · General**), driven by a **pure, unit-tested** nav model (`web/lib/navModel.ts`, 8 tests). All 14 existing surfaces **re-homed** (Tasks→Issues, fleet→Agents, Preflight/Budgets→Costs, Governance→Operate, Memory→Learnings slot, Arturita→Board-Chat slot) — **reuse, not rebuild; everything still reachable**. Missing Paperclip areas (Routines, Pipelines, Review Queue, Artifacts, Adapters, Members & Access, Search) are honest **"coming soon" placeholders** (`PlaceholderView.tsx`) linking to the Epic-P gap plan. Per-group collapse + whole-rail icon-fold both **persist** (`localStorage`). Colorblind-safe (active = accent + bold + indicator bar), tokens only, Glassmorphism preserved. **Current story / next up: P0b** — promote the Cockpit stack (Inbox, Activity, Goals, Org, Approvals, Budgets, Secrets, Workspaces, Plugins) to first-class nav areas via a `CockpitPanel` section filter, reusing the section components. PLAN §0 **P0** · REQUIREMENTS **FR-P0a..c** · STATUS.

> **Prior session (2026-07-08) — Epic P / P2: model profiles shipped (main green: 867 backend tests · 11/11 evals · web build · 71 web tests):** the near-term **cost lever** from the Paperclip gap analysis (`docs/GAP-paperclip-config.md` §3, filed there as "P5"; PLAN §0 **P2**). Per-agent **primary + cheap model + reasoning-effort** profile (idempotent `primary_model`/`cheap_model`/`cheap_model_enabled`/`reasoning_effort` ALTERs; **defaults leave every existing agent unchanged** — null primary → `llm_model` stays primary, cheap off). A pure **routing helper** (`services/model-profile.ts` — `decideModelTier`/`planWakeModel`, 30 tests) picks **cheap** for ask-mode / low-stakes turns and **primary** for execute / heavier-reasoning turns, with an **explicit override** (`modelTierOverride[:agentId]`). It's **wired into `agent-executor`** (execute + ask paths) and **interoperates** with the F1 fallback chain (routed model = chain head when no explicit chain), the **preflight per-wake cap** (now prices the *routed* model), and **scoped budgets** (cheap tier lowers spend; cost-rates/unbounded flags unchanged). **Reasoning effort** threads through `llm-router` — Anthropic thinking budget · OpenAI `reasoning_effort` · Gemini `thinkingBudget`, **additive/guarded** (null-effort request byte-identical to before). Owner-gated `GET/PUT …/agents/:agentId/model-profile` + `GET …/available-models` (catalogue + **S8 custom models**); Governance-panel **Model profiles** section (primary/cheap/effort selects + cheap-routing toggle + colorblind-safe tier/effort badges); pure `lib/modelProfile.ts` (6 tests). **No new dangerous surface.** REQUIREMENTS **FR-P5..P8** · STATUS. Follow-ups: heartbeat/low-stakes auto-routing currently relies on the explicit override or a caller `stakes` hint (ask-mode is the only auto-cheap signal); an operator override in the UI (beyond deployConfig) is a possible next step.
>
> **Prior session (2026-07-08) — Epic P / P1: low-trust review mode shipped (main green: 836 backend tests · 11/11 evals · web build · 65 web tests):** the marquee net-new safety item from the Paperclip gap analysis (`docs/GAP-paperclip-config.md` §3). Per-agent **trust level** (`standard` default / `low_trust_review`) + **boundary set** (project/task/agent ids a contained agent may touch) + a **quarantine review queue** for gated actions (A2 taxonomy + create-agents/skills + assign-tasks). Reuses the tri-state approvals/inbox machinery (a `low_trust_review` approval row — **no parallel store**) and stacks in front of the A2 gate (a quarantined dangerous action still needs step-up — **not** a bypass). Pure `services/review.ts` + `lib/trust.ts`; idempotent `trust_mode`/`trust_boundary` ALTERs; owner-gated trust/review routes; orchestrator delegation chokepoint enforced; Governance-panel Trust section + 🛡 inbox quarantine chip (colorblind-safe). **No new dangerous surface.** PLAN §0 **P1** · REQUIREMENTS **FR-P1..P4 · NFR-9** · STATUS. Follow-ups logged in `docs/QUESTIONS-arturita-overnight.md` (auto-resume of an approved quarantined action; wiring the guard into the remaining action-producers).
>
> **Prior session (2026-07-08) — Arturita Epic J built to production (4 PRs, main green: 794 backend tests · 11/11 evals · web build · 41 web tests):** #201 the real **7Ei mark** as the orb centerpiece (`web/public/7ei-logo.jpg` + transparent vector); #202 **free-first pipeline backend** (`arturita-pipeline` resolvers + `GET/PUT /arturita/pipeline`; converse runs the LLM chain); #203 the **Config panel** chain editor in the tab; + **browser-direct local Ollama streaming** for the answer path (real tokens, on-device, $0; cloud fallback; `via` chip). **Go-live to fully local:** operator sets `OLLAMA_ORIGINS=https://app.7ei.ai` + restarts Ollama; optional Groq/Gemini keys (LLM free-tier fallbacks), NVIDIA key (hosted Chatterbox TTS); host whisper.cpp/Piper for local STT/TTS (J3). Live: `app.7ei.ai` → **Arturita** tab.
>
> **Prior session (2026-07-08) — Arturita Epic J research doc (docs-only):** operator refined the direction to **research-first, hold the build**. `docs/PRD-jarvis-tab.md` rewritten around (1) **brainstorm/conversation-partner-first** (Arturita commands the builder/executor agents only when explicitly told — interaction model §1) and (2) **free-first / self-hosted-first with configurable fallbacks per layer** (LLM local Ollama→free-tier cloud · STT whisper.cpp→Web Speech · TTS Piper/local-Chatterbox→browser; **Chatterbox resolved** = Resemble AI open-source MIT, local *or* NVIDIA-hosted). Comparison tables + config schema + fallback-trigger mechanics (reuse F1 breaker). Logged **DECISIONS S7**, PLAN §0 J2–J6 re-phased, REQUIREMENTS FR-47/48 · NFR-32. **Deeper build (J2–J6) held pending doc review.** The J1 scaffold below already shipped.
>
> **Prior session (2026-07-08) — Arturita J1, the Jarvis tab (main green: 781 backend tests · 11/11 evals · web build · 27 web tests):** a new Cockpit **"Arturita"** tab (🌸) — a Glassmorphism conversational surface with a **reactive HUD orb** (idle/listening/thinking/speaking). Default = **Arturita answers directly**; she routes into the task/agent flow **only** on an explicit build/do/delegate request or a destructive intent (decision surfaced per turn). Backend `arturita-converse` + `POST /arturita/converse` (answer via the F1 fallback chain; delegate via B3 → a `pending` task at the A2 gate); web `assistant.logic`/`AssistantOrb`/`AssistantPanel`. **No dangerous surface.** Placeholder logo `web/public/arturita-logo.svg` (one-file swap — `ARTURITA_LOGO_SRC`). Spec: `docs/PRD-jarvis-tab.md`; PLAN §0 Epic J; REQUIREMENTS FR-43..46 / NFR-30..31.
>
> **Prior session (2026-07-08) — 2 PRs:** **#196** a **zero-auth static vault-graph preview** at `https://app.7ei.ai/vault-graph.html` (self-contained render of the enriched graph, 785 nodes / 108 named communities — an operator can inspect the Memory graph *without* connecting `GITHUB_VAULT_TOKEN`; the in-app Memory-tab graph stays token-gated); **#197** **B2** Cockpit voice panel (`VoiceSection.tsx` — push-to-talk + "Arturita" wake-word, routes recognized commands through `/arturita/voice`, TTS reply via B1 `local|provider`, approval-aware feed + inline tri-state approvals, colorblind-safe, 15 pure-logic tests). Voice go-live still pends a live raw-audio STT *engine* server-side (B2 captures client-side via the Web Speech API). See `docs/PLAN-arturita.md` §0.

**This is a checkpoint before a major update.** No feature epic is currently in flight. Read the **State of the app** section below to orient, then the **Incoming major update** placeholder for the plan the operator will drop in. Do **not** start new feature work off the old open-candidates list (R4 vault RAG / R6 Sync-Registry / MCA-81 follow-ups / `app/` archive) unless the operator directs it — the major-update plan supersedes it.

Ship per convention: one PR per story, tests + evals + build green, squash-merge with `--admin`, bump `STATUS.md`, mirror the milestone to the vault. Ask before anything touching the live OpenClaw adapter, credentials, or vendor consoles.

---

## State of the app

### Full surface at a glance (2026-07-15)
The app is well past the four v2 epics documented below. What a cold agent should know is live today:
- **Arturita (voice-first chief-of-staff)** — the "Arturita" Cockpit tab + Command Center: glass HUD orb, streamed conversation (answer-directly default, explicit-only delegation), push-to-talk + wake-word voice, local-first LLM/STT/TTS pipeline (Ollama · whisper.cpp · Piper) with cloud fallback. Every dangerous action routes through the A2 approval gate. (Epics A/B/C/E/F/J.)
- **Agents experience (Epic AG)** — a Staff card grid + a six-tab agent detail page (Dashboard · Instructions · Skills · Configuration · Runs · Budget), incl. the managed instructions bundle and avatar upload.
- **Claude Code office agent (Epic CC)** — Claude Code is a first-class `runtime:'claude_code'` fleet executor: propose-and-approve by default (plan mode → `machine_exec` A2 approvals with verbatim argv), autonomous host exec OFF behind two guards + the CC5 denylist.
- **Invite-based agent onboarding (Epic ONB — COMPLETE)** — the inverted token lifecycle: invite → self-describe → human board approval → one-time key claim. See the "Epic ONB — complete" block above; operator guide `docs/RUNBOOK-agent-onboarding.md`.
- **Memory graph** — the in-app Memory tab (vault picker + d3-force graph, colorblind-safe) + the zero-auth static preview at `app.7ei.ai/vault-graph.html`; graph built from the TARCO vault via Graphify + local Ollama ($0).
- **Multi-tenant isolation** — org membership enforced surface-wide (R-4 + HIGH-1), fail-closed, with a CI leak-guard; grandfathered on `organisations.ownerId`. Model: `docs/SECURITY-posture.md` §4.
- **Audit trail** — ON for sensitive writes + the onboarding surfaces, 90-day retention, registry-driven redaction, owner-gated + tenant-isolated reads. Telemetry deliberately OFF. `docs/SECURITY-posture.md` §5.
- **Current invariant: 1263 backend tests · 11/11 evals · typecheck clean · web build green.**
- **One open product call:** per-org skills tenant-privacy — global-library skills (`skills.orgId` null) stand down from the membership gate by design (a shared library, not a tenant record); per-org skills are gated. Confirm this is the intended read or move to per-tenant skill isolation. (Flagged in `docs/AUDIT-MCA-membership.md`.)
- **Working convention:** every security-relevant story ships, then **stops for an independent audit** by a separate session (the stage→audit protocol). Audit docs are the durable record; see `docs/SECURITY-posture.md` §1.

### Architecture snapshot
Monorepo (npm workspaces), all merged to `main`, auto-deploys on merge. (The table below predates the Arturita/CC/AG/ONB epics — treat the "Full surface" list above as current; the per-workspace stacks are unchanged, only the counts grew: backend now **1263 tests**, ~60 services.)

| Workspace | Stack | Deploy | Notes |
|---|---|---|---|
| **backend/** | Node 22 · TypeScript · Fastify · Drizzle · Turso/libSQL | Fly `7ei-backend` (fra) | 44 services, ~158 API paths / 26 tag groups, 769 tests. Live: https://7ei-backend.fly.dev |
| **web/** | Next.js 15 App Router · Clerk (dev instance) | Vercel `app.7ei.ai` | **PRIMARY UI.** Dashboard = `web/app/dashboard/` + 15 `cockpit/` section components |
| **adapters/** | Python OpenClaw runtime, mac-mini installer, presets | self-hosted | External BYO-agent execution. **Live adapter is off-limits without asking.** |
| **cli/** | `7ei-mc` zero-dep Node CLI over the agent API | npm `@7ei/mc` | `openapi`, `onboard`, task/agent verbs |
| **evals/** | Orchestration eval harness (11 scenarios) | CI | Part of the merge invariant |
| **app/** | React Native (Expo) | — | **LEGACY/frozen.** Archive decision still open; do not build here. |

**Data flow:** operators drive agents from the web Cockpit → backend tasks/runs → internal agents run the LLM loop in-process (`agent-executor.ts`), external agents execute via adapters and report back. The scheduler tick (1 min) drives heartbeat, watchdog, and routine sweeps. Turso is the system of record; the Obsidian vault holds shared memory/knowledge (not code).

### The four v2 epics — what shipped (detail in STATUS.md)
- **MCA-82 Theme** (#155/#156/#159): light+dark CSS-variable themes, `ThemeProvider` + three-state toggle, colorblind-safe status helpers, glass chrome (`.mc-glass`) on sidebar/drawer/modals, ⌘K command palette, no-raw-hex invariant, WCAG AA contrast audit both modes, theme-aware PWA `theme-color`.
- **MCA-83 Work surface / failure UX** (#157/#158/#161/#168/#169): W1 recovery cards + `system_notice` comments; W2 reasoned blocker chips + next-up + sub-task cost rollups; W3 task thread wake-on-comment; W4 declarative per-task watchdogs (edge-triggered); W5 ask-mode (`work_mode` execute|ask, single-turn answer to thread).
- **MCA-84 Visibility** (#160/#162/#163): V1 heartbeat 24h timeline (per-agent lanes); V2 tri-state approvals + inbox retry rows + board read receipts; V3 per-wake preflight cap + cheap-model config validation.
- **MCA-85 DX** (#164/#167): D1 self-describing API (`/api/openapi.json` auto-collected from Fastify's `onRoute` hook + `7ei-mc openapi`); D2 `llms.txt` + `npx @7ei/mc onboard`.
- **Security (unplanned)** (#165/#166): moved org/agent route groups behind Clerk (openapi.json had exposed public tenant-scoped routes) with an `auth-scoping.test.ts` lock-down; per-org webhook shared-secret verification (`services/webhook-auth.ts`, HMAC-per-org, gated on `WEBHOOK_SIGNING_SECRET`).

### Conventions (binding)
- **Ship via squash-merge with `--admin`** — one PR per story; merge auto-deploys Fly + Vercel. Keep GitHub Actions green.
- **`npm audit` CI check is known non-blocking** — it fails on every PR; merge with `--admin` once functional checks are green.
- **Pure-helper services** — business logic lives in `backend/src/services/*.ts` as pure, individually-tested helpers (`node --test`); routes stay thin and push schemas *into* services (routes→services is the only allowed import direction — a service must never import routes). New logic → new pure helper + tests.
- **Idempotent migrations** — schema changes are idempotent `ADD COLUMN` / `CREATE TABLE ... IF NOT EXISTS` in `backend/src/db/setup.ts` (currently ~69). No migration framework; boot re-applies safely.
- **Colorblind rule** (operator is red-green colorblind, `docs/DESIGN_SYSTEM.md`): red is **never** the lone CTA and is **always** iconed; status is never color-only (icon + text + shape); active = purple, done = ✓, paused = ⏸. Enforce in every new UI surface.
- **Docs bump per PR** — update `STATUS.md` at each shipped story; mirror milestones to the vault `07-Agents/`. Keep root `CLAUDE.md` slim (<70 lines); durable state goes in `STATUS.md`, not `CLAUDE.md`.
- **Boot guard** — `backend/src/tests/boot.test.ts` fails on route collisions; the openapi integration test asserts auth scoping.

### Current surface inventory
- **Backend services (44):** executor/runtime/router/scheduler core; governance + governance2 + budget + preflight; recovery/worksurface/thread/watchdogs/askmode (W-epic); timeline/receipts/approvals/inbox (V-epic); openapi/llms-txt (D-epic); webhook-auth (security); memory/agent-memory/consolidation/vector-search; connectors/google-auth/vault-connector/telegram-bot/outbound-webhooks; hiring/orgchart/orchestrator/goals/routines/plugins/portability/secrets/workspaces/runs/tickets/telemetry/push/heartbeat-engine/document-ingest.
- **Web dashboard:** `web/app/dashboard/` root + 15 `cockpit/` sections (AgentFleet, TaskBoard, Inbox, Budgets, Preflight, Timeline, Goals, Plugins, Secrets, Workspaces, OrgChart, StatsRow, HireDialog/AddAgentWizard, shared) + TaskDrawer, RecoveryCard, CommandPalette, ThemeProvider.
- **API:** ~158 paths / 26 tag groups, self-described at `GET /api/openapi.json`. Public surfaces: health, openapi, `llms.txt`, OAuth handshake, model catalogue, inbound webhook receivers (now secret-verified), agent-api (own token). Everything tenant-scoped is behind Clerk.
- **CLI:** `7ei-mc` — `openapi` (token-free), `onboard` (Clerk-session), task/agent verbs (agent token).

### Known debt & loose ends
- **Standing items** (see STATUS.md → Standing items): Jira transitions pending (Atlassian OAuth unavailable in build sessions); `WEBHOOK_SIGNING_SECRET` not yet set (receivers open in dev — re-register integrations after setting); 7ei.ai apex DNS for the `llms.txt` mirror; vault milestone mirror owed.
- **Go-live user-only actions** (`GO-LIVE.md` §1–4): Clerk **prod** keys (`pk_live`/`sk_live`) on Vercel; Google consent-screen sensitive scopes + test user; NVIDIA key rotation → `MC_LLM_API_KEY` secret + vault GitHub PAT rotation; Mac-mini adapter move (unload laptop launchd). **Memory-bus features stay dormant until `GITHUB_VAULT_TOKEN` is set.**
- **MCA-81 follow-ups:** bound-account email shows `—` until the OAuth callback stores userinfo; agent-executor Drive search doesn't yet honor `driveScope`.
- **`app/` (Expo):** legacy/frozen — archive-vs-keep decision unresolved.
- **Clerk is a dev instance** — prod instance is a go-live prerequisite.
- **Old open-candidates** (R4 vault RAG on Turso native vectors, R6 Sync-Registry automation): parked; superseded by the incoming major-update plan unless the operator re-prioritizes.

---

## Incoming major update — Arturita (voice-first personal AI agent)

> **⚠️ DECISION LOCK 2026-07-08 — S1–S6 all CONFIRMED + two new requirements.** The operator confirmed the whole S-block and made a **material wallet-model change** (S4): from *read/prepare/never-sign, no key custody* → **bounded autonomous signing from a dedicated capped burner** (autonomous **< $100** per-tx, ≥ $100 → A2 approval; local encrypted keystore / delegated session key; **testnet only this wave, mainnet behind an explicit go flag**). Also confirmed: **S1** voice `local|provider` config (interim `provider` = Chatterbox TTS via NVIDIA API — key in the encrypted store, verified **not** committed to git; placeholder in `.env.example`), **S2** Telegram-only v1, **S3** whole-machine access + minimal self-protection denylist, **S5** push-to-talk default, **S6** broad exec (destructive still A2-gated). New **Epic H — Packaging & Distribution** (macOS signed/notarized bundle, TCC permission wizard, auto-update, fresh-machine bootstrap, v2 native iPhone app). Full detail: `docs/DECISIONS-arturita.md`; cascaded into PRD §2/§7.3/§7.4/§10/§11, PLAN (Epic E revised + Epic H new + §0 tracker), REQUIREMENTS (FR-38..42, NFR-25..27, wallet FRs).
>
> **✅ Decision-lock build wave SHIPPED (2026-07-08 pm) — 5 PRs, main green throughout (750 backend tests · 11/11 evals):** **#185** docs decision-lock · **#186** E2 wallet **policy engine** + fail-closed signing gate (autonomous < $100 / ≥ $100 → approval; **mainnet OFF**, no key in code) · **#187** F1 fallback wired into the live executor + `/health` breaker health · **#188** B1 voice `local|provider` config + Chatterbox/NVIDIA TTS + `/voice` endpoint · **#189** C1 `adapters/arturita-host/` daemon (real read/preview/undo, **destructive fail-closed behind the A2 `approved` flag**, S3 whole-machine root + self-protection denylist, verified over HTTP). **No mainnet wallet signing, no irreversible action shipped — every dangerous path stays behind A2.** Remaining (operator/go-live): load `NVIDIA_API_KEY` into the encrypted store (verified NOT in git); C2 backend→daemon proxy + `host_actions` audit; E2 live testnet signer + funded burner; B1 raw-audio STT + local engine; D-epic (Telegram bot token + `WEBHOOK_SIGNING_SECRET`); Epic H installer. See `docs/QUESTIONS-arturita-overnight.md` for the full deferral list.


> **The plan has landed: `docs/PRD-arturita.md`** — a voice-first personal AI agent persona (the operator's chief-of-staff, female counterpart to Arturito) layered onto the existing Mission Control surface. Voice from the desk (Cockpit) and remote from iPhone via Telegram (voice notes, text, files, one-tap approvals); full-but-bounded machine control via a new hardened local host; email/calendar via the existing Google connectors; and **read+prepare-only** crypto wallet awareness (MetaMask/Brave) with **human-in-the-loop signing — never auto-sign, never key custody**.
>
> **Design commitment:** it reuses existing primitives, it does not fork them — the tri-state **approval flow** gates every dangerous action (`file_destructive`/`wallet_tx`/`email_send`/`machine_exec`); **preflight/budget** bound LLM failover cost; **watchdogs**, **wake-on-comment**, **ask-mode**, **heartbeat/timeline**, the **HMAC Telegram receiver**, the scoped **secrets store**, and the **mac-mini adapter pattern** all carry through. See the PRD's §5 reuse map.
>
> **New agent? Start at `docs/ONBOARDING-arturita.md`** — cold-start read order + conventions + repo map + how to find "current story." Full doc set: **PRD** (`docs/PRD-arturita.md`, intent) → **PLAN** (`docs/PLAN-arturita.md`, stories + the §0 living status tracker) → **DECISIONS** (`docs/DECISIONS-arturita.md`, S1–S6 provisional/confirmed) → **REQUIREMENTS** (`docs/REQUIREMENTS-arturita.md`, FR/NFR master acceptance checklist) → this HANDOFF (current story).
>
> **Story-level plan is filed: `docs/PLAN-arturita.md`** — Epics A–G broken into stories (A1…G2) with scope, acceptance, dependencies, a sequencing/critical-path diagram, and a **§0 status tracker** (per-story todo/in-progress/done + PR link — update it on every merge). Epics A–G are **not yet filed as MCA issues** (Atlassian OAuth unavailable in build sessions — file interactively and back-fill the numbers).
>
> **Overnight build (2026-07-08) — safe spine + all safe non-blocked work shipped.** Nine PRs squash-merged to `main`, invariant green throughout (**707 backend tests · 11/11 evals · web build**):
> - **A1 #174** persona + command sessions + single-operator binding + `/panic` (`arturita-session.ts`; `arturita_sessions`/`arturita_bindings`/`arturita_nonces`).
> - **A2 #175** dangerous approval types (`file_destructive`/`wallet_tx`/`email_send`/`machine_exec`) + machine-rendered verbatim summaries + step-up gate (`dangerous-approvals.ts`, `decideApproval`).
> - **A3 #176** intent classifier + two-phase destructive confirm (`intent.ts`).
> - **F1 #177** LLM fallback chain + circuit breaker, cost-bounded (`llm-fallback.ts`).
> - **E1 #178** wallet read/prepare/**simulate** — never sign, no key custody (`wallet.ts`, `wallet_intents`, `arturita-wallet.ts`).
> - **C1 planner #179** host safety logic — allowlist-root/denylist/blast-radius/canonicalization/undo — **fail-closed** (`HOST_EXECUTION_ENABLED=false`, S3-gated), no daemon, no FS writes.
> - **B1 helpers #180** voice pure core (STT confidence gating, wake-word, provider fallback ordering) (`voice.ts`).
> - **B3 #181** ask-vs-execute routing from voice (`arturita-routing.ts`, reuses `askmode`/`intent`/`thread`).
> - **F2 #182** degraded/offline queue-replay (exactly-once, nonce-guarded) + watchdog specs (`arturita-resilience.ts`).
>
> **Current story: none in flight — all remaining stories are operator-gated.** Everything left needs a `PROVISIONAL` S-decision confirmed or a live surface: **B1-full/B3-endpoint** need S1 (STT/TTS keys); **B2** needs S5; **C2/C3** must not ship real host write/exec until **S3**/**S6** are `CONFIRMED`; **D1/D2** need S2 + `WEBHOOK_SIGNING_SECRET`; **E2** needs **S4** (WalletConnect id + test wallet). **Safety rule held all night: no dangerous surface shipped — every dangerous path is behind the A2 approval gate + a fail-closed default.** **First read: `docs/QUESTIONS-arturita-overnight.md`** (the morning decision list). Confirm S1–S6 in `docs/DECISIONS-arturita.md` to unblock the next wave.
>
> **Resolve first (PLAN §3) — decisions/spikes that block the first stories of B/C/D/E, none requiring feature code:** **S1** STT/TTS provider (blocks B1; rec: local-first + optional cloud), **S2** iPhone surface — Telegram-only vs PWA (blocks D framing; rec: Telegram-only for v1), **S3** mac-control adapter approach — custom hardened daemon vs wrap an MCP (blocks C1; rec: custom daemon for the write/destructive path), **S4** WalletConnect integration proof (blocks E2; provider already decided in PRD §12), **S5** wake-word (rec: push-to-talk default), **S6** `machine_exec` allowlist scope (rec: empty at launch). Epic A has **no** external blockers — start it while the spikes run in parallel.
>
> **Conventions unchanged:** pure helpers + `node --test`, idempotent migrations, colorblind rules (DESIGN_SYSTEM v2), ship-via-squash-admin, docs bump per PR, invariant green each merge. Go-live gates in PRD §11 extend `GO-LIVE.md` (`WEBHOOK_SIGNING_SECRET` becomes a hard prerequisite for Telegram remote control).

---

## Shared memory / vault
Canonical Obsidian vault: `/Users/artutito/7Ei-MC_TARCO` (repo `Arturito7ei/7Ei-MC_TARCO`, content under `vault/`). Latest status doc: `vault/07-Agents/STATUS-Mission-Control-2026-07-02.md` (mirror the v2-epics-complete milestone here). Plan of record: `vault/01-Projects/Paperclip-Gap-Analysis-v2-2026-07-06.md`. Connectors: Jira via Atlassian Rovo OAuth (cloudId `5dadc567-085a-4cd8-99a3-c0bd9886fee9`, projects MCA + OS — **OAuth unavailable in build sessions**), Slack, Gmail. **7Ei_OS PR #3** (protocols: MC as primary coordination + memory bus) — after merge, re-sync the `Protocols/7Ei_OS/` vault mirror.

## Verify before trusting the above

```bash
cd /Users/artutito/Developer/7Ei-Mission_Control_App
git log --oneline -15
cd backend && npm test && npm run evals   # expect 769 tests + 11/11
cd ../web && npm run build
curl -s https://7ei-backend.fly.dev/api/health   # expect 200, db: connected, scheduler: running
```
