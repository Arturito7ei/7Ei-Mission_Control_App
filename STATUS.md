# 7Ei Mission Control — Status

_Last updated: 2026-07-15 (**Epic H — H0 packaging spike BUILT: an Electron shell (`apps/desktop/`) boots the packaged/loopback mesh — Fastify backend + Next standalone UI — on a local libSQL file, verified from the built unsigned `.app`; Electron confirmed for H1**; prior: **Epic ONB COMPLETE — ONB7 docs/go-live: the onboarding runbook, a consolidated go-live checklist, and the security-posture summary (docs-only)**; prior: **Multi-tenant membership hardening — HIGH-1 closed: the ~25 top-level RECORD routes are now membership-gated too; the gate is truly surface-wide**; prior: **org membership enforced surface-wide (the R-4 fix)**; prior: Epic ONB — **audit M5: the shell-execution default is now OFF for new agents, existing grandfathered**; prior: audit H-1 the audit trail ENABLED for sensitive writes with 90-day retention; prior: ONB6 the create-invite UI + CLI [onboarding usable end-to-end]; prior: ONB4 the one-time key claim [core ONB1–ONB4 complete]; prior: ONB3 audit H-1 closed; prior: ONB3 the join + board-approval gate; prior: ONB2 the onboarding document; prior: ONB1 the onboarding spine) · auto-maintained by the build agent (bumped at each story/phase)._

**Latest (2026-07-15) — ✅ EPIC H · H0 PACKAGING SPIKE BUILT & LANDED. The Electron bundling approach is de-risked end-to-end; the packaged mesh boots on a local file DB inside a built (unsigned) `.app`. STOPPED before H1 (per the spike scope).**

H0 was a de-risk spike to prove the `docs/DESIGN-packaging.md` recommendation before H1 commits to it. It is built in **`apps/desktop/`** (the seed for H1, not throwaway) and was verified **from the built `.app`**, not just `npm run`.

- **What it proves.** Launching `dist/mac-arm64/7Ei Mission Control.app` starts, as children of Electron's own Node: (1) the **Fastify backend** with `MC_DEPLOYMENT_PROFILE=packaged`, `DATABASE_URL=file:…/mc.db` (idempotent migrations run on first boot), bound `127.0.0.1:8787`; (2) the **Next.js UI** from its `standalone` build on `127.0.0.1:8788`; (3) a **BrowserWindow** on the local Next server once `/api/health` is green. `/api/health` returns `{status:ok, db:connected}` from the built app in ~3 s. Unsigned `.app` (619 MB) + `.dmg` (195 MB) via `electron-builder`. Runs with **no dev toolchain** on the host (`tsx` + backend `node_modules` incl. the libSQL native addon + the Next standalone all ship in `Contents/Resources/`, unpacked).
- **Top de-risk findings.** (a) `@clerk/fastify`'s `clerkPlugin` installs a **global** hook that 500s *every* request (incl. public `/api/health`) with no publishable key — fixed by gating it on `CLERK_PUBLISHABLE_KEY` (the H6 direction; the packaged profile is an auth *bypass*, **not** security-complete until H6). (b) The **libSQL native addon loads cleanly under Electron's Node 20 ABI** — the top native-module risk is a non-issue. (c) `next build` doesn't copy `.next/static`/`public` into standalone — the build step does. (d) Plain `tsc` output isn't runnable (`moduleResolution: bundler`) → the spike forks source via `tsx`; **H1 should compile/bundle + prune** to shrink the 619 MB `.app`.
- **Decisions confirmed for H1:** **Electron** (no Tauri bake-off needed) · Node-child via `tsx`-now/compiled-bundle-H1 · web = Next standalone-as-child (§2.3 option a) · pure local libSQL file · `electron-builder.yml` structured so signing is a **config addition** (H1/Apple-account-gated), not a rearchitecture.
- **Minimal prod edits (additive, hosted no-op, verified):** `backend/src/index.ts` (clerk-plugin gate + `HOST` env for loopback bind) · `web/next.config.ts` (`output:'standalone'` behind `DESKTOP_BUILD=1`). **Hosted proven untouched** — Fly backend + Vercel web build/deploy exactly as before.
- **Explicitly NOT built (later Epic H):** signing/notarization (H1), Tray + TCC wizard (H1/H2), auto-update (H3), config/secret bootstrap + per-install Keychain keys (H4), real single-operator loopback auth + fail-closed boot (H6).
- **Where to look:** `apps/desktop/` (shell, `main.cjs`, build script, `electron-builder.yml`, README), `docs/DESIGN-packaging.md` §15 (spike results).

**Invariant green: 1263 backend tests · 11/11 evals · backend + web typecheck clean · hosted web build emits no standalone. `allowShell`, adapters, audit hooks, and the deployment posture untouched. Stopped before H1.**

---

**Prior (2026-07-15) — ✅ EPIC ONB COMPLETE (ONB7 — docs / go-live, docs-only, no feature code). Invite-based agent onboarding is documented, legible, and go-live-ready.**

Epic ONB (invite-based agent onboarding) is finished. The security core (ONB1–ONB4), the create-invite UI + CLI (ONB6), and the audit-trail enablement all shipped and were independently audited; ONB7 wraps the epic with documentation that makes the whole state legible to a fresh agent or operator.

- **`docs/RUNBOOK-agent-onboarding.md`** — the operator's end-to-end guide: per-runtime invite → self-onboard → board-approval → one-time claim for each **available** runtime (`openclaw_local`, `claude_code`, `cursor`, `openai_generic`), the create-invite UI + `7ei-mc invite create`/`onboard --invite` CLI, an **ON-vs-OFF-today** table, the two deployment profiles (packaged/loopback vs hosted-with-`MC_ENABLE_REMOTE_ONBOARDING`), the exact go-live switches, and troubleshooting.
- **`GO-LIVE.md` consolidated** into ONE authoritative 16-item checklist (was scattered §1–8): Fly secrets (`SECRETS_ENC_KEY`/`RUN_TOKEN_SECRET`/`WEBHOOK_SIGNING_SECRET`), remote-onboarding enable, `GITHUB_VAULT_TOKEN`, Telegram bot token, local Ollama/Whisper/Piper stack, **wallet mainnet (`WALLET_MAINNET_ENABLED` — testnet-only today)**, apex DNS for `llms.txt`, audit-trail tuning (now ON), Jira epics still unfiled, plus the Clerk/Google/rotate/Mac-mini/Claude-Code items — each with status, risk, effort.
- **`docs/SECURITY-posture.md`** — the whole security model in one place: the inverted-token onboarding lifecycle, the **four ONB invariants**, low-trust-by-default, the multi-tenant membership gate + leak-guard, the audit trail, the recurring fail-closed patterns, wallet no-custody-until-enabled, and the machine-exec/shell posture — with the audit index.
- **ONB5 explicitly DEFERRED** (optional, gateway/webhook-only): the four available runtimes need no server-side reachability check; ONB5 is a precondition only for the still-unbuilt gateway/webhook runtimes, so deferring it ships nothing insecure. Build it fail-closed alongside the first gateway runtime.
- **Master docs refreshed:** PLAN §0 marks ONB1–4/6/7 + audit-H1 `done`, ONB5 `deferred`, and fills the placeholder PR numbers (ONB3 #250/#252, ONB6 #255, audit-trail #257/#258/#259); HANDOFF's state-of-the-app updated; vault `07-Agents` milestone mirrored.
- **Not touched:** any feature code, adapters, or the deployment posture — ONB7 is documentation only.

**Invariant unchanged: 1263 backend tests · 11/11 evals · typecheck clean (no code touched). The stage→audit protocol and the one open product call (per-org skills tenant-privacy) are noted as the working convention.**

---

**Prior (2026-07-15) — MEMBERSHIP HARDENING, HIGH-1 CLOSED: the ~25 top-level RECORD routes are now membership-gated. The gate is truly surface-wide, fail-closed. Stopped for the re-audit.**

The independent audit of the R-4 fix (`docs/AUDIT-MCA-membership-review.md`) found it was **not** surface-wide: `resolveRequestOrg` knew only `:orgId`/`:agentId`/`:taskId`, so ~25 record routes whose org lived in another param (`:projectId`, `:goalId`, `:itemId`, `:skillId`, generic `:id` for secrets/budgets/plugins/workspaces/attachments/watchdogs/scheduled/webhooks/policies/revisions) resolved `scoped:false` → the gate stood down → any logged-in user could read/delete another org's secret, confidential knowledge doc, project, policy, webhook, agent revision. Now closed.

- **URL-PREFIX → owning-table derivation.** `resolveRequestOrg` gained `RECORD_ORG_ROUTES` (`middleware/rbac.ts`): 14 route-prefix→table entries deriving the org from the record (the generic `:id` is disambiguated by route PREFIX, not param name). Each **fails closed** — missing/foreign record → 403, exactly like the `:agentId`/`:taskId` tail. `:orgId`/`:agentId`/`:taskId` precedence unchanged; backward compatible (no `routeUrl` → prefix tier skipped).
- **FLAGGED + handled — global vs per-org skills.** `skills.orgId` is nullable: a `null` row is a shared **global-library** skill → the gate **stands down** (readable, not a tenant record); a per-org custom skill (`orgId != null`) **is** membership-gated; a missing skill fails closed. The global-library collection routes (`GET/POST /api/skills`, `sync`, `obsidian-sync`) are org-agnostic and on the leak-guard's EXEMPT allowlist.
- **Widened leak-guard — the hole can't reopen.** `membership-scoping.test.ts` replaced the `/api/orgs/:orgId/*` filter with an **allowlist-negation sweep over ALL secured routes**: every secured route must resolve an org OR be on a short justified EXEMPT allowlist (16 routes); a new secured route that resolves `scoped:false` unlisted FAILS the test. A self-test proves the guard has teeth; a per-route behavioural sweep 403s a non-member on every non-exempt route; targeted tests drive a non-member at each real ORG record → 403 and a member → non-403. The boot now also registers **`webhookRoutes`** — it was **missing** from the original boot (the blind spot that hid the webhook leak). Red-proof: disabling the fix reds the sweep + the record-route proofs.
- **Not touched:** adapters, telemetry, `allowShell`, the agent-token API (`/api/agent/*`, separate scope — proven unaffected), the public onboarding surface (invite/join/claim — untouched). No invariant weakened.

**Invariant green: 1263 backend tests (+10) · 11/11 evals · typecheck clean. Full design + prefix→table map + flagged edge: `docs/AUDIT-MCA-membership.md`. Stopped for the re-audit.**

---

**Prior (2026-07-15) — MULTI-TENANT MEMBERSHIP HARDENING: org membership is now enforced SURFACE-WIDE (the R-4 fix). Stopped for the independent audit.**

The pre-existing cross-tenant gap the ONB2/ONB3 audits kept surfacing: the whole `/api/orgs/:orgId/*` surface was Clerk-AUTHENTICATED but not membership-CHECKED — only ~35 of ~159 org-scoped routes ran `requireOrgRole`, so any logged-in user could act on ANY org by swapping `:orgId`. Now closed with ONE shared, tested mechanism.

- **ONE scope-level gate.** `requireOrgMembership` (a `preHandler` on the whole Clerk-secured scope, `index.ts`) enforces baseline `member` for the org every request targets — resolved from `:orgId`, or derived from the `:agentId`/`:taskId` **record** where the path has no org id (the R-4 tail). It reuses the single `enforceOrgRole` core; it **fails closed** (an id that resolves to no row → 403, never a skip); it skips `OPTIONS` (CORS). Because it rides the scope, a NEW org route is covered the moment it registers — it can't be added ungated. Stricter per-route `requireOrgRole('owner')` gates still layer on top.
- **Grandfathered — the operator is never locked out.** `enforceOrgRole` now honours `organisations.ownerId` as an implicit **owner** when there's no `org_members` row (nothing backfilled pre-membership orgs). Every existing owner keeps full access with zero migration; only a genuine non-member/wrong-org request newly 403s. Driven test proves a legacy rowless owner still gets in.
- **Two record-derived routes hardened in-handler** (`multi-org.ts`): `GET /api/users/:userId/orgs` is now **self-only**; `transfer`/`clone` now also membership-check the **target** org (the gate covered the source).
- **Exempt set (precise + tested):** the agent-token API (`/api/agent/*`, token auth, separate scope — proven unaffected), the public onboarding surface (invite/join/claim — token-addressed, mint-nothing), `arturita/panic` (session-token authed in-handler), `auth/google` (OAuth handshake — pre-existing public), health/ready/openapi. None changed.
- **Leak-guard extended.** `membership-scoping.test.ts` is behavioural: it enumerates EVERY `/api/orgs/:orgId/*` route (>80) and drives a non-member at each → all 403; member/owner positives; record-derived + grandfather + agent-API-unaffected cases. Plus `rbac-membership.test.ts` unit-pins the primitives. The MCA-85 guard's boot now mirrors `index.ts` (same hook).
- **Not touched:** adapters, telemetry, `allowShell`/`MC_ALLOW_SHELL`, the live adapter, the deployment posture. No invariant weakened.

**Invariant green: 1253 backend tests (+18) · 11/11 evals · typecheck clean. Full design + route inventory + flagged edge cases: `docs/AUDIT-MCA-membership.md`. Stopped for the independent audit.**

---

**Prior (2026-07-15) — Epic ONB / audit M5 RESOLVED: the SHELL-EXECUTION DEFAULT is now OFF for new agents; existing agents (incl. the live OpenClaw ops agent) are GRANDFATHERED. Stopped for the independent audit.**

The operator took the M5 product call: align the shell-execution default to OFF, without breaking the live ops agent. The ONB1 audit flagged the drift — the server registry declares `allowShell: false` ("off unless the operator opts in") while the Add-Agent/Hire wizard's paste-able run-block emitted `MC_ALLOW_SHELL=1`, so every UI-onboarded agent got shell-on, contradicting the safe default. Now the two agree on OFF.

- **Enforcement is CLIENT-ONLY — confirmed end to end.** `MC_ALLOW_SHELL` is read at `adapters/openclaw/mc_adapter.py:31` into the local `ALLOW_SHELL`, which gates `shell_execute` (:105) and the `llm_execute` tool loop (:175). **No server/registry code gates shell or `machine_exec` from an agent's `allowShell`** — the registry field is only the `agentDefaultsPayload` contract's declared default (validated + stored, never consulted to block). So the wizard default is the *only* lever, and flipping it **cannot** retroactively disable a running agent.
- **New agents ship shell-OFF.** `web/lib/adapterProfile.ts`: the openclaw profile no longer hardcodes `MC_ALLOW_SHELL=1`. `mcEnv`/`runBlock` now append an explicit `MC_ALLOW_SHELL=0` for shell-capable runtimes (openclaw, custom) — matching the registry — with an operator opt-in (`{ allowShell: true }` → `=1`) surfaced as an **advanced, off-by-default checkbox** in both the Add-Agent wizard and the Hire dialog. Cursor/Claude Code never emit the flag (plan-mode / inbox model — unchanged).
- **Existing agents GRANDFATHERED — automatic.** The live OpenClaw ops agent runs from its own `mc.env` on its host (`~/.openclaw/mc-adapter/`), which the wizard never rewrites; the adapter reads the flag from that local env; and there is no server gate to flip. Its `MC_ALLOW_SHELL=1` is untouched → it keeps running. **No server gate was touched.**
- **Registry ↔ wizard reconciled.** Both now declare shell-off as the default (registry `allowShell: false` was already correct + test-locked at `adapter-registry.test.ts:65`; the wizard now matches). Tests lock: a new agent's run-block does NOT enable shell; an explicit opt-in does; non-shell runtimes never emit the flag; the wizard default agrees with the registry default.
- **Not touched:** the live adapter install, telemetry, the deployment posture, any server-side gate. No invariant weakened, no posture constant flipped.

**Invariant green: 1235 backend tests (unchanged — no backend edit) · 11/11 evals · web build clean · adapterProfile suite 13 tests (+6). Stopped for the independent audit.**

---

**Prior (2026-07-15) — Epic ONB / audit H-1: the AUDIT TRAIL is now LIVE for sensitive writes — the previously-dead subsystem records, safely and scoped. Stopped for the independent audit.**

The operator took the H-1 decision (GO-LIVE §7 option **b**): enable the audit trail for the sensitive half only, with retention. This turns on a previously-dead subsystem that writes to hosted Turso — done conservatively, behind every prerequisite the ONB2 audits demanded.

- **HOISTED so it actually fires.** The audit hook was a no-op because it lived inside an encapsulated `app.register()` child (H-1: it recorded **zero** rows for any sibling route). It is now installed on the **root instance** — a bare `auditLogPlugin(app)` before any `register()`, mirroring the already-correct `onRoute` hook — so its `onResponse` fires for every route. `audit-onb-enable.test.ts` **proves it records for a sibling** (the exact thing the ONB2 audit proved it did not). **Telemetry was deliberately left OFF** — a separate concern (in-memory span ring buffer, no Turso writes); its `telemetryPlugin` stays encapsulated.
- **SCOPED to sensitive only — no GET flood.** A pure, tested `shouldAudit(method, path)` records every mutating method (POST/PUT/PATCH/DELETE) **plus** the onboarding/invite/join/approval surfaces (`/agent-invites`, `/agent-join-requests`, `/approvals` — including security-relevant GET reads of the token-addressed doc), and **skips** the read-only dashboard-poll flood. So this is **not** one Turso INSERT on every request — only on the sensitive, mostly-low-volume half. The insert is fire-and-forget (`.catch()`-swallowed): it can never add latency to or fail the request it records.
- **RETENTION — bounded, not unbounded.** `services/audit-retention.ts`: rows older than `MC_AUDIT_RETENTION_DAYS` (default **90**; junk/0/negative safe-defaults to 90 so a typo can't wipe the table) are pruned on a daily scheduler tick (~03:00 UTC). Cutoff math, env parsing, and the daily gate are pure + tested; a `created_at` index (`setup.ts`) serves both the prune and the query `ORDER BY`.
- **REDACTION re-proven THROUGH the live hook.** End-to-end test: a real join request carrying a nested `apiKey`, a registry-declared `webhookAuthHeader` bearer, and an `mci_inv_` token in the path is driven through the now-live hook; the persisted row has the **path redacted to `:token`** and **no secret/token anywhere** (`buildAuditRow` → `redactPath` + recursive registry-driven `sanitizeBody`).
- **Reads stay gated.** `GET /api/orgs/:orgId/audit-log` and `…/traces` remain Clerk + `requireOrgRole('owner')` in the secured scope (from #248). Enabling writes did **not** open reads — a test boots both query routes and asserts 401 without a session.
- **Tripwire REWORKED, not deleted.** The `[ONB2-H1]` guard used to fail if the hook was hoisted (it held the hook shut). It now guards the **safety envelope**: it PERMITS the hoisted enablement, requires telemetry to stay off, and fails if a future change enables the hook **without** the redaction/sanitize/gating prerequisites (behavioral checks on `buildAuditRow` + `shouldAudit`).
- **Not touched:** `allowShell`/`MC_ALLOW_SHELL`, the live adapter, the deployment posture, adapters. No invariant weakened, no posture constant flipped.

**Invariant green: 1232 backend tests (+11) · 11/11 evals · typecheck clean · web build clean. Stopped for the independent audit.**

---

**Prior (2026-07-15) — Epic ONB / ONB6: the CREATE-INVITE experience — onboarding is now usable end-to-end. One paste onboards an agent. Stopped for the independent ONB6 audit.**

The operator-facing half of the epic. No backend change — the ONB1–ONB4 contract was already complete; ONB6 surfaces it.

- **Create-invite dialog (Cockpit → "✉ Invite an agent").** Owner-gated. Pick the allowed runtime(s), single-use (default) vs bounded multi-use, a TTL; on create the dialog shows **exactly once** the invite token + the **copy-able onboarding prompt** (`buildOnboardingPrompt`, ~40 lines) with copy buttons + the doc URL, and says plainly they are shown once and unrecoverable. When hosted-join is closed it says so honestly.
- **The raw claimed agent key is NEVER shown.** The dialog reveals the invite token and the prompt — never a claimed agent token (there is no code path to one). `operatorCanSeeClaimedKey` stays `false`; the claimed key is minted only when the joining agent claims it and only the claimer ever sees it.
- **The adapter picker renders FROM the server registry** (`GET /api/adapters`), not client-side `adapterProfile.ts` — `pickableAdapters` = `invitable && available`, never `internal`. This closes the ONB1 audit's "second client-side source of truth" clause **for the invite flow**. (Residual: `adapterProfile.ts` still generates the *manual* Add-Agent/Hire `mc.env` run-block client-side — a different concern — so the manual path's 4-runtime list is not yet registry-driven; noted for the auditor + ONB7.)
- **Invite list + revoke; join requests in the Inbox.** Active invites list with **colorblind-safe** status pills (icon+label+tone) + owner-gated **Revoke** (reuses the ONB1 routes). Pending **join requests surface in the shipped Inbox** as distinct `🤝 Agent wants to join` cards that now render their machine-generated self-declared/unverified warnings — decided through the ONB3-H1-hardened owner-gated `applyJoinDecision` (reuse, no forked UI).
- **CLI.** `7ei-mc invite create` (Clerk-authed operator; prints the invite token + the pastable prompt; **never a claimed agent token**) and `7ei-mc onboard --invite <token>` — the **agent-side** client (join → poll the claim until a human approves → claim once → write a **chmod-600 `mc.env`** with only `MC_BASE_URL`/`MC_AGENT_TOKEN`/`MC_WORKDIR` + flags, **never an LLM key**; the token is written to the file and **never printed**). Legacy `onboard` unchanged.
- **Not touched:** the audit/telemetry hooks (still a no-op), `allowShell`/`MC_ALLOW_SHELL`, the live adapter, the deployment posture. No backend change, no invariant weakened. **Stopped after ONB6 for the independent audit — ONB5/ONB7 not started.**

**Invariant green: 1221 backend tests (unchanged — no backend edit) · 11/11 evals · web build clean + 186 web tests (+5 ONB6) · 15 CLI tests (+8 ONB6).**

---

**Prior (2026-07-15) — Epic ONB / ONB4: the ONE-TIME KEY CLAIM — the credential lands, once, after approval. The onboarding core (ONB1–ONB4) is complete:**

The last piece of the inverted token lifecycle. An approved agent finally gets a credential — minted only after a human approves, claimed exactly once by the party that will use it, never through an operator's clipboard or a log.

- **The claim secret is minted at JOIN, hash-only.** A `mcc_` secret (256-bit, `services/claim.ts`) is generated when the agent submits its join request, stored as a **sha256 hash** with a TTL clamped to ≤ the invite's expiry, and returned to the agent **exactly once** in the join response. Only the hash is in the DB — a read yields no working secret (the `arturita_bindings` pattern).
- **The claim is fail-closed with NO oracle.** Public `POST /api/agent-join-requests/:id/claim-api-key` `{claimSecret}`. **Every failure is one identical flat 404** — unknown request · not approved · **missing agent row** · wrong secret · expired · already claimed · lost race. (Deliberate hardening beyond design §3.6's distinct 403/409/410, matching the epic's no-oracle posture — flagged for the auditor.) The secret is checked with a **constant-time compare**, never a SQL `=` or a Node `===`.
- **The agent-must-exist caveat is honoured (ONB3 auditor #3).** The claim re-reads the agent row and fails closed if it is absent or already credentialed — it never mints against `status='approved'` alone, which also neutralises M-1's ghost-agent shape.
- **Single-use is two atomic CAS statements.** The claim consume is one conditional UPDATE (`claimed_at IS NULL` is the compare-and-set; `claimed_at` set + secret hash **cleared** in the same statement); the `mca_` token is then minted under a **second CAS** (`api_token_hash IS NULL`) and stored **hash-only** on the agent. A concurrency test proves two simultaneous claims yield **exactly one token**.
- **The raw token appears once, to the claimer, nowhere else.** Never persisted plaintext (hash-only on the agent), never logged (`mcc_`/`mca_` redacted by `log-redaction.ts`), never in an operator UI — `operatorCanSeeClaimedKey` stays literal `false`. The claimed agent stays `low_trust_review` (the A2 gate still applies to dangerous actions); `claude_code` stays plan-mode.
- **Posture + guard honest.** `TOKEN_CLAIM_IMPLEMENTED` flips **true**; the landmine guard now asserts the claim route exists **iff** the constant (both directions). Exposure follows the deployment profile exactly like the join — hosted prod answers a flat 404 without `MC_ENABLE_REMOTE_ONBOARDING`. `perIpRateLimit()` wired onto the claim (the fixed socket/`Fly-Client-IP` keying).
- **Not touched:** the audit/telemetry hooks (still a no-op — operator's cost call), `allowShell`/`MC_ALLOW_SHELL`, the live adapter. No invariant weakened. **Stopped after ONB4 for the independent audit — ONB5/ONB6 not started.**

**Invariant green: 1221 backend tests (+12 ONB4, ONB3 tests updated) · 11/11 evals · web build clean.**

---

**Prior (2026-07-15) — Epic ONB / ONB3 audit H-1 CLOSED: the board-approval gate is now owner-gated on BOTH doors — the ONB4 blocker is cleared:**

The independent ONB3 audit (`docs/AUDIT-ONB3.md`) proved one HIGH: the generic `POST /api/approvals/:id/decide` route — the door the shipped Inbox card actually calls — carried **no membership and no role check**. An authenticated user with **no `org_members` row** could approve a join and mint an agent (200), while the dedicated owner route refused the same caller (403). The route has no `:orgId` path param, so `requireOrgRole` no-ops on it (the R-4 trap) and the MCA-85 leak guard couldn't see it. This was the blocker that had to land **before ONB4** turns "approved" into "claimable credential".

- **Membership is now always required, and the role is data-driven from the approval TYPE.** New pure helper `services/approval-authz.ts` — `requiredRoleForApproval()`: **owner** for the agent-minting types (`agent_join_request`, `agent_create`, and a `low_trust_review` wrapping one), **member** for every other well-formed type (approval types are open-ended — an agent's plan emits `[APPROVAL: <type>]` — so everyday cards keep their member reach and are not over-restricted), and **owner (fail closed)** for an absent/malformed type. A new minting card type is one array entry from being gated; it cannot drift open.
- **One enforcement path, both doors.** New `enforceOrgRole()` in `middleware/rbac.ts` is the single membership + role check; `requireOrgRole` delegates to it. Unlike the preHandler it **never skips on a missing org** — the decide route derives `orgId` **from the approval row** (not the path) and enforces the mapped role **before any decision logic runs**. So the dedicated owner routes and the generic route share one check, and a future card type can't slip in ungated.
- **Proven, driven, fails-against-main.** `tests/onb3-approval-gate.test.ts` (9 cases through the real route + real gate + real in-memory DB, three real identities): non-member → 403 on any decide (join card + generic card, **zero agents minted**), member → 403 on `agent_join_request`/`agent_create` but **allowed** on a lower-stakes card, owner → allowed on all. Each security case fails against the pre-fix `main`. `auth-scoping.test.ts` `[ONB3-H1]` adds the route to the leak-guard net by name.
- **Not touched:** the audit/telemetry hooks (still a no-op — operator's cost call), `allowShell`/`MC_ALLOW_SHELL`, the live adapter. No invariant weakened, no posture constant flipped. The pre-existing cross-tenant gap (`/api/orgs/:orgId/*` is Clerk-authed but not membership-checked) is **still open, still its own PR**.

**Invariant green: 1209 backend tests (+10) · 11/11 evals · typecheck clean. H-1 CLOSED → ONB4 may now merge.**

---

**Prior (2026-07-14) — Epic ONB / ONB3: the JOIN REQUEST + the BOARD-APPROVAL GATE — a human decides before any credential exists:**

The story that inverts the token lifecycle. An external agent can now **describe itself** to Mission Control, and a human decides. **No token is minted at any point in ONB3** — that is ONB4, deliberately unbuilt.

- **Public `POST /api/agent-invites/:token/join`** — the first unauthenticated *write* in the app. It creates **no agent and no credential**: it creates a row in the owner's approval queue. The response is `{ requestId, status, claimPath }` — never a token, never a claim secret. Proven against a **real database**: after a join, the `agents` table is empty.
- **The body is strictly typed, registry-validated, and has NO free-text field** (`.strict()` — an unknown key is refused, not ignored). `capabilities` is an **allow-listed enum array**, not prose; `agentName` is charset-restricted (it is rendered on a human's card); `agentDefaultsPayload` is validated field-by-field against the adapter registry. This is the ONB2 re-audit's carried caveat honoured literally: a third-party secret in free text under an innocuous key would reach `audit_logs.metadata` unredacted, so **no such field exists**.
- **ONB1 audit H1 — CLOSED.** The single-use consume is **one atomic conditional UPDATE**: `used_count = used_count + 1` computed **in SQL**, `used_count < max_uses` **in the WHERE clause**, every precondition re-asserted with the write, `rowsAffected !== 1` → the identical flat 404, and the join row written **only after** the consume wins. The advisory `consumeUsePatch` is **deleted** — there is now exactly one consume path and it is the safe one. A **TOCTOU test** (two callers that have both already read `used_count = 0`) fails against a read-then-write consume and passes against the CAS; a multi-use invite admits exactly `maxUses` and no more.
- **The board-approval gate reuses the SHIPPED tri-state queue** (`approval_requests`, type `agent_join_request`) — no parallel store. The card is **machine-generated**, labels every agent-authored value *self-declared, unverified*, and shows **no secret value** (only which secret fields were supplied, by name). Approving it in the existing Cockpit Inbox and via the owner API run the **same** decision path, so an approved card cannot exist without the agent actually being created. A double-approve is a **409**, never a second agent.
- **On approve:** the agent is created **contained** — `low_trust_review` **regardless of runtime** (invariant #3, read from the constant rather than re-derived), an explicit non-empty capability list (an empty one is *allow-all* in governance2 — that is the footgun), an explicit boundary — and with **`api_token_hash = NULL`**. **On reject:** nothing is minted and the secrets the agent supplied are **deleted**.
- **Declared secrets → the encrypted store** (AES-256-GCM), parked pre-approval in an **inert scope** (`join_request`) that `resolveSecretsForAgent` does not resolve, so no agent can read them; approval re-scopes them to the agent that now exists. The join row persists only the key **names**.
- **`perIpRateLimit()` finally has a caller** (10/min on the join) — the re-audit's **M-3**: rate limiting must exist *before* remote onboarding is ever enabled in prod. It is now a **checked hardening control**, so a regression closes the posture rather than opening an unlimited door.
- **The posture stays honest.** `PUBLIC_JOIN_IMPLEMENTED` flips to **true** (the surface exists) and a new `TOKEN_CLAIM_IMPLEMENTED` stays **false** (the claim does not). `publicJoinEnabled` still requires the profile + the operator enable, so **hosted production — no `MC_ENABLE_REMOTE_ONBOARDING` — answers the join route with a flat 404 today.** The ONB1 landmine guard is *stricter*, not relaxed: the join route must exist iff its constant says so, and **no claim route may exist in any scope** while `TOKEN_CLAIM_IMPLEMENTED` is false.

**Invariant green: 1195 backend tests (+28) · 11/11 evals · web build clean.** **Not touched, on purpose: the audit/telemetry hooks (still a no-op, still the operator's cost call) and `allowShell`/`MC_ALLOW_SHELL` (still a product decision).** **ONB4 not started — stopped for the independent ONB3 audit.**

**(2026-07-14) — Epic ONB / RE-AUDIT of the hardening: two gaps #248 left one layer short (`docs/AUDIT-ONB2-hardening.md`):**

An independent re-audit of #248 (auditor did not build it). **Verdict: PASS-WITH-FIXES.** The three prescribed fixes are sound — the audit surface really is out of the public scope, `sanitizeBody` really does recurse, the span really is redacted, and the `[ONB2-H1]` tripwire is real. But **H-2 and H-3 were each solved one layer short of the boundary, in the same way**: the mechanism was fixed and the *scope* of the mechanism was left at its old, too-narrow default. Both are now closed, with guards.

- **R-2 (HIGH) — a declared adapter secret was still persisted in PLAINTEXT.** `sanitizeBody` decided "is this key a secret?" against a hand-written substring list, but the **adapter registry** is what actually declares them — and `http_webhook.webhookAuthHeader` (`secret: true`, a bearer `Authorization` header value) matches **none** of `key|token|secret|password|…`. Probed against the merged code: it went into the row verbatim. This is H-3 *exactly as originally written* — the doc tells joining agents to send their secrets inside `agentDefaultsPayload` — surviving the fix for H-3, because the recursion reached the key and then failed to recognise it. **The registry is now the source of truth for both** (`allSecretFieldKeys()`): a new adapter's secret is redacted the moment it is *declared*, and a guard test loops every adapter × every declared secret field so the two lists can never drift again.
- **R-1 (MEDIUM) — `/api/traces` was authenticated but NOT isolated.** `spans` is **one process-wide buffer shared by every tenant**, so #248's fix narrowed the route from *public* to *any authenticated Clerk user* — who could still read every other org's span metadata. **And the obvious fix does not work:** `requireOrgRole` **silently skips every check** when the route has no `:orgId` (`middleware/rbac.ts` — "No org context — skip RBAC"), so bolting it onto a bare `/api/traces` would have produced a gate that *looks* like one and enforces nothing. Fixed by giving it an org to be scoped to: **`GET /api/orgs/:orgId/traces`**, owner-gated on a real `:orgId`, spans **filtered to the caller's org**, and an **unattributed span is served to nobody**. The bare `/api/traces` is **removed** (no caller anywhere in the monorepo). It also closes a structural blind spot: the MCA-85 net only inspects `:orgId|:agentId` routes, so a bare `/api/traces` serving cross-tenant data was **invisible to the guard by construction** — under `:orgId` it is inside the net for good.
- **Rulings on the rest:** the depth cap's `'[TRUNCATED]'` is **safe containment, not a redaction hole** (the whole subtree is dropped, and strings are scrubbed *before* the depth check) · `redactTokensInText`'s four prefixes are a **belt for our own credentials, not a secret detector** — that is the right contract, keep it, and **ONB3 must not add a free-text field to the join body** · the guard's no-op audit sink is **route-tagging coverage only and claims nothing about persistence** — correct as built.
- **Known follow-ups:** `llm.call` spans carry no org id, so the org-scoped traces route **under-reports** (the correct direction to fail — isolation first, utility second; fixing it means threading an org id through `LLMStreamOpts`). And `requireOrgRole`'s skip-when-no-`:orgId` is a **live footgun** left flagged, not fixed: making it fail-closed could 403 live routes and needs its own PR.

**Invariant green: 1167 backend tests (+7) · 11/11 evals · web build.** **H-1 and `allowShell` still untouched — both remain operator/product calls. ONB3 is CLEAR TO START, and not started.**

**(2026-07-14) — Epic ONB / pre-ONB3 SECURITY HARDENING: the audit trail is now safe to enable — but it is NOT enabled (#248):**

The three ONB2-audit findings (`docs/AUDIT-ONB2.md`) that had to close **before** ONB3 wires the join route. All three were dormant today and would have gone live together the moment anyone "just fixed the audit hook":

- **H-2 — the audit surface is no longer public.** `GET /api/orgs/:orgId/audit-log` and `GET /api/traces` were registered *inside* the audit/telemetry hook plugins, and `src/index.ts` registers those plugins in the **public** block — so the audit-log route inherited **no Clerk hook and no RBAC**: any caller who knew an `orgId` could read that org's audit log, and `/api/traces` served real spans (path, provider, model, org id, user id) to anyone. Only the H-1 no-op (an empty table) kept it from being a live cross-tenant leak — luck, not design. Both query routes are **split out of their plugins** (`auditLogQueryRoutes`, `telemetryQueryRoutes`) and registered in the **secured** scope; the audit log is additionally **`requireOrgRole('owner')`** (an audit log is an owner artefact).
- **…and the guard gap that let it through is closed.** The MCA-85 public-leak net (`bootLikeIndex()`) **never registered the audit-log or telemetry plugins** — which is exactly why a `:orgId`-scoped public route sat there unnoticed. Both plugins are now booted in the guard (with a no-op sink), and the guard asserts both routes are Clerk-tagged. A plugin-registered route can no longer hide from the leak guard.
- **H-3 — `sanitizeBody` recurses.** It redacted only **top-level** secret-shaped keys and copied object values in **whole, un-walked** — so a secret nested inside `agentDefaultsPayload` (the key ONB2's own onboarding document **instructs joining agents to put their `apiKey` / `x-openclaw-token` into**) would have been persisted to `audit_logs.metadata` **in plaintext** the day ONB3 lands. It now walks objects **and arrays**, depth-capped at 8, applies the same key test at **every depth**, and runs every surviving **string** through `redactTokensInText` — so a token echoed inside an error message or a URL is scrubbed too, and NIT-1's exported-but-never-called helper is finally wired into production.
- **M-1 — the telemetry span URL is redacted.** The span carried the **raw** `req.url` into `http.url` / `http.route` / the span name, and those spans were served by the (then public) `/api/traces`. Same `redactPath` helper as the audit row and the request logger: **one helper, every sink**, no drift.
- ⚠️ **H-1 stays OPEN, deliberately — it is an OPERATOR DECISION.** Both hooks are still encapsulated siblings in `src/index.ts`, so they still fire for **no route**, and `audit_logs` still records **nothing**. This PR makes the trail **safe-to-enable-by-construction**; it does not enable it. **Enabling it** means hoisting the hooks (`fastify-plugin`, or add-at-root before any `register()`), which switches on **one Turso `INSERT` per HTTP request, forever**. Pair it with (a) a **retention policy** and (b) a **`SENSITIVE_METHODS`-only filter** (skipping the read-only `GET` flood — the expensive, low-value half). A tripwire test now **fails** if the hooks are quietly hoisted without that decision.

**Invariant green: 1160 backend tests (+7) · 11/11 evals.** **ONB3 not started.**

**(2026-07-14) — Epic ONB / ONB2: the onboarding document — the self-describing contract an agent can drive itself from (#246):**

- **Pure `services/onboarding-doc.ts`.** `buildOnboardingDoc(invite, registry, posture, baseUrlCandidates)` is a **pure function** (injectable `now`, zero I/O) that renders the whole agent-readable contract: the **reachable-base-URL candidates** + the probe rule (`GET <candidate>/api/health`, take the first 200, put it in `agentDefaultsPayload.mcApiUrl` — *if none answer, escalate to the human; never guess a URL*) → **which `adapterType`s this invite allows**, each with its fields, secret fields, defaults, runtime note and a worked example → the **join request** → the **wait for a human approval** → the **one-time claim**. Every adapter shape is read **FROM the ONB1 registry** and nothing is re-described here — a test asserts each note/example/field-list is the registry's, so a second source of truth cannot quietly appear. Declared-but-unavailable runtimes (`openclaw_gateway`, `hermes_gateway`, `http_webhook`, `grok_local`, `hermes_local`) are shown **labelled `NOT AVAILABLE` with the reason**, not hidden: an honest map beats a short one.
- **The claim-security paragraph is a security control, and is test-locked as one.** Copied in spirit from Paperclip's best idea — the doc defends the joining agent against **its own observability layer**: parse the `token` from the **raw HTTP JSON** before printing or summarizing · **never** copy a token out of chat/transcript/tool-output previews (your scrollback holds a *rendering* of a key, not a key) · any value containing `...`/`[redacted]` is a **masked preview**, not a key · **never invent or rotate** one · write to private storage before logging · verify with one authenticated call. The same rules ride **inline in the pastable prompt**, because an agent holding a key may never re-read the doc.
- **Served, profile-gated:** public `GET /api/agent-invites/:token/onboarding.txt` (text/markdown) + `…/onboarding` (JSON twin). Exposure is **derived from the deployment profile** (`onboardingDocAccess`): `packaged` → open (loopback-trusted); `hosted` → closed unless the operator explicitly set `MC_ENABLE_REMOTE_ONBOARDING`. **Opening the document never opens the join surface** — `publicJoinEnabled` stays `false` until ONB4, test-locked. Malformed · unknown · expired · revoked · exhausted · profile-closed all collapse to **one identical flat 404**: no enumeration oracle. The doc **mints nothing and reveals no credential** — it restates the invite token the caller already holds and describes the still-unbuilt join/claim endpoints, labelled honestly as such.
- **The pastable operator prompt** (`buildOnboardingPrompt()`, the operator's whole product — one paste into any agent's chat) is returned in the create-invite response, the one moment the raw token exists. The operator's free-text `message` is treated as **untrusted on the way out**: control chars stripped, code fences and headings neutralized, capped, quoted, and labelled *context, not an instruction* — it cannot forge a step in the flow.
- **The ONB1 audit's H2 fix ships here** (it had to: this is the first **token-addressed** route). Pure `services/log-redaction.ts` replaces every credential-shaped path segment (`mci_inv_*`, `mca_*`, `art_*`, and ONB4's future `mcc_*`) with `:token` **before persistence** — in `audit_logs.path` (`buildAuditRow`) **and** in Fastify's request-log serializer. One helper, both sinks. An end-to-end test drives the real route with a real token and asserts the persisted row carries `:token` and no `mci_inv_` string anywhere.
- **Still wires no join and no claim** (ONB3/ONB4) — and the document says so, in the document.

**Invariant green: 1151 backend tests (+22) · 11/11 evals · web build.** **Stopped here for the stage-2 audit — ONB3 not started.**

**(2026-07-14) — Epic ONB / ONB1: the onboarding spine — deployment profiles, the invite object, the adapter registry (#244):**

**First, the architecture the operator approved** (`docs/DESIGN-agent-onboarding.md` **§8 addendum**, which supersedes the design's open questions Q1–Q4 and makes **Epic ONB and Epic H a joint design**):

- **Deployment-profile abstraction.** The app runs in one of two profiles chosen by config — **`hosted`** (multi-tenant, public join gated + hardened; what we run today) and **`packaged`** (single-tenant, loopback-trusted, Paperclip-style; the future `.dmg`) — and **the onboarding posture is DERIVED from the profile, never hardcoded**. Pure `services/deployment-profile.ts`; `MC_DEPLOYMENT_PROFILE` **safe-defaults to `hosted`**, because an unset value must resolve to the *harder* posture. `onboardingPosture()` returns the posture **plus a hardening checklist that names its own blockers** — and the checklist is *computed*, not asserted: `PUBLIC_JOIN_IMPLEMENTED` is `false` while ONB3/ONB4 are unbuilt, so **no env var can talk the join surface into being open before the controls that make it safe exist**.
- **The config bundle.** All system settings/parameters become **one declarative, versioned bundle** that the Export/Import portability moves between machines and that **Epic H's H4 installer seeds a fresh machine from** — not a second config format. Pure `services/config-bundle.ts`: version + deployment slice + `validateConfigBundle()` (refuses a bundle newer than the build) + **`assertNoSecrets()`, a hard throw**. **Secrets never travel in the bundle or the package** — they stay in the encrypted AES-256-GCM store. Its token-based secret detector (catches `apiKey`, `x-openclaw-token`, `webhookAuthHeader`; correctly ignores `sessionKeyStrategy`, `paperclipApiUrl`) is the *same* one the adapter registry uses to route payload secrets to the encrypted store — **one detector, so the bundle rule and the payload rule cannot drift**. (It earned its keep immediately: it rejected our own first name for an invariant field, `revealClaimedTokenInUi`, because it contains "token". It is now `operatorCanSeeClaimedKey`.)
- **Four posture defaults, baked in as INVARIANTS rather than switches**, each test-locked: (1) **public join endpoint OFF by default**, gated by profile; (2) **invites single-use by default** (multi-use = an explicit, bounded opt-in — we invert Paperclip's default); (3) **every invite-created agent lands in `low_trust_review` regardless of runtime** (extends CC3 beyond `claude_code`); (4) the **raw claimed token never reaches an operator UI, clipboard or log** — only the claiming agent reads it, once, from the raw HTTP response.

**Then ONB1 itself — the invite object + the server-side adapter registry:**

- **`agent_invites`** (idempotent `CREATE TABLE IF NOT EXISTS` + a **unique `token_hash` index**, nothing backfilled, no existing table touched). The invite is a **door, not a credential**: **hash-only** storage — the raw `mci_inv_*` token is shown to the operator **exactly once** at create and is unrecoverable (a lost invite is re-created, not recovered). Same hash + TTL + single-use pattern as `arturita_bindings` (A1). The token is **128-bit**, deliberately stronger than the design's sketched 12 chars, because on a backend that will eventually expose a public join endpoint an invite token *is* a bearer credential.
- **Pure `services/agent-invites.ts`** — the state machine with **`revoked > expired > accepted > active`** precedence (an operator who shuts a door always sees it read *revoked*), and the fail-closed rule that **expired / revoked / exhausted / unknown all collapse to one identical `not_found`**, so the lookup can't be used as an oracle to enumerate valid invite tokens. Out-of-range TTL or use-count is **refused, not silently clamped** — "I asked for 30 days and got 7" is exactly the surprise a security control must not spring.
- **Pure `services/adapter-registry.ts`** — the load-bearing shared schema ONB2–ONB4 build on. Ten adapterTypes (`openclaw_local` · `openclaw_gateway` · `claude_code` · `cursor` · `openai_generic` · `http_webhook` · `hermes_gateway` · `hermes_local` · `grok_local` · `internal`), each with capability flags, its `agentDefaultsPayload` contract, its **secret fields**, and an **`available`** flag — **declared ≠ available**: an unavailable adapter is describable (so the onboarding doc stays an honest map) but a join naming it is refused *with the reason*. `internal` is **never invitable**. Every entry maps onto the legacy `agents.runtime` column, so **nothing about the existing schema changes**. `validateDefaultsPayload()` is an **allowlist**, not a sanitizer: unknown keys, `__proto__`/`constructor`/`prototype`, size/key-count/string caps, enums and types are all refused; declared secrets are **split OUT of config** into the encrypted store; and an **undeclared secret-shaped key is refused loudly rather than dropped quietly** (a dropped credential is a credential the joining agent thinks it configured). No secret field carries a default — **we never invent a credential**. **`claude_code` defaults to `plan`** (propose-and-approve) and its enum *cannot express* autonomy, with a tripwire test that fails if anyone ever adds one; `openclaw_local.allowShell` defaults off.
- **Routes.** Owner-gated `POST/GET /api/orgs/:orgId/agent-invites`, `POST …/:inviteId/revoke` (idempotent), `GET …/onboarding-posture` (reports *why* the public surface is closed). The **only** public route is `GET /api/adapters` — a static, org-agnostic, secret-free description of the runtimes we speak, which a joining agent must be able to read before it holds any credential. **Nothing of the join/claim flow is wired**: no public join endpoint, no join request, no claim, no token minting. Those land in ONB3/ONB4 behind the board-approval gate + the per-IP rate limit.

**Invariant green: 1129 backend tests (+56) · 11/11 evals · web build.** **Stopped here for the stage-1 audit — ONB2 not started.**

**Prior (2026-07-14) — Epic P / P1: sidebar restructure — a shorter rail, nothing lost (#241):**

P0b promoted every Cockpit section to a first-class nav area, which worked but left the rail **long**. P1 shortens it. The rule throughout: **remove from the rail, never from the app** — every surface stays routable (⌘K palette + any deep link to its id still renders it), and `web/lib/navModel.ts` remains the single IA source that `Sidebar.tsx` and the dashboard render from.

**Six surfaces off the rail** (`HIDDEN_ITEMS`): **Search · Issues · Goals · Pipelines · Workspaces · Artifacts**. Search/Pipelines/Artifacts were "coming soon" placeholders; Issues/Goals/Workspaces are real surfaces whose components, routes and data are **untouched** — they simply aren't in the sidebar. All six still resolve through `findNavItem`, still appear in the command palette, and still render.

**Five surfaces folded into a parent's tab bar** — new `PageTabs.tsx`, driven by `navPageTabs()`: **Costs → `Costs | Budgets`**, **Connectors → `Connectors | Plugins`**, **Inbox (renamed **Inbox / Comms**) → `Inbox | Comms`**, **Settings → `Settings | Adapters | Secrets`**. Opening a hosted tab keeps its **parent** lit in the rail (`navSelectedId`), so the rail can't lose the user inside a tabbed page. No panel was rebuilt: each tab renders the exact component it always did (a promoted Cockpit section, a dashboard tab, or the honest Adapters placeholder).

The model now partitions every surface into exactly one of **rail · hosted tab · off-rail**, and the tests assert that partition — plus each removal, each fold target, the full rail order, and the carried-over **"every prior surface still reachable (nothing lost)"** invariant, which now spans all three homes. **17 nav tests** (was 10); the removals and folds can't silently regress.

**Invariant green: web build · 161 web tests (+9) · backend untouched (1073 tests).**

**Prior (2026-07-14) — Epic AG follow-up 2: the avatar-Remove 400 + three UI fixes (#239):**

**BUG — avatar Remove returned `HTTP 400: Bad Request` (the layer under the CORS fix).** Once CORS stopped blocking the DELETE preflight, the request finally reached the backend — and was refused **before any handler ran**. `web/lib/api.ts` set **`Content-Type: application/json` on every request**, body or not. A DELETE has no body, and Fastify's stock JSON parser rejects exactly that pair: **`FST_ERR_CTP_EMPTY_JSON_BODY`** — *"Body cannot be empty when content-type is set to 'application/json'"* → 400, which the client surfaced as a bare `HTTP 400`. The route, the Zod validators and the owner gate were all innocent; `avatar_url` was never nullable-constrained and the DELETE handler was correct all along. **Why the existing route test passed:** it injected the DELETE **without a Content-Type**, so it never reproduced the browser's request. Fixed at both layers — the client no longer claims a JSON body it isn't sending (`apiHeaders`), and the API now installs a **tolerant JSON parser** (`middleware/body-parser.ts`, mirroring the `cors.ts` precedent: a transport default that breaks a healthy route belongs in a tested module) that parses an **empty body to `{}`** while still refusing genuinely broken JSON with a message that names the problem. This fixes **every** bodiless write across the dashboard, not just the avatar. Regression-guarded by a test that drives the real handler with the **browser's exact headers**, plus one that asserts the raw Fastify default still 400s — so the bug can't come back unnoticed.

**FIX — Dashboard avatars now match Agent Configuration.** The Dashboard's Agent Squad, the Agents table and the cockpit **Agent fleet** each rendered a bare `avatarEmoji`, so an agent with an uploaded picture showed its picture in the Staff grid and its emoji on the Dashboard. All three now render the **same `<AgentAvatar>`** the Staff grid and the agent header use (uploaded picture → else icon/emoji), and the cockpit payload carries `avatarUrl` so it has the data to do it. One component, one source — the surfaces cannot disagree again.

**FIX — clicking an agent lands on Configuration.** `DEFAULT_AGENT_TAB` is now `configuration` (was `dashboard`). Every entry point routes through it — the cockpit fleet card (now clickable), the Staff card, the Agents table row, the command palette, and a tabless deep link (`#agents/<id>`) — so the default moved in one place. All six tabs stay reachable from the tab bar; explicit deep links (`#agents/<id>/runs`) are unchanged.

**FIX — the "Arturita" nav item is now "Command Center".** Label only: the nav id stays **`assistant`** (it is the dashboard Tab value and is deep-linked), the panel heading matches the nav, and the **agent named Arturita and the assistant persona are untouched** — she is still the one who answers, and the subtitle still says so.

**Invariant green: 1073 backend tests · 11/11 evals · typecheck · web build · 152 web tests.**

**Prior (2026-07-14) — Epic AG follow-up: the two operator-reported bugs + two features (#235–#237):**

**BUG — one root cause, three symptoms (#235).** `@fastify/cors` v11 defaults `methods` to **`GET,HEAD,POST`** and we never set it, so the deployed backend answered every preflight with an `Access-Control-Allow-Methods` that omitted PUT/PATCH/DELETE. The browser therefore **refused to send** those requests at all. `fetch` rejects on a blocked preflight exactly as it does on a dead host, and `web/lib/api.ts` collapsed that to *"Network error — backend unreachable"* — which pointed the investigation at an undeployed route. The routes were deployed and healthy the whole time (an unauthenticated `PUT …/files` returned 401, not 404). This one default broke the **Instructions save** (PUT), the **avatar Remove** (DELETE — while upload, a POST, kept working, which is exactly why it looked like an avatar-specific bug), and made the **Skills checkboxes** look read-only. Fixed with an explicit method list in `middleware/cors.ts`, guarded by a test that injects a real preflight per verb **and** walks the route table so a future dependency default cannot silently re-break it. Error surfacing now names the method, the path and CORS as a cause, and the Instructions tab names the failing file and says the draft is still in the editor.

**Skills — editable (#236).** Tick to install, untick to uninstall, saved as you go, optimistic with rollback. A second, independent bug was found and fixed here: `resolveSelection` refused **any** name absent from the library, and the checkbox list resends the whole selection — so on any agent carrying an **orphaned** skill (its library row deleted underneath it), **every** toggle returned `400 Unknown skill(s)`. An orphan the agent already has is now kept; unticking it is the only way to drop one; a genuinely unknown name is still refused and never partially applies.

**Custom adapter (#237).** Agent → Configuration → **Custom models**: display name, base URL, model id, optional API key, and a **Test connection** probe. OpenAI-compatible is the general case, so it covers **NVIDIA NIM**, Together, vLLM, a local server, or any OpenAI-standard provider. Reuses the **#207** custom-model service rather than forking it (`probeEndpoint` was lifted out of the Arturita route into the service — one probe, not two). Agent models register into `custom_models`, **not** `arturita_llm_chain` (that chain is Arturita's ordered *failover* list; defining a model for another agent must not silently change what Arturita falls back to); `available-models` merges both. The key is stored **AES-256-GCM encrypted**, never returned or logged, and scrubbed from audit rows. **The interop gap this closes:** `agent-executor` read only the *plaintext* `<slug>_api_key` while the save path only ever writes the *encrypted* `<slug>_api_key_enc` — a custom model with a key could not have authenticated on a single run. It now resolves credentials through `resolveLlmCreds` for every provider, on the primary call and every fallback hop.

**Net-new test coverage:** the AG write paths are now driven **end-to-end against a real SQLite file through the real owner gate** (`agent-detail-routes.test.ts`, `custom-model-agent.test.ts`) — a save that does not survive a re-read now fails a test. Invariant green: **1065 backend tests · 11/11 evals · web build · 146 web tests**.

**Prior (2026-07-14) — Epic AG: the Agents experience — Staff grid + a six-tab agent detail page — EPIC AG COMPLETE (AG1–AG7, #227–#233):** the Agents area now opens on a **Staff card grid** matching the operator's mockup — a card per agent with a large **avatar** (uploaded picture, falling back to the icon/emoji), name, email/handle, a **colorblind-safe status dot** (shape + text label + colour: ⬡ RUNNING / ⚠ NEEDS ATTENTION / ✓ IDLE, with **attention outranking running** so a busy agent holding a blocked task never reads as healthy), and the three metric chips (**Tasks Active · Token Cost today · Last active**). Clicking a card opens a **six-tab agent detail page** (deep-linkable `#agents/<id>/<tab>`): **Dashboard** (Latest Run · four 14-day charts · Recent Tasks · Costs strip) · **Instructions** (the managed markdown bundle) · **Skills** (Installed + Other, with a real uninstall) · **Configuration** (avatar upload · identity · Reports-to · Adapter + Model) · **Runs** (history + per-run log) · **Budget** (cap + spend). Built by **reuse first** — runs, tasks, costs, skills, adapter/model, budget/preflight, heartbeat and the org chart were all already there. Only **three** things were genuinely missing and are net-new: the **managed instructions bundle** (`agent_files` — the gap doc's #1 config gap: per-agent AGENTS.md/HEARTBEAT.md/SOUL.md/TOOLS.md + operator-added `.md`, owner-gated, path-traversal-proof), **avatar upload/storage** (`avatar_url`, a capped 256KB data URI — PNG/JPEG/WebP/GIF, **SVG refused**; the browser downscales to 256px/WebP first), and the **per-task token split** (`input_tokens`/`output_tokens`/`cached_tokens`) that makes the Costs strip real rather than decorative. **Safety of the migration:** every change is an idempotent nullable ALTER (or `CREATE TABLE IF NOT EXISTS`) with **nothing backfilled**, so existing agents are byte-for-byte unchanged — most importantly, the instructions bundle **only feeds the system prompt for files the operator actually saved**, so an agent whose bundle was never edited gets exactly the prompt it got before. Write paths are **owner-gated, field-allowlisted and snapshotted** to `configRevisions` (the legacy unvalidated `PATCH /api/agents/:id` was deliberately *not* extended), and a Reports-to change that would create a **reporting loop is refused** so the org chart stays a tree. Tokens only, no raw hex; Glassmorphism preserved. Invariant green: **1029 backend tests · 11/11 evals · web build · 135 web tests**. PLAN §0 **AG1–AG7** `done` · REQUIREMENTS **FR-AG1..AG8**.

**Prior (2026-07-13) — Epic CC / CC6: autonomous-exec mode (OFF by default) + go-live — EPIC CC COMPLETE:** the final story wires the (optional) autonomous posture and ships it **disabled by default, fail-closed behind three preconditions**: guard #1 `CC_AUTONOMOUS=1`, guard #2 `CC_AUTONOMOUS_CONFIRM=1`, **and** the CC5 command denylist being importable on the host — `resolve_permission_mode` collapses to `plan` if any one is missing (mirrors the wallet-mainnet / machine_exec two-guard pattern; the denylist is now a **hard precondition** for autonomy). Even in the autonomous posture, every command still passes the `cc_guard.py` hook → CC5 denylist: catastrophic/privilege/exfil commands are **refused**, unknown commands are **proposed** (never auto-run), only fully-allowlisted read-only commands run un-attended. New **`cc_adapter.py --doctor`** prints the exact resolved posture + why autonomy is blocked; **`setup.sh`** installs a launchd keep-alive (chmod-600 `mc.env`, `--doctor` + `--once` verify, propose-and-approve). **Docs:** README "Autonomous execution" section (exact enable/disable steps), `adapters/CLAUDE.md`, **GO-LIVE.md item 6** (host prereqs: `claude` CLI logged in, `mca_` token, workboundary), vault milestone. **Autonomous host execution is never on by default; nothing runs un-approved on the machine.** Invariant green: **934 backend tests · 11/11 evals · web build · 48 adapter tests**. PLAN §0 **CC6** `done` → **Epic CC complete (CC1–CC6)**.

**Prior (2026-07-13) — Epic CC / CC5: semantic command allowlist/denylist (the new safety gate):**

**Prior (2026-07-13) — Epic CC / CC5: semantic command allowlist/denylist (the new safety gate):** the one genuinely-new safety control the design flagged (R4) — before CC5 there was **no** semantic shell-command denylist; command control rested only on the A2 approval + the host not exposing an exec endpoint. New pure, **exhaustive** `services/cc-denylist.ts` (`evaluateCommand()`): classifies a shell command **deny** (fs-catastrophe `rm -rf /` · raw-disk `dd/mkfs/diskutil` · fork-bomb · privilege `sudo/doas/su` · system/SIP/Gatekeeper/firewall · pipe-to-shell `curl|sh` · reverse-shell `nc -e` / `/dev/tcp` · secret-exfil `.ssh`/`.aws`/keychain · anti-forensics `history -c`) / **allow** (opt-in read-only allowlist — `git status`, `npm test`, …) / **gate** (default — unknown ⇒ A2 approval, never auto-run). Precedence **deny > allow > gate**, per-segment chain analysis (`a && sudo rm -rf /` → deny), fail-closed. **Integrated into `prepareApprovalRecord`**: a denylisted `machine_exec` proposal is **refused pre-approval** (400, no row written) and the `allowlisted` flag is stamped honestly on the card. A **Python twin** `adapters/claude-code/cc_denylist.py` gives the host guard hook the same rules — the autonomous path (CC6, off by default) now enforces the denylist, allowing only fully-allowlisted commands and refusing denylisted ones. 36 backend + 13 adapter tests (incl. guard autonomous-path e2e). Invariant green: **934 backend tests · 11/11 evals · web build · 48 adapter tests**. PLAN §0 **CC5** `done`. **Next: CC6** — wire autonomous-exec behind the two operator guards (OFF by default) + go-live docs.

**Prior (2026-07-13) — Epic CC / CC4: UI/CLI adapter wiring:**

**Prior (2026-07-13) — Epic CC / CC4: UI/CLI adapter wiring:** hiring/adding a Claude Code agent now prints the **correct** adapter run-command + env (before CC4 a `claude_code` hire fell through to the **OpenClaw** run-command). New pure **`web/lib/adapterProfile.ts`** is the single source of truth for per-runtime adapter path + `mc.env` + copy-paste run block; `claude_code` prints `python3 adapters/claude-code/cc_adapter.py` with `CC_PERMISSION_MODE=plan` (propose-and-approve — no OpenClaw shell flags, and never an autonomous flag from the wizard). Both **`HireDialog`** and **`AddAgentWizard`** refactored onto it (openclaw/cursor blocks unchanged). Added an optional **externalEndpoint** field to the wizard (sent only when set). CLI already accepts `--runtime claude_code`. 8 web tests. Invariant green: **web build · 112 web tests · 898 backend tests · 11/11 evals**. PLAN §0 **CC4** `done`. **Next: CC5** (command denylist) → **CC6** (autonomous-exec, off by default).

**Prior (2026-07-13) — Epic CC / CC3: secure-by-default registration for code executors:**

**Prior (2026-07-13) — Epic CC / CC3: secure-by-default registration for code executors:** closes the allow-all-when-empty footgun for the Claude Code runtime. New pure **`services/code-executor.ts`** (`secureRegistration()`): a `claude_code` agent now registers **contained** — `trust_mode:'low_trust_review'` + an **explicit, non-empty** capability list (`memory:write`, `attachment:write`, and the new **`machine_exec`** capability — an empty list means allow-all, the footgun) + a `trust_boundary` seeded from the target workspace/project (empty = most restrictive, since the low-trust boundary is fail-closed). Non-code runtimes (openclaw/cursor/custom) keep their **legacy allow-all/standard** defaults — no regression — and explicit caller values always win. Wired into **both** create paths: `POST …/agents/external` (the wizard) and the goal-driven **hire-confirm** path. 16 tests in the CC suite. Invariant green: **898 backend tests · 11/11 evals · web build**. PLAN §0 **CC3** `done`. **Next: CC4** (UI/CLI adapter wiring) + **CC5** (command denylist).

**Prior (2026-07-13) — Epic CC / CC2: propose-and-approve `machine_exec` bridge:** closes a real safety hole and gives the Claude Code agent a correct propose channel. **Backend:** the agent-facing `POST /api/agent/approvals` previously stored the agent's own `summary`/`payload` verbatim for a **dangerous** approval — model prose on a `machine_exec` card. Now it routes through a shared pure **`prepareApprovalRecord`** (in `dangerous-approvals.ts`): for the four A2 danger types the summary is **machine-rendered from the structured `action`** (verbatim `argv` for `machine_exec`), the payload is tagged `requiresStepUp`, and a malformed payload is **fail-closed 400** (no row written). The human A2 route (`tasks.ts`) was refactored onto the same helper — one source of truth for the verbatim-render rule. **Adapter:** new **`cc_guard.py`** PreToolUse guard hook — when Claude could attempt a command, it turns each `Bash` call into a `machine_exec` approval (`argv=['sh','-lc',<command>]`, raw command + cwd for audit) and **denies** the call, so **nothing runs on the host**; installed via `--settings` whenever a non-`plan` posture is active (or `CC_GUARD=1`). Pure hook helpers in `cc_headless.py` + a subprocess e2e test. Invariant green: **891 backend tests · 11/11 evals · web build · 35 adapter tests**. PLAN §0 **CC2** `done`. **Next: CC3** (secure-by-default registration) + **CC4** (UI/CLI wiring).

**Prior (2026-07-13) — Epic CC / CC1: Claude Code as an office engineering agent — host adapter (poll loop):** Claude Code now joins the fleet as a first-class `runtime:'claude_code'` external executor (design: `docs/DESIGN-claude-code-agent.md`). New **`adapters/claude-code/`** (stdlib-only, its own thing — the live OpenClaw install is untouched): a pure, unit-tested core **`cc_headless.py`** (argv builder, `--output-format stream-json` parser, result/cost/session extractor, secret redaction, worktree planning, and the **fail-closed permission-mode gate** — 25 unittest) + the daemon **`cc_adapter.py`** (claim assigned task → run a **headless `claude -p`** in the workspace/`cc/`-worktree → stream stream-json to `/runs/:id/log` → post `/result` + `/heartbeat`; resumes a prior `claude` session via `sessionState`). The flagged **headless contract is nailed** against the installed CLI: `claude -p --output-format stream-json --verbose --permission-mode plan`. **Safety — propose-and-approve only:** `plan` mode means Claude reads/analyses and **proposes**, never edits/executes on the host; any non-`plan` (autonomous) mode collapses back to `plan` unless **both** operator guards are set (CC6, off by default). New `npm run smoke:claude-code` drives the real adapter with a **fake `claude`** (no network). Invariant green: **882 backend tests · 11/11 evals · web build · 25 adapter unittest**. PLAN §0 **CC1** `done`. **Next: CC2** (machine_exec approval bridge) + **CC3** (secure-by-default registration).

**Prior (2026-07-13) — Arturita voice input: graceful STT + FREE local Whisper (2 PRs):** the operator got **"Speech Error: network"** on push-to-talk even after the LLM went local. Root cause = the **STT / mic-capture** leg (not the TTS `speak()` path fixed earlier): both voice panels capture via the Web Speech `webkitSpeechRecognition`, whose Chromium backend is **Google's cloud STT — which Brave disables**, so the constructor feature-detects as present (mic enables) but every `.start()` fires `onerror` with `error:'network'`, dead-ending voice even online, printed verbatim. **PR #217 (graceful):** new pure `classifySttError()` in `talkDiagnostics.ts` maps `network` → a specific, **non-fatal** "voice input isn't available" status (names **Brave** when detected) and steers to the text box + local Whisper; `permission`/`no-mic`/`unknown` get actionable lines; `no-speech`/`aborted` stay benign. Both panels stop the recognizer and show a **colorblind-safe** notice (icon+label, never a raw code); typing is the always-available fallback. Shared `lib/browserEnv.ts` (`detectBrave`/`hasWebSpeechStt`). **PR (Whisper):** a **zero-dep** Node bridge **`adapters/arturita-stt`** wraps the already-installed `whisper` CLI (+ffmpeg) — the **browser** POSTs captured mic audio to `127.0.0.1:8790` (same trust model as local Ollama; CORS-scoped via `ARTURITA_STT_ORIGINS`, mirroring `OLLAMA_ORIGINS`) → `{ text }` → the converse flow. Works in **Brave**. Web `lib/whisper.ts` (client) + `lib/sttEngine.ts` (pure engine resolver: whisper → web_speech → typing, honouring the ⚙ Pipeline STT chain); `AssistantPanel` push-to-talk uses **MediaRecorder → Whisper** when the bridge is reachable, else Web Speech, else typing; the self-test STT leg now probes Whisper + reports the ONE start command. **Verified end-to-end** (macOS `say` → webm/opus → bridge → correct transcript + CORS header). **The one command:** `cd adapters/arturita-stt && ARTURITA_STT_ORIGINS=https://app.7ei.ai npm run stt`. Invariant green: **web build · 103 web tests · 7 arturita-stt tests**.

**Prior (2026-07-08) — Epic P / P0b: promote Cockpit sections to first-class Paperclip areas:** the Cockpit "Operations" mega-stack is now also reachable as **discrete nav areas** matching Paperclip. `CockpitPanel` gained an `only` **section filter** — the *same* composition root (one data load, all the existing section components) renders either the **full Operations stack** or a **single focused area**, with a lighter focused header (title + Refresh; no StatsRow/Hire/Add). Eight sections are promoted into the right groups: **Inbox · Activity** (Overview), **Goals · Org** (Workspace), **Workspaces** (Operate), **Budgets** (Delivery), **Plugins · Secrets** (Company). Reuse, **no rebuild** — every section is the existing component. Approvals stay reachable via **Inbox** (inline tri-state) + **Governance**, so no duplicate queue. Nav model gained a `section` kind + `isSection`/`navSectionKey` helpers (**9 web tests**). Invariant green: **web build · 80 web tests**. PLAN §0 **P0** `done` · REQUIREMENTS **FR-P0c** · `docs/IA-paperclip-mapping.md`.

**Prior (2026-07-08) — Epic P / P0a: Paperclip IA re-fold — nav shell + mapping:** the web dashboard nav now **mirrors Paperclip's information architecture** (`docs/TRD-paperclip.md` §3). The old flat 14-item sidebar is replaced by a **folded, grouped, collapsible rail** in Paperclip's group order (**Overview · Workspace · Operate · Delivery · Company · General**), driven by a **pure, unit-tested nav model** (`web/lib/navModel.ts`). Every one of the 14 existing surfaces is **re-homed** under the right group (Tasks→**Issues**, agent fleet→**Agents**, Preflight/Budgets→**Costs**, Governance→**Operate**, Memory→**Learnings** slot, Arturita→**Board Chat** slot, …) — **nothing rebuilt, everything still reachable**. Paperclip areas we don't have yet (**Routines, Pipelines, Review Queue, Artifacts, Adapters, Members & Access, Search**) appear as honest **"coming soon" placeholders** (`PlaceholderView.tsx`) that name the area and link to the Epic-P gap plan — the IA reads complete without faking features. Per-group **collapse** and whole-rail **icon-fold** both **persist** to `localStorage`. Colorblind-safe (active = accent + bold + indicator bar, never color alone), **tokens only**, Glassmorphism preserved. **P0b** (promote the Cockpit sections — Inbox/Activity/Goals/Org/Approvals/Budgets/Secrets/Workspaces/Plugins — to first-class areas, reusing the section components) is the next slice. Web: `lib/navModel.ts` (**8 tests**) + `Sidebar.tsx` + `PlaceholderView.tsx`; mapping in `docs/IA-paperclip-mapping.md`. Invariant green: **web build · 79 web tests**. PLAN §0 **P0** · REQUIREMENTS **FR-P0a..c**.

**Prior (2026-07-08) — Epic P / P2: model profiles (per-agent primary + cheap model + reasoning effort — the near-term cost lever):** each agent now carries a **model profile** — an optional explicit **primary** model, an optional **cheap** model, and a **reasoning-effort** setting (low/medium/high). A pure router (`services/model-profile.ts`) decides the tier per turn: **cheap** for ask-mode / low-stakes turns, **primary** for execute / heavier-reasoning / orchestration turns, with an **explicit override** (`modelTierOverride[:agentId]`) that wins. It's **wired into the executor** (`planWakeModel` selects the model + provider on both the execute and ask paths), and **interoperates with every shipped cost lever**: the routed model is the **F1 fallback-chain head** when no explicit chain is configured (an explicit chain stays the operator's override), the **preflight per-wake cap** now prices the *routed* model, and a cheap tier genuinely lowers **scoped-budget** spend (cost-rates / unbounded flags unchanged). **Reasoning effort** flows to the live LLM call, mapped per provider (Anthropic extended-thinking budget · OpenAI `reasoning_effort` · Gemini `thinkingBudget`) — additive and guarded, so a null-effort agent's request is byte-identical to before. Defaults keep **every existing agent unchanged** (null primary → `llm_model` stays primary; cheap off). Backend: pure `services/model-profile.ts` (**30 tests**) + idempotent `primary_model`/`cheap_model`/`cheap_model_enabled`/`reasoning_effort` ALTERs + owner-gated `GET/PUT /orgs/:orgId/agents/:agentId/model-profile` + `GET …/available-models` (catalogue + S8 custom entries). Web: Governance panel **Model profiles** section (per-agent primary/cheap/effort selects + cheap-routing toggle + routing summary + colorblind-safe tier/effort badges, owner-only); pure `lib/modelProfile.ts` (**6 tests**). **No new dangerous surface.** Invariant green: **867 backend tests · 11/11 evals · web build · 71 web tests**. PLAN §0 **P2** · REQUIREMENTS **FR-P5..P8** · gap doc `docs/GAP-paperclip-config.md`.

**Prior (2026-07-08) — Epic P / P1: low-trust review mode (the marquee Paperclip-gap safety item):** agents now have a **trust level** (`standard` default — nothing changes for existing agents — or `low_trust_review`). A **contained** agent may only touch the resources in its **boundary set** (project / task / agent ids); any escape is **refused + logged**, and every **gated action** — the A2 taxonomy (file_destructive / wallet_tx / email_send / machine_exec) **plus** create-agents / create-skills / assign-tasks — is **quarantined**: held in a review queue for explicit human approve/reject before it can take effect. The queue **reuses the existing tri-state approvals / inbox machinery** (a `low_trust_review` approval row — no parallel store) and stacks **in front of** the A2 gate, never bypassing it: a quarantined dangerous action still needs **step-up** to approve. Backend: pure `services/review.ts` (trust/boundary/taxonomy/decision/row-builder — **24 tests**), idempotent `trust_mode`/`trust_boundary` ALTERs, owner-gated `GET/PUT /orgs/:orgId/agents/:agentId/trust` + `POST …/review-evaluate` (fail-closed: 202-held on quarantine, 403 on escape) + `GET …/review-queue`, decide-route step-up inheritance, orchestrator delegation chokepoint enforced. Web: Governance panel **Trust & containment** section (per-agent toggle + boundary editor, owner-only, colorblind-safe icon+label+tone) + a **🛡 quarantine chip + warnings** on review cases in the inbox; pure `lib/trust.ts` (**5 tests**). **No new dangerous surface** — the gate only ever withholds an action. Invariant green: **836 backend tests · 11/11 evals · web build · 65 web tests**. PLAN §0 **P1** · REQUIREMENTS **FR-P1..P4 · NFR-9** · gap doc `docs/GAP-paperclip-config.md`.

**Prior (2026-07-08) — Arturita custom LLM insertion (S8):** the operator can now add an **arbitrary model** from the Config panel instead of only presets. A **"Custom model"** form (display name · base URL · model id · type · optional key) POSTs `…/arturita/custom-model`; the entry slots into `arturita_llm_chain` and rides the **F1 breaker/failover** like any built-in. General case = **OpenAI-compatible** (`/chat/completions`); plus the **keyless local base-URL / Ollama-style** case. Backend: pure `services/custom-model.ts` (namespaced slug, http(s) validation, `applyCustomModel`/`removeCustomModel` deployConfig mutation, `resolveLlmCreds`/`hasStoredKey`) + `routes/arturita-custom-model.ts` (owner-gated add/**test**/delete; the test button probes GET `/models`→1-token `/chat/completions`). **Key is stored AES-256-GCM encrypted** (`<slug>_api_key_enc`) — never returned, never logged (masked tail only); converse `resolveCreds` decrypts custom hops; `llm-router` keyless guard relaxed for base-URL endpoints. Config schema documented in **DECISIONS S8 · REQUIREMENTS FR-49**. Invariant green: **812 backend tests · 11/11 evals · web build · 60 web tests**.

**Prior (2026-07-08) — Arturita talk-path bugfix (graceful degrade + self-test):** the tab surfaced a raw "network error" and voice silently died. Root cause — two legs failed opaquely: (1) **browser TTS** used `SpeechSynthesisUtterance` without selecting an on-device voice, so Chrome's network-backed voices threw `SpeechSynthesisErrorEvent.error === 'network'` and playback died with the error swallowed; (2) every backend/local-Ollama transport failure mapped to the generic `web/lib/api.ts` "Network error — backend unreachable" with no leg/fix hint. Fix — new pure `web/lib/talkDiagnostics.ts` (voice selection, TTS-error classification, per-leg error descriptions with fix hints, self-test shaping — 16 tests): `speak()` now prefers a `localService` voice + auto-retries local on a network error + shows a specific non-fatal status; converse/local-Ollama errors surface which leg failed + the fix (e.g. **set `OLLAMA_ORIGINS`**); added a **🩺 Talk-path self-test** to the Pipeline panel (backend/Ollama/TTS/STT, colorblind-safe icon+label rows). Text reply is always shown; the tab never dead-ends. Invariant green: **57 web tests · web build**.

**Prior (2026-07-08) — Arturita Epic J to production (4 PRs):** the greenlit build of the Jarvis "Arturita" tab. **#201** the real **7Ei mark** (honeycomb) wired as the orb centerpiece (transparent vector of the operator's raster; `web/public/7ei-logo.jpg` source). **#202** the **free-first pipeline backend** — pure `arturita-pipeline` resolvers (LLM/STT/TTS chains, free-first defaults, S1 privacy filter, `usableLlmChain` prune+guarantee) + `GET/PUT /arturita/pipeline`; converse now runs the configured LLM chain. **#203** the **Config panel** — a per-layer chain editor (reorder/mode/add/remove/reset) in the tab. **+ local-Ollama PR** — **browser-direct local Ollama streaming** for the answer path (real token streaming, on-device, $0; graceful cloud fallback; `via` provenance chip). Brainstorm-first + A2 gating unchanged. **Go-live to fully local:** set `OLLAMA_ORIGINS=https://app.7ei.ai` (+ restart Ollama) so the browser can reach it; optionally add Groq/Gemini keys (free-tier LLM fallbacks) and the NVIDIA key (hosted Chatterbox TTS). Invariant green: **794 backend tests · 11/11 evals · web build · 41 web tests**.

**Prior (2026-07-08) — Arturita Epic J research doc (`docs/PRD-jarvis-tab.md`, research-first rewrite):** the operator refined the direction to **research-and-document first, hold the build**. The spec is now built around two principles: (1) the tab is a **brainstorming/conversation partner FIRST** — Arturita answers directly and commands the builder/executor agents **only when explicitly told to** (interaction model in PRD §1); (2) **free-first / self-hosted-first with configurable fallbacks on every layer**. It carries three grounded comparison tables + a config schema — **LLM** (local Ollama `llama3.2:3b`→`qwen3:8b`→`qwen2.5:14b` → free-tier Groq/Gemini), **STT** (whisper.cpp Metal → browser Web Speech), **TTS** (Piper/local-Chatterbox → browser SpeechSynthesis; **Chatterbox resolved** = Resemble AI open-source MIT, runs local *or* NVIDIA-hosted). Logged as **DECISIONS S7**; PLAN §0 J2–J6 re-phased; REQUIREMENTS **FR-47/48 · NFR-32**. Docs-only PR. **J1 scaffold (below) already shipped; deeper build held pending review.**

**Prior (2026-07-08) — Arturita J1, the Jarvis tab (Epic J):** a new Cockpit **"Arturita"** tab (🌸) — a Glassmorphism conversational surface with a **reactive HUD orb** (idle/listening/thinking/speaking, colorblind-safe). Default behaviour: **Arturita answers directly**; she only routes into the task/agent flow when the operator **explicitly** asks to build/do/delegate (or the intent is destructive) — the routing decision is shown on every turn. Backend: pure `arturita-converse.decideConverseMode` + `POST /arturita/converse` (answer via the **F1 fallback chain** `streamLLMWithFallback`; delegate via B3 routing → a `pending` task at the **A2 gate**). Web: `assistant.logic` + `AssistantOrb`/`AssistantPanel`, push-to-talk + wake-word + typed, streamed replies, glass hero (tokens only, light+dark). **No dangerous surface** — chat turns take no actions. Placeholder logo at `web/public/arturita-logo.svg` (one-file swap; see `ARTURITA_LOGO_SRC`). Spec: `docs/PRD-jarvis-tab.md`. **+24 tests (12 backend · 12 web).**

**Live:** backend on Fly (`7ei-backend`, v1.3.0), web on Vercel (`app.7ei.ai`), Turso DB. All PRs merged to `main`.
**Zero-auth vault-graph preview:** https://app.7ei.ai/vault-graph.html — self-contained render of the enriched graph (785 nodes / 108 named communities), no login, no `GITHUB_VAULT_TOKEN` needed (#196).
Health `GET /api/health` → 200, `db: connected`, `scheduler: running`. Invariant green: **781 backend tests · 11/11 evals · web build green · 27 web tests**.
Full write-up in the shared vault: `07-Agents/STATUS-Mission-Control-2026-07-02.md` · plan: `01-Projects/Paperclip-Gap-Analysis-v2-2026-07-06.md`.

## Paperclip gap-bridge v2 — 4 / 4 epics complete
| Epic | Stories (all ✅) | PR trail |
|---|---|---|
| **MCA-82** · Theme (design system v2) | T1 foundation · T2 glass chrome · T3 hex sweep + WCAG AA audit | #155 · #156 · #159 |
| **MCA-83** · Work surface / failure UX | W1 recovery cards · W2 blocker chips + next-up + cost rollups · W3 thread wake-on-comment · W4 task watchdogs · W5 ask-mode | #157 · #158 · #161 · #168 · #169 |
| **MCA-84** · Visibility | V1 heartbeat 24h timeline · V2 tri-state approvals + inbox retry + read receipts · V3 per-wake preflight cap + model validation | #160 · #162 · #163 |
| **MCA-85** · DX | D1 self-describing API (`/api/openapi.json` + CLI) · D2 `llms.txt` + `7ei-mc onboard` | #164 · #167 |
| _Unplanned security_ | Auth-scoping hardening (org/agent groups → Clerk) · per-org webhook shared-secret verification | #165 · #166 |

**All four v2 epics (MCA-82/83/84/85) complete.** Per-story detail below. Standing items (Jira transitions, webhook-secret rollout, apex DNS, vault mirror) tracked in **Standing items** at the foot of this file.

## Next: major update — Arturita (voice-first personal AI agent)

> **DECISION LOCK 2026-07-08:** S1–S6 all **CONFIRMED**; wallet model **CHANGED** (S4 — bounded autonomous signing from a dedicated capped burner: autonomous **< $100** per-tx, ≥ $100 → A2 approval; local encrypted keystore / delegated session key; **testnet only this wave, mainnet behind an explicit go flag** — this **overrides** the old never-sign/no-custody invariant). Also: **S1** voice `local|provider` config (interim `provider` = Chatterbox TTS via NVIDIA API, key → encrypted store only, **verified not committed**, `.env.example` placeholder); **S2** Telegram-only v1; **S3** whole-machine access + minimal self-protection denylist; **S5** push-to-talk default; **S6** broad exec (destructive A2-gated). New **Epic H — Packaging & Distribution**. Detail: `docs/DECISIONS-arturita.md`, cascaded into PRD/PLAN/REQUIREMENTS.

Full planning/tracking layer filed: **PRD** `docs/PRD-arturita.md` (intent) · **PLAN** `docs/PLAN-arturita.md` (Epics A–G → stories A1…G2 + §0 living status tracker) · **DECISIONS** `docs/DECISIONS-arturita.md` (S1–S6, provisional/confirmed) · **REQUIREMENTS** `docs/REQUIREMENTS-arturita.md` (FR/NFR master acceptance checklist) · **ONBOARDING** `docs/ONBOARDING-arturita.md` (cold-start). Vault mirror: `07-Agents/Arturita.md`. A voice-first personal-agent persona layered on the existing surface: desk voice (Cockpit) + remote iPhone control via Telegram (voice/text/files/one-tap approvals), hardened local machine control, Google email/calendar, and **read+prepare-only** crypto wallet (human-in-the-loop signing, **no key custody, never auto-sign**). Reuses the shipped primitives — tri-state approvals gate all dangerous actions, preflight/budget bound LLM failover, plus watchdogs/wake-on-comment/ask-mode/heartbeat/HMAC-Telegram/secrets/mac-mini-adapter. Epics A–G (PRD §10), **Epic A safety spine ships first**. See `HANDOFF.md` → "Incoming major update" and the §0 tracker in `docs/PLAN-arturita.md`.

### Arturita build — story tracker (live)
| Story | What | Status | PR |
|---|---|---|---|
| **A1** · Persona, sessions & `/panic` | `services/arturita-session.ts` (pure: session mint/verify/revoke + TTL/step-up freshness, single-operator binding, nonce guard, panic plan); `arturita_sessions`/`arturita_bindings`/`arturita_nonces` tables; owner-scoped Arturita persona seed; session/bind routes + **`POST panic`** (owner-authed via session token). | ✅ done | #174 |
| **A2** · Dangerous approval types + step-up | `services/dangerous-approvals.ts` (pure: `file_destructive`/`wallet_tx`/`email_send`/`machine_exec` classification, **machine-rendered verbatim** action summaries + scam/danger warnings, step-up evaluation); `decideApproval` gains a step-up gate (approve a dangerous type ⇒ needs a fresh command session, else 403); approvals POST regenerates the summary from a structured `action` (ignores model prose). | ✅ done | #175 |

| **A3** · Intent classifier + two-phase confirm | `services/intent.ts` (pure: `classifyIntent` tiers safe/destructive/critical + maps to A2 approval types; `confirmationPhraseFor`; `isConfirmed` two-phase — bare "yes" rejected for the top tier, action-verb restatement or tap required; sub-threshold STT re-prompts, never guesses). Table-driven tests over a destructive/safe/ambiguous corpus. | ✅ done | #176 |

| **F1** · LLM fallback chain + circuit breaker | `services/llm-fallback.ts` (pure: `parseFallbackChain` from deployConfig; `classifyLlmError` → 6 failure classes + failover/breaker guidance; circuit breaker `recordFailure`/`isProviderHealthy` with window + cooldown/re-probe; `planFallback` walks the chain, skips open breakers, drops hops over the preflight per-wake cap, parks with a plain-language reason when exhausted). **Pure decision layer only** — live wiring into the `agent-executor`/`streamLLM` retry loop is a deliberate follow-up (kept off the LLM hot path in the overnight session). | ✅ done | #177 |

| **E1** · Wallet read + prepare + simulate | `services/wallet.ts` (pure: `buildUnsignedTx` key-free assembly; `decodeCalldata` ABI-layout decode of a curated selector table; `summarizeSimulation` gas/revert; `checkCaps` per-tx/per-day/allowlist; `detectScamSignals` new-address/setApprovalForAll/unlimited/drain/unknown-contract; `weiToEth`/`weiToGwei`; **`assertNoKeyMaterial` no-key-custody invariant**). `wallet_intents` table (no key material). Routes `POST …/wallet/prepare` + `…/:id/simulate` + `GET …/wallet/intents` — **no signing endpoint by design**. | ✅ done | #178 |

| **C1** (pure planner) · Host safety logic | `services/host-planner.ts` (pure, **fail-closed**: `HOST_EXECUTION_ENABLED=false` + `assertExecutionEnabled()` throw until S3; `canonicalizePath`/`isWithinRoot` no-escape prefix check; `hitsDenylist` catastrophic-target hard-deny; `classifyBlastRadius` auto-safe/needs-approval/refuse with destructive-never-auto-safe; `decideAccess`; `planHostOp` combined plan → `file_destructive` approval; undo-journal `buildUndoEntry`/`isReversible`). **No daemon, no routes, NO filesystem execution** — the write/destructive path stays blocked on **S3**. | ✅ done | #179 |

| **B1** (pure helpers) · Voice gateway core | `services/voice.ts` (pure: `normalizeTranscript`; `hasWakeWord`/`stripWakeWord`/`shouldProcessCapture` — push-to-talk default, wake-word opt-in; `gateTranscript` empty/reprompt/accept confidence gate; `orderVoiceProviders` cloud→alt→local→text-only with **local-only for sensitive/wallet contexts** (S1 privacy default); `nextVoiceProvider`; `AUDIO_RETENTION` discard-after-transcription marker). Provider adapters + the `/voice` endpoint land when an **S1** STT/TTS provider is configured. | ✅ done | #180 |

| **B3** (pure routing) · Ask-vs-execute from voice | `services/voice-routing.ts` (pure: `isQuestion`; `routeVoiceCommand` → question→`ask` single-turn, work order→`execute`, **destructive→execute-always** even if phrased as a question; `isFollowUp` re-enters the thread via wake-on-comment). Reuses `intent.ts` + `askmode.ts` + `thread.ts` — no new loop. Endpoint wiring pends the B1 voice endpoint (S1). | ✅ done | #181 |

| **F2** (pure helpers) · Degraded/offline + watchdogs | `services/arturita-resilience.ts` (pure: `actionNeed` local/cloud/host; `routeForConnectivity` — local runs offline, cloud queues offline with a spoken notice, host fails closed when the daemon is down; `planReplay` idempotent nonce-guarded exactly-once replay on reconnect; `defaultArturitaWatchdogs` runtime/cost/no_activity via the shipped `watchdogs.ts`). Queue store + host-health wiring pends B1/C1 execution. | ✅ done | #182 |

| **C1** (daemon scaffold) · Local Host | `adapters/arturita-host/` — a hardened **localhost+token** Node daemon (real `/list`+`/read`+`/preview`+`/apply`+`/undo`). **S3 whole-machine root + minimal self-protection denylist** (own config/token, `.arturita-keystore` burner, SSH/keychains/`.env`/wallet vaults, OS system-integrity `/System`,`/usr`≠`/usr/local`,…); symlink-safe realpath resolution; blast-radius caps. **Reads/previews safe; destructive `/apply` FAILS CLOSED without `approved:true`** (A2 gate) + stages originals to a 10-min undo journal. Refuses to start without a token; binds 127.0.0.1 only. `setup.sh` + launchd + README + 7 action-layer tests; verified end-to-end over HTTP. Backend `host-planner.ts` hardened (system-integrity denylist + `buildPreviewManifest`). `machine_exec` not exposed yet (C3). | ✅ done | #189 |

| **B1** (config + provider + endpoint) · Voice gateway | `services/voice-config.ts` (pure: `resolveVoiceMode` `local\|provider` per-context — **sensitive → forced local**; `selectVoiceProvider` → Chatterbox/NVIDIA \| local \| text-only, never cloud in local mode; `parseVoiceModeSetting` from deployConfig). `services/voice-provider.ts` (`chatterboxNvidiaSynthesize` Bearer-auth NVIDIA TTS; `synthesizeSpeech` never throws — degrades to text on outage; audio not persisted). `POST …/arturita/voice` (Clerk-secured): gate STT confidence → resolve mode → route ask/execute (B3) → create Arturita task → spoken reply. Key read from the encrypted secret store at call time (never logged/persisted). Live raw-audio STT + local engine = go-live. | ✅ done | #188 |

| **F1** (executor wiring) · LLM fallback on the live path | `services/llm-fallback-runtime.ts` (`streamLLMWithFallback` wraps the executor's `streamLLM` call — walks `arturita_fallback_chain` from deployConfig on failure via the shipped pure `planFallback`/`classifyLlmError`; module-level circuit-breaker registry skips unhealthy providers across wakes; cost-bounded — over-cap hops dropped; **behaviour identical to a bare call when no chain configured**). `/health` gains `llm.providers`/`llm.unhealthy` breaker health. Wired the primary executor call + task `llmModel`/result provider reflect the model that answered. | ✅ done | #187 |

| **E2** (policy engine) · Wallet bounded-signing policy *(S4 model change)* | `services/wallet-policy.ts` (pure: `evaluateWalletPolicy` → `autonomous_sign\|require_approval\|refuse` with per-tx threshold (default **$100**)/per-day cap/allowlist/scam/simulate-before-sign; fail-closed `checkSigningGate`/`assertSigningAllowed` keeping **mainnet signing off** unless both switches on; `classifyNetwork`; `SIGNER_MODELS` — WalletConnect can't do unattended signing). `wallet_policy` table (both switches default 0). Routes `GET/PUT …/wallet/policy` + `POST …/wallet/:id/evaluate` (no signing — real testnet signer is go-live). Keystore design: `docs/WALLET-KEYSTORE-arturita.md`. **No key material anywhere; no mainnet signing.** | ✅ done | #186 |

Safety gate: **A2 (on `main`) is the mechanical approval-type gate** every dangerous surface depends on. **S1–S6 CONFIRMED (2026-07-08).** Wallet mainnet autonomous signing stays behind `WALLET_MAINNET_ENABLED`/`WALLET_AUTONOMOUS_SIGNING_ENABLED` (both default off) + a final operator go; real destructive host ops route through A2 with two-phase confirm. **No mainnet wallet signing; no irreversible action without the operator.**

### Epic M — Memory & Vault Graph (operator request 2026-07-08)
| Story | What | Status | PR |
|---|---|---|---|
| **M1/M2** (backend) · Vault graph + Graphify | `services/vault-graph.ts` (pure: `buildNativeGraph` from markdown — [[wikilinks]]/#tags/frontmatter, folder clusters, degree, unresolved-count, file cap; `parseGraphifyGraph` normalizes a Graphify `graph.json` into the same node/edge model, scoped to the vault root, dropping `.obsidian/`+out-of-root leaks). `GET …/memory/graph` (Graphify-first: prefers `<root>/graphify-out/graph.json`, native fallback, TTL-cached, `?rebuild=1`/`?tags=0`, `hasGraphify`+`rebuildCommand`). `vaultTree` (1-call recursive listing). Graph built from the TARCO vault + **semantic pass via local Ollama `qwen2.5:14b`** (S-M1 resolved) — **110 communities / 109 named concepts / god-node hubs / GRAPH_REPORT.md**, 786 nodes/747 edges, **$0 / no key / nothing external**; committed to `vault/graphify-out/`. `parseGraphifyGraph` surfaces `community`/`communityName`; tab shows concepts on hover/search/meta. 19 unit tests. | ✅ done | #192, #194 |
| **M3** (web) · Vault picker + d3-force graph map | MemoryPanel upgrade — vault picker at top + `VaultGraph.tsx` interactive force-directed map (folder-clustered Okabe–Ito palette, zoom/pan/search/hover-highlight/click-to-open, d3-force). | ✅ done | #192 |
| **M+** (web) · Zero-auth vault-graph preview | `web/public/vault-graph.html` — a self-contained, dependency-free render of the enriched Graphify graph (785 in-vault nodes / 108 named communities), reachable at `app.7ei.ai/vault-graph.html` **without login or `GITHUB_VAULT_TOKEN`** (the Memory-tab graph stays token-gated). `build-vault-graph-preview.mjs` precomputes the d3-force layout at build time and bakes positioned nodes/edges inline (no CDN/d3 at runtime → survives any CSP, works offline). Normalization mirrors `parseGraphifyGraph`; Okabe–Ito colorblind-safe, label-first; served public because `middleware.ts` excludes `.html`. | ✅ done | #196 |
| **B2** (web) · Cockpit voice panel | `web/app/dashboard/cockpit/VoiceSection.tsx` — push-to-talk default + "Arturita" wake-word opt-in (S5); browser Web Speech capture with an always-present typed fallback; routes the recognized command through `POST …/arturita/voice` (B1 endpoint + B3 ask/execute); spoken reply via the configured mode (provider TTS bytes → else local `SpeechSynthesis`); approval-aware action feed (destructive execute flagged ⛔ needs-approval) + live approvals rendered inline with the tri-state controls; colorblind-safe (icon+text+shape, red never lone CTA), design tokens only. Pure decision logic (`voicePanel.logic.ts`) + **15 web tests** via the Node 22 built-in runner (zero new deps). Live raw-audio STT engine = go-live. | ✅ done | #197 |

## Paperclip gap-bridge — 5 / 5 phases shipped
| Epic | Phase | Status |
|---|---|---|
| MCA-47 | 1 · Execution core (atomic checkout, run telemetry, deps, overspend) | ✅ Done |
| MCA-52 | 2 · Adapters (http executor, presets) + `7ei-mc` CLI | ✅ Done |
| MCA-56 | 3 · Attachments/work-products, ticket timeline, workspace preview URLs | ✅ Done |
| MCA-60 | 4 · Execution policies + rollback, permissions, run-tokens, plugin jobs | ✅ Done |
| MCA-65 | 5 · Orchestration evals, PWA, self-host Docker | ✅ Done |

## UI (epic MCA-69)
| Story | Status |
|---|---|
| MCA-71 · Task detail drawer (timeline/attachments/runs/comments) | ✅ Done |
| MCA-72 · Governance panel (policies, permissions, revisions/rollback) | ✅ Done |
| MCA-70 · Design tokens + shared primitives | ✅ Done |
| MCA-73 · Accessibility + responsive/mobile | ✅ Done |

**MCA-69 (UI epic) — complete.**

## Go-live (runbook: `GO-LIVE.md`, PR #147)
Engineering done: adapter now pulls `MC_LLM_API_KEY` from the encrypted secret store at boot (no plaintext key on disk);
`adapters/mac-mini/setup.sh` one-command installer + launchd keep-alive; `GO-LIVE.md` documents each step.

Remaining **user-only** console actions (assistant can't create accounts / enter credentials / rotate tokens):
1. Clerk **production** instance + `pk_live`/`sk_live` on Vercel.
2. Google consent-screen sensitive scopes (Gmail/Calendar) + add test user.
3. Rotate NVIDIA key → set as `MC_LLM_API_KEY` secret; rotate vault GitHub PAT.
4. Run `adapters/mac-mini/setup.sh` on the Mac mini; unload the laptop's launchd service.

## Shared-memory bus (vault proposal R1 + R3)
- **MCA-76** (2026-07-02): weekly consolidation routine (Sun ≥04:00 UTC) — losslessly archives >7-day session blocks from each agent's `recent.md` to `archive-recent.md` (prune-then-archive with failure guard), then creates a review task for the org's orchestrator with a consolidation report (promotion per Memory-Protocol via gated memory writes). New `services/consolidation.ts` + 14 tests (448 total).
- **MCA-75** (2026-07-02): `POST /api/agent/memory/session-summary` (namespaced append to `Memory/agents/<slug>/recent.md`, same capability/policy gates as memory writes); `buildSystemPrompt()` now injects org + agent long-term vault memory (TTL-cached, truncated, non-critical on failure); nightly scheduler job exports each agent's DB memory KVs to `Memory/agents/<slug>/kv.md`. New `services/agent-memory.ts` + 19 tests (434 total).

## UI
- **P2 org chart canvas** (2026-07-14): the Org surface is now an interactive pan-and-zoom org chart instead of the MCA-80 indented text list. Studied the live Paperclip org page (`/EIA/org`) by pulling its SPA bundle: it uses **no charting library at all** — a hand-rolled viewport (`data-testid="org-chart-viewport"`) with its own pointer/wheel/pinch handlers, two stacked layers sharing one `translate(pan) scale(zoom)` transform (an `<svg>` of orthogonal elbow paths under an absolutely-positioned card layer), fixed 200×100 cards, and a drag threshold that suppresses the click after a pan. We reproduced that approach rather than adding React Flow (~500 kB for a static tree we already model) — **zero new dependencies**. New pure helper `web/lib/orgLayout.ts`: `buildOrgTree` (flat roster → tree; the web mirror of `services/orgchart.ts`, same loop-guard — a `reports_to` cycle, a self-reference, or a manager outside the set promotes the agent to a root so no agent is lost and no input can hang the renderer), `layoutOrgTree` (tidy top-down: leaves packed left-to-right, each manager centred over its subtree span, one elbow edge per reporting line), `fitToView` (centres + scales the tree, never magnifies past 1×) and `zoomAbout` (the canvas point under the cursor/centre stays put), zoom clamped 0.2–2. `OrgChart.tsx` is now just wiring: cards render the shared `AgentAvatar` (uploaded picture → emoji fallback), name, title/role, the runtime/model line and a 2-line job description, with a status dot that carries **both** colour and glyph plus an SR-only status label (colorblind rule 3 — never colour alone). Drag to pan, wheel/pinch or the +/−/⤢ controls to zoom, click a card to open that agent's Configuration page; glass/token styling only, responsive, empty-state when the org has no agents. **Import/Export company** (top-left) wire to the existing MCA-PC D3 portability endpoints — Export downloads the secret-scrubbed bundle as JSON; Import is **fully wired** (not stubbed) and, per those endpoints' existing semantics, remaps the bundle into a **new** organisation rather than overwriting the current one. `GET /api/orgs/:orgId/orgchart` now also returns the flat `agents` roster (plus `avatarUrl`/`llmModel`/`jobDescription`) so the canvas derives and lays out its own tree through the tested helper; the pre-nested `tree` stays for existing callers. Backend **1,073 tests**, web **181 tests** (+19 layout/geometry), 11/11 evals, web build green.
- **W5 ask-mode** (2026-07-07, epic MCA-83 story W5 — Work surface, **closes epic MCA-83 W**): a task now carries a `work_mode` — `execute` (the full loop: workspace checkout, RAG/Drive/memory context, delegation + synthesis, tool side-effects) or `ask`, a question rather than a work order. An ask is a **single LLM turn**: no workspace/checkout, no RAG/Drive/goal context, no `[REMEMBER]`/`[WEBHOOK]`/`[DELEGATE]` machinery — the answer is posted straight to the ticket thread (agent-authored `answer` comment) and mirrored to `task.output`. New pure helper `services/askmode.ts` (`normalizeWorkMode`/`isAskMode` — anything unknown = execute; `buildAskSystemPrompt` — a lean identity + org + memory prompt that deliberately omits the execute-loop directives so it never advertises tools an ask can't run, and is honest about the one-reply constraint; `ASK_ANSWER_KIND`). Executor: `executeAgentTask` routes an ask task to the new **`answerAskTask`** lean path (reuses the governance/scoped-budget/preflight guards above it + the daily-budget/concurrency slot, skips all the workspace setup); **W3 wake-on-comment re-enters through the same path**, so an answered ask becomes a thread you can keep asking (each follow-up = another single-turn answer with the prior thread as context). Idempotent `ALTER TABLE tasks ADD COLUMN work_mode` (default `execute`); task-create accepts `workMode`; new ergonomic **`POST /api/agents/:agentId/ask`** (Clerk-secured) creates an ask task + fires the run, returns `{ taskId }`. Web: Cockpit **Agent fleet** cards gain a 💬 Ask affordance (inline composer, Enter/Escape, opens the answer in the task drawer); TaskDrawer badges an ask ticket (💬 Ask, accent pill) so the operator reads the thread, not "output". Backend **588 tests** (+8 askmode helpers), 11/11 evals, web build green. **Epic MCA-83 (Work surface / failure UX) complete.**
- **W4 task watchdogs** (2026-07-07, epic MCA-83 story W4 — Work surface, Paperclip gap G2): declarative checks an operator attaches to a task; the scheduler evaluates them each tick and posts an in-thread notice when one flips state — "stop babysitting long runs." New `services/watchdogs.ts` pure helpers (`parseWatchdogSpec` validate/normalize · `describeWatchdog` label · `evaluateWatchdog` check→{triggered,message} · `watchdogTransition` edge-trigger decision) + a DB sweep `runWatchdogSweep()` (mirrors `runHeartbeatSweep`), wired into the 1-min scheduler tick after the heartbeat sweep. Four **kinds**: `runtime` (a *running* run older than N min — the flagship), `cost` (task cost over $X), `no_activity` (task idle over N min, never on a `done` task), `status` (task reaches a watched status: blocked/failed/done/in_progress). **Edge-triggered** by design: `task_watchdogs.state` (ok|triggered) flips and a `system_notice` comment fires only on the transition (⚠ triggered / ✓ cleared) — a task sitting over-threshold for an hour posts *once*, not sixty times (the deliberate correction to the competitor teardown's activity-log spam). The notice is an ordinary `system_notice`, so it renders in the thread and feeds the W1 recovery card's evidence for free. The `no_activity` activity signal excludes `system_notice` comments (incl. the watchdog's own) so it can't flap triggered→cleared on its own post. New `task_watchdogs` table (idempotent CREATE + task/enabled indexes); routes on the Clerk-secured task surface: `GET/POST /api/tasks/:taskId/watchdogs`, `PATCH/DELETE /api/watchdogs/:id`. Web: TaskDrawer gains a **Watchdogs** section (after Runs) — existing checks with ✓/⚠ state icons (triggered = amber + text, colorblind-safe, never color-only) + remove, and an add row (kind Select → threshold input with unit hint, or a status Select). Backend **580 tests** (+10 watchdog helpers), 11/11 evals, web build green. **NEXT: W5 ask-mode (closes epic MCA-83 W).**
- **D2 llms.txt + `7ei-mc onboard`** (2026-07-07, epic MCA-85 story D2 — DX, closes epic D): the two agent-facing on-ramps. **`llms.txt`** (per [llmstxt.org](https://llmstxt.org)) — a concise install/context doc for AI agents/tools: H1 title + blockquote summary, then quick-start (install `@7ei/mc`, `onboard`, drive the agent), the API section pointing at D1's `/api/openapi.json` as the machine-readable source of truth, the token-authed agent-API verbs, and doc links. New pure builder `services/llms-txt.ts` `buildLlmsTxt({ apiUrl, appUrl })` (interpolates the live `PUBLIC_URL` into absolute links, trailing-slash-safe); served public as `text/markdown` at **`GET /llms.txt`** on the API host, with a static mirror at `web/public/llms.txt` so it also resolves at the web/apex domain (7ei.ai/llms.txt on Vercel). **`npx @7ei/mc onboard`** — wraps the org + external-agent + token mint into one guided flow: `--org-name` creates a new org (auto-hires Arturito) or `--org <id>` targets an existing one, then mints an external agent (`--name/--role/--runtime/--provider/--model`) and prints its `MC_AGENT_TOKEN` **once** plus the `export` lines. Both mint endpoints are Clerk-secured, so onboard authenticates with the operator's Clerk session JWT (`MC_CLERK_TOKEN`), not an agent token; `--dry-run` prints the planned requests without calling the backend. New pure planner `cli/onboard.mjs` (`parseFlags`/`parseOnboard`/`orgRequest`/`agentRequest`) + runner in `mc.mjs`. Backend **570 tests** (+4 llms.txt), CLI **7 tests** (+5 onboard), 11/11 evals, web build green. **Epic MCA-85 (DX) complete.** NEXT: W4 task watchdogs, W5 ask-mode.
- **Per-org webhook receivers — shared-secret verification** (2026-07-07, follow-up to the auth-scoping hardening / MCA-85 D1): the two public per-org inbound receivers — `POST /api/telegram/webhook/:orgId` (`comms.ts`) and `POST /api/jira/webhook/:orgId` (`jira-webhook.ts`) — had **no signature check**, so anyone who guessed the URL could POST forged Telegram/Jira events for any org (fake messages, spoofed issue-status syncs that flip local task state). The auth-scoping pass had correctly kept these public (external services carry no Clerk session) but left the residual open. New pure helper `services/webhook-auth.ts`: `deriveWebhookSecret(serverSecret, channel, orgId)` = `HMAC-SHA256(serverSecret, "channel:orgId")` — deterministic (no per-org storage) and bound to both org **and** channel (no cross-tenant/cross-channel replay); `verifyWebhookSecret()` constant-time compares (never throws on length/format mismatch); `checkWebhook()` returns authorized/enforced. **Telegram**: `register` now passes the derived token as Telegram's `secret_token`, which Telegram echoes back in `x-telegram-bot-api-secret-token` on every update; the receiver verifies it. **Jira** (Cloud can't set a custom header): `webhook-url` mints the URL with `?secret=<derived>`; the receiver checks the query param (or an `x-webhook-secret` header). Gated on `WEBHOOK_SIGNING_SECRET` (falls back to legacy `TELEGRAM_WEBHOOK_SECRET`/`JIRA_WEBHOOK_SECRET`): set → enforced (403 on missing/forged/cross-org token, before any DB work); unset → receivers stay open (dev parity with the global Telegram webhook). **Re-register both integrations after setting the secret.** New `webhook-auth.test.ts` (11 tests: pure-helper determinism/replay + receiver 403/200 integration inject for both channels). Backend **566 tests** (+11), 11/11 evals, web build green.
- **Auth-scoping hardening — org/agent route groups moved behind Clerk** (2026-07-07, follow-up to MCA-85 D1): D1's `/api/openapi.json` made every route's auth scope visible and surfaced that several tenant-scoped groups had been registered in the **public** scope with no Clerk session — a real data-exposure/abuse hole. Audited the full route table and moved the affected groups into the Clerk-secured scope in `index.ts`: **jira** (`/orgs/:orgId/jira/*`), **gmail/comms** (inbox, gmail threads/send, telegram + meet send), **notifications** (`/orgs/:orgId/notifications` + push register — closes a push-token-hijack vector), **memory** (`/agents/:agentId/memory` read/write), **outbound webhooks** (`/orgs/:orgId/webhooks` — was an unauthenticated SSRF/exfil surface), **usage** (`/orgs/:orgId/usage`,`/limits`), and **skills** (read + write/sync — anon skill injection, which agents execute; the web app already sent a token and `docs/API.md` already marked it auth-required). Two files that mixed an external **webhook receiver** with org-scoped reads were split so only the receiver stays public: `comms.ts` → `commsWebhookRoutes` (Telegram receiver); `jira-webhook.ts` → `jiraEventRoutes` (secured events/live/webhook-url) vs. the public Jira receiver. Genuinely-public surfaces unchanged: inbound webhook receivers, the Google OAuth handshake, the static model catalogue, `agent-api` (its own token), routine URL-token trigger, `openapi`/`health`. **No client breaks** — every web callsite already passed a Clerk token (verified). New **lock-down test** (`auth-scoping.test.ts`): boots the app like `index.ts` and fails if any `:orgId`/`:agentId` route is public outside a short, self-verifying allowlist (webhook receivers + OAuth), and asserts the secured scope actually returns 401; the openapi integration test now asserts the moved routes carry `clerkAuth` and the receivers stay open. Backend **557 tests** (+2), 11/11 evals, web build green.
- **D1 self-describing API — `/api/openapi.json` + `7ei-mc openapi`** (2026-07-07, epic MCA-85 story D1 — DX, Paperclip gap G6): the API now describes itself, so agents/tools self-configure instead of drifting against a hand-written doc. New `services/openapi.ts` pure helpers — `buildOpenApiSpec()` assembles a valid, deterministic **OpenAPI 3.1** document; `zodToOpenApiSchema()` converts the constructs our validators use (object/string+min/max/email, number/int, boolean, enum, literal, array, record, nullable/optional/default, union-of-literals→enum; unknown → accept-anything, never throws); plus `openApiPath`/`pathParameters`/`tagForPath`/`defaultSummary`. **The route table is collected live from Fastify's `onRoute` hook** (in `index.ts`), so every registered path is in the spec — it *cannot* drift the way `docs/API.md` does. Auth is tagged by three composable hooks: a baseline root hook (`none`) that fires for all routes, plus scoped hooks in the secured scope (`clerk`) and a new wrapper scope around the agent API (`agentToken`); rank makes it order-independent, auto HEAD/OPTIONS filtered. Request bodies + summaries come from the **actual Zod validators** via `documentEndpoint()` — routes push their schema into the service (routes→services is the allowed import direction; a service must not import routes), wired across the whole agent-facing surface (`documentAgentApi()` in `agent-api.ts`). New public `GET /api/openapi.json` (served from the live route table + docs; version + `PUBLIC_URL` server). CLI: `7ei-mc openapi` fetches the spec token-free (`mc.mjs` now only requires `MC_AGENT_TOKEN` for non-public commands). Live spec: **158 paths, 26 tag groups**. `docs/API.md` header now points to the endpoint as the machine-readable source of truth. Backend **555 tests** (+15: 14 helper/wiring + 1 end-to-end inject), CLI test +1, web build green. **NEXT per plan order: D2** (llms.txt + `npx 7ei-mc onboard`); then W4 watchdogs, W5 ask-mode.
- **V3 per-wake preflight cap + cheap-model config validation** (2026-07-07, epic MCA-84 story V3 — Visibility, closes epic V's cheap-wins tier / Paperclip gap G9): two small levers on the existing budget/cost code. **Per-wake preflight cap** — before an internal wake runs the LLM, bound its *worst-case* cost (full input context at the input rate + a full `max_tokens` completion at the output rate) and skip the wake if it would exceed a configured per-wake ceiling; the task is parked `blocked`/`needs_attention` with a plain-language reason and a durable `system_notice` comment (feeds the W1 recovery card). Distinct from the cumulative scoped budgets in `budget.ts` (those stop an agent once *total* spend crosses a limit) — this stops a *single* wake from being surprisingly expensive (runaway context, an accidental Opus swap). Runs before any expensive setup (RAG/Drive/memory fetches) so a capped wake costs nothing. Opt-in via `deployConfig.maxCostPerWakeUsd` (org-wide) with an optional `maxCostPerWakeUsd:<agentId>` override; null/≤0 = no cap. **Cheap-model config validation** — new `services/preflight.ts` pure helpers (`estimateWakeCost`/`preflightWake`/`parseCapUsd`/`validateModelConfig`/`validateRoster`) audit each agent's model: unpriced models (no `COST_RATES` entry — spend can't be tracked or capped) and models whose output rate exceeds the "cheap" threshold ($5/M output — passes Haiku/GPT-4o-mini/Flash/DeepSeek/Qwen-turbo/local, flags Sonnet/Opus/GPT-4o) are surfaced with reasons. Unpriced models under a cap are *allowed but flagged* `unbounded` (never silently block a working custom/local runtime). New `GET /api/orgs/:orgId/preflight` (cap + per-agent validation rows + warn count) and `PUT` (set/clear the cap in deployConfig). Web: `cockpit/PreflightSection.tsx` after Budgets — editable per-wake cap (accent CTA, never red) + per-agent audit rows (✓/⚠ iconed + amber, colorblind-safe) with est-max-wake-cost and flagged issues. Backend **540 tests** (+19), web build green.
- **V2 tri-state approvals + inbox retry rows + read receipts** (2026-07-07, epic MCA-84 story V2 — Visibility): closes the three Paperclip failure-UX gaps that live in the inbox and board. **Tri-state approvals** — approvals were binary (approve|reject); added `revision_requested` + a reviewer note (the revision loop). New `services/approvals.ts` pure helper `decideApproval()` validates the tri-state and computes the record patch (`revision_requested` *requires* a note — the loop is pointless without telling the requester what to change; approve/reject take an optional trimmed note). New `approval_requests.decision_note` column (idempotent ALTER). `/api/approvals/:id/decide` now accepts `{ decision, note }` for all three states. InboxSection gains an inline "↩ Request changes" composer (Enter to send, Esc to cancel) between ✓ Approve and ✕ Reject — every action iconed, red never the lone CTA (colorblind rule). **Inbox retry rows** — `buildInbox` marks failed tasks `retryable` and carries their `output` as a truncated inline `error`; the executor catch now persists the failure text as `output` (external result path already did) so the row shows *why* it failed with no extra fetch; InboxSection failed rows render the red error line + a "↻ Retry" button that re-executes via the existing `POST /api/tasks/:id/execute`. **Board read receipts** — new `services/receipts.ts` pure helpers (`taskActivityAt` = max(createdAt, completedAt); `unreadTaskIds` — unread when activity is newer than the operator's receipt, or never opened); new `task_reads` table + `POST /api/tasks/:id/read` (one row per user/task, bumped on open); `/cockpit` tags each task `unread` for the caller; cockpit TaskBoard cards are now click-to-open (wired to the page drawer) and unread cards carry a "● new" chip (accent + text, never color-only), cleared optimistically + durably on open. Backend **521 tests** (+15: approvals 6, receipts 8→ inbox 3), web build green.
- **W3 task thread + wake-on-comment** (2026-07-07, epic MCA-83 story W3 — Work surface): commenting on an idle ticket picks the work back up (Paperclip parity — the async office loop, closed). New `services/thread.ts` pure helpers (`decideWake` — a *user* comment on a task that has an agent and no in-flight run wakes it; `isWakeableStatus` — pending/assigned/blocked/failed/done, `done` reopens the ticket, `in_progress` deliberately excluded since an internal agent mid-run writes no run row so `activeRun` can't prove it and a wake would double-fire; a stalled in_progress is normalized to pending by the heartbeat orphan sweep; `hasActiveRun`; `threadHistory` — projects the prior thread into the executor's `conversationHistory`, agent→assistant / user+system_notice→user, oldest-first, capped+truncated; `buildWakeInput` — frames the comment as a follow-up). No new store: the thread IS `task_comments`, the wake reuses `executeAgentTask` (which owns every task/run transition — internal re-runs the LLM loop, external re-notifies). `POST /api/tasks/:id/comments` now decides + fires the wake (posts a durable "Agent woken" system-notice, replays the thread), returns `{ comment, woke, wakeReason }` (optional `wake` body flag forces/suppresses); `GET …/comments` enriches agent authors with name+emoji so the thread reads as a conversation. Web TaskDrawer: composer promises the wake up front (`Comment & wake` button + hint) when the task is wakeable, shows an accent-tinted "agent woken — re-running…" confirmation (colorblind-safe: ↩ glyph + accent, never color-only) and reloads to surface the new run, renders agent comments as `emoji name`. Backend **506 tests** (+10), web build green.
- **V1 heartbeat 24h timeline** (2026-07-07, epic MCA-84 story V1 — Visibility): per-agent lanes of activity blocks over the last day — the office's pulse (who worked, when, on what). New `services/timeline.ts` pure helpers (`runActivity`/`taskActivity`/`mergeActivity` normalize the two activity sources, `buildHeartbeatTimeline` projects them onto a 24h window as start/width percentages). No new store: external agents write `agent_runs` (authoritative start/end per session), internal agents record `durationMs`/`completedAt` on the task itself — `mergeActivity()` reconciles so each wake shows once (runs win; a task with runs contributes no task-derived block). New `GET /api/orgs/:orgId/timeline` (last-24h runs ∪ ongoing runs, recent tasks → merged → lanes; idle non-terminated agents included so you see who is *not* working). Web: `cockpit/TimelineSection.tsx` — one lane per agent (emoji/name + heartbeat status icon + run-count/active-time/spend), a track with axis ticks (24h→now) + gridlines, blocks coloured on the status table, ongoing runs striped (a shape cue, not colour), a persistent colour→status legend; wired into CockpitPanel after Agent fleet, loads with the cockpit payload + skeleton. Colorblind-safe: every block carries a text tooltip/aria-label and the legend keys colour→status; `orphaned` aliased → failed in `status.ts`. Backend **496 tests** (+14), web build green.
- **T3 hex sweep + contrast audit** (2026-07-06, epic MCA-82 story T3): completes the theme migration. Verified no raw hex remains in any dashboard component (T1 already re-pointed cockpit `shared.tsx`/`status.ts` domain colors at the semantic/purple CSS vars); dropped the dead `--7ei-*` raw-hex brand aliases from `globals.css` to hold the "raw hex only in `tokens.ts`" invariant. **WCAG AA contrast audit both modes** (scripted): all text/surface pairs pass AA (≥4.5:1) on page `--s0` and card `--s1`; fixed `--muted` which was failing for hints/labels/placeholders/idle status — light `#8a8a88`→`#6b6b6b` (3.5→5.3:1), dark `#555555`→`#7e7e7e` (2.6→4.7:1); documented `--accent` on dark as a kept AA-large brand item (3:1 non-text rule, never body copy). **PWA `theme-color` now theme-aware**: the pre-paint script + ThemeProvider point the browser `theme-color` meta at the resolved page surface (`--s0`: light `#f5f5f3` / dark `#070707`) so the address/title bar matches the app in both modes incl. explicit toggle override (no flash); new `themeSurface()` single-source helper; manifest `theme_color`/`background_color` corrected `#0a0a0a`→`#070707` (matched no token). Web-only; build green, backend 482 tests untouched. **Epic MCA-82 (theme) complete: T1→T2→T3.**
- **W2 work surface** (2026-07-06, epic MCA-83 story W2): reasoned blocker chips + next-up + sub-task cost rollups. New `services/worksurface.ts` pure helpers (`nextUp`/`readyQueue` — the single task the office should pick up next: unstarted, every upstream blocker `done`, highest priority then oldest; a blocker id absent from the set counts as still blocking, so readiness is never over-claimed; `rollupCost` — parent's own spend + sub-tasks' spend/tokens). `buildRecovery()` gains a `blockerTasks` input and emits reasoned `blockers` chips (upstream title + status, open-first) so the recovery card shows *why* a task is blocked, not just a count. Routes: `/recovery` resolves `blockedBy` ids → chips; `/subtasks` now returns `{ subtasks, rollup }`; `/cockpit` returns `nextUp`. Web: RecoveryCard renders blocker chips (iconed + status-coloured, colorblind-safe); TaskDrawer shows a sub-task cost roll-up row + header "incl. subtasks" total; TaskBoard gains a "Next up" banner (accent-tinted, ▶ glyph — never color-only) and flags the head card in To-do. Backend **482 tests** (+12), web build green.
- **W1 recovery cards** (2026-07-06, epic MCA-83 story W1): failure/recovery UX (the Paperclip gap). New `services/recovery.ts` pure helper `buildRecovery()` derives a structured recovery card from existing state (task status + run history + system-notice comments) — no new failure store. `GET /api/tasks/:taskId/recovery` returns `{ owner, sourceRun, evidence, nextAction, since, reason }` (or null), enriched with the owner agent's name. Failures now post a durable `system_notice` comment to the ticket thread from both execution paths (in-process executor catch + external-adapter result endpoint); new idempotent `task_comments.kind` column (user|agent|system_notice). Web: `RecoveryCard.tsx` — red LEFT border + ⚠ heading (prominence by placement, retry button is purple not red per colorblind rule 4), structured owner/source-run/evidence/next-action rows, "open · Nm ago" until decision; rendered at the top of `TaskDrawer`, with a Retry-run action (re-executes then reloads so the card self-clears) and Add-a-note. System-notice comments render iconed/tinted in the thread; TaskBoard flags failed/blocked cards with ⚠. `TextInput` primitive now forwards refs. Backend **470 tests** (+10), web build green.
- **T2 glass chrome** (2026-07-06, epic MCA-82 story T2): controlled glassmorphism on chrome only (DESIGN_SYSTEM v2) — sidebar, TaskDrawer, all cockpit modals (shared `Modal`), and a new ⌘K command palette all frosted via a single `.mc-glass` utility (translucent `--glass` fill + 16px blur, solid `--s1` `@supports` fallback; never on content cards/rows). New `CommandPalette.tsx` shell: keyboard-first (⌘/Ctrl-K toggle, ↑↓/↵/esc), grouped Navigate + Theme commands, colorblind-safe selection (accent fill + ring, not color-only); designed for Epic V to extend by concatenating Commands. Sidebar gains a `Search… ⌘K` launcher (desktop-only). Tokenized `--shadow-modal`/`--shadow-drawer` replace the baked-in dark drop-shadow so chrome reads right in both themes. Web-only; build green, backend 460 tests untouched.
- **MCA-86** (2026-07-06, epic MCA-82 story T1): 7Ei theme foundation — light+dark via CSS variables (`tokens.ts` v2 theme map, single source of raw hex), ThemeProvider + three-state toggle (system/light/dark, pre-paint script, no flash), colorblind-safe status helpers (⬡ active purple, ✓ done, ⏸ paused, ⛔/✕ red always iconed) applied to KPI strip/fleet/task statuses, primary buttons now purple / danger = red outline, official 7-hexagon mark top-left (currentColor, both modes). Glass chrome = T2.
- **MCA-81** (2026-07-06, epic MCA-78 story 3 — EPIC COMPLETE): KPI strip (dense variant A: agents health/tasks/spend/approvals), loading skeletons across panels, consolidated Google card (one grant, per-service rows + toggles), gear settings on connector cards (Google: switch-account via `?switch=1` re-consent, calendarId, Drive scope; Vault: repo/root/branch + replace-token). Backend: `GOOGLE_CONNECTOR_CONFIG` config-as-secret + GET/PUT config endpoints; config honored in gmail/calendar/drive routes (default-open). 460 tests. Note: bound-account email shows '—' until callback stores userinfo (follow-up); agent-executor drive search doesn't honor driveScope yet (flagged).
- **MCA-80** (2026-07-03, epic MCA-78 story 2): CockpitPanel 808→129-line composition root + 13 section components under `dashboard/cockpit/`; Cockpit + TaskDrawer + page.tsx migrated to the shared api client, primitives, and 28px density; a11y additions (aria on icon actions/toggles, progressbar semantics); fixed the undefined `s.tag` style bug.
- **MCA-79** (2026-07-03, epic MCA-78 story 1): UI foundation — tokens gain density/text/space scales (28px rows/controls), shared primitives `dashboard/ui.tsx` (Button/Card/Pill/TextInput/DenseTable/SectionLabel), shared API client `web/lib/api.ts` (Clerk-token aware + MCA-77 error mapping); Connectors/Governance/Memory panels migrated. Cockpit split = story 2.
- **MCA-77** (2026-07-03): Connectors UX from design critique — inline Replace-token (GitHub/HF/vault; no disconnect needed for rotation), honest error mapping (network vs HTTP status + 401/403 hint), status pills (Connected/Failing/Untested) + last-tested time, truthful header count, design-token adoption in the panel.

## Refactors
- **MCA-74** (2026-07-02): `routes/all.ts` (1,383 lines) split into domain modules (orgs/agents/tasks/projects/costs/skills/auth/credentials), `all.ts` kept as barrel; `sendPushNotification` + token map extracted to `services/push.ts` (fixes the routes→services inversion). No behavior change; 415 tests + 11/11 evals green.

## Standing items (carry forward)
Not blockers for the code — deferred console/ops actions and follow-through owed after the v2 epics:
- **Jira transitions pending** — MCA-82/83/84/85 stories are shipped in code but not transitioned in Jira. Atlassian Rovo OAuth is **unavailable in these build sessions**, so the assistant cannot move them; transition MCA + OS issues to Done in the next interactive/authorized session (cloudId `5dadc567-085a-4cd8-99a3-c0bd9886fee9`).
- **`WEBHOOK_SIGNING_SECRET` rollout** (PR #166) — set the secret via Fly/Cockpit to enforce per-org Telegram/Jira receiver verification. It's **open in dev until set**. After setting it, **re-register both integrations** (Telegram `secret_token`, Jira `?secret=` URL) so they carry the derived token — see the W-side note in `services/webhook-auth.ts`.
- **7ei.ai apex DNS for `llms.txt`** (PR #167) — `web/public/llms.txt` mirror resolves at the web/apex domain only once apex DNS points at Vercel; API host already serves `GET /llms.txt`.
- **Vault milestone mirror** — mirror the v2-epics-complete milestone into the Obsidian vault `07-Agents/` (repo `Arturito7ei/7Ei-MC_TARCO`, content under `vault/`), alongside `STATUS-Mission-Control-2026-07-02.md`.
- **Go-live user-only console actions** — unchanged, see `GO-LIVE.md` §1–4 (Clerk prod keys, Google sensitive scopes, NVIDIA/vault key rotation, Mac-mini adapter move).

## Verify
`cd backend && npm test` · `npm run evals` · `cd web && npm run build` · self-host: `docker compose up -d --build`.
