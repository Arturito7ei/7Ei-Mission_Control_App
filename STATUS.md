# 7Ei Mission Control — Status

_Last updated: 2026-07-18 (**FIX-1 — FOUR DEFECTS FROM A LIVE VISUAL PASS AGAINST app.7ei.ai (web + mobile).** Four things the 2800-test suite structurally could not see, because each is a property of what the operator READS, not of what a function returns. **① THE INBOX / ACTIVITY CONTRADICTION — case (a): the shared wording was lying; the Inbox was RIGHT.** Live, the Inbox read "Nothing needs a decision right now" while Activity, one click away, badged SIX rows "Awaiting decision". Neither surface was wrong. The Inbox lists PENDING APPROVALS (`routes/tasks.ts` filters `approvalRequests.status = 'pending'`); those six rows were TASKS, and `tasks.status` **DEFAULTS to `'pending'`** in `db/schema.ts` — every task is born pending, which means QUEUED, waiting for an AGENT. `taskOutcome('pending')` and `approvalOutcome('pending')` both normalise to the feed outcome `pending`, and one shared `OUTCOME_LABEL` rendered both with the operator's phrase, turning routine queue depth into six phantom obligations. **No functional gap: nothing was missing from the Inbox.** Fixed with a kind-aware `outcomeLabel(kind, outcome)` — and note the DIRECTION, which a test forced: the obvious shape (default "Awaiting decision", override `task`) was written first and REJECTED, because every *other* kind still inherited the operator's phrase and the contradiction was one new kind away from returning. The phrase is now GRANTED to the single pair that earns it (`approval_filed`/`pending`), the default is neutral ("Queued"), and an invariant test pins that set to EXACTLY ONE member — so what the feed badges and what the Inbox offers buttons for cannot diverge again. Two collisions of the same class fixed alongside: a `blocked` task read "Refused" (nobody refused it) and a `cancelled` agent run read "Refused". Tone follows the label via `awaitsOperator()`, so `warn` — the "this wants you" colour — is no longer spent on a queued task. **② THE MEMORY GRAPH WAS EMPTY IN PRODUCTION** ("⬡ Graphify · 0 notes · 0 links" over a vault whose Reader tab listed the whole tree). Two independent causes, both fixed in CODE rather than config, because the operator should not have to know either: **(a)** the route's candidate loop `break`ed on any truthy parse and `parseGraphifyGraph` never returns null — a zero-node parse counted as SUCCESS and short-circuited the native-wikilink fallback. The loop is lifted into `selectGraphifyGraph()` (a `read` callback is its only I/O, so the decision is reachable by a test at all — the route had NO test coverage whatsoever) and an empty parse now falls through. **(b)** `source_file` is only meaningful relative to wherever graphify ran: a `graph.json` generated INSIDE the vault records `00-Index/foo.md`, and scoping that against root `vault` matched nothing. **Root-relative paths now resolve — YES, deliberately** — but the retry is conditioned on `kept === 0 && rawNodes.length > 0`, the only case where it can change an answer: a correctly-rooted graph keeps ≥1 node so the out-of-vault filtering is untouched, and a genuinely empty payload has no raw nodes so the empty state survives. It widens the accepted PREFIX, never the escape — the `..` traversal guard is asserted through the retry. **③ THE AGENT PICKER CLIPPED ITS OWN LABEL** at the baseline. Geometric, not cosmetic: a hard `height: 26` with `boxSizing: border-box`, zero vertical padding, and `text.md`'s fontSize taken WITHOUT its 18px lineHeight. Now `minHeight: density.ctrl` + explicit lineHeight + padding + `appearance: none`, so the box GROWS rather than crops at any zoom or with a long agent name. **AND YES — app-wide `color-scheme` was added**, keyed on `[data-theme]` (not a `prefers-color-scheme` media query, because this app has an explicit toggle that overrides the OS and the two must not disagree). GC-1 flagged this as out of scope and worked around it locally; it is the root fix, and it closes the class for all five raw `<select>`s in `web/app/dashboard/`. **④ THE ACTIVITY LOG WAS ~70% PLUMBING.** `projectAuditEvent` sets `title` to the raw `action` column (`post.orgs`, from `classifyAction`'s machine fallback) and `target` to `METHOD + ' ' + path` with a full org UUID. Audit rows now render a human phrase (`POST arturita › converse`) — general by construction, eliding ids and the org prefix rather than special-casing endpoints, which would go stale silently as routes are added — with the raw pair kept in the tooltip. Runs of 2+ consecutive audit rows collapse to one tappable "N routine audit events" line. **NOTHING IS REMOVED AND NOTHING IS REORDERED:** every item is retained in the collapsed run (expanding is a client toggle, not a refetch), the feed stays chronological, a LONE audit row is left alone, and the Audit filter chip passes `collapse: false` as a strict pass-through — all four asserted. **AUTHZ UNTOUCHED:** zero server-payload changes; the role-based kind-dropping and allow-list projection are byte-identical, and all four fixes are display-layer or parser-layer. **Tests: 24 new** (backend 9, shared web⇄phone 15), **every guard mutation-proven — 11 mutations, each turning the suite red on the semantically correct tests, baseline green after restore** (`docs/FIX-1-mutation.md`), including restoring each shipped bug verbatim. The web⇄phone helpers are pinned OUTPUT-wise across the whole kind×outcome cross product and both collapse modes, not merely name-checked. Backend **1862/1862** · evals 11/11 · typecheck clean · web **311/311** + build · mobile **357/357** + typecheck + export. **Parity: MIRRORED** — phone ships the same labels, the same audit phrasing and the same collapsing, from the same hand-copied module under the existing drift tripwire. The picker clip is **N/A on mobile** (a `Pressable` + `Modal`, no native `<select>`, no UA metrics). **Visually UNVERIFIED against the running apps** — the fixes are reasoned from the geometry and the data, and the live pass that found them is the check that should be repeated after deploy. **THE TEST-SHAPED GAP THIS EXPOSES:** all four defects are cross-surface *semantic* properties — "two surfaces must not contradict each other", "a confident count must not be zero when the source is non-empty" — and the suite had no shape for them. The one-member `AWAITING_DECISION` invariant is the first of that shape; `selectGraphifyGraph` exists so a route decision is testable at all. Both are worth generalising. `fix-1-live-pass`. **AUDIT ROUND 2 (PASS-WITH-NITS) — two Mediums folded, plus the generalisation.** The auditor CONFIRMED case (a) with stronger evidence than was cited: `inboxKind()` (services/inbox.ts:29-35) returns null for `status='pending'`, so a queued task is STRUCTURALLY incapable of entering the Inbox — the empty Inbox alongside six queued tasks was the only possible behaviour, not a coincidence. **MEDIUM-1 was a regression THIS PR created:** `appearance: none` (needed to drop the UA metrics that were half the clipping) also removes the native dropdown caret, and nothing drew a replacement — the picker lost every cue that it IS a dropdown. A caret is now drawn in a positioned wrapper (`pointer-events: none`, `aria-hidden`) with right padding reserving its lane so a long name ellipsises before reaching it. **MEDIUM-2 was real and mine:** the root-relative retry genuinely widened out-of-vault scoping — when EVERY node is foreign the `kept === 0` condition also trips, and a scope of `''` makes `inVault` accept any prefix, so a graph.json of `/Users/...` paths went from correctly-0 to fully rendered with bogus clusters (`Users`, `.github`). Medium not High because `isSafeVaultPath` independently refuses the READ, so it was a trust/correctness bug rather than disclosure — but a parser that renders a node it would refuse to open is still lying. The retry pass now rejects paths that cannot be vault-root-relative by construction (absolute, `~`), and the code comment claiming the scoping was 'untouched' has been corrected to say what is true: untouched for MIXED payloads, deliberately relaxed for all-foreign ones. **Nits folded:** the stale comment asserting the app 'never declares color-scheme' (this PR made it declare it); the fully-silent `catch {}` that made a CORRUPT graph.json indistinguishable from NO graph.json — both reported `hasGraphify: false` — now `console.warn` + a `graphifyError` field surfaced on BOTH surfaces, because a field nobody renders is half a fix on a change whose thesis is 'stop lying about the graph'; and `p.includes('..')` over-rejecting a legitimate note named `a..b.md`, now a segment check. **TEST-PLACEMENT GAP CLOSED:** every FIX-1 activity test lived in the MOBILE workspace, so breaking `web/lib/activityKinds.ts` left the `Web (web)` job GREEN and only Mobile went red — and with no branch protection, a red job reports rather than blocks. `web/lib/activityKinds.test.ts` now makes the web job answer for the web module; verified by breaking ONLY the web copy and watching the web suite go red. **THE GENERALISATION, built and proven:** `backend/src/tests/inbox-activity-agreement.test.ts` seeds one row of every status of ALL SIX kinds, drives the REAL activity route and the REAL inbox route over a REAL database, applies the ACTUAL shipped label function (imported from web/lib, not re-implemented — re-implementing it would be exactly the module-against-module test that missed the bug), and asserts SET EQUALITY between {rows the feed badges AWAITING_DECISION} and {rows the Inbox will decide}. **The rule it encodes: any phrase promising the operator an action must be pinned to the set of rows the action surface will actually accept.** It bites on the original bug (G1) AND on action-surface drift (G3 — the Inbox stops listing pending approvals) AND on any kind earning the phrase (G4/G6/G7). **Two findings from running it, both of which changed the test:** the first draft seeded only tasks and approvals, so a promise attached to an unseeded kind was INVISIBLE and G4/G6/G7 all passed — the fixture is only as strong as the rows it runs over (the same 'fixture sized to the mechanism' trap this repo has hit before); and a first G4 attempt was a FALSE NEGATIVE, injecting a duplicate `agent_run` object key that the existing entry later in the literal overwrote, so the mutation never applied — logged because a silently-ineffective mutation reads exactly like a well-covered guard. **M7 also initially survived** (nothing pinned the absolute-path rejection as RETRY-ONLY, so hoisting it to pass 1 would silently drop legitimate `/vault/…` paths); the missing test was written for it. **16 mutations total across both rounds, every one biting the semantically correct tests.** Backend **1873/1873** · evals 11/11 · web **329/329** + build · mobile **357/357** + typecheck + export. Still visually unverified — and note that the caret was the SECOND CSS-shaped defect here found by a human rather than a test, which is the strongest argument in this repo for a rendering check of some kind. `fix-1-live-pass`.)_

> **After a reboot, start at [`docs/HANDOVER.md`](docs/HANDOVER.md)** — where main is, the suite
> numbers, the open operator actions (Fly keys, OAuth redirect URI, `ALLOWED_ORIGINS`, branch
> protection), and the known-open follow-ups. This file is the full narrative log; that one is the
> cold-start briefing.

### 2026-07-21 — AAD-2: the "+ Agent" and "Delete agent" UI (web + mobile)

Phases 2–4 of `docs/PLAN-agent-add-delete.md`, on top of AAD-1's backend (`3665a1f`).
**Client-only: no endpoint, no schema, no gate changed.**

**"+ Agent".** The shipped ONB6 `InviteAgentDialog` is now mounted where an operator
actually looks for it — the **Agents roster** and the **Org** section — both of which
had no add affordance at all, because all three creation buttons live in the
Operations toolbar behind `{!focused && …}`. No second mint path, no new store; the
dialog keeps its honest "public join is closed on this deployment" banner. The picker
stays registry-driven, and now also **shows the five deferred runtimes** (openclaw_gateway,
http_webhook, hermes_gateway, hermes_local, grok_local) as inert "not yet available"
rows with the registry's own reason — they were previously just absent, which read as
"unsupported" rather than "declared, not yet dispatchable". **None is selectable and
none was flipped to `available: true`.** Invited agents still land `low_trust_review`
with shell OFF (untouched — `secureRegistration()` and the registry default).

**Delete agent.** A **Danger zone** at the foot of Agent Settings → Configuration, wired
to AAD-1's owner-gated `DELETE /api/orgs/:orgId/agents/:agentId`. Typed-name confirmation,
consequences listed (credential revocation is the part "delete" does not convey), cleared
only on a confirmed 2xx — a 403 never renders as success (the APPR-1 lesson). On success
the page routes back to the roster and re-reads it.

**Owner gating.** New shared `useOrgRole` hook hoists the role signal the web already
had — server-computed `isOwner` from `GET …/activity` — and `canDeleteAgent` fails closed
(`'admin'`, unknown, null → false). Non-owners get a named note, not a button that only
403s. The phone reuses its existing `orgRole`.

**Parity: MIRRORED in the same PR.** `apps/mobile` gains the invite sheet (Agents + Org)
and the Danger zone, over the same endpoints, with `invites.ts` / `agentDelete.ts` pinned
against the web modules by drift tripwires. Zero-dep: RN's built-in `Share`, no clipboard
package. **Visually verified on web** (dev server + stub backend): both entry points, the
picker's available/not-available split, the confirm dialog, the real DELETE on the
org-scoped path, and the control disappearing when `isOwner` is false. **Mobile is
compile-verified only** (typecheck + 369 tests + `expo export`) — no simulator run.

Backend **1898/1898** · evals 11/11 · web **333/333** + build · mobile **369/369** +
typecheck + export. `aad-ui-web-mobile`.

### 2026-07-19 — session status: main is `17f172e`, green, nothing in flight

Test, CI and Deploy all success on `17f172e`; `https://7ei-backend.fly.dev/api/health` 200,
`db: connected`, v1.3.0. Every requested story is merged.

**Live visual pass — COMPLETE for web.** A read-only Chrome pass against app.7ei.ai after the
FIX-1 deploy confirmed all four fixes live and correct: memory-brain graph renders (153 notes /
119 links); Inbox and Activity agree — queued tasks read "Queued"; Activity audit noise collapses
into expandable "N routine audit events" and rows read `POST arturita › converse` rather than raw
UUID URLs; agent picker shows full label + caret. **Mobile remains visually unverified** (no
simulator run).

**Open — operator-side only, none of it in the repo:** branch protection on `main` (still off, so
no check gates a merge), the red `npm audit` (5 pre-existing dev-tooling vulns — it is why the
Security Scan is permanently red and carries no signal), operator credentials incl.
`ALLOWED_ORIGINS` (currently `*`), `OPENAI_API_KEY` on Fly, and the Google OAuth redirect URI.
Plus a zombie Dispatch "Disconnected / Approve once" card held **remotely** — dismiss it on the
device or toggle the Dispatch connection; approving can never clear it. Details:
[`docs/HANDOVER.md`](docs/HANDOVER.md).

**Recommended next build: GC-2 — thread persistence.** Command Center chat history is client-side
`useState` and dies on refresh; it is the most user-visible gap remaining.

### 2026-07-18 at a glance — shipped at `b164184`, all suites green

Backend 1873/1873 · evals 11/11 · web 329/329 · mobile 357/357 · typechecks + web build + mobile export clean.

| Area | Story | SHA / PR |
|---|---|---|
| Connectors | CONN-4b GitHub PAT + Jira | `adf06b3` #313 |
| Connectors | CONN-5 per-agent Google OAuth | `f2a3b08` #314 |
| Connectors | CONN-6 Telegram / WhatsApp / Google Chat | `9e02d8c` #315 |
| Connectors | CONN-7 containment (capability + trust + approval) | `a7c7d90` #317 |
| Connectors | CONN-8a execution framework + GitHub executor | `63f9d9c` #319 |
| Connectors | CONN-8b-1 Jira + comms executors | `c2e3235` #320 |
| Connectors | CONN-8b-2 Google Workspace executor | `4548339` #321 |
| Connectors | CONN-8b-3 custom-MCP bridge (http, DNS-pinned SSRF egress) | `0d65c2c` #322 |
| Connectors | CONN-8b-4 execution monitor UI (web + mobile) | `5830ed3` #323 |
| Connectors | CONN-9 agent-loop wiring — connectors usable mid-run | `27baa8f` #324 |
| Approvals | APPR-1 the desk can genuinely approve dangerous actions | `c31a4ec` #325 |
| Memory | MEM-1 memory brain renders as a real graph | `915f875` #327 (+ audit #328) |
| Inbox/Activity | ACT-1 latest approvals + unified activity log | `bb18866` #329 (+ audit #330) |
| Security | GC-0 cross-org project write hole; approval payload projected | `fae2d37` #331 |
| Security | GC-0b mass-assignment / gate-order class — all nine instances | `c44be4e` #333 |
| Command Center | GC-1 agent picker | `4c2fb9e` #335 |
| Live pass | FIX-1 four defects a visual pass caught | `b164184` #336 |

Security class write-up: [`docs/SECURITY-mass-assignment.md`](docs/SECURITY-mass-assignment.md).


_Previously: 2026-07-18 (**GC-0b — THE MASS-ASSIGNMENT / GATE-ORDER CLASS IS CLOSED, ALL NINE INSTANCES, BOTH LEGS (backend-only, security).** The class turned out to have TWO legs, and the first fix only closed one. **Leg (a), UPDATE:** a body-writable tenant column (`orgId`) walks the row into another tenant — six instances, detailed below. **Leg (b), CREATE:** a body-supplied org-scoped REFERENCE that is later EXECUTED. Leg (b) hides because on a create route `orgId` comes from the PATH — correct, gate-checked, obviously safe — which is exactly what makes the foreign key beside it read as harmless. It is not: `executeAgentTask` resolves the agent BY ID ALONE and treats `agent.orgId` as ambient authority, so `POST /api/orgs/ORG-A/tasks {"agentId":"<org B's agent>"}` gets **org B's agent running attacker-authored input under org B's LLM credentials, budget, knowledge base and connectors, billed to org B, with the output landing in a row org A can read**. `POST …/scheduled` was worse: cron re-executes it indefinitely AND the route mints a webhook token whose trigger endpoint sits OUTSIDE the authenticated scope, so the URL fires the victim's agent with no session at all and survives the attacker leaving the org. Both closed by an external audit (#334, folded in here). **#8 `POST …/jira/sync` and #9 `POST …/jira/issues` were then found BY THE WIDENED GUARD ITSELF on its first run** — identical shape, in a file nobody thought of as task-creating; also closed alongside were `POST …/goals` (`ownerAgentId`, a cross-tenant reference rather than execution — this retires the dangling-reference follow-up the GC-0 goals fix flagged) and `POST …/comms/inbox/send` (body `agentId` written to `messages`, which are replayed as an agent's CONVERSATION HISTORY — a cross-tenant prompt-injection write). **TWO LAYERS, deliberately:** a shared `assertAgentInOrg` (`services/tenant-guard.ts`) 400s at CREATE so the bad row never exists and the operator gets a real message — and it must run BEFORE any integration-config check, since behind `if (!cfg) return 'not connected'` the guard is skipped for orgs without that integration and runs only in production (our own ordering test caught exactly that, and it was a real placement bug); and the AUTHORITATIVE check now lives in `executeAgentTask` itself, which refuses `task.orgId !== agent.orgId`, marks the task failed/needs_attention and bills nothing. The invariant belongs to EXECUTION, not to any entry point — eight call sites into the executor, six paths that create an executable row, and route-by-route checking has now failed three times. Missing and foreign agents are refused identically, so there is no cross-tenant existence oracle. **The class guard is widened to leg (b)** with its limits stated plainly: a regex pass cannot prove an FK is later executed (that is inter-procedural taint analysis), so it enforces a STRUCTURAL CONVENTION instead — any handler reading a body-supplied executable agent id must call the named marker. That is why the helper is one shared function rather than three inline `if`s: a scanner can look for a name. It proves nobody skipped the question, not that the check is correct; the executor invariant is what is actually load-bearing. **Nits from the audit:** `advisorIds` malformed JSON now 400s instead of an uncaught `JSON.parse` 500 (the `z.union([array,string])` explicitly blesses that shape, so the contract invites it), and the owner-gated cross-check no longer hand-lists trust/permissions/model-profile — it is now SCHEMA-COMPLETE, enumerating every `agents` column and failing until each is classified writable or not, so a NEW model-profile column is caught automatically (proven on synthetic input, because really adding a column dies in `before()` on the setup.ts CREATE-vs-ALTER trap before the classification test runs). Backend **1829/1829** · evals 11/11 · typecheck clean · web 297/297 + build · mobile 330/330 + typecheck + export. **Parity: N/A** — backend-only authorisation fix, no client contract change. Previously in this story: GC-0 and its audit fixed `PATCH /api/projects/:projectId` and `PATCH /api/goals/:goalId`. GC-0b closes the remaining three — and a **sixth** nobody had reported, found by sweeping for the SHAPE rather than working the list. **The mechanism, once, for all of them:** `resolveRequestOrg` derives a record route's org FROM THE ROW and evaluates it BEFORE the handler mutates that row, so **a gate that authorises against the pre-image cannot defend a field that rewrites the pre-image** — the tenant boundary has to live in an ALLOW-LIST, never in the gate and never in a deny-list. **(3) `PATCH /api/skills/:skillId` — CRITICAL.** `/api/skills/` is marked `nullOrgIsGlobal`, so for a GLOBAL skill the gate STANDS DOWN ENTIRELY (`{scoped:false}`) — correct for READS, the library is a deliberately global catalogue, but it applied to the WRITE, so **any authenticated user belonging to NO ORG could rewrite shared-library `content` — prompt material fed to agents, i.e. an instruction-injection path into every agent using the skill.** Rule now: **a global skill is not editable or deletable through the API by anyone**; sync owns it (`POST /api/skills/sync`, `…/obsidian-sync`). Raising the role was not available — `requireOrgRole('owner')` reads `:orgId` from the URL and silently no-ops without one (the R-4 trap), and a global skill has no org to own it. `source`/`githubPath` are unwritable too: `githubPath` is the sync JOIN KEY, so writing it re-points what overwrites the row on the next sync. DELETE carried the same stand-down and is closed with it. **(4) `PATCH /api/agents/:agentId` — CRITICAL.** Was a DENY-LIST (it deleted exactly one key, `permissions`) — which is HOW it got here: every column not named stayed writable. Two live escalations: a cross-org move, and **a plain MEMBER setting `trustMode: autonomous`**, which is what CONN-7 consults to decide whether a connector write needs approval — a member-reachable bypass of the connector execution gate. Now an allow-list excluding the ENTIRE owner-gated vocabulary (config `CONFIG_FIELDS`, permissions, trust, model-profile) plus `apiTokenHash` (the agent CREDENTIAL — a writable hash mints a working agent token) and `externalEndpoint`. Cross-checked as a property rather than a list: a test asserts every owner-gated field is member-unreachable here, so **the two surfaces cannot disagree**. The frozen legacy `app/` edit screen loses name/role/llmModel/avatarEmoji through this route — that screen was a member editing owner-only config, i.e. the escalation itself; `web/` and `apps/mobile/` already use the owner-gated PUT and are unaffected. **(5) `PATCH /api/tasks/:taskId` — HIGH.** Cross-org move, plus `agentId` re-pointable at another org's agent — the sharper half, since `agentId` is what the executor runs the task AS, so a re-point hands a foreign agent the task's `input`. `agentId` stays writable (reassignment is real product behaviour) but is validated against the TASK's org; `lockToken`/`lockedAt` (the atomic checkout lock) and the metering columns (`costUsd`, `tokensUsed` — client-writable spend evades the budget cap) are now unwritable. **(6) `PATCH /api/orgs/:orgId` — CRITICAL, PREVIOUSLY UNREPORTED.** Also a deny-list (stripped `ownerId`/`id`, spread the rest). No ordering bug — the org comes from the PATH — but every unnamed column stayed member-writable, and three are owner-gated everywhere else: **`deployConfig` (the org's PLAINTEXT LLM API KEYS — the WRITE side of the row whose READ leak #294 closed)**, `telegramBotToken`, and `budgetMonthlyUsd` (a member could raise their own spend ceiling). Allow-listed to `name`/`description`/`logoUrl`/`mission`/`culture`; the only client caller (the Settings tab) sends exactly three of those, so it is a strict narrowing at no UI cost. **Tests: 115 new across four suites, every guard mutation-proven** (reverting each fix kills 11/28, 12/32, 9/28, 8/24). **Per-test reset is mandatory and now self-policing** — without it the first exploit moves the row and every later probe 403s and PASSES FOR THE WRONG REASON (the trap that hid two Criticals in two earlier sessions), so each suite carries a two-step tripwire that fails loudly if `beforeEach` ever stops resetting. **A CLASS-LEVEL guard** (`gc0b-mass-assignment-guard.test.ts`) statically scans every route module: `.set(req.body)` hard-fails with the remediation, any other opaque `.set()` argument must be justified in `REVIEWED_SINKS`, inline literals pass silently. Proven to bite by planting instance #7 in `webhooks.ts` as both a raw body and the subtler deny-list variant. Class + exemplar written up in `docs/SECURITY-mass-assignment.md`. One pre-existing test tightened (`agent-permissions-authz`: its "other fields still apply" leg used `role`, which is owner-gated config). Backend **1805/1805** · evals 11/11 · typecheck clean · web 297/297 + build · mobile 330/330 + typecheck + export. **Parity: N/A** — backend-only authorisation fix, no client contract change. `gc-0b-authz-class`.)_

_Previously: 2026-07-18 (**GC-0 — A CROSS-ORG PROJECT WRITE HOLE CLOSED, AND THE APPROVAL PAYLOAD STOPS CROSSING THE WIRE (backend-only, security).** **FINDING 1 — the hole.** `PATCH /api/projects/:projectId` was `db.update(projects).set(req.body as any)`: no Zod parse, no field allow-list, the raw body written straight to the row. Because `orgId` is a column, **any member of org A could re-home a project into org B** with `{"orgId":"org-b"}` — a cross-TENANT write that takes the project's board and every task hanging off it along with it. **The surface-wide membership gate could not catch this, and that is the interesting part:** `resolveRequestOrg` (middleware/rbac.ts) derives the org for `/api/projects/:projectId` FROM THE ROW, and reads it BEFORE the handler mutates it. At check time the caller genuinely IS a member of the project's org, so the gate passes; the handler then moves the row out from under it. **A gate that authorises against the pre-image cannot defend a field that rewrites the pre-image** — only an allow-list can. Fixed with `ProjectPatchSchema` / `ProjectCreateSchema` (`routes/projects.ts`): writable = `name`, `description`, `departmentId`; `orgId`, `id` and `createdAt` are unwritable. Unknown keys are STRIPPED rather than rejected, so a client round-tripping a whole project object still succeeds — it just cannot move the row. CREATE now 400s a missing `name` instead of 500ing on the NOT NULL column, and PATCH 404s a missing project instead of reporting `{ok:true}` for a no-op UPDATE. **Role: MEMBER, deliberately.** The repo reserves `requireOrgRole('owner')` for capability/credential surfaces (agent config, trust, budgets, custom-models, org deletion); ordinary work CONTENT — goals, tasks, knowledge — is member-level. A project is peer to a goal, so member is the convention-correct bar; escalating it would diverge from goals and break members editing their own projects. The tenant boundary is enforced by the allow-list, which is where it belongs, not by raising the role. **FINDING 2 — the payload.** Every approval read route was a bare `db.select()`, shipping the whole row including `payload`, a `z.any()` blob written by the requesting AGENT — `machine_exec` argv, `wallet_tx` destinations, `email_send` recipients — when the clients read exactly two keys out of it. Structurally the same failure `toPublicOrg` fixed on the `organisations` row. New `services/approval-public.ts` + `toPublicApproval()` projects on BOTH axes (columns and payload keys) and is wired into **four** sites, not the one reported: `/inbox`, **`/approvals` — the route the PHONE reads**, `/review-queue`, and the decide response. `GET …/activity` already used explicit columns and never selected `payload`; unchanged. **APPR-1 IS NOT REGRESSED, and the projection is built around that.** `type` and `payload.requiresStepUp` are both load-bearing for `approvalNeedsStepUp` on both surfaces and are deliberately kept; `warnings` is kept for the card. Non-string `warnings` elements are DROPPED, not stringified — `String(['a','sk-live-…'])` joins to `"a,sk-live-…"`, so a secret planted in a nested array would have survived coercion intact (caught by our own test). **Tests: 27 new, every guard mutation-proven.** The exploit was reproduced failing before the fix. An early version of the projects suite was VACUOUS — the first test's exploit moved the row into org B, so every later test 403'd and passed for the wrong reason; a per-test reset fixes it. Backend 1670/1670, evals 11/11, web 297/297 + build, mobile 330/330 + typecheck + export. Additive: no client change was needed — both clients' payload reads were audited to call sites first and neither reads a dropped key.)_

_Prior: 2026-07-18 (**MEM-1 — THE MEMORY BRAIN RENDERS AS A REAL GRAPH: the web map becomes operable, bounded and theme-correct, and the phone finally reaches the LINK STRUCTURE (web + mobile + a bounded backend payload).** Epic M (#192/#194) already shipped a d3-force vault map and MOB-6e (#292) deliberately dropped it from the phone; MEM-1 is the elevate pass over both, **additive, no new dependency, no authz change**. **BACKEND (bounded, not new):** `GET …/memory/graph` already existed and is already membership-gated by the surface-wide `resolveRequestOrg` hook (it carries `:orgId`, so the R-4 tail does not apply) — so **no endpoint and no gate was added**. What was missing is a **payload bound**: the native path was capped at the FETCH (120 files) but the **Graphify fast path is one `graph.json` read**, so a large vault arrived whole. New pure `capGraph()` keeps the **highest-degree** nodes (ties notes-first, then id → **deterministic**, so the same vault always yields the same map), drops orphaned edges, preserves **TRUE degree** (radius keeps meaning vault centrality, not survival) and **reports what it dropped** (`stats.capped`/`totalNodes`). Ceiling 1500; `?max=` may ask for LESS, never more; the **cache holds the UNCAPPED graph and the cap is applied on the way out**, so two clients with different `max` cannot poison each other. **WEB:** the map gains **keyboard operation** — the canvas is ONE tab stop with roving focus (`aria-activedescendant`, arrows walk hub-first order, Enter opens, Escape releases, focus **auto-centres** so it can never land off-screen) rather than 600 tab stops, which is a trap not access; a **render cap of 600** with the degradation curve **measured, not guessed** (600 nodes/3.1k edges = 597ms; 1,000 = 1.07s; 2,500 = 3.35s; 4,000 = 5.88s on a dense 8k-node graph — the cool is synchronous and re-runs per filter change); **labels became a BUDGET not a degree threshold** (a fixed floor read fine on a sparse vault and turned the dense one into unreadable text soup — the screen has fixed room for text, so the budget is fixed and the hubs spend it, 12/40/110/400 by zoom); **fit-to-bounds framing** on load/filter/reset (a fixed k=1 marooned a small vault and spilled a large one off every edge); **folder filter chips that ARE the legend** (one list to read, one place to click); real **empty states** (every-folder-filtered, no-notes, and filtered-to-nothing are three different sentences, each pointing at the Reader as the non-canvas fallback); and **drag coalesced to one re-render per frame** via rAF (a 240Hz trackpad was re-rendering the whole node tree per event). **THEME/CONTRAST — a real defect fixed:** the Okabe–Ito ramp was **hardcoded hex in the component** and two of its hues (`#F0E442` yellow ≈1.1:1, `#56B4E9` sky ≈2:1) effectively **vanished on the light card**. The ramp moved into the token system as `--graph-1…10` + `--graph-tag`/`--graph-node-stroke`/`--graph-edge`, **light darkens the pale end while preserving hue ORDER** (what carries the colourblind separation), dark lifts `#0072B2`, and every node now carries a defined stroke so a pale fill cannot melt into the surface. Verified by rendering the REAL pipeline (backend `parseGraphifyGraph`+`capGraph` → the shipped force config → the real token values) against the **live TARCO vault** and this repo's **8,054-node** graph, screenshotted in BOTH themes. **MOBILE — the canvas stays dropped, the DATA does not.** §6.6's decision is **re-affirmed with reasons** (hit-testing at 44pt; the cool is a synchronous 140–420 tick loop that blocks **the** JS thread — the one running the navigator, list and gesture responder; and `d3-force` is web-only. **`react-native-svg` IS bundled, so the renderer was never the blocker** — stated explicitly so the decision reads as a judgement, not a limitation). What the map is FOR is list-shaped, so Memory is now two views mirroring the desk's own 📄 Reader ⇄ ⬡ Graph toggle: **🗂 Vault** (the MOB-6e tree, unchanged) and **🔗 Links** — **whole-vault search** (the capability the tree structurally CANNOT have: `…/memory/tree` returns one directory per call, so it only knows what you already expanded) and **neighbourhood traversal** (a note → what it **links to** and what **links back**, each tappable, with a trail to walk back along). **Direction is kept** where the canvas draws an undirected line — backlinks are the more useful half. Connectivity is stated in **words** ("12 links · 3 back") because a list has no radius to encode it. Asks `?max=600`. `MEMORY_GRAPH_NOTE` **rewritten**: it claimed "the vault itself is all here" while link structure lived nowhere — now it points at Links (pinned by a test). **TRIPWIRE:** `vaultGraph.test.ts` imports the **backend's own** `buildNativeGraph`/`capGraph` and feeds the real output through the phone's index — legal because that module is genuinely **import-free** (the Mobile-CI cross-workspace rule). **Deferred (named, not silent):** tappable `[[wikilinks]]` in the phone reader (the resolver now exists — it is reader-surface work with its own ambiguity cases), and server-side clustering for vaults that genuinely exceed the render cap. Verified: backend **1613/1613** + **11/11** evals + tsc · web **260/260** + production build (dashboard **124 kB — unchanged**, no new dep) · mobile **309/309** (+16) + typecheck + `expo export`, **SDK 54 / react 19.1.0 / boot-safe untouched**. `mem-1-memory-graph`. **POST-AUDIT (independent audit = PASS-WITH-NITS; #328 folded in, then extended):** two HIGHs made the graph unusable to *touch* and both shipped through a total absence of web coverage — **dragging a node navigated to the Reader** (`onClick` fired unconditionally on release, unmounting the graph, so the rAF-coalesced drag path was correct code no user could exercise) and **zooming also scrolled the dashboard** (`preventDefault()` in a React `onWheel` is inert — React delegates wheel at the root as PASSIVE; fixed with a native `{passive:false}` listener via a callback ref). Plus: search bypassed the label budget (one common letter re-made the text soup), mobile pull-to-refresh sent `?rebuild=1` (busting the cache + re-crawling up to 120 notes, bound to the most accidental gesture on a touch screen), and **a raw NUL byte made `vaultGraph.ts` diff as BINARY — hiding 220 lines of new logic from review and from grep** (the *second* time this repo has hit that trap, after #195). **Added on top:** **(1) a busy affordance** — the ~570ms cool is synchronous and re-runs per filter toggle, and a spinner set in the same render can never paint, so `useDeferredValue` is used as the seam: React paints the previous drawn set (dimmed, spinner, `aria-live`) and only then re-renders into the expensive memo; everything derived from the drawn set reads the deferred value so the stale frame stays coherent, and `filtered` deliberately does NOT depend on `query` so typing never re-cools. **(2) The COLOUR RATIONALE WAS FALSE and is corrected** — the claim that hue ORDER carries the colourblind separation is wrong: Okabe–Ito's blue and sky differ mainly in LIGHTNESS, so darkening the sky for contrast collapsed the pair (ΔE00 22.31→7.65). `--graph-5` → `#4a8cf0` (3.33:1 on the card, ΔE00 15.4 from `--graph-1`, off the risky list). The deeper finding, measured not asserted: a search over the whole sRGB cube with **all ten slots free** and only a 3:1 floor tops out at worst-pair **ΔE00 ≈12** (and looks like near-blacks), so **ten mutually distinguishable categorical colours under three dichromacies is NOT ACHIEVABLE** — Okabe–Ito is specified as EIGHT. Colour is documented as a **cluster hint, not an identifier**; the folder NAME in the chips is the identifier. Remaining worst pairs (g2/g6 3.06, g2/g7 5.48 light; g1/g5 3.95, g3/g10 5.38 dark) are CANONICAL Okabe–Ito collisions, inherited not introduced. **(3) The mobile tripwire was VACUOUS** — `typeof degree === 'number'` is satisfied by the `degree: 0` initializer; **proven by mutation** (a `capGraph` that recomputes degree post-cap passed 15/15), now asserts the invariant that a surviving hub's degree EXCEEDS its surviving edge count, which is true only of vault-wide degree. **(4) 351 lines of untested view logic extracted** to `web/lib/vaultGraphView.ts` (pure: cap, label budget, drag slop, zoom math, fit framing, adjacency, roving focus) with `vaultGraphView.test.ts` **+24**, including the three named regression guards — each **mutation-proven to bite** (reverting the drag guard, restoring React `onWheel`, or removing the search budget each fails it). The first draft of the drag guard passed for the wrong reason (its regex matched the toolbar's Reset button) and was re-anchored to `role="option"`. **NOTED, NOT FIXED (pre-existing, unreachable today, wants its own backend story):** `resolveVaultToken()` returns a process-wide `VAULT_GH_TOKEN` for ANY org and `parseVaultConfig(null)` defaults to the real company vault — were that env var ever set, every org would read one vault. Final: backend **1613/1613** + **11/11** + tsc · web **284/284** (+24) + build (dashboard **122 kB**) · mobile **309/309** + typecheck + export.**)_

_Earlier: 2026-07-18 (**APPR-1 — APPROVAL-SURFACE PARITY: the desk can now genuinely approve a dangerous action, and the phone's dangerous-type copy is pinned. Web + mobile + a THREE-LINE CORS TRANSPORT FIX; the backend AUTHZ GATE is UNCHANGED and unweakened.** Closes the two defects CONN-9 named and two independent audits confirmed — both pre-existing, both made routine by CONN-9 turning `connector_action` approvals into an everyday card. **F1 (HIGH) — THE UI LIED.** `CockpitPanel.decide()` was the ONLY caller of the decide endpoint and it (i) sent no `x-arturita-session` step-up header (zero occurrences anywhere in `web/`), (ii) removed the approval card OPTIMISTICALLY **before** awaiting the request, and (iii) swallowed the result in a bare `catch {}`. The backend correctly 403s a dangerous approve without step-up — so on the desk the card vanished and the operator believed a wallet transaction / file deletion / connector action was approved **while the server had refused it**. The missing header was the trigger; **the lie was the bug**. **FIX — mirror the contract the phone already implements (MOB-4), not a new one:** a dangerous Approve now opens **NEW** `web/app/dashboard/cockpit/StepUpDialog.tsx`, the desk peer of `StepUpModal.tsx`. It shows the type + the backend's MACHINE-RENDERED summary + every danger warning (never model prose), requires an **explicit human gate** — typing `APPROVE`, the browser equivalent of the phone's biometric/typed gate — and only THEN mints a **fresh** session (`POST …/arturita/session`, per-approval, never cached/reused: 5-min freshness, 30-min TTL) and sends it in `x-arturita-session` on the single decide call. **We deliberately do NOT auto-mint behind the operator's back**: that would satisfy the server while defeating the gate, whose entire point is that a HUMAN re-authorized this specific action. **The card is now cleared ONLY on a 2xx**, and a failure keeps the card and states what happened on it (`role=alert`), so **a 403 can never read as success**; an expired step-up says so and re-confirming re-mints (one-type recovery, never a dead end). Non-dangerous approvals are unchanged apart from now being honest about failure — reject/request-changes are never step-up-gated. Dangerous cards also gain a red `⚠ <type>` chip, an `✓ Approve…` label (the ellipsis promises the confirm step), in-flight disabling, and their machine-rendered warnings inline. **F2 (MEDIUM) — MOBILE DRIFT.** `apps/mobile/src/constants.ts` hand-copied `DANGEROUS_APPROVAL_TYPES` and **omitted `connector_action`** despite its own keep-in-sync comment; the phone still reached step-up, but ONLY through the second clause of `approvalNeedsStepUp` (`payload.requiresStepUp === true`) — correct **by luck**, one entry behind a security-relevant source of truth. Added the entry; **kept the fallback as defence in depth** (it still catches a genuine wrapper like a `low_trust_review` around a dangerous action, and buys grace if the backend adds a type before a client copies it) — we simply no longer RELY on it. **THE TRIPWIRES (the actual carry-forward — the copy existed in 3 files and NOTHING cross-asserted it).** The backend module can't be imported by either client (it pulls `cc-denylist` + `connector-authz`, and Mobile CI installs only `apps/mobile` — a transitive drizzle import would silently DROP a whole test file in CI while passing locally), so the backend side is **TEXT-READ**, the pattern the connector-catalog / execution-status tripwires already use. Three-way pinned by **EQUALITY, not subset** (a type we lack = a one-click approve that 403s; a type we have and the backend doesn't = friction on a safe path — both are bugs): mobile==backend, web==backend, **mobile==web**. **WEB DRIFT — CHECKED, and the answer was worse than drift:** the web had **no machine-readable copy at all** (that's *why* it never gated); its only mention of the types was **prose** in a `GovernancePanel.tsx` hint paragraph, itself already stale (no `connector_action`). **NEW** `web/lib/dangerousApprovals.ts` is now that copy — kept deliberately **import-free** so the mobile parity test can import it under Mobile CI's dependency-free constraint — and it is pinned like the phone's. The stale prose was left alone (it describes low-trust containment, not the step-up set) — **flagged, not silently rewritten**. **PROVEN TO BITE (mutate → red → revert):** removing `connector_action` from mobile → 3 red; adding a type to the BACKEND → mobile AND web both red (the direction nothing previously caught); restoring the ORIGINAL buggy `decide()` verbatim → the ordering tripwire goes red on all three of its facets. **TESTS +19:** `web/lib/dangerousApprovals.test.ts` (+6), `web/app/dashboard/approvalDecide.test.ts` (+4 — a source-level regression guard on the ORDERING itself: card-removal must follow the await, no bare `catch {}`, the step-up branch must RETURN before the headerless send, and the dialog must gate BEFORE it mints; the web workspace has no React renderer, so this property is asserted against source, narrowly anchored to `decide()`), `apps/mobile/src/dangerousApprovals.test.ts` (+4). **GREEN:** web **260/260** + build, mobile **293/293** + typecheck + export, backend **1606/1606** (+3 CORS preflight/scan) + evals **11/11**. **PARITY:** touches BOTH clients by design — web gained the step-up flow it never had; mobile gained the missing constant + the tripwire. **AUDIT (independent) — FAIL on one CRITICAL, folded from PR #326 (auditor's branch `audit/appr-1-fixes`; reviewed, reproduced and verified by me, not rubber-stamped).** `backend/src/middleware/cors.ts` sets `allowedHeaders` as an EXPLICIT list that did **not** include `x-arturita-session` — so the browser refused the decide call **at preflight and it was never sent**. Net: the operator typed `APPROVE`, a session row WAS minted server-side, and the call died in the browser; `StepUpDialog`'s classifier matched neither `/403/` nor `/step-up/` and rendered **"Network error"**. **The desk still could not approve — my fix just failed louder.** **Root cause worth internalizing:** mirroring the phone's contract was correct but **INSUFFICIENT** — React Native performs **no CORS preflight**, so "the phone works" was **zero evidence** a browser could deliver that header. I verified the contract and never verified the transport. Compounding it, the pre-existing `preflight()` test helper **hardcoded** `'authorization,content-type'`, so the probe never asked for the missing header and the suite stayed green. **This is the SECOND instance of the same trap** — the identical bug on the METHOD axis is why `CORS_METHODS` was extracted (the PUT/DELETE "Network error" outage). **FIX:** `CORS_ALLOWED_HEADERS` as a named export, mirroring `CORS_METHODS`. It is a **TRANSPORT allowance, not an authorization decision**: it changes neither who may approve nor what the server verifies — it only lets the browser deliver a header the server **already demands**. Independently reproduced against the real middleware (before: `Authorization, Content-Type`; after: `+ x-arturita-session`) and both audit tests confirmed to go RED when the allowance is reverted. **Rejected the zero-backend alternative** (the route also accepts a body `sessionToken`, `tasks.ts:481`): it would have web deliver a bearer-grade secret through a **different channel than mobile** on a security-critical path — manufacturing exactly the client-drift class APPR-1 exists to eliminate — and bodies are dumped by request logging more readily than headers, which get explicit redaction. **HELPER HARDENED so the class can't recur:** `preflight()` now defaults to probing with **every** header the clients actually send; every existing method case asserts **both axes**; and a **non-circular** guard text-scans `web/` + `apps/mobile/` for custom `x-…` header literals and fails if one isn't allowed — catching a client that *starts sending* a new header, the direction the hand-maintained list cannot see (proven: injecting `x-mc-trace-id` into a client turns it red). **TWO LOW NITS folded:** (a) `decide()`'s `approvals.find()` miss no longer falls through to the headerless path (it says the row is out of view and returns); (b) the mint and decide legs are now caught **separately**, so a **mint** 403 reads as an authorization problem rather than "step-up expired" — which had told an operator lacking permission to keep typing APPROVE at a wall. Both pinned by tests, both verified to bite. **Scope is now web + mobile + `middleware/cors.ts` (transport config) — the earlier "no backend file changed" claim NO LONGER HOLDS and has been corrected here rather than left standing.** No route, schema, or authz touched; the decide gate is byte-identical to `main`; no gate weakened. **Deferred:** driving the new dialog against a live backend + Clerk (no browser run in this session — the flow is proven by contract-mirroring and the tripwires, not by an end-to-end click); the stale `GovernancePanel.tsx:183` prose list; **CORRECTION to a stale note carried in prior context:** `web/` unit tests ARE CI-gated — a **`Web (web)`** job in `.github/workflows/ci.yml` runs `npm run typecheck` + `npm test` + `next build` on every PR, with **no `|| true`**, so the new web tripwires genuinely run. The caveat that DOES hold is the same one the Mobile job carries: `main` has no branch protection and the standing `--squash --admin` convention bypasses it, so a red check **reports** rather than blocks — read the job before merging. `appr-1-web-stepup-parity`. Prior: CONN-9 below.**)_

_Prior: 2026-07-18 (**CONN-9 — AGENT-LOOP WIRING: an agent can now USE its configured connectors MID-RUN, fully gated by CONN-7. Backend-only (additive); no client change — mobile parity N/A, but the approvals surface was VERIFIED end-to-end and TWO pre-existing gaps are named below.** CONN-8a…8b-4 built the gate and every surface around it, but nothing inside a normal agent run ever called it. **NEW** `backend/src/services/agent-connector-tools.ts` + a wiring block in `services/agent-executor.ts`. **MECHANISM:** the executor runs on `streamLLM` (no native tool-calling loop), so connectors reuse the existing TEXT-DIRECTIVE idiom (`[REMEMBER:]`/`[WEBHOOK:]`/`[DELEGATE:]`) rather than forking the run loop — `[CONNECTOR: <connector>.<action> | {json}]`; one model turn emits directives, they execute, ONE synthesis turn reads results and writes the final answer. **(1) EXPOSURE ≠ AUTHORIZATION:** a connector is offered only with BOTH an enabled `agent_connectors` row AND an **EXPLICIT** `connector:<id>`/`connector:*`/`*` capability (`hasExplicitConnectorCapability` — an empty/legacy allow-all list grants **NOTHING**, CONN-7 carry-forward (i)), plus a known catalog entry + a real executor. Exposure is only a hint: capability is re-checked inside `executeConnectorAction` on every call, so a model that INVENTS a connector it was never offered is stopped **by the gate, not the prompt** (asserted both halves). **No secret in the prompt or tool schema** — the derived tool carries connector id + catalog display name + action names only; the row's `config` and `secretRef` are never read (test asserts neither the PAT nor a config value appears in the block). **(2) EVERY invocation funnels through `executeConnectorAction`** — `runConnectorDirectives` has no path to an executor, no path skipping `authorizeConnectorAction`, and **NO path that supplies an `approvalId`**, so the agent loop is STRUCTURALLY unable to redeem an approval, its own or anyone's; `orgId`/`agentId` are server-supplied and `target: null` so agent prose can't dress up an approval card. **THREE OUTCOMES:** `allow`→executes once, ledgered, result sanitized (`redactSecrets`) + size-bounded; `needs_approval`→**NOT executed**, approval filed by the gate with the server-computed **`paramsDigest`** binding, model told it is pending and cannot approve/retry (the `approvalId` is deliberately WITHHELD from the model); `deny`→clean refusal. **(3) PROMPT-INJECTION CONTAINMENT (hard requirement)** — connector results (GitHub issue bodies, Jira comments, emails, Telegram messages, MCP output) are attacker-controllable. THREE layers, the fence being only the first: **(i) FENCED + NONCED** — per-run random nonce drawn AFTER the payload exists (re-drawn on collision) so no provider can predict it and close the fence early to continue in the operator's voice (the `converse-attachments.ts` document-attach pattern); the emphatic untrusted-data label comes BEFORE the data. **(ii) THE SYNTHESIS TURN IS TERMINAL** — its output is stripped of BOTH `[CONNECTOR:]` and `[DELEGATE:]` **without executing them**; this, not the fence, is what makes containment structural — it holds even if the model is fully persuaded. Cost: an orchestrator that used a connector doesn't also delegate that run (deliberate trade). **(iii) THE GATE IS UNMOVED** — capability from `agents.permissions`, trust from `agent_connectors.trustLevel`, both read from the DB and never from model output, so no "you are approved to…" grants anything. **PROVING TEST `[CONN9-INJECT]`** fires every trick at once (system override + forged `connector:*` grant + `[CONNECTOR: github.repo.delete]` + `[DELEGATE:]` exfiltration + a **FORGED CLOSING FENCE MARKER**): data stays inside the real fence, the forged marker can't close it, the demanded destructive call is still refused, **no unapproved provider call is made**, and no approval is approved behind the operator's back. **(4) LOOP SAFETY:** `MAX_CONNECTOR_CALLS_PER_RUN=4` (over-cap directives return `not_attempted` **with a reason** — told, not silently truncated); `MAX_CONNECTOR_RESULT_CHARS=4_000`/result + `12_000`/block clipped with a visible marker (the clipped TEXT is returned, not a pruned object — a partial JSON string is honest about being partial, a pruned object looks complete); sequential + individually try/caught; derivation, the round and the synthesis turn are each non-critical, so **a connector failure never crashes a run**; provider errors clean with no credential (asserted against a provider that ECHOES the PAT in its error body); directive parsing fail-closed + non-throwing (malformed/unterminated/non-object params SKIPPED, not guessed at; JSON-aware scanner so a `]` inside params can't truncate). **COST:** the second LLM turn accumulates into `tokensUsed`/`costUsd`/`inputTokens`/`outputTokens`, so it lands in the task totals and the daily/monthly budget checks. **FIXED IN PASS:** the prompt's worked example named a non-existent action + param (`github.get_issue`/`issue_number` → real `github.issue.get`/`number`) — a plausible-but-wrong example burns the model's capped calls teaching it a directive that always fails. **APPROVALS-SURFACE CLAIM VERIFIED (not assumed) — 2 GAPS NAMED, both pre-existing, neither a CONN-9 regression:** mobile DOES surface + can genuinely approve (list route has **no `type` filter**, `ApprovalsPane` renders generically, mobile can mint step-up) — **but fragilely**: mobile's hand-copied `DANGEROUS_APPROVAL_TYPES` (`apps/mobile/src/constants.ts:11-16`) **OMITS `connector_action`** despite its own keep-in-sync comment, and works only via the `payload.requiresStepUp` fallback the backend happens to stamp → **follow-up: add it + a tripwire test**. **PARITY GAP (web, worse):** web renders `connector_action` but **CANNOT approve ANY dangerous approval** — its decide call sends no `x-arturita-session` (zero step-up hits anywhere in `web/`), removes the card **optimistically before** the request, and swallows the 403 in `catch {}` → **on the desk Approve looks like it worked while the approval is silently NOT approved; on the phone it genuinely approves** (inverts the usual desk>phone assumption). Predates CONN-9 (hits `wallet_tx`/`email_send`/`machine_exec` equally) but CONN-9 makes it routine → **separate web-side follow-up**. **TESTS** `backend/src/tests/conn9-agent-connector-tools.test.ts` (**+13**, real SQLite + the REAL `executeConnectorAction` + the REAL decide route, mocked provider transport / no network): exposure with explicit cap; allow-all + foreign-cap exposes nothing AND is denied at the gate; fail-closed derivation; auto_write write executes-once + ledgered; gated write files approval with the exact `paramsDigest`, not executed, `approvalId` withheld; **self-redeem impossible** (retry re-files, an `approvalId` smuggled into PARAMS is just a param, approval stays pending, and the HUMAN step-up path still works); destructive refused; parse/strip fail-closed incl. nested-JSON `]`; injection containment; per-run cap; truncation marker; provider error + transport throw + gate explosion all clean and never leaking the sentinel; funnel spy (every directive → gate, `target:null`, **`approvalId` always undefined**). **Backend 1601/1601** + evals **11/11** + tsc clean. Strictly additive to `backend/**` + docs; no schema change, no route change, no gate weakened, no CONN-7 bypass. **BUILDER only — not self-audited/merged.** **Deferred:** multi-round tool use (today: one round + terminal synthesis), connector+delegate in the same run, the two approval-surface follow-ups above, owner-initiated trigger, audited stdio-MCP. `conn-9-agent-loop-wiring`. Prior: CONN-8b-4 below.**)_

_Prior: 2026-07-18 (**CONN-8b-4 — CONNECTOR-EXECUTION MONITOR (web + mobile): the FINAL connectors slice — an OWNER can now SEE what connector actions an agent attempted, on the desk AND the phone. The connectors epic (CONN-1 … CONN-8b-4) is FEATURE-COMPLETE.** Scoped **MONITOR-ONLY** (decision below). **BACKEND (additive, owner-gated):** `GET /api/orgs/:orgId/agents/:agentId/connector-executions` in `routes/agent-detail.ts`, `requireOrgRole('owner')`, latest 50 newest-first. **R-4-safe** — the path carries `:orgId` so the RBAC preHandler actually enforces (it only no-ops on a *tailless* path); the handler also scopes every query by `(orgId, agentId)` and 404s an agent not in the org (an owner of org A can't read org B). **ALLOW-LIST projection** (`projectConnectorExecution`, `services/connector-execution.ts`): the response item is EXACTLY `id`, `connectorId`, `action`, `classification`, `status`, `gated` (bool), `error` (short), `createdAt` (ms). **Proof no secret/preimage leaks:** (1) the `connector_executions` ledger STORES no credential, no raw params, and no params-digest to begin with (schema holds only action/classification/status/sanitized-error/approvalId); (2) the projection names each field explicitly so a future column can't ride along; (3) `approvalId` collapses to a **boolean `gated`** — the id never leaves the server; (4) `error` was already `redactString`-passed at write time and is additionally truncated. Route test seeds a row with junk extra keys and asserts the body carries none of `approvalId/params/paramsDigest/secret/token/orgId/agentId`; owner 200, member 403, unknown agent 404, limit capped, cross-tenant/other-agent rows absent. **WEB** (`ConnectorsTab.tsx`): read-only "Recent activity" section — per row: connector icon+action, classification pill, **Approval** pill when gated, colour-blind-safe status badge (Executed/Failed/Running), relative time, short error on failure; Refresh button; dashboard token system. **MOBILE PARITY** (`screens/AgentConnectors.tsx`): same monitor as a native `Card` list, same fields + status vocab, phone `Chip` tones; Refresh button (embedded in the detail ScrollView, so no nested RefreshControl); `Api.agentConnectorExecutions` hits the identical route (client work only — one backend, one contract). RN-core only, boot-safe, Expo SDK 54 untouched. **DRIFT TRIPWIRE:** ledger status vocab is one source of truth — backend `CONNECTOR_EXECUTION_STATUSES` (used by NAME in every ledger write: running/succeeded/failed); `EXECUTION_STATUSES`/`EXECUTION_CLASSIFICATIONS` hand-copied to web+mobile; `apps/mobile/src/agentConnectors.test.ts` (Mobile-CI-gated) asserts **cross-platform** (phone==desk) AND **backend** (both == the array text-read from source) parity — an **equality** check (a client status the ledger never writes is as wrong as a missing one); `web/lib/agentConnectors.test.ts` mirrors the backend pin. **MONITOR-VS-TRIGGER DECISION — MONITOR-ONLY:** the story allowed an optional owner "run this action" trigger funnelled through `executeConnectorAction` (so CONN-7 authz/approval/step-up/params-digest/single-use all still apply). We deliberately did NOT ship it, per the story's explicit "prefer monitor-only if non-trivial/bypass-risky": a safe trigger needs a per-connector action picker + params form + the approval-**redemption** round-trip on BOTH clients, and the params-digest binding (approved params ≡ executed params) is security-critical — subtly wrong on either client = a CONN-7 bypass or a params-mismatch trap. **There is NO execution path in this stage** (the LIST route is read-only). Trigger is a documented follow-up (future CONN slice) that MUST route through `executeConnectorAction` + reuse the Inbox/Approvals + step-up UI (config-only on the phone). **Backend 1588/1588** + evals **11/11** + tsc clean; **web build + 247 tests**; **mobile 288/288** (+3 tripwires) + typecheck + `expo export`. Strictly additive across `backend/**` + `web/**` + `apps/mobile/**` + docs; one additive owner-gated route, no gate weakened, no CONN-7 bypass. **BUILDER only — not self-audited/merged.** **Remaining: NONE for the epic** — optional/future: owner-trigger follow-up, wiring `executeConnectorAction` into `agent-executor.ts`, audited stdio-MCP execution. `conn-8b-4-execution-ui`. Prior: CONN-8b-3 below.**)_

_Prior: 2026-07-18 (**CONN-8b-3 — CUSTOM-MCP INVOCATION BRIDGE: the RISKIEST and only OPEN-ENDED executor — an agent can now invoke tools on its configured custom MCP server through the CONN-8a framework, gated by CONN-7. Backend-only; mobile parity N/A (execution UI is 8b-4).** Unlike every fixed-host provider the MCP **URL is user-configured** → a real **SSRF/egress** surface, and tool names are **opaque**. `services/connector-mcp.ts`, registered as `mcp` in `EXECUTORS`. **v1 = http-transport MCP servers ONLY; stdio (spawning a local command = arbitrary host command execution) is DELIBERATELY not executed — fail-closed "stdio not yet supported", deferred to a later audited stage.** The action string **IS** the MCP tool name, `params` **ARE** its args → one JSON-RPC `tools/call`; a built-in `tools.list` meta-read is offered for display but gating never trusts the server's self-description. New framework hooks (additive): **`ConnectorExecutor.invoke(action,ctx)`** open-ended dispatch (any opaque tool name → one handler when no fixed `actions[action]` spec matches; class already resolved by `classifyConnectorAction` so authz unaffected); **`escalateAllowToApproval(action,config)`** (supersedes `mustEscalateUnknownWrite` for MCP); **`defaultHttpClient`** (per-executor default transport); `ExecutorContext.config` (the connector's non-secret config, threaded to executors). **OPAQUE-TOOL ESCALATION (carry-forward ii, generalized) — the key containment:** `classifyConnectorAction('mcp',…)` = WRITE by default, destructive-named (`hasDestructiveVerb`) = DESTRUCTIVE → **always approval even under `auto_write`**; and any otherwise-ALLOWED tool (read- OR write-classified) that is NOT on the operator's per-connector **`autoApproveTools`** allow-list (new NON-secret MCP config field) **escalates to `needs_approval`** — so neither `auto_write` nor a `get_*`/`list_*` name can blanket-approve an arbitrary tool. Empty/absent allow-list ⇒ every opaque tool needs approval; only `tools.list` is exempt. **SSRF/EGRESS policy (the crux):** (1) **synchronous URL-shape guard** (`validateMcpUrlShape`, before any dial) — **https only** (http + all other schemes rejected, no localhost dev exception), **no embedded userinfo** (`user:pass@host` host-spoof rejected), and a **literal-IP host validated on the spot** (the one case `net.connect` skips the custom lookup; IPv6 brackets stripped); (2) **DNS-pinning transport** (`createMcpHttpsClient`, node:https, the executor's `defaultHttpClient`) — its custom `lookup` resolves the host, **refuses if ANY resolved address is a blocked range**, returns **only validated addresses** so `net.connect` dials exactly what was checked → **no DNS-rebinding TOCTOU** (mixed public+private answer refused wholesale, no cherry-pick); **redirects NOT followed** (3xx = error, redirect-to-internal blocked at connect on the new host anyway); 10s timeout + 1MB cap; `boundedHttpClient`/global-fetch intentionally NOT used (can't pin). **Blocked ranges** (`isBlockedAddress`, IPv4+IPv6, fail-closed on non-IP): `0/8`,`10/8`,`100.64/10`,`127/8`,`169.254/16` (**incl. `169.254.169.254` metadata**),`172.16/12`,`192.0.0/24`,`192.168/16`,`198.18/15`,`224/4`,`240/4`; `::1`,`::`,`fc00::/7`,`fe80::/10`,`ff00::/8`, IPv4-mapped (`::ffff:127.0.0.1` dotted + `::ffff:7f00:1` hex). **Credential never leaks:** optional bearer (`CONNECTOR_MCP_SECRET` from the env bag → `ctx.secrets`) used ONLY as `Authorization: Bearer` to the configured server; `redactSecrets` backstop scrubs an echoing server; ledger keeps action/classification/status/sanitized-error only (sentinel proves absent from result/error/ledger). **Gated exactly like the others + params-digest intact** (same CONN-7 authz, single-use approved-once via `connector_executions` UNIQUE(`approval_id`), step-up, server-computed `paramsDigest` — approve params X, redeem Y → rejected, nothing dialed); no new table, no route change — registering `mcp` in `EXECUTORS` lights up the existing execute route. **Tests** `backend/src/tests/connector-execution-mcp.test.ts` (+17, mocked transport / no real network / no real MCP): taxonomy + open-ended registration; opaque invoke→WRITE→needs_approval EVEN under auto_write (not allow-listed); destructive-named→always approval; allow-listed under auto_write→executes-once (action=tool-name, params=args, bearer as auth); gated write→approve+step-up→executes-once→replay-rejected; params-digest mismatch→rejected no-dial; `tools.list` free read; **stdio→fail-closed no-dial**; SSRF egress guard tested DIRECTLY (private-range blocker over every required IPv4/IPv6 incl. metadata+mapped; URL-shape over http/userinfo/literal-private; DNS-pinning lookup refuses private-resolving + mixed answers + pins public; real node:https client refuses to open a socket when host resolves private); server redirect not treated as success; bearer never in result/error/ledger. Existing CONN-8a `[CONN8A-NOEXEC]` test updated (mcp now HAS an executor). Backend **1578/1578** + evals **11/11** + tsc clean. Additive to `backend/**` + docs + STATUS (framework gained optional `invoke`/`escalateAllowToApproval`/`defaultHttpClient`/`ExecutorContext.config` + MCP config `autoApproveTools` — all additive). **BUILDER only — not self-audited/merged.** **Remaining (CONN-8b):** CONN-8b-4 web/mobile execution trigger+monitor UI; optional wiring of `executeConnectorAction` into `agent-executor.ts`; a later audited stage for stdio MCP execution. `conn-8b-3-mcp-bridge`. Prior: CONN-8b-2 below.**)_

_Prior: 2026-07-18 (**CONN-8b-2 — REAL GOOGLE WORKSPACE EXECUTOR (Gmail / Calendar / Drive) + AUDIT NITS FOLDED: the FIRST OAUTH-credentialed executor in the CONN-8a framework — an agent can now send email / create calendar events / touch Drive with the user's Google account, gated exactly by CONN-7. Backend-only; mobile parity N/A (execution UI is 8b-4).** Every path fail-closed. **CREDENTIAL SOURCE differs:** not the env bag — the agent's per-agent OAuth **access token** in CONN-5's AES-encrypted **`agent_oauth_tokens`**. New **`credentialKind:'google_oauth'`** on the executor makes `runExecutor` resolve it via CONN-5 **`ensureFreshAgentGoogleToken`** (decrypt → refresh-if-within-60s → re-encrypt) into `ctx.oauthAccessToken` (+ `ctx.oauthScopes`), leaving `ctx.secrets` empty; resolver **injectable** (`ExecuteOptions.googleTokenResolver`) so tests never touch Google. **Refresh token never reaches an executor.** No-connection / revoked (resolver null or throws) → fail closed, NOT executed. **EXECUTOR** (`services/connector-google.ts`), each `class` == `classifyConnectorAction('google',action)` (asserted): READ `gmail.list`/`gmail.get`, `calendar.list`/`calendar.event.get`, `drive.list`/`drive.file.get`; WRITE (approval unless `auto_write`) **`gmail.send`** (RFC 5322 → base64url → `messages/send`), `calendar.event.create`, `drive.file.create`/`drive.file.update`; DESTRUCTIVE (always approval, even trusted) `calendar.event.delete`, `drive.file.delete`, `gmail.delete` (fails closed — CONN-5 never grants `gmail.modify`). **SSRF closed by construction:** hosts HARDCODED (`gmail.googleapis.com`, `www.googleapis.com`); ids + `calendarId` validated then `encodeURIComponent`-encoded; query via `URLSearchParams`; To/Cc/Subject reject control chars (header-injection guard); `redirect:'error'` + 10s + 1MB cap unchanged. **Scope enforcement (best-effort, fail-closed):** pre-check `ctx.oauthScopes` → clean "reconnect with X" not a raw 403. **Token never leaks:** access token only in the `Authorization` header + registered for `redactSecrets`; sentinel proves BOTH access + refresh tokens absent from result/error/ledger. **AUDIT PASS-WITH-NITS — both folded before merge: NIT-1 (framework-level, elevated to high-consequence by 8b-2):** the approval bound connectorId+action+agentId but NOT the params, so an approved `gmail.send to bob` could be redeemed to send to **eve** with different content. Fixed in the shared file+redeem path — `fileConnectorActionApproval` now stores a **server-computed sha256 `paramsDigest`** of the canonicalized (key-sorted) params (the agent can't forge it), and `redeemAndExecute` recomputes the digest from the submitted params and **requires a match** (mismatch/missing → rejected, nothing runs); the operator card's TARGET is now derived **server-side from the real params** (gmail.send → to+subject, calendar/drive → summary/name/id — never the untrusted agent label, never the body). **NIT-2:** a pure `.`/`..` `calendarId` is rejected. **Gated exactly like GitHub** (same single-use approved-once via `connector_executions` UNIQUE(`approval_id`), same step-up); **no new table, no route change** — registering `google` in `EXECUTORS`. **Tests** `backend/src/tests/connector-google.test.ts` (+17, no real network / no real Google): taxonomy + credential-kind, READ executes (injected resolver AND real `ensureFreshAgentGoogleToken` over a seeded encrypted row), hardcoded hosts, `gmail.send` WRITE→approval→approved+stepped-up→executes-once→replay-rejected, **CONN8B2-PARAMBIND (tampered-params redeem rejected + nothing sent; exact params w/ shuffled keys executes once; card shows the real recipient not the agent label)**, DESTRUCTIVE-under-`auto_write` still needs approval, token-never-leaks (access+refresh sentinels), no-connection/revoked → fail closed, SSRF host-fixed + header-injection rejected, missing-scope clean reconnect, `gmail.delete` fail-closed. **Rebased onto CONN-8b-1 (#320)** — `EXECUTORS` map now unions github/jira/telegram/whatsapp/google_chat/**google**. Backend **1549/1549** + evals **11/11** + tsc clean. Additive to `backend/**` + docs + STATUS (`ensureFreshAgentGoogleToken` gained a `scopes` return field — additive). **Remaining (CONN-8b):** custom-MCP bridge (8b-3, honoring carry-forward ii) + web/mobile execution trigger/monitor UI (8b-4); optional wiring into `agent-executor.ts`. `conn-8b-2-google-executor`. Prior: CONN-8b-1 below.**)_

_Prior: 2026-07-18 (**CONN-8b-1 — JIRA + COMMS (TELEGRAM / WHATSAPP / GOOGLE CHAT) EXECUTORS: four more connectors can now ACTUALLY invoke real provider calls, plugged into the CONN-8a framework unchanged (same interface, same CONN-7 authz gate, same single-use approvals, same credential-at-execution + `redactSecrets` backstop). Backend-only; mobile parity N/A (execution UI is CONN-8b-4).** No route change — each executor registers in the framework's `EXECUTORS` map and is reached by the existing `POST /api/agent/connectors/:connectorId/execute`. **(1) JIRA** (`services/connector-jira.ts`) — taxonomy-aligned (each class asserted `= classifyConnectorAction('jira',action)`): `issue.get`/`issue.search`→**READ**, `issue.create`/`issue.comment`/`issue.transition`→**WRITE**, `issue.delete`→**DESTRUCTIVE** (always approval, even trusted). Basic auth from agent-scoped `JIRA_EMAIL:JIRA_API_TOKEN`. **SSRF (host is per-tenant, not hardcodable):** base host comes from the stored `JIRA_BASE_URL` secret — **never a param** — re-validated on EVERY execution (https, no embedded userinfo, Atlassian Cloud host via CONN-4a's `isAtlassianHost` `*.atlassian.net`); we then dial only that URL's **origin** (scheme+host, discarding any path/query/fragment) + validated/encoded segments (issue key charset-restricted, JQL encoded into the query). Self-hosted Jira refused at execution (fail-closed). **(2) TELEGRAM** (`services/connector-telegram.ts`) — WRITE `message.send` → POST `https://api.telegram.org/bot<token>/sendMessage` (**host HARDCODED**); token from `TELEGRAM_BOT_TOKEN`, chat id from `chatId` param or stored `TELEGRAM_CHAT_ID`. **Token-in-URL leak defence:** the token is in the URL path → the URL is itself a secret, so it is NEVER put into an error/returned value (errors surface only Telegram's short `description`+status), and the token is validated to a strict `^[0-9]+:[A-Za-z0-9_-]+$` charset so it can't break out of the fixed-host path; `redactSecrets` strips the token value as a backstop. **(3) WHATSAPP** (`services/connector-whatsapp.ts`) — WRITE `message.send` → POST `https://graph.facebook.com/v21.0/<phoneNumberId>/messages` (**host + API version HARDCODED**); Bearer `WHATSAPP_ACCESS_TOKEN`, `phoneNumberId` from param or stored `WHATSAPP_PHONE_NUMBER_ID` (validated digits-only + encoded — can't escape the path), recipient+text in the JSON body. **(4) GOOGLE CHAT** (`services/connector-google-chat.ts`) — WRITE `message.send` → POST the stored `GOOGLE_CHAT_WEBHOOK_URL`. **Webhook-URL-as-secret defence:** the incoming-webhook URL embeds a key+token in its query, so the whole URL is BOTH the credential AND the dial target — it is NEVER param-supplied and is re-validated on every execution (https, no userinfo, host **exactly** `chat.googleapis.com`) before dialing, and never placed into an error (errors surface only a status); `redactSecrets` strips the URL value as a backstop. **(5) GATING unchanged** — WRITE stays approval-gated unless the (agent,connector) pair is `auto_write`; Jira `issue.delete` DESTRUCTIVE → always approval; credential decrypted only at execution, filtered to just this connector's env keys, used only in the auth header/URL, never returned/logged/stored; ledger keeps action/classification/status/**sanitized**-error only. **Tests** `backend/src/tests/connector-execution-jira-comms.test.ts` (+12, mocked transport, no real network): taxonomy alignment for all four + all four registered; Jira READ executes (mocked); WRITE→approval-not-executed→approved+stepped-up→executes-once (+replay rejected); Jira `issue.delete` destructive→always-approval-under-auto_write; comms sends gated then execute against hardcoded/validated hosts; **credential-never-leaks per connector** (sentinels incl. Telegram bot-token-in-URL + Google Chat webhook-URL-as-secret, in result + error + ledger); **SSRF** — Jira baseUrl restricted to Atlassian (non-Atlassian + userinfo-spoof + bad issue key refused, no call), Google Chat webhook restricted to `chat.googleapis.com` (evil host / `chat.googleapis.com.evil.com` suffix / userinfo / non-https all refused), Telegram token-metachar refused. Backend **1544/1544** + evals **11/11** + tsc clean. Additive to `backend/**` + docs + STATUS (no schema change, no route change — reuses CONN-8a's `connector_executions` ledger + execute route). **BUILDER only — not self-audited/merged.** **Remaining (CONN-8b):** CONN-8b-2 Google Workspace (OAuth) executor · CONN-8b-3 custom-MCP invocation bridge (honoring carry-forward ii) · CONN-8b-4 web/mobile execution trigger/monitor UI; optional wiring of `executeConnectorAction` into `agent-executor.ts`. `conn-8b-1-jira-comms-executors`. Prior: CONN-8a below.**)_

_Prior: 2026-07-17 (**CONN-8a — CONNECTOR EXECUTION FRAMEWORK + FIRST REAL EXECUTOR (GITHUB): an agent can now ACTUALLY invoke a connector action, strictly gated by CONN-7, with a real credential. Backend-only; mobile parity N/A (no UI in 8a).** The highest-consequence stage of the epic — every path fail-closed. **(1) THE FRAMEWORK** (`services/connector-execution.ts`, one entry `executeConnectorAction({orgId,agentId,connectorId,action,params,approvalId?})`): scope-checks the agent belongs to the org + connector is known/configured → **explicit-capability tightening (CONN-7 carry-forward i)**: execution requires an EXPLICIT `connector:<id>`/`connector:*`/`*` cap — `hasExplicitConnectorCapability` **denies an empty/legacy allow-all permission list** (unlike CONN-7's `isCapabilityAllowed`), so an agent with no declared perms can't make real calls by default → **a real executor must exist** or it's refused (only GitHub in 8a; jira/google/comms/**mcp** fail-closed) → the CONN-7 decision (`decideConnectorAuthorization` with the tightened cap): READ→run, WRITE→run only if `auto_write`, DESTRUCTIVE/UNKNOWN→approval; plus **carry-forward ii** (`mustEscalateUnknownWrite`): an OPAQUE tool the executor can't vouch for (MCP's open surface) escalates to needs_approval EVEN under auto_write. **deny→nothing runs; needs_approval→files a `connector_action` approval (the SAME machine-rendered card + step-up the Inbox uses, via the shared `fileConnectorActionApproval`) and returns `pending_approval`+approvalId, NOT executed; allow→execute once.** **(2) APPROVED-ONCE, NO REPLAY, NO UN-APPROVED RUN:** a needs_approval action does NOT run just because the agent holds an approvalId — the agent re-invokes with that approvalId AFTER the operator approves it in the Inbox WITH step-up (the decide route only reaches `approved` after a fresh command session). On redemption the framework requires the approval to be `connector_action`, in-org, **`approved`**, and **bound to this exact (agent, connector, action)** (an approval for X can't run Y; agent A can't redeem agent B's). **Single-use is atomic** via a UNIQUE index on new table `connector_executions.approval_id`: the redemption is CLAIMED (row inserted) BEFORE the provider call, so a replay or concurrent double-redeem hits the constraint → rejected (at-most-once — a failed call still consumes the approval; recovery is re-approval, not replay). **(3) GITHUB EXECUTOR** (`services/connector-github.ts`) — fixed-surface adapter; actions + taxonomy alignment (each class MUST equal `classifyConnectorAction('github',action)`, asserted): `repo.get`/`issues.list`/`issue.get`→**READ**, `issue.create`/`issue.comment`→**WRITE**, `repo.delete`→**DESTRUCTIVE**. **SSRF closed by construction**: host HARDCODED to `api.github.com`, URLs are that constant + validated/encoded segments, owner/repo charset-restricted, issue number a positive int — params never supply a URL. Uses the stored agent-scoped `GITHUB_TOKEN`. **(4) CREDENTIAL** decrypted ONLY at execution, filtered to just this connector's env keys, used only in the Authorization header, **never returned/logged/stored**; a deep `redactSecrets` pass over result + every error string is the backstop; the ledger stores action/classification/status/**sanitized**-error only. **Bounds:** 10s timeout + 1MB response cap (`boundedHttpClient`); provider failures → clean `ConnectorProviderError`, never a raw body. **(5) WIRING** — `POST /api/agent/connectors/:connectorId/execute` (`routes/agent-api.ts`, behind `agentAuth`): org+agent come from the agent token, NEVER the body, so it's inherently org-scoped + agent-in-org; status map executed→200 / pending→202 / denied→403 / provider-error→502 / rejected→409. **`agent-executor.ts` intentionally NOT modified** — wiring execution into the internal LLM loop is deferred to CONN-8b; the integration point is this same `executeConnectorAction` entry. **Tests** `backend/src/tests/connector-execution.test.ts` (+19, no real network): deny (no-cap / legacy allow-all / not-configured / cross-tenant), no-executor fail-closed, READ executes (mocked transport, host-fixed), WRITE→approval-not-executed, un-approved redemption rejected, step-up-refused-stays-pending, **approved+stepped-up executes exactly once + replay rejected**, approval bound to action+agent, auto_write WRITE executes, DESTRUCTIVE-under-auto_write still needs approval, credential-never-leaks (sentinel: echoed-token result + error + ledger), SSRF host-fixed, secured route 401/403/202. Backend **1529/1529** + evals **11/11** + tsc clean. Additive to `backend/**` + docs + STATUS (new `connector_executions` table, idempotent migration; `connector-authz.ts` gained the shared `fileConnectorActionApproval` with no behavior change). **BUILDER only — not self-audited/merged.** **Remaining (CONN-8b):** Jira/Google/comms executors + the custom-MCP bridge (honoring carry-forward ii) + web/mobile execution trigger/monitor UI; optional wiring into `agent-executor.ts`. `conn-8a-execution-github`. Prior: CONN-7 below.**)_

_Prior: 2026-07-17 (**CONN-7 — CONNECTOR CONTAINMENT: THE `connector:` CAPABILITY + PER-CONNECTOR TRUST + APPROVAL/STEP-UP MODEL IS NOW ENFORCED (backend policy + web + mobile trust toggle).** This DEFINES and ENFORCES the policy CONN-8 (execution) MUST consult before running ANY connector action — **CONN-7 itself does NOT execute connectors.** Operator-approved model: **READ runs freely; WRITE/SEND needs approval by default via the EXISTING dangerous-approval + step-up flow; an owner-set per-agent per-connector TRUST toggle can auto-approve WRITEs for a trusted pair; DESTRUCTIVE actions ALWAYS need approval, even when trusted.** Fail-closed throughout. **(1) Action TAXONOMY** (`services/connector-authz.ts` `CONNECTOR_ACTION_TAXONOMY`) — the data map CONN-8 consults: per connector, verbs classify READ/WRITE/DESTRUCTIVE + a `defaultClass` (github/jira/google → **unknown** = fail-closed needs-approval EVEN when trusted; telegram/whatsapp/google_chat → **write** = send; mcp → **write** = a tool call is WRITE by default). A DESTRUCTIVE-keyword guard (`delete|destroy|drop|purge|remove|wipe|revoke|truncate`) forces a stray `delete_*` to DESTRUCTIVE on any connector so trust can never auto-approve it. **(2) TRUST column** — additive `trust_level` on `agent_connectors` (`approval_required` default | `auto_write`), idempotent migration (in BOTH the `CREATE TABLE` and the ALTER list — the ALTER runs before the CREATE on a fresh DB); owner-set via new owner-gated `PUT …/connectors/:cid/trust` (enum-validated, 404 on unconfigured); an ENUM, **never a secret** (added to `PUBLIC_CONNECTOR_FIELDS`, column-classification test still green). **(3) CAPABILITY enforcement** — `hasConnectorCapability` requires `connector:<id>` (or `connector:*`/`*`) via `governance2.isCapabilityAllowed`; absent → **deny** (the reserved namespace is now real; `agent-permissions.ts` already accepted these caps as writable). **(4) ENFORCEMENT service** — `authorizeConnectorAction({orgId,agentId,connectorId,action})` → `{decision: allow|needs_approval|deny, reason, classification, approvalId?}`: capability→deny-if-missing → configured→deny-if-not → READ→allow → DESTRUCTIVE/unknown→needs_approval (trust ignored) → WRITE→allow-if-`auto_write`-else-needs_approval; missing/cross-tenant agent, unknown connector, blank action all → deny. On `needs_approval` it FILES an `approval_requests` row of the NEW dangerous type **`connector_action`** via `prepareApprovalRecord` (machine-rendered summary, never model prose) so it lands in the Inbox and **requires the SAME step-up** (`x-arturita-session`) the phone/desk enforce on `/approvals/:id/decide`. Reused, not rebuilt: `dangerous-approvals.ts` gained one type + one renderer; the decide gate already binds it. **(5) UI (web + mobile)** — owner-only per-connector **Write trust** toggle in the accordion ("Require approval for writes" ↔ "Auto-approve writes (trusted)") with an explicit note that destructive actions always require approval; shown for any CONFIGURED connector; backend is the enforcer. Mobile mirrors (SDK 54 / react 19.1.0 / boot-safe, RN-core only); Google on the phone stays config-only (trust it from the web dashboard, mirroring CONN-5). **Tests:** `backend/src/tests/connector-authz.test.ts` (+24 — taxonomy read/write/destructive + fail-closed-unknown + destructive-keyword override · the pure decision matrix · `authorizeConnectorAction` files the approval + **no credential in the card** · write-trusted→allow but destructive-trusted→needs_approval · missing-cap→deny · not-configured→deny · cross-tenant→deny · **NO step-up bypass** · owner-only trust (member→403), invalid enum→400, unconfigured→404, cross-tenant→404 · every backend connector has a taxonomy entry); client parity `TRUST_LEVELS`+`isTrusted` mirrored web⇄mobile (cross-platform tripwire agrees). Backend **1507/1507** + evals **11/11** + tsc clean · web **build clean · 244/244** · mobile **npm test 285/285 · typecheck clean · expo export bundles (boot-safe)** · **SDK 54 / react 19.1.0 / boot-safe untouched** (RN core only). Additive to `backend/**` + `web/**` + `apps/mobile/**` + docs + STATUS. **BUILDER only — not self-audited/merged. CONN-8 (execution) MUST call `authorizeConnectorAction` first and only proceed on `allow`.** `conn-7-connector-containment`. Prior: CI-WEB-1 below.**)_

_Prior: 2026-07-17 (**CI-WEB-1 — `web` NOW RUNS ITS UNIT TESTS + PRODUCTION BUILD IN CI — closing the same gap for the desk that CI-MOB-1 closed for the phone.** Web unit tests were gated by **no workflow**: `test.yml` runs backend only, `ci.yml` had only the `check` install/typecheck matrix + the `mobile` job, and `deploy.yml` just calls `vercel`. So a red web test reached `main` unseen — exactly how CONN-5 left `web/lib/agentConnectors.test.ts` **RED on `main`** until CONN-6 happened to fix it. **One file, one new job, additive** (`.github/workflows/ci.yml` gains `web`, mirroring the `mobile` job's shape): `npm ci --legacy-peer-deps` (off the committed `web/package-lock.json` — validated to resolve clean against react 19 / next 15) → `npm run typecheck` → `npm test` (**243/243**, `node --test` over `app/**` + `lib/**`) → `npm run build` (`next build` must pass, the web/CLAUDE.md merge invariant). Node 22 + `cache: npm` on `web/package-lock.json`, same as `mobile`. **`check`/`mobile`/backend/`app` jobs untouched; `npm audit` stays non-blocking; no app source touched.** Verified before wiring: web **243/243 · typecheck clean · build clean** on `main`. **⚠️ Precisely scoped exactly like CI-MOB-1: this makes a red web check VISIBLE on every PR, it does NOT make it BLOCKING** — `main` has no branch protection, and the `--squash --admin` convention bypasses protection even once added; requiring the `Web (web)` check is a separate operator-only GitHub-settings toggle (GO-LIVE item alongside the mobile one). **Operator-authorized exception** to the root guide's `.github/workflows/` prohibition — this IS the change that makes web tests real in CI. `ci-add-web-tests`. Prior: CONN-6 below.**)_

_Prior: 2026-07-17 (**CONN-6 — TELEGRAM + WHATSAPP + GOOGLE CHAT BECOME PER-AGENT COMMUNICATION CONNECTORS (config + credential storage; backend + web + mobile).** The three Communication rows move from "coming soon" to **available** on both surfaces — config + an encrypted, agent-scoped credential that rides the existing env-injection path. **The backend does NOT yet send/receive — that is the CONN-8 execution bridge (decision D).** **Signal stays OUT of scope** (no official API). **Catalog** (`services/agent-connectors.ts`): `telegram` / `whatsapp` / `google_chat`, all `category:'Communication'`, `authType:'token'`, `hasSecret`+`secretRequired`; config schemas zod `.strict()`, all NON-secret fields optional — telegram `{botUsername?,chatId?}`, whatsapp `{phoneNumberId?,businessAccountId?}`, google_chat `{space?}`; the credential is always the WRITE-ONLY `secret`, never config. **Env-var contract (`CONNECTOR_ENV_KEYS`) — the same wire that made github/jira real:** telegram → **`TELEGRAM_BOT_TOKEN`** (secret) + **`TELEGRAM_CHAT_ID`** (non-secret) — **CONFIRMED** name (`routes/telegram-webhook.ts` + the openclaw adapter both read it); whatsapp → **`WHATSAPP_ACCESS_TOKEN`** (secret) + **`WHATSAPP_PHONE_NUMBER_ID`**/**`WHATSAPP_BUSINESS_ACCOUNT_ID`** (non-secret) — **conventional Meta Cloud API names, NO in-repo consumer yet** ⚠️ operator-confirm; google_chat → **`GOOGLE_CHAT_WEBHOOK_URL`** (the incoming-webhook URL IS the credential — it embeds a key+token — so stored as the write-only secret, never config) — **conventional, NO in-repo consumer yet** ⚠️ operator-confirm. Non-secret ids are also written to the encrypted secret store (env-injection is the only channel to the runtime) AND kept in `config` as the returnable display source; `primarySecretKey` is recorded as `secretRef`. **`test` is a SAFE STUB** (no provider is dialed — a test asserts `fetch` is never called — avoiding the SSRF surface of an arbitrary webhook/graph host); real pings land with execution (CONN-8). **Web + mobile:** real inline config forms (write-only secret + non-secret inputs), save/test/delete, masked status, owner-gated + 403 identical to the github/jira rows; a shared `useCommsConnector` hook factors the save/test/delete plumbing on each platform so the write-only-secret contract lives in one place. **SECURITY:** the credential lives ONLY in the encrypted `secrets` store, NEVER in the `agent_connectors` row / any read/list/get/test/put response / any log; `toPublicConnector()` masks unchanged; disconnect purges EVERY agent-scoped env row (credential + non-secret ids). **Tests:** `backend/src/tests/agent-connectors-comms.test.ts` (+10 — catalog/auth · strict config validation · env-key contract · owner-403/required-cred-400 · **`[CONN6-EXEC]` resolved bag carries all runtime keys** · **sentinel leak sweep** (no credential/key name in any response) · `test`-is-a-stub (no dial) · disconnect purge · cross-tenant 404); both client available-sets now `[google_chat, telegram, whatsapp, github, jira, google, mcp]`, SUBSET tripwire + cross-platform validator parity green, + form-validation & config-to-form leak tests on both clients. (Also fixed a **pre-existing red on `main`**: CONN-5 updated the web catalog to one Google row but left `web/lib/agentConnectors.test.ts` asserting the old three — brought the two web assertions back green.) Backend **1483/1483** + evals **11/11** + tsc clean · web **build clean · 243/243** · mobile **npm test 284/284 · typecheck clean · expo export bundles (boot-safe)** · **SDK 54 / react 19.1.0 / boot-safe untouched** (RN core inputs only). Additive to `backend/**` + `web/**` + `apps/mobile/**` + docs + STATUS. **BUILDER only — not self-audited/merged; the `connector:` capability + trust containment is still CONN-7, execution CONN-8.** `conn-6-comms-connectors`. Prior: CONN-5 below.**)_

_Prior: 2026-07-17 (**CONN-5 — PER-AGENT GOOGLE OAUTH (CALENDAR/GMAIL/DRIVE): WEB FULL FLOW, MOBILE CONFIG-ONLY.** An agent can now connect its OWN Google account — a single per-agent OAuth connection (catalog id `google`, authType `oauth`) granting the operator-selected Calendar/Gmail/Drive scopes. **Storage — agent-scoped + ENCRYPTED (an improvement over the org connector's plaintext):** new `agent_oauth_tokens` table (`schema.ts`, idempotent CREATE in `setup.ts`) keyed by `(org_id, agent_id, provider)`, `access_token_enc`/`refresh_token_enc` AES-256-GCM via `services/secrets`, plus `expires_at`/`scopes`/`account_email`. **Deliberately NOT the `secrets` table**, so a refresh token is never swept into the agent env bag (`GET /api/agent/secrets`); never projected by `toPublicConnector`, never returned to a client, never logged. The org `oauth_tokens` (plaintext, org-keyed) is untouched. **State security — unforgeable + single-use + expiring + PKCE:** new `agent_oauth_states` table whose `id` IS the `state` param (256-bit random, server-side only, bound to one `(org, agent, connector)`, carries the PKCE `code_verifier`, expires in 10 min, spent exactly once via an atomic conditional UPDATE) — replaces the org flow's forgeable `state=orgId`; **PKCE S256** binds the code. `services/agent-google-auth.ts` owns the crypto/HTTP + state/token store; `services/oauth-redirect.ts` is the redirect allow-list (only `ALLOWED_ORIGINS` — no open redirect; the Google `redirect_uri` is always our fixed callback). **Routes:** owner-gated **start** `POST …/agents/:agentId/connectors/google/oauth/start` (validates agent-in-org, ≥1 service, mints state+PKCE, returns the consent URL — no token); **public callback** `GET /api/agent-connectors/google/callback` (registered beside `authRoutes` — Google sends no app JWT; the state row IS the authz) spends the state once, exchanges the code (PKCE), fetches the account email, stores ENCRYPTED tokens, records the connector row `status='connected'`, bounces to `/dashboard?google=connected&agent=…` **with no token in the URL** (bad/expired/reused state, denied consent, or vanished agent → `?google=error`, no token issued); owner-gated **disconnect** best-effort revokes at Google + purges the token row; the generic configure POST/PUT **reject** the oauth connector (no half-connected row). **Runtime reach:** `agent-executor.ts`'s Drive-context block now PREFERS the agent's own Google token (`ensureFreshAgentGoogleToken` → decrypt → refresh + re-encrypt in place → `searchDriveFiles`), falling back to the org token — same backend-side mechanism the org connector uses, so the connection actually reads that account's Drive. **UI:** web `ConnectorsTab` gets a real Google panel (service checkboxes · Connect → launch OAuth → return banner · connected email + granted scopes · Disconnect, owner-only); **mobile `AgentConnectors` is CONFIG-ONLY** — the `google` row shows status + account email + services + "Connect Google from the web dashboard", **no OAuth button/flow** (the phone can't complete OAuth without an EAS dev build). **The 3 Google service rows collapsed to ONE `google` row**; both client catalogs' available-set is now `[github, jira, google, mcp]` and the SUBSET tripwire (client ⊆ backend `AGENT_CONNECTORS`) + cross-platform parity stay green. **SECURITY tests** (`backend/src/tests/conn5-google-oauth.test.ts`, +20): state forgery/expiry/replay rejected · callback issues no token on bad state · tokens encrypted at rest + never in any response · owner-gate (member→403) · tenant (org A ✗ agent B→404) · runtime resolve + refresh/re-encrypt · disconnect purge · generic-write guard. Backend **1473/1473** + evals **11/11** + tsc clean · web **build clean** · mobile **npm test 281/281 · typecheck clean · expo export bundles** · **SDK 54 / react 19.1.0 / boot-safe untouched** (RN core only). Additive to `backend/**` + `web/**` + `apps/mobile/**` + docs + STATUS. **BUILDER only — not self-audited/merged; the `connector:` capability + trust containment is still CONN-7.** `conn-5-google-oauth`. Prior: CONN-4b below.**)_

_Prior: 2026-07-17 (**CONN-4b — GITHUB (PAT) + JIRA (BASIC) BECOME USABLE PER-AGENT CONNECTORS ON WEB **AND** MOBILE.** The two connectors CONN-4a made real on the backend now have working config rows in the agent Connectors accordion on **both** surfaces (web `app/dashboard/agent/ConnectorsTab.tsx`, mobile `src/screens/AgentConnectors.tsx`), over the SAME CONN-1/CONN-4a API (no backend change). **`github` + `jira` moved from the "coming soon" set to the AVAILABLE set** in BOTH client catalogs (`web/lib/agentConnectors.ts` + `apps/mobile/src/agentConnectors.ts`) — so their rows render a real form instead of a disabled note. **GitHub form:** optional `username` (display label, non-secret) + a write-only **Personal Access Token** → `POST/PUT …/connectors/github` `{config:{username?}, secret:PAT}`; **Jira form:** `baseUrl` (URL-validated) + `email` (non-secret config) + a write-only **API token** → `POST …/connectors/jira` `{config:{baseUrl,email}, secret:token}`. Both wire the CONN-4a **Test** (GitHub → real `api.github.com/user` check; Jira → Atlassian-host check) and **Disconnect**. Client-side validation mirrors the CONN-4a backend zod (github: username ≤120; jira: baseUrl valid URL ≤2048, email valid ≤320; token non-empty on FIRST configure) with the server as final validator. **SECURITY (identical to custom MCP): the token field is WRITE-ONLY** (`type=password` / `secureTextEntry`), seeded from `''` never a read, cleared after save, **blank-on-save preserves the stored token** — the API returns only masked status + non-secret config (username/baseUrl/email + accountLabel), **NEVER the token**; new tests assert `*ConfigToForm` can't surface a credential even if one leaks into `config`. **Owner-gating + 403 handling identical to the existing rows** (backend is the real gate; non-owner read-only note; mutating 403 preserves edits incl. the typed secret). **Parity:** both clients' available-set is now `[github, jira, mcp]`, and the **SUBSET tripwire** (client ⊆ backend `AGENT_CONNECTORS`) stays green; the cross-platform parity test still asserts the phone == the desk field-for-field, now including github/jira validators. Web **build clean · 239/239** (+8) · mobile **npm test 280/280 · typecheck clean · expo export bundles (boot-safe)** · **SDK 54 / react 19.1.0 / boot-safe untouched** (RN core inputs only, no native module). Additive to `web/**` + `apps/mobile/**` + docs + STATUS. **BUILDER only — not self-audited/merged; the powerful-token containment (`connector:` cap + trust) is still CONN-7.** This completes GitHub + Jira as usable per-agent connectors on both surfaces. `conn-4b-github-jira-ui`. Prior: CONN-4a below.**)_

_Prior: 2026-07-17 (**CONN-4a — GITHUB (PAT) + JIRA (BASIC) BECOME REAL PER-AGENT CONNECTORS (backend).** `github` (auth `token`) + `jira` (auth `basic`) added to the agent catalog (`services/agent-connectors.ts`), config schemas zod `.strict()`: GitHub `{ username? }` (label only — the PAT is NOT config), Jira `{ baseUrl: url(), email: email() }` (baseUrl URL-validated). **REAL, not just stored — the credential rides the existing agent-secrets env-injection:** the agent-scoped `secrets` KEY **is** the env-var the runtime receives (`GET /api/agent/secrets` → `resolveSecretsForAgent` bag → adapters inject VERBATIM `os.environ[k]=v`). **Env-var contract (`CONNECTOR_ENV_KEYS`):** github → **`GITHUB_TOKEN`** (confirmed — matches the org connector's `secretKey` AND the backend's own `process.env.GITHUB_TOKEN` consumer in `routes/skills.ts`); jira → **`JIRA_BASE_URL` + `JIRA_EMAIL`** (non-secret) **+ `JIRA_API_TOKEN`** (secret) — conventional names, **no in-repo Jira env consumer exists** (org Jira uses a `JIRA_CONNECTION` JSON blob, not env), so ⚠️ **operator: confirm your runtime's Jira tooling reads exactly these** (one edit in `CONNECTOR_ENV_KEYS`/`connectorSecretEntries` if not). Jira's base/email are non-secret yet also stored in the (encrypted) secret store because env-injection is the only channel to the runtime — `config` still holds them as the returnable display source. **`test` is a REAL, SSRF-safe provider check** (not a stub): GitHub → `GET api.github.com/user` (host hardcoded); Jira → `{baseUrl}/rest/api/3/myself` **only when `isAtlassianHost(baseUrl)`** (`*.atlassian.net`) — a self-hosted host is NOT dialed; 8s timeout, token never echoed. **SECURITY:** token ONLY in the encrypted `secrets` store, NEVER in the `agent_connectors` row / any read / any log; `toPublicConnector()` unchanged (base/email/username returnable, token not); owner-gated + tenant-scoped as CONN-1; disconnect purges every agent-scoped env row (token AND non-secret base/email). Backend **1452/1452** (+15, `tests/agent-connectors-github-jira.test.ts`: catalog/auth · Jira URL+email validation · env-key contract · `isAtlassianHost` SSRF guard · member 403 / required-credential 400 · **`[CONN4A-EXEC]` resolved bag carries the runtime keys** · sentinel leak sweep · live-test known-hosts-only + records `error` on failure · disconnect purge · cross-tenant 404) + evals **11/11** + tsc green. Additive to `backend/**` + `docs/DESIGN-per-agent-connectors.md` (CONN-4a marked, env keys documented) + STATUS. **BUILDER only — not self-audited/merged; the powerful-token containment (`connector:` cap + trust) is still CONN-7.** **No web/mobile/desktop touched — CONN-4b enables the client rows next, so mobile parity is deferred to CONN-4b.** `conn-4a-github-jira-backend`. Prior: CONN-3 below.**)_

_Prior: 2026-07-17 (**CONN-3 — THE PHONE MIRRORS THE CONNECTORS ACCORDION: web+mobile parity complete.** New owner-gated **Connectors** section on the mobile agent detail (`apps/mobile/src/screens/AgentDetailScreen.tsx`, rendered by `ConnectorsSection` in the new `screens/AgentConnectors.tsx`), directly under Configuration/Settings — mirroring CONN-2 over the **same CONN-1 contract, no backend change** (four owner-gated verbs added to the mobile api client: `agentConnectors`/`saveAgentConnector`/`testAgentConnector`/`deleteAgentConnector`). **Accordion grouped by category exactly as the operator listed** (a local collapsible — the MOB-6e MemoryScreen idiom, no new dep): Communication (Google Chat · Telegram · WhatsApp · Signal), IT / Project management (GitHub · Jira), Google Services (Google Calendar · Gmail · Google Drive), Custom MCP servers (opens by default); each connector a row with a colorblind-safe status **Chip** (✓ Connected / ○ Not connected / ⋯ Coming soon / — Out of scope). **Only the custom MCP server is REAL in v1** — inline panel: name · transport http|stdio (segmented control) · url|command+args · optional **write-only** token (`secureTextEntry`) → `POST …/connectors/mcp`, masked status, `POST …/test`, `DELETE`; optimistic edits reconcile to the server's masked row, a failed save keeps the operator's edits incl. the typed secret. **Every other connector is a disabled row with the same honest stage note as the web** (GitHub/Jira → CONN-4, Google trio → CONN-5, Telegram/WhatsApp/Google Chat → CONN-6, **Signal out of scope**) — the UI only ever POSTs `mcp`. **SECURITY: no stored secret is ever displayed** — the read projection carries no credential (not even a `secretRef`), the token input is write-only, seeded from `''` (never a read), cleared after a successful save, blank-on-save keeps the stored token; a test asserts the read state never carries secret/token/secretRef. **AUTHZ: owner-only, backend is the real gate** — the list GET is itself owner-gated, so a **403 on load** renders a clean read-only "owner-only" note; a known member gets that note without a round-trip; the phone offers the surface to an owner OR when the role is genuinely unknown (fail-OPEN to the backend gate, MOB-7d pattern). Pure catalog + MCP validation in `apps/mobile/src/agentConnectors.ts` (a parity-pinned mirror of `web/lib/agentConnectors.ts`); `agentConnectors.test.ts` (+18) has **two tripwires** — a cross-platform import of the dep-free web module asserting the phone's groups + validation verdicts equal the desk's, and a backend text-read of `AGENT_CONNECTORS` asserting the client "available" set can't drift from the server catalog. `apps/mobile` **npm test 275/275 · typecheck clean · expo export bundles (boot-safe)** · **SDK 54 / react 19.1.0 / boot-safe untouched** (RN core inputs only, no native module). Additive to `apps/mobile/**` + docs + STATUS. **This completes the web+mobile parity for the connectors accordion.** `conn-3-mobile-connectors-accordion`. Prior: CONN-2 below.**)_

_Prior: 2026-07-17 (**CONN-2 — THE WEB CONNECTORS TAB: an accordion of an agent's connectors, grouped by category, custom MCP working end-to-end.** New owner-only **Connectors** tab on the web agent detail (`web/app/dashboard/agent/ConnectorsTab.tsx`; `'connectors'` added to `AGENT_TABS` between Configuration and Runs). **Accordion grouped by category exactly as the operator listed:** Communication (Google Chat · Telegram · WhatsApp · Signal), IT / Project management (GitHub · Jira), Google Services (Google Calendar · Gmail · Google Drive), Custom MCP servers. **Only the custom MCP server is REAL in v1** (the CONN-1 pilot) — add/configure (name · transport http|stdio · url|command+args + optional write-only secret) → `POST …/connectors/mcp`, masked status, `POST …/test`, `DELETE`. **Every other connector renders as a disabled row with an honest note** pointing at its stage (GitHub/Jira → CONN-4, Google trio → CONN-5, Telegram/WhatsApp/Google Chat → CONN-6, **Signal out of scope**) — never a fake save, because the CONN-1 catalog holds only `mcp` and calling an unknown connectorId 404s. **SECURITY: no stored secret is ever displayed** — the read projection carries no credential (not even a `secretRef`), the token input is **write-only** (`type=password`, cleared after a successful save, blank keeps the stored token). **AUTHZ: owner-only, backend is the real gate** — the list GET is itself owner-gated, so a **403 on load** renders a clean read-only "owner-only" note rather than a scary error; a mutating 403 surfaces in the row with the operator's edits (incl. the typed secret) **preserved** for retry. Pure catalog + client-mirror MCP validation in `web/lib/agentConnectors.ts`; **parity tripwire** (`agentConnectors.test.ts`) reads the backend `AGENT_CONNECTORS` source and fails if the client "available" set drifts from it. Web **build clean · typecheck clean · 234/234** (+13: category rendering, available-set, masked-only display, MCP validation mirroring the zod schema, backend-catalog parity). Additive to `web/**` + docs + STATUS. **CONN-3 mirrors this to the phone** (same contract). Prior: CONN-1 below.**)_

_Prior: 2026-07-17 (**CONN-1 — EPIC CONN BEGINS: the secure per-agent connectors backend + one config-only connector (custom MCP) end-to-end.** New `agent_connectors` table (`orgId, agentId, connectorId, status, config[NON-secret json], accountLabel, secretRef, useOrgConnection, lastTestedAt, lastError, timestamps` + `UNIQUE(orgId,agentId,connectorId)`) — additive/idempotent/reversible migration in `db/setup.ts` (`DROP TABLE` to undo, nothing backfilled). **Credentials are AGENT-OWNED** (encrypted into the existing `secrets` store at `agent` scope, `scopeId=agentId`, referenced by `secretRef`) with an optional `useOrgConnection` inherit flag — **the credential is NEVER stored in the row, NEVER returned to any client.** API `routes/agent-connectors.ts`, every verb **`requireOrgRole('owner')`** on `/api/orgs/:orgId/agents/:agentId/connectors[...]` (the org-scoped path so the gate binds — not the R-4 tail-trap): list (masked), get one, POST configure, PUT config (credential untouched), POST test (a **safe stub** — does NOT dial the arbitrary MCP URL from the backend; SSRF deferred), DELETE (row + agent-scoped secret). Reads go through **`toPublicConnector()`** — an allow-list projection (twin of `toPublicOrg`); `secretRef` is a never-projected field and a classification test fails if a new column is unclassified. The pilot connector is **custom MCP** (`connectorId='mcp'`, zod-validated `{name, transport, url|command, args}` `.strict()` + optional secret) — chosen because it exercises config **and** secret storage without OAuth. **Full tool invocation is planned as CONN-8** (a backend MCP client bridge); v1 stores config + rides the existing agent-secrets env-injection (proven: `resolveSecretsForAgent` agent-override wins over a company default). Backend **1437/1437** (+16, `tests/agent-connectors.test.ts`: member 403 / owner 201 / junk-config 400 / cross-tenant 404 / credential-never-in-any-response / delete-removes-secret / agent-override-wins / migration-idempotent / column-classification; new module swept by `membership-scoping.test.ts`) + evals **11/11** + tsc green. Surgical to `backend/**` + `docs/DESIGN-per-agent-connectors.md` (decisions A–F CONFIRMED, roadmap CONN-1…CONN-8) + STATUS. **UI is CONN-2 (web) / CONN-3 (mobile) — no web/mobile/desktop touched here, so mobile parity is N/A for this backend-only story.** `conn-1-agent-connectors-backend`. Prior: SEC-AGENT-PERMS below.**)_

_Prior: 2026-07-17 (**SEC-AGENT-PERMS — the per-agent permissions write is now OWNER-gated + validated, closing the hole MOB-7d flagged.** `PUT /api/orgs/:orgId/agents/:agentId/permissions` replaces the old `PATCH /api/agents/:agentId/permissions`, which was **member-gated** (the surface-wide `:agentId`→org hook) and **unvalidated** (`body.permissions.map(String)` straight into `db.update`) — so **any org member could rewrite ANY agent's capability caps to arbitrary strings**, while every sibling agent-write route (config/trust/model-profile/skills) is `requireOrgRole('owner')` + validated. Now it is too: owner-only via the org-scoped path (the old tail path made `requireOrgRole('owner')` no-op — the documented R-4 trap), caps validated against the runtime vocabulary (`services/agent-permissions.ts`: `*`, bare `machine_exec`, or `<memory|attachment|connector>:<action|*>`), a member 403s, an unknown cap 400s, and **allow-all semantics are unchanged** (empty/absent → `[]` → allow-all). The legacy `PATCH /api/agents/:agentId` now **strips `permissions`** so it can't be a side door. The **only** client that writes permissions — the web `GovernancePanel` — moved to the new URL/verb in the same PR (its trust/model-profile siblings already 403 members there); mobile Governance is read-only (does not write permissions); no other caller (CLI/desktop) touches it. Backend **1420/1420** + evals **11/11** + tsc green · web build + **222/222** green · `tests/agent-permissions-authz.test.ts` (member 403 / owner 200 / invalid 400 / empty→allow-all / cross-tenant 404 / legacy side-door closed). Surgical to `backend/**` + one web line + docs. `fix-agent-permissions-authz`. Prior: MOB-7d below.**)_

_Prior: 2026-07-17 (**MOB-7d — THE AGENT DETAIL BECOMES EDITABLE: owner-gated agent settings from the phone.** MOB-6b named this deferral; this is the write layer, additive to `apps/mobile/**`, **no backend route added, no gate weakened, no backend/web/desktop source touched.** The phone edits an agent over the **same owner-gated, validated routes the web uses** — never the legacy unvalidated PATCH: **Identity & adapter** (`PUT …/config`), **Model profile** (`PUT …/model-profile` — confirm, spend+capability), **Trust tier** (`PUT …/trust` — confirm, containment), **Skills** (`GET`/`PUT …/skills`, optimistic + rollback). Every route is `requireOrgRole('owner')`, so the phone learns the caller's role from `/api/orgs` (`memberRole`, threaded through auth as `orgRole`): a known **member** sees the read-only screen, an **owner** gets Edit, an **unknown** role (pasted token) is offered Edit *with a caution* and the backend 403 is the real enforcer — no gate loosened. Client validation mirrors the server message-for-message (`agentEdit.ts` ↔ `validateConfigPatch` + `wouldCycle`); a failed save keeps the edits and names the error. **⚠️ Flagged, not shipped:** per-agent **permissions** editing is deferred because its route is member-gated + unvalidated server-side — a phone owner-gate there would be client-only (pre-existing backend hole). Tripwires (`agentEdit.test.ts`, +26) import the dep-free real sources (`agent-config.ts`, `agentSkills.ts`, `trust.ts`). Mobile **254/254** (+26) · typecheck clean · `expo export` bundles (1290 modules) · **react 19.1.0 + SDK 54 untouched, boot-safe**. `mob-7d-agent-settings-edit`. Prior: MOB-7a/7b/7c below.**)_

_Prior: 2026-07-16 (**MOB-6f — GOVERNANCE · SETTINGS · CONNECTORS BUILT: the last three operator-facing menus land on the phone, all **read-only**, **zero backend change** — every endpoint already served the web. **Governance** (`GovernanceScreen.tsx`) = `GET …/policies` + `…/agents` + `…/revisions`, the web panel's own calls (its fourth, `…/available-models`, is deliberately NOT made: it exists only to fill the model-profile `<select>`, and there is no editor to fill); **Settings** (`SettingsScreen.tsx`) = `GET /api/orgs`, the same source the web's form seeds from; **Connectors** (`ConnectorsScreen.tsx`) = `GET …/connectors`. **Re-scoped:** this story absorbed 6h + 6k + the Settings half of 6j — the three are ONE problem (a web panel that is an *editor wrapped around a reading*, where only the reading travels) — and **displaced Overview to MOB-6l**, which is NOT built. **Every write deferred, none dropped:** add/remove policy, save permissions, trust tier + boundary (owner-only), model profile, rollback; the Settings form + doc-ingest; connect/OAuth, test, disconnect, token rotation, the Google toggles and gear sheets. Governance is read-only on purpose — it decides *what an agent is allowed to do*, and a mis-tap there (Remove beside a policy, a one-tap rollback, a trust flick from Standard to Low-trust) has no undo. **The trap the story existed to not fall into: an empty permission list means ALLOW ALL, not 'allowed nothing'** (backend `permissions == null/[]` → legacy allow-all; the web's own hint says "Empty = allow all") — rendering `[]` as "none" would tell the operator an agent is locked down while it is unrestricted, so the empty case is a LABEL (`⚠ Allow all (unrestricted)`), pinned across null/''/'[]'/'{}'/garbage. **⚠️ Found while building (pre-existing, reported not absorbed): `GET /api/orgs` is `db.select().from(organisations)` — the WHOLE row — and that row has a `telegramBotToken` column.** The phone has received that payload since MOB-1 (`ConnectScreen`); narrowing it is a BACKEND change, out of scope for an apps/mobile-only story. Settings therefore renders a fixed **allow-list** (`SETTINGS_FIELDS`), never a spread of the org, never a key walk, nothing logged — and `OrgSettingsLite` types only the harmless fields so a future `{...org}` can't typecheck onto the screen. **Audit nit folded in (#293): the secret guard is now a deny-list AND a regex.** A name-based check only catches a field that *announces* itself — `telegramBotToken` does; **`deployConfig` does not**, and it is the column that stores **LLM API keys, some in plaintext** (org creation writes `<provider>_api_key` straight into it). That was the blind spot exactly where it cost most: a column that IS a credential without being NAMED one. `CREDENTIAL_BEARING_FIELDS` now names it, a test proves the **deny-list** (not the regex) is what catches it, and another asserts every denied column still exists — a deny-list entry pointing at nothing is a dead guard that also hides that the credential moved. The rendered allow-list is unchanged. **No secret value is rendered on any of the three screens; no mutating call exists anywhere in the PR** (all five new `Api` helpers are bare GETs). **Tripwires pinned to the server where possible:** trust semantics vs `web/lib/trust.ts` (import), the connector registry vs `backend/src/services/connectors.ts` (import — stronger than a web-copy mirror: a connector added or re-categorised server-side now fails CI instead of vanishing from the phone), and the Settings allow-list vs `backend/src/db/schema.ts` (import — asserts the `telegramBotToken` hazard is REAL and excluded). Honestly NOT import-tripwirable and pinned as literals instead: `CAP_HINTS`, the `CATEGORY_ORDER` *ordering*, and Settings' three labels — all JSX-bound. `npm test` **147/147** (+42) · typecheck clean · `expo export` bundles · SDK 54 + react pins untouched · additive `apps/mobile/**` + docs only.**)_

**Latest (2026-07-17) — ⚛️ EPIC H5/MOB · MOB-7a: THE PHONE'S COMMAND CENTER IS THE DESK'S — REACTOR, VOICE CONTROLS, AND TASKS FOLDED INTO THE INBOX.** Two mirrors, **zero backend change** — every endpoint already existed. **(1) The reactor.** `Reactor.tsx` draws the web's J7 brushed-silver arc-reactor with **`react-native-svg`** (15.12.1, the SDK 54 bundled pin): same 200×200 view box, same radii, same dash patterns, same counter-rotation, the same 7-hexagon mark, the icy core — and the **BLACK 7Ei honeycomb** at its centre. The decisions are a **port** of `web/app/dashboard/reactor.logic.ts` (Metro can't import out of `apps/mobile/`), and the port is **pinned**: `reactor.test.ts` imports the **web module** and deep-equals every voice state field-for-field, asserts the silver ramp / core stops / every state accent **equal `web/app/dashboard/tokens.ts` `themes.dark`**, and — because RN can't consume CSS — **reads `web/app/globals.css`** and asserts each ported motion period against the real keyframes. The metal stays **state-independent**; only the inner accent ring tints (muted grey → Aztec purple), so state reads from **icon + label + motion**, never hue. **⚠️ One deliberate token divergence, and it's the logo:** the mark sits on the core, `--reactor-core-1` is near-white in **both** themes, and the web's *dark* token paints the mark that **same value** — the wash-out its *light* theme documents fixing by going black. So the phone takes the **light** `--reactor-logo-fill` (`#000000`) onto its dark surface, pinned to that web token by a test rather than hardcoded. Verified by rendering both states from the real module values: the honeycomb is crisp, the rings read as machined metal. **(2) Voice — live, gated, and honest about which is which.** **Spoken replies: LIVE** (`expo-speech`, on-device, stock Expo Go — §3.2's "cheapest voice win", and §3.3's advice taken: the inert backend TTS is ignored). **Push-to-talk: LIVE once the deployment has a key** — `expo-av` records m4a → `POST …/arturita/transcribe` (the MOB-5a leg); with no key the route answers **503 `not_configured`** and the UI says so, **names the fix** (`OPENAI_API_KEY` on Fly or Cockpit → Secrets), latches the chip, and keeps typing working — it never crashes and never pretends. **Wake word: RENDERS, DISABLED** — it needs a dev build (§3.2), so the toggle mirrors the desk's arrangement but refuses and explains, because a toggle that flips and silently never listens is worse than none. **The chips don't all match the desk's strings, on purpose:** the web says `🔒 Local Whisper (free, on-device)` because on the desk it *is*; the phone says **`hosted Whisper`**, since the clip leaves the handset for the org's backend — a "local" chip would misstate where the operator's audio goes, and a test asserts the label never claims it. Same for the LLM chip: `127.0.0.1` on a phone is the *handset*, so it claims `🔒 local · <model>` **only** when the backend reports a local runtime on the reply itself. **§3.2's documented web bug was NOT ported** (the Whisper path bypassing `decideSubmit`) — push-to-talk's correct behaviour *is* submit-verbatim, so the phone gets there by the right road. **(3) The Inbox fold.** The web's Inbox is one tabbed page (`Inbox | Tasks | Comms`); the phone had two separate screens — one product, two shapes. `InboxScreen.tsx` is now a segmented shell: **Inbox → approvals, Tasks → the Task Log**. **The MOB-4 step-up path is untouched** — the old screen moved to `ApprovalsPane.tsx` **verbatim, byte-for-byte from the first import to EOF** (only the component name differs; diffed mechanically, not eyeballed), because a layout change must not reach into the gate that approves dangerous actions from a phone. **`tasks` is not orphaned:** still a web surface, still a destination, still in More — it opens the same screen on its Tasks segment. **Comms is left out on purpose** (`beyond` on the web, `planned` here) — and `inboxSegments.test.ts` derives the segments from *the web's tabs ∩ the phone's ready screens*, so **the day Comms ships, that test fails until the control grows a segment**; the fold can't silently drift back apart. **(4) Boot safety — four new native packages, so four new ways for #296/#297's white screen to return.** `react-native-svg`, `expo-av`, `expo-speech` and `expo-file-system` are **all** reached through `lazyNativeModule` at the point of use, `import type` only at module scope. A host without one loses that leg, not the app. **`bootSafety.test.ts` is the new standing guard**: it scans every source file and fails on any value-import of a native package, or a `require` outside the loader — **verified to actually bite**, because a guard that can't fail is worse than none. **(5) Two audit fixes folded in (#300, closed).** **Medium — the guard had its own blind spot:** it rooted at `src/`, so it never looked at **`App.tsx` or `index.ts`** — the boot path's *first two frames* (`index.ts` registers `App.tsx`, which imports `navigation.tsx`), and precisely the two files a bad value-import is most likely to be added to. A mutation planted in `App.tsx` **passed**. It now roots at the **package** (skipping `node_modules`/`dist`/`.expo`/`assets`/`android`/`ios`) and two assertions require both files to be in the scanned set, so re-rooting at `src/` fails loudly rather than letting the blind spot return; re-verified by planting the bug **in `App.tsx`** (red, named by file, green on revert). **Low — audio residue:** `expo-av` writes each recording to the cache and never cleans up, so every push-to-talk left an m4a of the operator's voice on the device. The backend already refuses to persist audio (AUDIO_RETENTION, PRD §7.8); the phone now holds the same line. `deleteClip()` runs in a **`finally` around the whole send** — the clip goes on success, on failure, **and on the "Reconnecting…" branch that returns before uploading at all** (cleanup only after a successful upload would leak on every transcription failure) — plus on unmount-mid-recording, which never uploads. Deletion never throws and nothing about it is logged (the URI names a file holding their voice). Uses SDK 54's `new File(uri).delete()` guarded by `exists`, **not** the legacy `deleteAsync(uri, {idempotent:true})`: on `expo-file-system`'s main entry the legacy methods are deprecated and now **throw at runtime**. **Follow-up noted, not fixed:** the ring-multiplier tripwire pins `RING_MULT` to literals and can only check that `--rk-mult` still appears in `globals.css` — the per-group values live inline in the web's JSX, which `node --test` can't load, so that one axis can drift silently (periods/keyframes/ripples *are* stylesheet-pinned). `npm test` **215/215** (+58) · typecheck clean · `expo export` bundles (4 MB, 1283 modules) · **react/react-dom still exactly 19.1.0**, overrides untouched, `reactVersion.test.ts` green · **SDK 54 untouched** (all four deps taken verbatim from `bundledNativeModules.json`, so they ship inside stock Expo Go) · additive: `apps/mobile/**` + docs only — backend/web/desktop untouched. `mob-7a-cc-parity-inbox-tasks`.

**Prior (2026-07-16) — 🛡️ EPIC H5/MOB · MOB-6f: GOVERNANCE · SETTINGS · CONNECTORS BUILT — THE LAST THREE OPERATOR MENUS ARE REAL SCREENS, ALL READ-ONLY.** Three placeholders became screens with **zero backend change**: **Governance** (`GET …/policies` + `…/agents` + `…/revisions`), **Settings** (`GET /api/orgs`), **Connectors** (`GET …/connectors`). The story was **re-scoped** — it absorbed **6h + 6k + the Settings half of 6j** because the three are one problem (each web panel is an *editor wrapped around a reading*; only the reading travels to a phone) — and it **displaced Overview to MOB-6l**, which is **not built**. **Every write is deferred, none dropped**, and each screen says so in a banner rather than showing dead controls: policy add/remove, permissions save, trust tier + boundary (owner-only), model profile, **rollback**; the Settings form and doc-ingest; **connect/OAuth**, test, disconnect, token rotation, Google toggles, gear sheets. Governance is read-only *on purpose*: it decides what an agent is allowed to do, and none of those controls has an undo. **The trap:** an empty permission list means **allow all**, not "allowed nothing" — so `[]` renders as `⚠ Allow all (unrestricted)`, never as a blank, because the obvious code would have told the operator a wide-open agent was locked down. **⚠️ Pre-existing leak found and reported (not fixed here — it's backend):** `GET /api/orgs` returns the whole `organisations` row, which carries a **`telegramBotToken`**; the phone has had that payload since MOB-1. Settings renders a fixed allow-list of three prose fields — never a spread of the org — and a test asserts the hazard column is real *and* excluded. **No secret value is rendered anywhere; no mutating call was added.** Tripwires import the **server** where they can (`web/lib/trust.ts`, `backend/src/services/connectors.ts`, `backend/src/db/schema.ts`); the JSX-bound constants that can't be imported are pinned as literals and named as copies. `npm test` **147/147** (+42) · typecheck clean · `expo export` bundles · **SDK 54 untouched** · additive: `apps/mobile/**` + docs only. **Still placeholders:** `overview`/`cockpit` (6l), task detail (6c), `comms`/`projects`/`skills`/`goals`/`workspaces`/`usage`/`plugins` (6i), `secrets` (6j).

**Prior (2026-07-16) — 🧠 EPIC H5/MOB · MOB-6e: MEMORY + ORG BUILT — THE TWO HEAVY WEB VIEWS ARE NATIVE TREES ON THE PHONE.** Both read-only, mirroring the web over the SAME endpoints — **zero backend change**. **Memory** (`MemoryScreen.tsx`): `GET …/memory/tree?path=` + `GET …/memory/file?path=` — a collapsible vault tree plus a note reader. **Org** (`OrgScreen.tsx`): `GET …/orgchart` — an indented reporting tree; tapping an agent opens the same detail the roster pushes, because the web's card opens the agent too. **The plan named both endpoints right and one shape wrong, which is the story's most useful output:** `…/memory/tree` is **not** a vault tree — it lists **one directory** (a GitHub Contents call per folder), and there is no whole-vault endpoint at any depth. So the tree **fetches per expand**. That's the better shape anyway — the phone never fetches the vault it isn't looking at — but it was a correction, not a plan. **Both canvases dropped; both hierarchies kept.** The web's ⬡ Graph is a `d3-force` simulation (at 390pt, an un-hit-testable hairball) and the org canvas is pan/zoom geometry answering *"where on a 2000px canvas does this card sit"* — a question a phone column never asks. Indentation says the same thing about depth. **Dropped, not deferred**, and each screen says so itself rather than leaving the operator hunting for a gesture that isn't coming. **Memory's tree is collapsible where the web's is a browser:** the web replaces the list when you click a folder; the phone splices children in beneath it so the rest of the vault stays put — same entries, same order, different traversal. **Tripwires:** `org.test.ts` imports the web's **real** `buildOrgTree` and diffs orphans, self-references, and a genuine `a → b → a` cycle — the one that most earns its keep, since a cycle reaching a hand-rolled walk is an **infinite loop**, and on a phone that's a hang with no console to explain it. `memory.test.ts` imports the **backend's real** `isMarkdownPath`/`isSafeVaultPath` and diffs `isNotePath` extension-for-extension, because that predicate decides what the tree offers as *tappable*. **⚠️ One web-copied constant has no import tripwire, and it's flagged in-code:** `RUNTIME_BADGE` lives in a JSX file that `node --test` cannot load. **Not hypothetical — this file's first draft guessed all five badges wrong and invented a key that doesn't exist**; a hand-check against the web caught it, and the code now says plainly that the check is a grep, not a test. **The markdown reader is a parser, not an HTML pipe** — the web escapes then `dangerouslySetInnerHTML`s; RN has no innerHTML, so the same subset parses to a block tree rendered as `<Text>`. Vault notes are **untrusted** (any agent with a vault token writes there): the web has to escape correctly, the phone has nowhere for a `<script>` to go. **Both trees are flattened, never nested** — one `FlatList`, `depth` as a number, so a 2,000-note vault scrolls like a 20-note one; Memory only walks expanded folders. **Defaults are opposite on purpose:** a vault opens closed (you want one note), a chart opens open (you want the shape). **A folder that fails to load keeps everything already fetched** rather than blanking the tree you're standing in, and "still fetching" never renders as "empty" — that's a lie that resolves. **Deferred by decision:** editing a note (`PUT …/memory/file` exists and **both** clients decline — the web has no editor either, so building one here would invent a surface the desk lacks), tappable `[[wikilinks]]`, opening links out of untrusted note content, and the vault picker / Import-Export company (desk writes with org-wide blast radius). **Absorbed MOB-6g** — Memory and Org are one problem. **One Low from the audit, fixed in-branch:** `MemoryScreen` called `loadDir()` from inside the `setExpanded` updater — React double-invokes updaters under StrictMode to surface exactly that, so opening a folder would have fired two GETs. Fixed, and generalised into a source-level guard that rejects side effects inside any functional state updater across `screens/*.tsx` — **proven by reintroducing the defect and watching it go red**. Verified: **105/105 tests** (was 62) · typecheck clean · `expo export --platform ios` bundles (3.6 MB, 1131 modules) · `npm install` clean, no ERESOLVE, **no dep added**, **SDK 54 + react pins untouched**. Additive: `apps/mobile/**` + docs + STATUS.md; backend/web/desktop untouched. `mob-6e-memory-org`.**

**Prior (2026-07-16) — 💰 EPIC H5/MOB · MOB-6d: COSTS · BUDGETS · ACTIVITY BUILT — DELIVERY'S COST PAIR AND THE EVENT FEED ARE REAL SCREENS. Three placeholders became screens, all **read-only**, mirroring the web over the SAME endpoints — **zero backend change**. **Costs** (`CostsScreen.tsx`): `GET /api/orgs/:orgId/tasks` + `…/agents` — the web's four stat cards (Total Spend · Total Tokens · Done · Agents) and its roster-ordered By Agent breakdown. **Budgets** (`BudgetsScreen.tsx`, the web's hosted tab under Costs): `GET …/budgets` — each policy with the server's own `spend`/`state`/`pct` verdict, so a breach means the same thing on both clients. **Activity** (`ActivityScreen.tsx`): `GET …/timeline`. **The plan's endpoint guesses were all wrong, and the correction is the story's most useful output:** `…/usage` is the *Usage & Limits* tab (rate limits — its own surface, still planned), `…/preflight` is a cockpit section with no nav id of its own (so the `budgets` blurb stopped promising it), and **`/api/orgs/:orgId/costs` exists but the web never calls it** — server-aggregated, windowed 7d/30d/90d, and effectively dead code. We **deliberately did not use it**: it's windowed and the web's sum isn't, so the same org would report a different total per device. **Mirroring the web's contract beat optimising the phone's transport** — that's the parity rule's whole point; if the windowed aggregate is the better product answer, the **web** changes first and the phone follows. **The phone is one notch more honest than the desk here, additively:** `/tasks` is capped at **200 rows server-side**, so "Total Spend" is a recent-window total on BOTH clients — the web says nothing, the phone prints the scope. Web-side follow-up logged, not fixed here (additive scope). **"Activity" is NOT an audit log:** the obvious source (`audit_logs`) is written by a **no-op plugin** — that feed would have shipped looking deliberate and rendered empty forever. The web's Activity is a 24h heartbeat swimlane, so the phone reads that and flattens it: keep the data, drop the projection (24h across ~340pt makes a 20-minute run four pixels wide). **`activity.test.ts` imports the backend's REAL timeline builder** rather than a fixture — and caught a live trap immediately: **a run block's cost comes from the RUN row, not the task it points at**, so a run can report `0` while its task cost real money; the screen omits a zero instead of printing `$0.00000`, because "not recorded here" ≠ "free". `costs.test.ts` pins the genuine 4dp-vs-5dp split between the two web views so the mirror can't be tidied into one and quietly change a number. **⚠️ The 6b audit's roster nit — fixed, and it was three bugs at once:** `AgentsScreen` hand-rolled its mapping, so a **`running` agent showed as idle on the roster but active on the detail screen** (the literal `=== 'active'` never collapsed the aliases — and `failed`/`stopped`/`terminated` all read as idle too: **the roster could not show a dead agent**), the glyphs were invented (● vs the table's ⬡ — not cosmetic when the glyph IS the colorblind signal), and a local `heartbeatTone` painted an active heartbeat **green**, undoing the rule that active is the accent *because* green/red is the pair the operator can't see. Both chips now route through `status.ts`. **This is the second time a correct, tested helper simply wasn't called**, so the fix is generalised into a sibling **source-level guard**: no screen may re-declare a canonical helper of its own — **verified by reintroducing the defect and watching it go red**. **Deferred by decision:** the per-agent bars (printed as a share number instead — same fact, legible, screen-readable, hue-free; and the web's 1% bar-width floor is deliberately NOT copied, since printing it would round a real 0.2% up to "1%"), the swimlane, period/groupBy + CSV export (the web has no period control either), budget create/delete (a hard-stop that can halt org spending is a desk decision), and a task detail from an Activity row → **MOB-6c**. Verified: **62/62 tests** (was 37) · typecheck clean · `expo export --platform ios` bundles (3.56 MB) · `npm install` clean, no ERESOLVE, **no dep added**, **SDK 54 + react pins untouched**. Additive: `apps/mobile/**` + docs + STATUS.md; backend/web/desktop untouched. `mob-6d-costs-activity`.**

**Prior (2026-07-16) — 📱 EPIC H5/MOB · MOB-6b: AGENT DETAIL + THE TASK LOG BUILT — THE ROSTER NO LONGER DEAD-ENDS. Tapping an agent on the phone did nothing; `tasks` was a placeholder. Both are now real screens, **read-only**, mirroring the web over the SAME endpoints — **zero backend change**, because the phone is a thin REST client to the same hosted backend and every call already served the web. **Agent detail** (pushed from a roster row): `GET /api/agents/:agentId` + `GET /api/orgs/:orgId/agents/:agentId/overview` — identity/status/heartbeat, key config (runtime · model · trust tier), latest run, recent tasks, task distributions, and the costs strip; the same two calls the web's `AgentDetail.tsx` + `DashboardTab.tsx` make, reading the same fields. **Tasks** (the `tasks` destination, under the Inbox grouping per P2's fold): `GET /api/orgs/:orgId/tasks` — the web's own log call — joined with `…/agents` for names/emoji and `…/approvals?status=pending` for the web's "approvals pending → Inbox" affordance (same wording, same threshold, jumping to the Inbox tab the phone already has). The web's 5-column table becomes one card per row on a 390pt screen: same five facts, layout a phone can hold. **Nav wiring:** a new `AgentDetail` stack route above the tabs (back button + iOS swipe-back for free); the detail is deliberately NOT a `navModel` surface (it isn't one on the web either — reached by drilling into the Agents area, not the rail), so **only `tasks` flipped `planned → ready`** and MOB-6c is rescoped to the task-detail drawer. **A third tripwire** pins the ported status table against the web's (`status.test.ts` imports both and compares canonicalisation + every glyph); the glyph is what the colorblind rule rests on, so it's the thing pinned — `statusColor()` returns CSS `var()` and can't cross into RN, so tone resolves against our palette. **Deferred by scope, not overlooked:** all write actions (Assign Task · Run Heartbeat · Pause/Resume) + the web's Instructions/Skills/Configuration/Runs/Budget tabs → **MOB-6b2**; task detail → **MOB-6c**; the four 14-day day-column charts dropped on purpose (unreadable at phone width; the label+count distributions survive and no data is lost). **⚠️ Independent audit: one High, fixed in-branch (#290 folded into #289) — the heartbeat chip rendered a HEALTHY agent as an absent one.** The detail screen passed the raw `heartbeatStatus` into the **task-status** table, where `green`/`amber` exist in neither the icon map nor the alias map — so both collapsed onto `idle`'s ○/neutral: green looked identical to "never checked in", and amber lost its warning (`stale` survived only by coincidence, being an alias key — which is why it read as working). The instructive part: `heartbeatStatus()` was **already written, exported, and tested** — the call site just didn't call it, so no amount of testing the helper would have caught it. Fixed with `heartbeatIcon()`/`heartbeatTone()` plus a **source-level guard** that fails if any screen hands a raw heartbeat to the status table, **proven by reintroducing the defect and watching it go red**. The glyph IS the colorblind signal, so this was that rule's own failure mode, not a cosmetic slip. Everything else PASSED (additive · read-only · endpoints mirror the web · MOB-4 untouched · the tripwire is real). Verified: **37/37 tests** · typecheck clean · `expo export --platform ios` bundles · `npm install` clean, no ERESOLVE, **no dep added**, **SDK 54 + react pins untouched**. Additive: `apps/mobile/**` + docs + STATUS.md. `mob-6b-agent-detail-tasks`.**

**Prior (2026-07-16) — ⚙️ CI-MOB-1: `apps/mobile` NOW RUNS IN CI — THE PARITY TRIPWIRES STOP FIRING INTO THE VOID. The standing web ⇄ mobile parity rule (root `CLAUDE.md`) is backed by tripwire tests that import the WEB modules and fail on drift — but **no workflow referenced `apps/mobile`**, so those tests only ever ran when a human remembered to. That is not a hypothetical: #286's nav change landed and `navModel.test.ts` sat **RED on `main`** until MOB-PAR-1 happened to look. The tripwire worked; nobody was watching it. **⚠️ Precisely scoped: drift is now VISIBLE (the check goes red on every PR), not yet BLOCKING** — `main` has no branch protection/rulesets (verified: protection → 404, rulesets → `[]`), so all checks here are advisory. Making it block is an operator-only Settings action — **GO-LIVE item 18** (and `npm audit` must NOT be required there: it's red on every PR by design). **One file, one new job, 48 insertions, 0 deletions.** `.github/workflows/ci.yml` gains `mobile`: `npm ci` → `npm run typecheck` → `npm test` (22/22, both parity tripwires) → `npm run export` (proves the app still bundles — pure Metro/Hermes, no Xcode, ~7s). Deliberate calls, each verified rather than assumed: **(1) `npm ci` bare** — the `check` matrix installs `npm install --legacy-peer-deps`, which would defeat this app's exact react/react-dom pins and swallow the ERESOLVE class of bug outright; bare `npm ci` off the committed lockfile resolves clean (react 19.1.8 dedupes, confirmed by a real clean install). **(2) ADDED, not replacing the legacy `app`** — the root guide freezes `app/` ("do not build new features here") but frozen ≠ dead, and no doc says its coverage is safe to drop; the matrix is still exactly `[backend, web, app]`. **(3) A standalone job, not a 4th matrix entry** — the matrix is wrong for this app in install command, typecheck command, AND has no test or build step; conditionals to bend it would have put backend/web/app at risk to save lines. **(4) No `web/` install needed** — the tripwires reach `../../../web/lib/navModel.ts` + `web/app/dashboard/assistant.logic.ts` by relative path, and both are pure data with zero imports of their own, so the checkout alone feeds them. `security.yml`/`test.yml`/`deploy.yml` untouched; the known-noisy non-blocking `npm audit` keeps its exact semantics. **Operator-authorized exception** to the root guide's `.github/workflows/` prohibition — this IS the change that makes the parity rule real. Additive: `.github/workflows/ci.yml` + `docs/DESIGN-mobile-parity.md` §6.3 + STATUS.md. **Zero application source touched.** `ci-add-apps-mobile`.**

**Prior (2026-07-16) — 📥 EPIC P / P2: TASKS FOLDED UNDER INBOX — THE TASK LOG IS CLICKABLE AGAIN. The operator asked to "fold the tasks under inbox and make sure I can access the tasks and approval dashboard from the dashboard when clicking on tasks". **The real defect: there was no "Tasks" to click.** P1 (#241) took `Issues` OFF the rail into `HIDDEN_ITEMS`, leaving the operator's own task log reachable **only** via ⌘K — a surface that renders a real Task Log, sitting behind a keyboard shortcut. P2 folds it back in as a **hosted tab of Inbox** (`Inbox | Tasks | Comms`), reusing P1's EXISTING `tabs` mechanism — no new IA pattern invented. **Tasks and approvals now read as one area**: the `Inbox` tab IS the approvals view (`CockpitPanel` section `inbox` → `InboxSection`, items + inline tri-state approvals), the Task Log sits beside it, and selecting Tasks keeps **Inbox lit** in the rail (`navSelectedId`) with the tab bar rendered — so approvals are one visible tab away, and the Task Log ALSO links across explicitly ("⏳ N approvals pending — review →", or "✓ No approvals pending — open Inbox →"), fed by the same `/api/orgs/:orgId/inbox` payload the Inbox tab renders from — **fetched LAZILY** (audit nit #3): `…/inbox` is a heavy query and the count renders only on the Task Log, so it is read on **entering that tab**, never on the default dashboard load — operators who never open Tasks never pay for it. Keyed on the tab, so landing on the Task Log **re-reads** it and the number isn't stale after deciding an approval on the Inbox tab (nit #2 — tab-enter, not a polling loop); a failure leaves the link reading zero, never breaks the page. The rail entry drops **"Inbox / Comms" → "Inbox"**: it was the ONLY parent enumerating a child (Costs/Connectors/Settings never did), and enumerating stops scaling at the second one — so this makes it consistent, not just shorter. **Governance is untouched** and still holds the full approvals config + low-trust review. **Full hyperlink audit (the explicit ask): all 29 surfaces resolve to a real renderer, 0 broken** — 14 `tab === id` renderers in `page.tsx`, 8 `CockpitPanel only=[key]` sections (every key valid against the `CockpitSectionKey` union), 7 honest `PlaceholderView` placeholders (`routines`·`review-queue`·`members`·`adapters`·`search`·`pipelines`·`artifacts` — intentional Epic-P gaps, NOT dead links); both hard-coded `selectTab()` targets (`skills`, `inbox`) resolve; external `<a href>`s are data-driven (connector `docsUrl` from the backend registry, `vaultHref`, workspace `previewUrl`) and unchanged. **Verified STATICALLY, not at runtime** — the checkout has no `.env`, so Clerk has no publishable key and `/dashboard` bounces to `/` before mounting; `next dev` compiles and serves it 200, but a click-through needs real Clerk keys + a live backend. Compensated with a machine-checked resolver over the real model + the real `page.tsx`/`CockpitPanel.tsx` source. Verified: **web 213/213, 0 fail** (211 → 213: three P2 nav tests added, one obsolete "Inbox / Comms" label assertion removed) · `tsc --noEmit` clean · `next build` clean. Additive: `web/lib/navModel.ts` + `web/app/dashboard/page.tsx` + nav tests + docs + STATUS.md. Backend, `apps/mobile`, and unrelated web untouched; reactor/Command Center styling, light-theme logo and document-attach preserved. `nav-fold-tasks-inbox`.**

**Prior (2026-07-16) — 📎 CC-ATT: ATTACH A DOCUMENT TO A COMMAND CENTER TURN — BUILT. The web/desktop composer gains a paperclip: pick any readable document (pdf, docx, pptx, xlsx, odt/odp/ods, txt, md, csv, tsv, json, log — ≤10 MB), see it as a removable chip (name · size), and send it with your message. Its text is extracted by the EXISTING officeparser path (`services/document-ingest.ts` → `extractText`, the same one knowledge ingest uses — no new parser), and fenced into that turn's prompt (with an unforgeable per-turn nonce marker) so the model actually uses it — and can't be talked out of it by the document. Never stored, never embedded, never logged; long documents are clipped to 40k chars and you're told. Same Clerk + `requireOrgMembership` gates as `/converse` (the route lives in the same secured scope). `apps/mobile` untouched — the mirror is a later story. Additive: `web/**` + `backend/**` + docs + STATUS.md. `cc-attach-documents`.**

**Prior (2026-07-16) — 🎙️ EPIC H5/MOB · MOB-5a: HOSTED STT BUILT — THE PHONE'S AUDIO FINALLY HAS SOMEWHERE TO GO. `POST /api/orgs/:orgId/arturita/transcribe` (multipart field `file` → `{ transcript, text }`) is the leg `DESIGN-mobile-expo.md` §6 always ASSUMED existed and never did: until now NO backend route accepted audio at all, and the only transcribers were the browser's Web Speech API (which React Native doesn't have) and a localhost-bound, unauthenticated daemon on the operator's Mac. This unblocks MOB-5c (push-to-talk). Server-side ONLY — no phone/web/desktop code touched. ⚠️ **It answers a clean 503 until the operator supplies `OPENAI_API_KEY` on Fly** (see below). Additive: `backend/**` + docs + STATUS.md. `mob-5a-stt-endpoint`.**

- **One adapter, two legs — because the local daemon was already OpenAI-shaped.** `adapters/arturita-stt` deliberately exposes an OpenAI-compatible `/v1/audio/transcriptions`, which is byte-for-byte what hosted Whisper takes. So `services/stt-provider.ts` is ONE network adapter that differs only by base URL and whether a key is attached — not two implementations. A third OpenAI-compatible engine (Groq, self-hosted whisper.cpp) is a config change, not code.
- **Which transcriber runs where.** `MC_STT_PROVIDER` = `auto` (default) · `cloud` · `local` · `off`. **On Fly only the cloud leg can EVER work** — `127.0.0.1:8790` there is the Fly VM, not the operator's Mac — so `auto` picks cloud when a key exists, else the local daemon when `MC_STT_LOCAL_URL` is set, else a clean **503 `not_configured`**. A **pinned** `cloud`/`local` never silently falls back to the other: quietly shipping audio to a cloud the operator pinned *away* from is a privacy downgrade, so it fails instead.
- **⚠️ OPERATOR ACTION — nothing transcribes on Fly without a key.** Needs **`OPENAI_API_KEY`** (Fly secret, or per-org via Cockpit → Secrets, which takes precedence — same order as `llm-router.ts:175`). **No new key type is invented**: it's the key `backend/CLAUDE.md` already lists as optional and that embeddings (`vector-search.ts:25`) + the OpenAI LLM leg already use. Whether it is actually SET on `7ei-backend` was **not verifiable from this session** (no `fly` CLI) — check `fly secrets list -a 7ei-backend`. Endpoint, auth and guard rails are live and correct regardless; only transcription waits.
- **Contract mirrors the daemon on purpose, so the web can repoint by URL alone.** Field **`file`** and a **`text`** key in the response are exactly what `web/lib/whisper.ts` already sends (`:81`) and reads (`:37`) — `transcript` is the canonical key, `text` the same value. That reuse is what the MOB-5a plan row asked for. Accepts m4a/x-m4a/mp4/aac (**what `expo-av` produces on iOS**), wav, webm, ogg, mp3, 3gpp, amr — `application/octet-stream` is REJECTED, so the phone client must send a real audio type.
- **Auth + multi-tenancy are the existing rails, not new ones.** Registered on the `secured` scope → **Clerk on hosted / loopback on packaged** (H6), 401 without a bearer. `:orgId` comes from the **path**, so the scope-wide `requireOrgMembership` preHandler gates it → 403 for a foreign org; no body/query field can name an org. Both the MCA-85 leak guard and `boot.test.ts` now mirror the new route, so it can't silently drift public.
- **Guard rails.** **10 MB** per clip (`MC_STT_MAX_BYTES`) — a per-route clamp deliberately *tighter* than the 25 MB global multipart limit that exists for **document** uploads · **60 s** timeout → 504 · 415 for non-audio/non-multipart · 413 oversize · 502 provider error · every failure is clean JSON, **never a stack**, and the upstream error body is dropped rather than echoed. **Duration is bounded indirectly** (bytes + timeout), not decoded — reading true m4a duration means MP4-atom parsing or `ffprobe`, a real dep for a guard the byte cap already gives; flagged in the doc so it isn't mistaken for an oversight.
- **Audio and transcripts never reach a log sink.** The transcript is user content: only its *length* is logged. Audio is held in memory for the provider call and never persisted (AUDIO_RETENTION, PRD §7.8). The key is a parameter throughout, never module state, never logged — and never sent to the keyless localhost daemon.
- **Transcribes only — the dangerous surface stays where the gate is.** It creates no task and runs nothing; the caller posts the transcript on to `…/arturita/voice`, which already gates confidence and routes destructive intents to the A2 approval path.
- **Two real bugs caught by verifying instead of assuming.** (1) The daemon matches `req.url === '/v1/audio/transcriptions'` **exactly**, so my first `LOCAL_STT_URL` (missing `/v1`) would have 404'd **every** local transcription — now fixed and locked by a test asserting both defaults resolve to the exact paths their providers serve. (2) The backend compiles with `strict: false`, so TS can't narrow a `{ok:true}|{ok:false}` union — the result type is flat, matching `voice-provider.ts:TtsResult`, which is flat for the same reason.
- **Verified.** `npm test` **1324/1324, 0 fail** · `npm run evals` **11/11** · `tsc --noEmit` clean. Beyond the mocked unit tests, an **end-to-end run against a real local HTTP server with the real `fetch`** proved the multipart body actually serializes on the wire: upstream received `/v1/audio/transcriptions`, the Bearer key, field `file`, filename `clip.m4a`, the real audio bytes and `model=whisper-1`; the route returned `{transcript,text,provider,bytes}`.
- **Still ahead:** MOB-5b (`expo-speech` TTS) · **MOB-5c** (push-to-talk — records and POSTs here; now unblocked) · MOB-5d · MOB-5e (wake word, dev build). This story is **stage→audit** — an independent auditor runs next; I did not self-audit or merge.

**Prior (2026-07-16) — 📱 EPIC H5/MOB · MOB-6a: NAV SHELL BUILT — `apps/mobile/` NOW REACHES ALL 30 MISSION CONTROL DESTINATIONS. The 4-tab hand-rolled bar couldn't hold ~25 more sections. Now: a 5-slot tab bar (Command · Inbox · Agents · Status · More) + a pushed section stack, driven by ONE ported data module. The 4 shipped screens are untouched; the other 25 destinations land on placeholders that state honestly why they're empty. Navigation is complete and non-crashing end-to-end today — stages 6b+ swap placeholders for real screens. Strictly additive (`apps/mobile/**` + docs + STATUS.md); backend/web/desktop/CI untouched. `mob-6a-nav-shell`.**

- **The pattern: tabs + a More list, not a drawer.** A drawer would cost `react-native-reanimated` + `react-native-gesture-handler` and *hide* the sections we're trying to surface. A pushed list is one tap in, one swipe back, scrolls to any length, and reads correctly under VoiceOver for free. The tab bar stays at 5 because iOS collapses a 6th into its own (worse) "More" — and the model, not the navigator, decides membership (`primary` in `navModel.ts`).
- **`src/navModel.ts` — the port, and the only surface list in the app.** Ids, labels, and group order (Overview · Workspace · Operate · Delivery · Company · General) come from `web/lib/navModel.ts`, so one surface has one name in both clients. The web's rail/hosted-tab/hidden split is *flattened* (a phone has no rail to fold) but recorded in `webHosted`/`webHidden` so the mapping stays auditable. Adding a screen later = flip `status:'ready'` + one line in `SCREENS` (`src/navigation.tsx`); nothing else enumerates surfaces.
- **Placeholders that don't lie.** Status is 3-valued because "no screen" has two different causes: **`planned`** (19 → the web has it, the phone's Clerk JWT already reaches the data, and a named `MOB-6x` builds it) vs **`gap`** (7 → unbuilt on the web too — the Epic-P placeholders; there's nothing to port and waiting won't help). A flat "coming soon" would blur exactly that distinction.
- **Reachable now — 30 destinations:** 4 real (Command Center · Inbox+step-up · Agents · Status) + 26 placeholder (Dashboard, Operations, Comms, Activity, Projects, Org, Issues, Routines, Governance, Review Queue, Costs, Budgets, Skills, Memory, Goals, Workspaces, Artifacts, Pipelines, Connectors, Plugins, Members, Usage, Settings, Secrets, Adapters, Search). **The 4 shipped screen files are byte-for-byte unchanged.**
- **Drift is tested, not hoped.** `src/navModel.test.ts` imports the mobile AND web models and asserts: every web surface has a mobile destination · the phone invents no id · labels match · `gap` ≡ the web's `kind:'placeholder'` · group order mirrors · tab bar ∪ More covers everything. Zero-dep `node --test --experimental-strip-types` (the repo's convention — no jest/vitest anywhere), **12/12**. CI doesn't run `apps/mobile`, so this is a local/audit gate.
- **Doc-arithmetic correction.** `DESIGN-mobile-parity.md` §1 claimed **30** web surfaces (18 rail + 6 hosted + 6 hidden). The model returns **29**: hosted tabs are **5** (`comms`, `budgets`, `plugins`, `adapters`, `secrets`), not 6. Corrected there; the "~12 real screens to port" conclusion is unaffected.
- **SDK 54 pin holds.** `expo install` picked `react-native-screens ~4.16.0` + `react-native-safe-area-context ~5.6.0` (both **bundled in Expo Go**) and `@react-navigation/{native@7,bottom-tabs@7,native-stack@7}` (pure JS). `react`/`react-dom` stayed **19.1.8** — `expo.install.exclude` respected, Clerk still deduped onto one React. Verified: `npm ci` clean (no `ERESOLVE`) · `tsc --noEmit` clean · `npm test` 12/12 · `expo export --platform ios` → **1114 modules → 3.5 MB Hermes** · `expo 54.0.36`.
- **Two regressions caught while wiring, worth knowing.** (1) `ConnectScreen` had no `SafeAreaView` of its own — it relied on `App.tsx`'s, which the navigator replaced; the pre-nav gate keeps one, or Connect renders under the notch. (2) A **cold start from a notification tap** resolves *before* `NavigationContainer` mounts, so routing straight at the ref would silently drop precisely the deep link that matters most — the target is buffered and flushed in `onReady`.
- **Deferred (flagged, not done).** Push routing still speaks MOB-3's own vocabulary (`'command'`) while the model uses the web's id (`'assistant'`); translated at one seam in `navigation.tsx` rather than renaming MOB-3's file or diverging from the web. No deep-link URL scheme / `linking` config yet (push taps route in-process; sections aren't URL-addressable). Nav state isn't persisted across reloads — same as the web, which loses its tab on reload.

**Prior (2026-07-16) — 📱 EPIC H5/MOB · MOB-5: EXPO GO SDK CEILING FIXED — `apps/mobile/` PINNED TO EXPO SDK 54. The operator's stock App Store Expo Go refused the app ("requires a newer version of Expo Go") because App Store Expo Go is frozen at 54.0.2 (2025-09-23) and only opens the SDK it ships — while the project targeted SDK 57. Downgraded to SDK 54 with ZERO source changes; all MOB-1…MOB-4 functionality intact. Operator just re-scans the QR (no reinstall, no dev build). Strictly additive (`apps/mobile/**` + docs + STATUS.md). `mob-fix-expo-go-sdk`.**

- **Root cause (counter-intuitive).** `npm view expo dist-tags` → `latest: 57.0.6`: SDK 57 *is* stable. The ceiling is the **client**. `curl "https://itunes.apple.com/lookup?id=982107779"` → **`54.0.2`**, identical across us/gb/es/de/jp. Expo's [changelog](https://expo.dev/changelog/expo-go-and-app-store-may-2026) confirms SDK 55's Expo Go is stuck in Apple review and 54 "remains the latest available through the App Store". **Trap:** Expo's own versions API advertises `iosClientVersion: 57.0.5` for SDK 57 — it's an internal mapping, not App Store truth (it claims 54.0.7 while the store ships 54.0.2). Only the iTunes lookup is ground truth.
- **The React pin.** SDK 54 nominally pins react `19.1.0`, but `@clerk/clerk-js@5.127.1` floors it at `~19.1.4` — at 19.1.0 npm nests a **second React** under `@clerk/clerk-expo` (a real latent hazard). `react-native@0.81.5` peers `^19.1.0`, so **19.1.8** satisfies both and dedupes to one React. Supersedes MOB-2's `react-dom@19.2.3` fix (§13.1 → §16.4).
- **Flagged, not fixed.** `android.package` = `ai.7ei.missioncontrol.mobile` fails Expo's config schema (the `7ei` component starts with a digit). **Pre-existing, Android-only** (iOS `bundleIdentifier` is fine), and renaming it is **app identity** — an operator call, not a dependency-realignment side effect. Blocks a future Play build only. Details: `docs/DESIGN-mobile-expo.md` §16.6.
- **Verified.** `npm install` clean (1041 pkgs, no `ERESOLVE`, **0** `invalid:` peers) · `tsc --noEmit` clean **with no source changes** · `expo export --platform ios` → **852 modules → 3 MB Hermes** · `expo config` → **`sdkVersion: 54.0.0`** (matches App Store Expo Go 54.0.2) · `expo-doctor` **17/18** (sole failure pre-existing, above) · additive (own npm root; root has no `workspaces`, so backend/web/desktop/CI untouched).
- **Full write-up:** `docs/DESIGN-mobile-expo.md` **§16**; operator-facing: `apps/mobile/README.md` § *Expo Go and the SDK pin* (incl. two one-liners to re-check the ceiling if it ever lifts).

**Prior (2026-07-16) — 📱 EPIC H5/MOB · MOB-4: ON-DEVICE STEP-UP FOR DANGEROUS APPROVALS BUILT. The phone can now APPROVE a dangerous action, closing the last remote-control gap (reject/request-changes were always one-tap; MOB-2/L1 had disabled dangerous Approve because a bare approve 403s). It conforms to the EXISTING backend step-up contract — no backend change. Strictly additive (`apps/mobile/**` + docs + STATUS.md); backend/web/desktop/CI untouched. `mob-4-stepup`.**

- **Same contract the backend already enforces (unchanged).** `POST /api/approvals/:id/decide` requires a fresh Arturita command-session token in the **`x-arturita-session`** header for a dangerous approve, else **403** (`routes/tasks.ts`). The token is minted at **`POST /api/orgs/:orgId/arturita/session`** (Clerk-secured; returns the plaintext once, stores only its SHA-256 hash), **valid 30 min / step-up-fresh 5 min** (`services/arturita-session.ts`). The phone now implements exactly this — it is in fact the **first client to mint step-up** (the web Cockpit still posts a bare decide; flagged, §15.1/§15.5).
- **On-device gate before minting.** Tapping Approve on a dangerous card opens `StepUpModal` showing the type + the backend's **machine-rendered** summary + every warning. The operator must pass a local gate first: **biometric** (Face ID / Touch ID / device passcode) via `expo-local-authentication` — a first-party module **bundled in Expo Go** (`expo/bundledNativeModules.json` → `~57.0.1`), so it runs today without a dev build — or, when no biometric hardware is enrolled (simulator / Face ID off), an explicit **type-`APPROVE`** fallback (the gate never silently downgrades; a dev build hardens it). Both isolated behind `try/catch` (fail-closed to typed).
- **Fresh session per approval, then discard.** On gate pass: mint a fresh session (`source:'desk'` — the phone is a first-party Clerk client like the web desk), attach it to the single decide call's header, discard it. **Never cached or reused** across approvals (respects the 5-min freshness + single-operator intent). The token is held only in a local `const`, **never logged, never in a URL**. A 403/expired surfaces as "confirm again" and Retry re-runs the gate **and re-mints** — no dead-end.
- **New/changed (all `apps/mobile/**`):** `src/stepup.ts` (pure danger-summary + biometric probe/gate wrappers), `src/screens/StepUpModal.tsx` (the gate→mint→decide modal), `src/api.ts` (`mintArturitaSession` + optional `sessionToken` header on `decideApproval`), `src/screens/InboxScreen.tsx` (dangerous Approve re-enabled, routed through the modal), `package.json`/`package-lock.json` (`expo-local-authentication@~57.0.1`), `app.json` (Face ID config plugin for the eventual dev build).
- **Verified.** `npm install` clean (766 pkgs, `expo-local-authentication@57.0.1` = the Expo-Go-bundled version, no `ERESOLVE`) · `tsc --noEmit` clean · `expo export --platform ios` bundles (**801 modules → Hermes**) · live hosted backend: `/api/health` 200 `db:connected`; `POST …/arturita/session` and `POST /api/approvals/:id/decide` both **401 without auth** (the Clerk layer that fronts the 403 step-up gate), incl. with a bogus `x-arturita-session` attached; header never appears in a URL (grep) · additive (own npm root, not in web/backend/desktop builds/CI). **Honest boundary:** the authenticated **403→200** path needs an operator **Clerk owner** token + a pending dangerous approval (builder doesn't hold it); it's contract-locked by `backend/src/tests/dangerous-approvals.test.ts` + the route wiring, and §15.4 has the exact operator curl recipe.
- **Flagged / deferred:** (1) the **web** Cockpit still has no client-side step-up — a dangerous approve from web would 403 today; non-blocking, reuse this contract in a follow-up. (2) `source` enum is `{desk, telegram}` — we mint as `desk` (cosmetic label, no security effect); a `mobile` value is a nicety, **not** a required backend change (we did not touch the backend). (3) Full green-path 200 = the operator step above.
- **Audit nits folded (PASS-WITH-NITS → merge).** **L1:** step-up routing now mirrors the backend gate exactly — `approvalNeedsStepUp()` = dangerous type **OR** `payload.requiresStepUp === true`, so a `low_trust_review` wrapping a dangerous action routes through the modal instead of one-tapping into a 403. **L2:** tapping Approve on a step-up item while `orgId` is momentarily null surfaces "Reconnecting — try again in a moment" instead of a silent no-op. Both mobile-only; typecheck + 801-module export re-verified.
- **BUILDER only** — did not self-audit or merge; an independent auditor reviewed (PASS-WITH-NITS, folded above). **stage→audit** (approving dangerous actions remotely).

**Prior (2026-07-16) — 📱 EPIC H5/MOB · MOB-3B: BACKEND PUSH HARDENING + APPROVAL EMIT BUILT. The three backend follow-ups MOB-3 flagged now ship: (1) an **identity-trust fix** (audit L1) so a device registers only under the authenticated session, (2) **persisted push tokens** (a `push_tokens` table that survives Fly restarts), and (3) an **approval-created push** so the operator's phone pings the moment an agent needs approval. Additive + reversible migration; multi-tenant isolation held; no existing auth/RBAC gate weakened. `mob-3b-backend-push`.**

- **(1) Identity trust — audit L1 closed.** `POST/DELETE /api/notifications/register` now derive the target user from the **authenticated session** (`req.auth.userId` — Clerk `sub` on hosted, loopback operator on packaged), never the request body. A body `userId` is accepted only if it **equals** the session (`403` on mismatch — the exploit where any authed user registered a device under another user's id and stole their pushes); unregister is scoped to the caller's own identity. Token never logged. (`routes/notifications.ts`)
- **(2) Push tokens persisted.** New `push_tokens` table (`id, user_id, token UNIQUE, platform, created_at, updated_at`) — additive/idempotent/reversible migration in `db/setup.ts` (`CREATE TABLE IF NOT EXISTS` + unique token index + user index; nothing backfilled — the old store was an in-memory `Map`). `services/push.ts` is now table-backed: register **upserts** (dedupe by device token; re-login re-points `user_id`), unregister deletes scoped to the caller, and `sendPushNotification` (and thus every sender — agent-executor, scheduler) reads tokens from the table. Delivery now survives a restart.
- **(3) Approval-created push — the remote payoff.** `notifyApprovalCreated()` fires fire-and-forget at **every** approval-insert site (operator `POST /approvals`, low-trust quarantine in tasks + orchestrator, agent-facing memory.write ×2 + `POST /api/agent/approvals`, agent-join card). It resolves `org.ownerId`, **reuses** the existing `sendPushNotification` → Expo plumbing (not duplicated), names the action, marks **step-up** for dangerous types (`file_destructive`/`wallet_tx`/`email_send`/`machine_exec`), and carries the approval id in `data.approvalId` so the phone's approval→Inbox deep-link works. Guarded so a push failure can never throw into the request path. *(Agent-stale left out — not adjacent to a single insert site; flagged.)*
- **Multi-tenant safety.** A token belongs to a user; the send path only ever looks up the **specific** owner user id resolved from the approval's `orgId` — one org can never target another's phone. No existing gate touched; the notifications route stays in the same `secured` scope.
- **Verified.** `tsc --noEmit` clean · **full suite 1295/1295 pass** (was 1280; **+15 new** in `src/tests/mob-3b-push.test.ts` covering the upsert/dedupe/persistence, the L1 403 + cross-user leak guard, and the approval emit incl. step-up + never-throws). `main` stays green. Evals not run locally (need live LLM keys) — CI/auditor covers them.
- **BUILDER only** — did not self-audit or merge; an independent auditor runs next. **stage→audit** (secured route + DB change).

**Prior (2026-07-16) — 📱 EPIC H5/MOB · MOB-3: PUSH-NOTIFICATION CLIENT BUILT (Expo Go), remote delivery staged behind a dev build. `apps/mobile/` now requests notification permission, wires a foreground handler, fires local test notifications, deep-links a notification tap to the right tab, and registers its Expo push token with the EXISTING backend endpoint — with ZERO backend change. Strictly additive (`apps/mobile/**` + docs + STATUS.md); backend/web/desktop/CI untouched.**

- **Wired to the existing endpoint (no new backend surface).** `POST /api/notifications/register` (unregister: `DELETE`) already existed; it's in the `secured` scope so it **requires `Authorization: Bearer`** (verified live: `401 {"error":"Unauthorized"}` without one). Identity for storage comes from the body `userId`, and the backend's senders target **`org.ownerId`** — so the phone registers under the **signed-in user's Clerk id** (`sub`), which equals `org.ownerId` for the operator. The client sends bearer **and** body `{userId, token}`. The Expo-push send path (`services/push.ts` → `exp.host`) already exists and already fires on task-complete / routine / budget.
- **What runs in Expo Go now:** the notification handler (foreground banner+sound), permission request, a **local "Send a test" notification** (proves handler + tap-routing wiring), tap→**deep-link** (approval → Inbox), and the backend registration call. **Gated behind a dev build:** minting a *remote* Expo push token (`getExpoPushTokenAsync` — guarded to **no-op gracefully** when no projectId, never throws) and thus a real push *arriving* on the phone.
- **Config-only switch, no code change.** The code reads the Expo project id from **`EXPO_PUBLIC_EAS_PROJECT_ID`** (`src/config.ts`). Absent (Expo Go) → local-only, Status panel shows "Dev build required". Present (after `eas init` + an EAS dev build) → remote token minted + registered and real pushes arrive. §14.5 has the exact operator steps.
- **Token hygiene + lifecycle.** The Expo push token is **never logged** (UI shows last-4 only). Registered on sign-in / permission-grant; **de-registered on sign-out** (the Sign-out button calls `deregister()` *before* clearing the session, while the bearer is still valid — a post-sign-out DELETE would 401); a changed token unregisters the old one first.
- **New/changed (all `apps/mobile/**`):** `src/notifications.tsx` (handler + `PushProvider`/`usePush` + tap-routing), `src/config.ts` (`EAS_PROJECT_ID`), `src/api.ts` (`registerPush`/`unregisterPush`), `src/auth.tsx` (`userId` seam — Clerk `useUser().id`, paste-mode JWT `sub` decode), `App.tsx` (wrap shell in `PushProvider` + wire tap routing), `src/screens/HealthScreen.tsx` (Notifications panel), `app.json` (`expo-notifications` plugin, inert in Expo Go), README.
- **Verified.** `npm install` clean (765 pkgs; `expo-notifications@~57.0.5` via `expo install`, no `ERESOLVE`) · `tsc --noEmit` clean · `expo export --platform ios` bundles (**796 modules → 2.8 MB Hermes**) · live `POST/DELETE /api/notifications/register` → `401` without a bearer (auth gate + exact path/method/body confirmed) · additive (`apps/mobile` own npm root, not in web/backend/desktop builds or CI). **Honest boundary:** a real remote push *landing on the phone* wasn't exercised — it needs an Expo project id + EAS dev build (operator step); everything up to the backend register call runs in Expo Go.
- **Flagged for the operator (separate BACKEND stories, not built here):** emit an Expo push on **`approval_requests` creation** (+ agent-stale) — the at-a-distance payoff; the send plumbing already exists, it's a fire-and-forget call at that transition. And **persist push tokens** (today an in-memory `Map`, clears on Fly restart; the client self-heals by re-registering, but a `push_tokens` table would make delivery durable). Both noted in §14.6.
- **Still ahead:** MOB-4 on-device step-up for dangerous approvals · MOB-5 voice (dev build). This story is **stage→audit** (remote-notification surface + device-token storage) — an independent auditor runs next; I did not self-audit or merge.

**Prior (2026-07-16) — 📱 EPIC H5/MOB · MOB-2: REAL CLERK SIGN-IN BUILT. `apps/mobile/` now signs into the SAME Clerk instance the web app uses; token-paste stays as the escape hatch so the app always boots. Strictly additive (`apps/mobile/**` + docs only); backend/web/desktop/CI untouched.**

- **Clerk-Expo, primary auth.** `@clerk/clerk-expo@2.19.42` + `<ClerkProvider>` with an `expo-secure-store` (iOS Keychain) `tokenCache`; `getToken()` is now async and mints a **fresh auto-refreshing session JWT** per call — no more 401-on-expiry. The bearer is **never logged**. Sign-in is native (email+password / "email me a code", `useSignIn`) — no browser, runs in Expo Go. The backend's `clerkAuth` + org-membership gates apply unchanged.
- **The SDK-57 peer conflict, solved without a dev build.** It wasn't clerk-expo's own peers (those accept SDK 57); it was transitive — `@clerk/clerk-react` floats `react-dom` to `19.2.7` (peer `react ^19.2.7`), clashing with Expo's pinned `react@19.2.3`. **Fix: pin `react-dom@19.2.3` (exact).** Plain `npm install` is then clean (no `--legacy-peer-deps`), and `expo export` bundles **736 modules → Hermes**, proving it runs in Expo Go. Details in `docs/DESIGN-mobile-expo.md` §13.
- **Token-paste preserved (no MOB-1 regression).** No Clerk key → the app shows the paste screen exactly as before; with a key, "Use a token instead" is still available behind the Clerk form. `authMode` (`clerk`/`paste`) + selected org persist in the Keychain.
- **Audit nits folded in.** **L1** — dangerous approvals (`file_destructive`/`wallet_tx`/`email_send`/`machine_exec`) now **disable the Approve button** with an honest "needs step-up (MOB-4)" note instead of a dead-end tap that always 403s; Reject/request-changes still always work. **L2** — the dangerous-type list is now a single shared constant (`src/constants.ts`), no hardcoded mirror.
- **Verified.** `npm install` clean (762 pkgs, no `ERESOLVE`/peer errors) · `tsc --noEmit` clean · `expo export --platform ios` bundles with Clerk enabled (736 modules) · live hosted backend, native no-`Origin` fetch: `/api/health` 200 `db:connected`, `/api/orgs` **401 without a token and 401 with a bogus bearer** (bearer is validated), no-`Origin` request passes (CORS is a non-issue for native fetch). Green-path 200 needs a live key + real creds (operator step); it's proven-equivalent to MOB-1's verified pasted-token 200 since the bearer is attached identically.
- **Still ahead:** MOB-3 push · MOB-4 on-device step-up for dangerous approvals · MOB-5 voice (dev build). This story is **stage→audit** (auth surface) — an independent auditor runs next.

**Prior (2026-07-16) — 📱 EPIC H5/MOB · MOBILE REMOTE PHASE-1 (MOB-1) BUILT. A runnable Expo (React Native) iPhone app at `apps/mobile/` that boots in Expo Go and remotely controls Mission Control through the HOSTED backend — the phone is a thin peer of the web app, not a client of the Mac mini.**

The phone reads/controls via the **same hosted REST API the web dashboard uses** (`7ei-backend.fly.dev`); the operator's Mac-mini/desktop agents keep reporting to the hosted backend exactly as before. **Additive** — everything is new files under `apps/mobile/` + one design doc; **no backend/web/desktop/auth code changed**, and `apps/mobile` is its own npm root so it never enters the `web`/`backend`/`apps/desktop` build or CI matrix.

- **What it does (P0 remote control).** **Command Center** — text chat to Arturita (`POST …/arturita/converse`), rendering the reply + a **"via" chip** (which provider/model answered, or that it was delegated to a task). **Inbox/Approvals** (the killer remote feature) — lists pending dangerous-action approvals and **approve / reject / request-changes** via `POST /api/approvals/:id/decide`. **Agents** — the roster with status + heartbeat. **Status** — live `/api/health` + session org.
- **Auth (phase-1 = token-paste; Clerk-Expo staged).** The operator pastes a Clerk session JWT + API URL; the app validates via `GET /api/orgs`, stores the bearer in the **iOS Keychain** (`expo-secure-store`), and scopes every call to the chosen org. **Clerk-Expo (same Clerk org, auto-refresh) is the recommended production auth — designed + staged as MOB-2**; the `src/auth.tsx` seam isolates token acquisition so screens (which use only `getToken()`+`orgId`) don't change when Clerk lands. Token-paste is the guaranteed-bootable fallback (`@clerk/clerk-expo` doesn't yet install cleanly against the current Expo Go SDK 57).
- **CORS is a non-issue** — native RN `fetch` sends no `Origin`, so the backend allow-list never gates the phone; no backend CORS change. **Push already has a register endpoint** backend-side — Expo push is a small additive **flagged** next story (MOB-3), not built this wave.
- **Verified.** `npm install` clean (no peer conflicts) · `tsc --noEmit` clean · `expo export --platform ios` bundles (592 modules → 1.5 MB Hermes) so it **loads in Expo Go** · live hosted backend probed (`/api/health` `{status:ok, db:connected}`; `/api/orgs` 401 without token).
- **Run:** `cd apps/mobile && npm install && npx expo start` → Expo Go (App Store) → scan the QR (same Wi-Fi, or `npx expo start --tunnel`). Env: `EXPO_PUBLIC_API_URL` (defaults hosted), `EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY` (once MOB-2 lands).
- **Where to look:** `apps/mobile/` (`App.tsx`, `src/{config,api,auth,store,theme,ui}.ts(x)`, `src/screens/*`, `README.md`), `docs/DESIGN-mobile-expo.md` (full plan — architecture, Clerk-Expo auth, prioritized features, push, Expo-Go-vs-dev-build, voice, the phased H5/MOB story plan, open questions). Colorblind-safe throughout (status = label + glyph, never hue).

**Additive — backend/web/desktop untouched. No test/build path changed (own npm root; CI matrix is explicit `[backend, web, app]`). Full plan + phased story roadmap (MOB-1…MOB-7) in `docs/DESIGN-mobile-expo.md`.**

**Prior (2026-07-15) — 🛰️ EPIC J · J7 COMMAND CENTER REACTOR SHIPPED. A Jarvis-style brushed-silver arc-reactor (counter-rotating metal rings + a translucent glowing/swirling core + the 7Ei honeycomb in glassmorphism) is now the PRINCIPAL Command Center view. Voice-reactive across idle/listening/thinking/speaking (idle "deep breath cycle" · listening ripples · thinking spins fastest · speaking pulses), colorblind-safe (state = icon + label + motion, never colour; metal is state-independent), fully disabled under `prefers-reduced-motion` (state still reads from the caption), metallic/core tokens for both themes. The voice pipeline (push-to-talk, transcript, ⚙ Pipeline config, delegate) is byte-unchanged behind a toggle that auto-opens on the first turn. Web-only — no backend/adapter/auth touched. Web build green · 202 web tests (11 new J7 reactor-logic tests).**

**Prior (2026-07-15) — 🔐 EPIC H · H6 LOOPBACK AUTH BUILT. The packaged profile now enforces a real single-operator local identity — Clerk swapped for a loopback session on `127.0.0.1`, per-install Keychain keys, fail-closed-on-default-key boot — gating the SAME write routes. Hosted Clerk path byte-identical. STOPPED for the independent H6 audit.**

H6 closes the H0/H1 **temporary auth bypass** and makes the packaged app both **usable** (a reachable authenticated dashboard) and **secure** (an authenticated local operator, not an open instance). It is **additive + profile-branched** — hosted is unchanged — and satisfies the four AUDIT-H1 requirements. **stage→audit: STOPPED HERE.**

- **Dual auth, profile-selected.** `backend/src/index.ts` chooses the secured-scope `onRequest` hook by profile: **hosted → `clerkAuth`** (unchanged), **packaged → `loopbackAuth`** (`middleware/loopback-auth.ts`). Both attach the *same* `req.auth.userId`, so `requireOrgMembership`, `requireOrgRole('owner')`, the audit/telemetry hooks, and every handler run **unchanged** — H6 swaps the identity source, keeps the gate.
- **The loopback identity (H-Q6: OS-user-bound session).** The Electron shell generates `MC_LOOPBACK_SESSION_SECRET` into the **macOS login Keychain** (`apps/desktop/src/keychain.cjs`) and injects it as the `Authorization` bearer on every window→backend request (`main.cjs` `onBeforeSendHeaders` — token never in page JS). `loopbackAuth` constant-time-compares it → the single **`local-operator`**, who is a seeded **owner** of the local org (`services/loopback-identity.ts`). A request **without** the session **401s**. Not an open instance.
- **Per-install keys + fail-closed boot (AUDIT-H1 #1/#2/#4).** The shell generates **three distinct** per-install keys (`SECRETS_ENC_KEY`, its own `RUN_TOKEN_SECRET`, `MC_LOOPBACK_SESSION_SECRET`) into the Keychain — the H0/H1 throwaway default is **gone**. `services/secret-keys.ts` `assertSecretKeysSafe()` **refuses to boot** the packaged profile on any missing/default/reused key (no-op in hosted).
- **Usable dashboard.** The desktop web build is packaged-flagged (`NEXT_PUBLIC_MC_PACKAGED=1`) so the dashboard renders **as the operator** (no Clerk sign-in) instead of bouncing to the landing route (the H0 `/dashboard` gap).
- **Honest flags.** The full **built-app** flow (Keychain under a real signed `.app`, the injected-header round-trip in a live BrowserWindow) is **verification-gated** — the same non-interactive limit as H1's sign/notarize run; the keychain helper, backend auth + fail-closed logic (real-DB tests), and the baked web flag were all verified here. Unsigned-app Keychain ACLs stabilise on the H1 signing run. No TCC wizard (H2), auto-update (H3), or config seeding (H4). `allowShell`/adapters/audit hooks untouched.
- **Where to look:** `backend/src/{middleware/loopback-auth.ts, services/secret-keys.ts, services/loopback-identity.ts, index.ts, tests/{loopback-auth,secret-keys}.test.ts}`, `apps/desktop/src/{keychain.cjs, main.cjs}`, `apps/desktop/scripts/build-desktop.mjs`, `web/app/dashboard/page.tsx`, `docs/DESIGN-packaging.md` §17.

**Invariant green: 1277 backend tests · 11/11 evals · backend + web typecheck clean · hosted web build (no `DESKTOP_BUILD`) emits no standalone. Hosted Clerk path byte-identical; `allowShell`, adapters, audit hooks, deployment posture untouched. Stopped for the H6 audit.**

<details><summary>Prior — ⏳ EPIC H · H1 BUILD PIPELINE BUILT (production-grade, minus the Apple cert). STOPPED for the H1 audit.</summary>

H1 hardened the H0 spike into a production-quality build pipeline — everything **up to** the Apple-account-gated sign/notarize run. All work is confined to **`apps/desktop/`**; the hosted Fly/Vercel build is byte-identical (verified). **stage→audit: STOPPED HERE.**

- **Compiled backend (goal #1).** `scripts/build-desktop.mjs` now bundles `backend/src/index.ts` with **esbuild** → one tree-shaken ESM `index.js` (5.1 MB) with the pure-JS deps inlined; only the two native/wasm-carrying packages stay external — **`@libsql/client`** (the DB `.node`) and **`officeparser`** (tesseract/pdfjs/canvas) — re-installed PINNED as a pruned prod `node_modules`. `main.cjs` forks the **compiled `index.js`** in packaged mode (dev mode still uses `tsx` for parity). The built `.app` ships **no `tsx`/`typescript`/`drizzle-kit`/TS source** — verified.
- **Sizes.** `.app` **619 → 463 MB** (−25%), `.dmg` **195 → 157 MB** (−19%). The remaining backend weight is `officeparser`'s OCR/PDF native stack (a real runtime dep, kept for correctness); the ~90 MB dev toolchain is gone. Electron frameworks (233 MB) are the irreducible floor.
- **Boots the packaged mesh from the built app (goal #3).** `dist/mac-arm64/7Ei Mission Control.app` → backend `/api/health` `{status:ok, db:connected}` on `127.0.0.1:8787` (fresh 468 KB `mc.db`, migrations on first boot), web 200 on `:8788`. `npm run dist:mac` is reproducible: correct `appId` (`ai.7ei.missioncontrol`), `productName`, **version from package.json** (`0.1.0`), and an **icon** (the 1024² 7Ei mark → `icon.icns`).
- **Signing WIRED but INERT (goal #2).** `hardenedRuntime: true` + `entitlements`/`entitlementsInherit` → `build/entitlements.mac.plist` (minimal JIT entitlements only) + `afterSign: scripts/notarize.cjs` (self-skips with no creds; logs `[notarize] skipped…`). `CSC_IDENTITY_AUTO_DISCOVERY=false` in the build scripts forces a deterministic unsigned build. **The flip** (once the Apple Developer ID exists): supply the cert (`CSC_LINK`/`CSC_KEY_PASSWORD`) + notarytool creds (`APPLE_API_KEY*` or `APPLE_ID`/`APPLE_APP_SPECIFIC_PASSWORD`/`APPLE_TEAM_ID`), drop the one env guard → electron-builder signs + notarizes + staples. No rearchitecture. (`docs/DESIGN-packaging.md` §16 · GO-LIVE "when you have the Apple Developer ID".)
- **Honest flags.** The signed/notarized artifact **does not exist yet** — that run is Apple-account-gated (H-Q1/H-Q2). Packaged auth is **still the temporary bypass** (no Clerk keys; `main.cjs` injects a throwaway `SECRETS_ENC_KEY`) — **NOT security-complete until H6.** No TCC wizard (H2), auto-update (H3), config seeding (H4), or loopback auth (H6) built. Tray menubar + dynamic ports deferred to a follow-on.
- **Where to look:** `apps/desktop/` (`scripts/build-desktop.mjs`, `src/main.cjs`, `electron-builder.yml`, `build/entitlements.mac.plist`, `scripts/notarize.cjs`, `build/icon.png`, README), `docs/DESIGN-packaging.md` §16.

**Invariant green: 1263 backend tests · 11/11 evals · backend + web typecheck clean · hosted web build (no `DESKTOP_BUILD`) emits no standalone. `allowShell`, adapters, audit hooks, and the deployment posture untouched. Stopped for the H1 audit.**

</details>

---

**Prior (2026-07-15) — ✅ EPIC H · H0 PACKAGING SPIKE BUILT & LANDED. The Electron bundling approach is de-risked end-to-end; the packaged mesh boots on a local file DB inside a built (unsigned) `.app`. STOPPED before H1 (per the spike scope).**

H0 was a de-risk spike to prove the `docs/DESIGN-packaging.md` recommendation before H1 commits to it. It is built in **`apps/desktop/`** (the seed for H1, not throwaway) and was verified **from the built `.app`**, not just `npm run`.

- **What it proves.** Launching `dist/mac-arm64/7Ei Mission Control.app` starts, as children of Electron's own Node: (1) the **Fastify backend** with `MC_DEPLOYMENT_PROFILE=packaged`, `DATABASE_URL=file:…/mc.db` (idempotent migrations run on first boot), bound `127.0.0.1:8787`; (2) the **Next.js UI** from its `standalone` build on `127.0.0.1:8788`; (3) a **BrowserWindow** on the local Next server once `/api/health` is green. `/api/health` returns `{status:ok, db:connected}` from the built app in ~3 s. Unsigned `.app` (619 MB) + `.dmg` (195 MB) via `electron-builder`. Runs with **no dev toolchain** on the host (`tsx` + backend `node_modules` incl. the libSQL native addon + the Next standalone all ship in `Contents/Resources/`, unpacked).
- **Top de-risk findings.** (a) `@clerk/fastify`'s `clerkPlugin` installs a **global** hook that 500s *every* request (incl. public `/api/health`) with no publishable key — fixed by gating it on `CLERK_PUBLISHABLE_KEY` (the H6 direction; the packaged profile is an auth *bypass*, **not** security-complete until H6). (b) The **libSQL native addon loads cleanly under Electron's Node 20 ABI** — the top native-module risk is a non-issue. (c) `next build` doesn't copy `.next/static`/`public` into standalone — the build step does. (d) Plain `tsc` output isn't runnable (`moduleResolution: bundler`) → the spike forks source via `tsx`; **H1 should compile/bundle + prune** to shrink the 619 MB `.app`.
- **Decisions confirmed for H1:** **Electron** (no Tauri bake-off needed) · Node-child via `tsx`-now/compiled-bundle-H1 · web = Next standalone-as-child (§2.3 option a) · pure local libSQL file · `electron-builder.yml` structured so signing is a **config addition** (H1/Apple-account-gated), not a rearchitecture.
- **Minimal prod edits (additive, hosted no-op, verified):** `backend/src/index.ts` (clerk-plugin gate + `HOST` env for loopback bind) · `web/next.config.ts` (`output:'standalone'` behind `DESKTOP_BUILD=1`). **Hosted proven untouched** — Fly backend + Vercel web build/deploy exactly as before.
- **Explicitly NOT built (later Epic H):** signing/notarization (H1), Tray + TCC wizard (H1/H2), auto-update (H3), config/secret bootstrap + per-install Keychain keys (H4), real single-operator loopback auth + fail-closed boot (H6).
- **Where to look:** `apps/desktop/` (shell, `main.cjs`, build script, `electron-builder.yml`, README), `docs/DESIGN-packaging.md` §15 (spike results).

**Invariant green: 1263 backend tests · 11/11 evals · backend + web typecheck clean · hosted web build emits no standalone. `allowShell`, adapters, audit hooks, and the deployment posture untouched. Stopped before H1.**

---

**Prior (2026-07-15) — ✅ EPIC ONB COMPLETE (ONB7 — docs / go-live, docs-only, no feature code). Invite-based agent onboarding is documented, legible, and go-live-ready.**

Epic ONB (invite-based agent onboarding) is finished. The security core (ONB1–ONB4), the create-invite UI + CLI (ONB6), and the audit-trail enablement all shipped and were independently audited; ONB7 wraps the epic with documentation that makes the whole state legible to a fresh agent or operator.

- **`docs/RUNBOOK-agent-onboarding.md`** — the operator's end-to-end guide: per-runtime invite → self-onboard → board-approval → one-time claim for each **available** runtime (`openclaw_local`, `claude_code`, `cursor`, `openai_generic`), the create-invite UI + `7ei-mc invite create`/`onboard --invite` CLI, an **ON-vs-OFF-today** table, the two deployment profiles (packaged/loopback vs hosted-with-`MC_ENABLE_REMOTE_ONBOARDING`), the exact go-live switches, and troubleshooting.
- **`GO-LIVE.md` consolidated** into ONE authoritative 16-item checklist (was scattered §1–8): Fly secrets (`SECRETS_ENC_KEY`/`RUN_TOKEN_SECRET`/`WEBHOOK_SIGNING_SECRET`), remote-onboarding enable, `GITHUB_VAULT_TOKEN`, Telegram bot token, local Ollama/Whisper/Piper stack, **wallet mainnet (`WALLET_MAINNET_ENABLED` — testnet-only today)**, apex DNS for `llms.txt`, audit-trail tuning (now ON), Jira epics still unfiled, plus the Clerk/Google/rotate/Mac-mini/Claude-Code items — each with status, risk, effort.
- **`docs/SECURITY-posture.md`** — the whole security model in one place: the inverted-token onboarding lifecycle, the **four ONB invariants**, low-trust-by-default, the multi-tenant membership gate + leak-guard, the audit trail, the recurring fail-closed patterns, wallet no-custody-until-enabled, and the machine-exec/shell posture — with the audit index.
- **ONB5 explicitly DEFERRED** (optional, gateway/webhook-only): the four available runtimes need no server-side reachability check; ONB5 is a precondition only for the still-unbuilt gateway/webhook runtimes, so deferring it ships nothing insecure. Build it fail-closed alongside the first gateway runtime.
- **Master docs refreshed:** PLAN §0 marks ONB1–4/6/7 + audit-H1 `done`, ONB5 `deferred`, and fills the placeholder PR numbers (ONB3 #250/#252, ONB6 #255, audit-trail #257/#258/#259); HANDOFF's state-of-the-app updated; vault `07-Agents` milestone mirrored.
- **Not touched:** any feature code, adapters, or the deployment posture — ONB7 is documentation only.

**Invariant unchanged: 1263 backend tests · 11/11 evals · typecheck clean (no code touched). The stage→audit protocol and the one open product call (per-org skills tenant-privacy) are noted as the working convention.**

---

**Prior (2026-07-15) — MEMBERSHIP HARDENING, HIGH-1 CLOSED: the ~25 top-level RECORD routes are now membership-gated. The gate is truly surface-wide, fail-closed. Stopped for the re-audit.**

The independent audit of the R-4 fix (`docs/AUDIT-MCA-membership-review.md`) found it was **not** surface-wide: `resolveRequestOrg` knew only `:orgId`/`:agentId`/`:taskId`, so ~25 record routes whose org lived in another param (`:projectId`, `:goalId`, `:itemId`, `:skillId`, generic `:id` for secrets/budgets/plugins/workspaces/attachments/watchdogs/scheduled/webhooks/policies/revisions) resolved `scoped:false` → the gate stood down → any logged-in user could read/delete another org's secret, confidential knowledge doc, project, policy, webhook, agent revision. Now closed.

- **URL-PREFIX → owning-table derivation.** `resolveRequestOrg` gained `RECORD_ORG_ROUTES` (`middleware/rbac.ts`): 14 route-prefix→table entries deriving the org from the record (the generic `:id` is disambiguated by route PREFIX, not param name). Each **fails closed** — missing/foreign record → 403, exactly like the `:agentId`/`:taskId` tail. `:orgId`/`:agentId`/`:taskId` precedence unchanged; backward compatible (no `routeUrl` → prefix tier skipped).
- **FLAGGED + handled — global vs per-org skills.** `skills.orgId` is nullable: a `null` row is a shared **global-library** skill → the gate **stands down** (readable, not a tenant record); a per-org custom skill (`orgId != null`) **is** membership-gated; a missing skill fails closed. The global-library collection routes (`GET/POST /api/skills`, `sync`, `obsidian-sync`) are org-agnostic and on the leak-guard's EXEMPT allowlist.
- **Widened leak-guard — the hole can't reopen.** `membership-scoping.test.ts` replaced the `/api/orgs/:orgId/*` filter with an **allowlist-negation sweep over ALL secured routes**: every secured route must resolve an org OR be on a short justified EXEMPT allowlist (16 routes); a new secured route that resolves `scoped:false` unlisted FAILS the test. A self-test proves the guard has teeth; a per-route behavioural sweep 403s a non-member on every non-exempt route; targeted tests drive a non-member at each real ORG record → 403 and a member → non-403. The boot now also registers **`webhookRoutes`** — it was **missing** from the original boot (the blind spot that hid the webhook leak). Red-proof: disabling the fix reds the sweep + the record-route proofs.
- **Not touched:** adapters, telemetry, `allowShell`, the agent-token API (`/api/agent/*`, separate scope — proven unaffected), the public onboarding surface (invite/join/claim — untouched). No invariant weakened.

**Invariant green: 1263 backend tests (+10) · 11/11 evals · typecheck clean. Full design + prefix→table map + flagged edge: `docs/AUDIT-MCA-membership.md`. Stopped for the re-audit.**

---

**Prior (2026-07-15) — MULTI-TENANT MEMBERSHIP HARDENING: org membership is now enforced SURFACE-WIDE (the R-4 fix). Stopped for the independent audit.**

The pre-existing cross-tenant gap the ONB2/ONB3 audits kept surfacing: the whole `/api/orgs/:orgId/*` surface was Clerk-AUTHENTICATED but not membership-CHECKED — only ~35 of ~159 org-scoped routes ran `requireOrgRole`, so any logged-in user could act on ANY org by swapping `:orgId`. Now closed with ONE shared, tested mechanism.

- **ONE scope-level gate.** `requireOrgMembership` (a `preHandler` on the whole Clerk-secured scope, `index.ts`) enforces baseline `member` for the org every request targets — resolved from `:orgId`, or derived from the `:agentId`/`:taskId` **record** where the path has no org id (the R-4 tail). It reuses the single `enforceOrgRole` core; it **fails closed** (an id that resolves to no row → 403, never a skip); it skips `OPTIONS` (CORS). Because it rides the scope, a NEW org route is covered the moment it registers — it can't be added ungated. Stricter per-route `requireOrgRole('owner')` gates still layer on top.
- **Grandfathered — the operator is never locked out.** `enforceOrgRole` now honours `organisations.ownerId` as an implicit **owner** when there's no `org_members` row (nothing backfilled pre-membership orgs). Every existing owner keeps full access with zero migration; only a genuine non-member/wrong-org request newly 403s. Driven test proves a legacy rowless owner still gets in.
- **Two record-derived routes hardened in-handler** (`multi-org.ts`): `GET /api/users/:userId/orgs` is now **self-only**; `transfer`/`clone` now also membership-check the **target** org (the gate covered the source).
- **Exempt set (precise + tested):** the agent-token API (`/api/agent/*`, token auth, separate scope — proven unaffected), the public onboarding surface (invite/join/claim — token-addressed, mint-nothing), `arturita/panic` (session-token authed in-handler), `auth/google` (OAuth handshake — pre-existing public), health/ready/openapi. None changed.
- **Leak-guard extended.** `membership-scoping.test.ts` is behavioural: it enumerates EVERY `/api/orgs/:orgId/*` route (>80) and drives a non-member at each → all 403; member/owner positives; record-derived + grandfather + agent-API-unaffected cases. Plus `rbac-membership.test.ts` unit-pins the primitives. The MCA-85 guard's boot now mirrors `index.ts` (same hook).
- **Not touched:** adapters, telemetry, `allowShell`/`MC_ALLOW_SHELL`, the live adapter, the deployment posture. No invariant weakened.

**Invariant green: 1253 backend tests (+18) · 11/11 evals · typecheck clean. Full design + route inventory + flagged edge cases: `docs/AUDIT-MCA-membership.md`. Stopped for the independent audit.**

---

**Prior (2026-07-15) — Epic ONB / audit M5 RESOLVED: the SHELL-EXECUTION DEFAULT is now OFF for new agents; existing agents (incl. the live OpenClaw ops agent) are GRANDFATHERED. Stopped for the independent audit.**

The operator took the M5 product call: align the shell-execution default to OFF, without breaking the live ops agent. The ONB1 audit flagged the drift — the server registry declares `allowShell: false` ("off unless the operator opts in") while the Add-Agent/Hire wizard's paste-able run-block emitted `MC_ALLOW_SHELL=1`, so every UI-onboarded agent got shell-on, contradicting the safe default. Now the two agree on OFF.

- **Enforcement is CLIENT-ONLY — confirmed end to end.** `MC_ALLOW_SHELL` is read at `adapters/openclaw/mc_adapter.py:31` into the local `ALLOW_SHELL`, which gates `shell_execute` (:105) and the `llm_execute` tool loop (:175). **No server/registry code gates shell or `machine_exec` from an agent's `allowShell`** — the registry field is only the `agentDefaultsPayload` contract's declared default (validated + stored, never consulted to block). So the wizard default is the *only* lever, and flipping it **cannot** retroactively disable a running agent.
- **New agents ship shell-OFF.** `web/lib/adapterProfile.ts`: the openclaw profile no longer hardcodes `MC_ALLOW_SHELL=1`. `mcEnv`/`runBlock` now append an explicit `MC_ALLOW_SHELL=0` for shell-capable runtimes (openclaw, custom) — matching the registry — with an operator opt-in (`{ allowShell: true }` → `=1`) surfaced as an **advanced, off-by-default checkbox** in both the Add-Agent wizard and the Hire dialog. Cursor/Claude Code never emit the flag (plan-mode / inbox model — unchanged).
- **Existing agents GRANDFATHERED — automatic.** The live OpenClaw ops agent runs from its own `mc.env` on its host (`~/.openclaw/mc-adapter/`), which the wizard never rewrites; the adapter reads the flag from that local env; and there is no server gate to flip. Its `MC_ALLOW_SHELL=1` is untouched → it keeps running. **No server gate was touched.**
- **Registry ↔ wizard reconciled.** Both now declare shell-off as the default (registry `allowShell: false` was already correct + test-locked at `adapter-registry.test.ts:65`; the wizard now matches). Tests lock: a new agent's run-block does NOT enable shell; an explicit opt-in does; non-shell runtimes never emit the flag; the wizard default agrees with the registry default.
- **Not touched:** the live adapter install, telemetry, the deployment posture, any server-side gate. No invariant weakened, no posture constant flipped.

**Invariant green: 1235 backend tests (unchanged — no backend edit) · 11/11 evals · web build clean · adapterProfile suite 13 tests (+6). Stopped for the independent audit.**

---

**Prior (2026-07-15) — Epic ONB / audit H-1: the AUDIT TRAIL is now LIVE for sensitive writes — the previously-dead subsystem records, safely and scoped. Stopped for the independent audit.**

The operator took the H-1 decision (GO-LIVE §7 option **b**): enable the audit trail for the sensitive half only, with retention. This turns on a previously-dead subsystem that writes to hosted Turso — done conservatively, behind every prerequisite the ONB2 audits demanded.

- **HOISTED so it actually fires.** The audit hook was a no-op because it lived inside an encapsulated `app.register()` child (H-1: it recorded **zero** rows for any sibling route). It is now installed on the **root instance** — a bare `auditLogPlugin(app)` before any `register()`, mirroring the already-correct `onRoute` hook — so its `onResponse` fires for every route. `audit-onb-enable.test.ts` **proves it records for a sibling** (the exact thing the ONB2 audit proved it did not). **Telemetry was deliberately left OFF** — a separate concern (in-memory span ring buffer, no Turso writes); its `telemetryPlugin` stays encapsulated.
- **SCOPED to sensitive only — no GET flood.** A pure, tested `shouldAudit(method, path)` records every mutating method (POST/PUT/PATCH/DELETE) **plus** the onboarding/invite/join/approval surfaces (`/agent-invites`, `/agent-join-requests`, `/approvals` — including security-relevant GET reads of the token-addressed doc), and **skips** the read-only dashboard-poll flood. So this is **not** one Turso INSERT on every request — only on the sensitive, mostly-low-volume half. The insert is fire-and-forget (`.catch()`-swallowed): it can never add latency to or fail the request it records.
- **RETENTION — bounded, not unbounded.** `services/audit-retention.ts`: rows older than `MC_AUDIT_RETENTION_DAYS` (default **90**; junk/0/negative safe-defaults to 90 so a typo can't wipe the table) are pruned on a daily scheduler tick (~03:00 UTC). Cutoff math, env parsing, and the daily gate are pure + tested; a `created_at` index (`setup.ts`) serves both the prune and the query `ORDER BY`.
- **REDACTION re-proven THROUGH the live hook.** End-to-end test: a real join request carrying a nested `apiKey`, a registry-declared `webhookAuthHeader` bearer, and an `mci_inv_` token in the path is driven through the now-live hook; the persisted row has the **path redacted to `:token`** and **no secret/token anywhere** (`buildAuditRow` → `redactPath` + recursive registry-driven `sanitizeBody`).
- **Reads stay gated.** `GET /api/orgs/:orgId/audit-log` and `…/traces` remain Clerk + `requireOrgRole('owner')` in the secured scope (from #248). Enabling writes did **not** open reads — a test boots both query routes and asserts 401 without a session.
- **Tripwire REWORKED, not deleted.** The `[ONB2-H1]` guard used to fail if the hook was hoisted (it held the hook shut). It now guards the **safety envelope**: it PERMITS the hoisted enablement, requires telemetry to stay off, and fails if a future change enables the hook **without** the redaction/sanitize/gating prerequisites (behavioral checks on `buildAuditRow` + `shouldAudit`).
- **Not touched:** `allowShell`/`MC_ALLOW_SHELL`, the live adapter, the deployment posture, adapters. No invariant weakened, no posture constant flipped.

**Invariant green: 1232 backend tests (+11) · 11/11 evals · typecheck clean · web build clean. Stopped for the independent audit.**

---

**Prior (2026-07-15) — Epic ONB / ONB6: the CREATE-INVITE experience — onboarding is now usable end-to-end. One paste onboards an agent. Stopped for the independent ONB6 audit.**

The operator-facing half of the epic. No backend change — the ONB1–ONB4 contract was already complete; ONB6 surfaces it.

- **Create-invite dialog (Cockpit → "✉ Invite an agent").** Owner-gated. Pick the allowed runtime(s), single-use (default) vs bounded multi-use, a TTL; on create the dialog shows **exactly once** the invite token + the **copy-able onboarding prompt** (`buildOnboardingPrompt`, ~40 lines) with copy buttons + the doc URL, and says plainly they are shown once and unrecoverable. When hosted-join is closed it says so honestly.
- **The raw claimed agent key is NEVER shown.** The dialog reveals the invite token and the prompt — never a claimed agent token (there is no code path to one). `operatorCanSeeClaimedKey` stays `false`; the claimed key is minted only when the joining agent claims it and only the claimer ever sees it.
- **The adapter picker renders FROM the server registry** (`GET /api/adapters`), not client-side `adapterProfile.ts` — `pickableAdapters` = `invitable && available`, never `internal`. This closes the ONB1 audit's "second client-side source of truth" clause **for the invite flow**. (Residual: `adapterProfile.ts` still generates the *manual* Add-Agent/Hire `mc.env` run-block client-side — a different concern — so the manual path's 4-runtime list is not yet registry-driven; noted for the auditor + ONB7.)
- **Invite list + revoke; join requests in the Inbox.** Active invites list with **colorblind-safe** status pills (icon+label+tone) + owner-gated **Revoke** (reuses the ONB1 routes). Pending **join requests surface in the shipped Inbox** as distinct `🤝 Agent wants to join` cards that now render their machine-generated self-declared/unverified warnings — decided through the ONB3-H1-hardened owner-gated `applyJoinDecision` (reuse, no forked UI).
- **CLI.** `7ei-mc invite create` (Clerk-authed operator; prints the invite token + the pastable prompt; **never a claimed agent token**) and `7ei-mc onboard --invite <token>` — the **agent-side** client (join → poll the claim until a human approves → claim once → write a **chmod-600 `mc.env`** with only `MC_BASE_URL`/`MC_AGENT_TOKEN`/`MC_WORKDIR` + flags, **never an LLM key**; the token is written to the file and **never printed**). Legacy `onboard` unchanged.
- **Not touched:** the audit/telemetry hooks (still a no-op), `allowShell`/`MC_ALLOW_SHELL`, the live adapter, the deployment posture. No backend change, no invariant weakened. **Stopped after ONB6 for the independent audit — ONB5/ONB7 not started.**

**Invariant green: 1221 backend tests (unchanged — no backend edit) · 11/11 evals · web build clean + 186 web tests (+5 ONB6) · 15 CLI tests (+8 ONB6).**

---

**Prior (2026-07-15) — Epic ONB / ONB4: the ONE-TIME KEY CLAIM — the credential lands, once, after approval. The onboarding core (ONB1–ONB4) is complete:**

The last piece of the inverted token lifecycle. An approved agent finally gets a credential — minted only after a human approves, claimed exactly once by the party that will use it, never through an operator's clipboard or a log.

- **The claim secret is minted at JOIN, hash-only.** A `mcc_` secret (256-bit, `services/claim.ts`) is generated when the agent submits its join request, stored as a **sha256 hash** with a TTL clamped to ≤ the invite's expiry, and returned to the agent **exactly once** in the join response. Only the hash is in the DB — a read yields no working secret (the `arturita_bindings` pattern).
- **The claim is fail-closed with NO oracle.** Public `POST /api/agent-join-requests/:id/claim-api-key` `{claimSecret}`. **Every failure is one identical flat 404** — unknown request · not approved · **missing agent row** · wrong secret · expired · already claimed · lost race. (Deliberate hardening beyond design §3.6's distinct 403/409/410, matching the epic's no-oracle posture — flagged for the auditor.) The secret is checked with a **constant-time compare**, never a SQL `=` or a Node `===`.
- **The agent-must-exist caveat is honoured (ONB3 auditor #3).** The claim re-reads the agent row and fails closed if it is absent or already credentialed — it never mints against `status='approved'` alone, which also neutralises M-1's ghost-agent shape.
- **Single-use is two atomic CAS statements.** The claim consume is one conditional UPDATE (`claimed_at IS NULL` is the compare-and-set; `claimed_at` set + secret hash **cleared** in the same statement); the `mca_` token is then minted under a **second CAS** (`api_token_hash IS NULL`) and stored **hash-only** on the agent. A concurrency test proves two simultaneous claims yield **exactly one token**.
- **The raw token appears once, to the claimer, nowhere else.** Never persisted plaintext (hash-only on the agent), never logged (`mcc_`/`mca_` redacted by `log-redaction.ts`), never in an operator UI — `operatorCanSeeClaimedKey` stays literal `false`. The claimed agent stays `low_trust_review` (the A2 gate still applies to dangerous actions); `claude_code` stays plan-mode.
- **Posture + guard honest.** `TOKEN_CLAIM_IMPLEMENTED` flips **true**; the landmine guard now asserts the claim route exists **iff** the constant (both directions). Exposure follows the deployment profile exactly like the join — hosted prod answers a flat 404 without `MC_ENABLE_REMOTE_ONBOARDING`. `perIpRateLimit()` wired onto the claim (the fixed socket/`Fly-Client-IP` keying).
- **Not touched:** the audit/telemetry hooks (still a no-op — operator's cost call), `allowShell`/`MC_ALLOW_SHELL`, the live adapter. No invariant weakened. **Stopped after ONB4 for the independent audit — ONB5/ONB6 not started.**

**Invariant green: 1221 backend tests (+12 ONB4, ONB3 tests updated) · 11/11 evals · web build clean.**

---

**Prior (2026-07-15) — Epic ONB / ONB3 audit H-1 CLOSED: the board-approval gate is now owner-gated on BOTH doors — the ONB4 blocker is cleared:**

The independent ONB3 audit (`docs/AUDIT-ONB3.md`) proved one HIGH: the generic `POST /api/approvals/:id/decide` route — the door the shipped Inbox card actually calls — carried **no membership and no role check**. An authenticated user with **no `org_members` row** could approve a join and mint an agent (200), while the dedicated owner route refused the same caller (403). The route has no `:orgId` path param, so `requireOrgRole` no-ops on it (the R-4 trap) and the MCA-85 leak guard couldn't see it. This was the blocker that had to land **before ONB4** turns "approved" into "claimable credential".

- **Membership is now always required, and the role is data-driven from the approval TYPE.** New pure helper `services/approval-authz.ts` — `requiredRoleForApproval()`: **owner** for the agent-minting types (`agent_join_request`, `agent_create`, and a `low_trust_review` wrapping one), **member** for every other well-formed type (approval types are open-ended — an agent's plan emits `[APPROVAL: <type>]` — so everyday cards keep their member reach and are not over-restricted), and **owner (fail closed)** for an absent/malformed type. A new minting card type is one array entry from being gated; it cannot drift open.
- **One enforcement path, both doors.** New `enforceOrgRole()` in `middleware/rbac.ts` is the single membership + role check; `requireOrgRole` delegates to it. Unlike the preHandler it **never skips on a missing org** — the decide route derives `orgId` **from the approval row** (not the path) and enforces the mapped role **before any decision logic runs**. So the dedicated owner routes and the generic route share one check, and a future card type can't slip in ungated.
- **Proven, driven, fails-against-main.** `tests/onb3-approval-gate.test.ts` (9 cases through the real route + real gate + real in-memory DB, three real identities): non-member → 403 on any decide (join card + generic card, **zero agents minted**), member → 403 on `agent_join_request`/`agent_create` but **allowed** on a lower-stakes card, owner → allowed on all. Each security case fails against the pre-fix `main`. `auth-scoping.test.ts` `[ONB3-H1]` adds the route to the leak-guard net by name.
- **Not touched:** the audit/telemetry hooks (still a no-op — operator's cost call), `allowShell`/`MC_ALLOW_SHELL`, the live adapter. No invariant weakened, no posture constant flipped. The pre-existing cross-tenant gap (`/api/orgs/:orgId/*` is Clerk-authed but not membership-checked) is **still open, still its own PR**.

**Invariant green: 1209 backend tests (+10) · 11/11 evals · typecheck clean. H-1 CLOSED → ONB4 may now merge.**

---

**Prior (2026-07-14) — Epic ONB / ONB3: the JOIN REQUEST + the BOARD-APPROVAL GATE — a human decides before any credential exists:**

The story that inverts the token lifecycle. An external agent can now **describe itself** to Mission Control, and a human decides. **No token is minted at any point in ONB3** — that is ONB4, deliberately unbuilt.

- **Public `POST /api/agent-invites/:token/join`** — the first unauthenticated *write* in the app. It creates **no agent and no credential**: it creates a row in the owner's approval queue. The response is `{ requestId, status, claimPath }` — never a token, never a claim secret. Proven against a **real database**: after a join, the `agents` table is empty.
- **The body is strictly typed, registry-validated, and has NO free-text field** (`.strict()` — an unknown key is refused, not ignored). `capabilities` is an **allow-listed enum array**, not prose; `agentName` is charset-restricted (it is rendered on a human's card); `agentDefaultsPayload` is validated field-by-field against the adapter registry. This is the ONB2 re-audit's carried caveat honoured literally: a third-party secret in free text under an innocuous key would reach `audit_logs.metadata` unredacted, so **no such field exists**.
- **ONB1 audit H1 — CLOSED.** The single-use consume is **one atomic conditional UPDATE**: `used_count = used_count + 1` computed **in SQL**, `used_count < max_uses` **in the WHERE clause**, every precondition re-asserted with the write, `rowsAffected !== 1` → the identical flat 404, and the join row written **only after** the consume wins. The advisory `consumeUsePatch` is **deleted** — there is now exactly one consume path and it is the safe one. A **TOCTOU test** (two callers that have both already read `used_count = 0`) fails against a read-then-write consume and passes against the CAS; a multi-use invite admits exactly `maxUses` and no more.
- **The board-approval gate reuses the SHIPPED tri-state queue** (`approval_requests`, type `agent_join_request`) — no parallel store. The card is **machine-generated**, labels every agent-authored value *self-declared, unverified*, and shows **no secret value** (only which secret fields were supplied, by name). Approving it in the existing Cockpit Inbox and via the owner API run the **same** decision path, so an approved card cannot exist without the agent actually being created. A double-approve is a **409**, never a second agent.
- **On approve:** the agent is created **contained** — `low_trust_review` **regardless of runtime** (invariant #3, read from the constant rather than re-derived), an explicit non-empty capability list (an empty one is *allow-all* in governance2 — that is the footgun), an explicit boundary — and with **`api_token_hash = NULL`**. **On reject:** nothing is minted and the secrets the agent supplied are **deleted**.
- **Declared secrets → the encrypted store** (AES-256-GCM), parked pre-approval in an **inert scope** (`join_request`) that `resolveSecretsForAgent` does not resolve, so no agent can read them; approval re-scopes them to the agent that now exists. The join row persists only the key **names**.
- **`perIpRateLimit()` finally has a caller** (10/min on the join) — the re-audit's **M-3**: rate limiting must exist *before* remote onboarding is ever enabled in prod. It is now a **checked hardening control**, so a regression closes the posture rather than opening an unlimited door.
- **The posture stays honest.** `PUBLIC_JOIN_IMPLEMENTED` flips to **true** (the surface exists) and a new `TOKEN_CLAIM_IMPLEMENTED` stays **false** (the claim does not). `publicJoinEnabled` still requires the profile + the operator enable, so **hosted production — no `MC_ENABLE_REMOTE_ONBOARDING` — answers the join route with a flat 404 today.** The ONB1 landmine guard is *stricter*, not relaxed: the join route must exist iff its constant says so, and **no claim route may exist in any scope** while `TOKEN_CLAIM_IMPLEMENTED` is false.

**Invariant green: 1195 backend tests (+28) · 11/11 evals · web build clean.** **Not touched, on purpose: the audit/telemetry hooks (still a no-op, still the operator's cost call) and `allowShell`/`MC_ALLOW_SHELL` (still a product decision).** **ONB4 not started — stopped for the independent ONB3 audit.**

**(2026-07-14) — Epic ONB / RE-AUDIT of the hardening: two gaps #248 left one layer short (`docs/AUDIT-ONB2-hardening.md`):**

An independent re-audit of #248 (auditor did not build it). **Verdict: PASS-WITH-FIXES.** The three prescribed fixes are sound — the audit surface really is out of the public scope, `sanitizeBody` really does recurse, the span really is redacted, and the `[ONB2-H1]` tripwire is real. But **H-2 and H-3 were each solved one layer short of the boundary, in the same way**: the mechanism was fixed and the *scope* of the mechanism was left at its old, too-narrow default. Both are now closed, with guards.

- **R-2 (HIGH) — a declared adapter secret was still persisted in PLAINTEXT.** `sanitizeBody` decided "is this key a secret?" against a hand-written substring list, but the **adapter registry** is what actually declares them — and `http_webhook.webhookAuthHeader` (`secret: true`, a bearer `Authorization` header value) matches **none** of `key|token|secret|password|…`. Probed against the merged code: it went into the row verbatim. This is H-3 *exactly as originally written* — the doc tells joining agents to send their secrets inside `agentDefaultsPayload` — surviving the fix for H-3, because the recursion reached the key and then failed to recognise it. **The registry is now the source of truth for both** (`allSecretFieldKeys()`): a new adapter's secret is redacted the moment it is *declared*, and a guard test loops every adapter × every declared secret field so the two lists can never drift again.
- **R-1 (MEDIUM) — `/api/traces` was authenticated but NOT isolated.** `spans` is **one process-wide buffer shared by every tenant**, so #248's fix narrowed the route from *public* to *any authenticated Clerk user* — who could still read every other org's span metadata. **And the obvious fix does not work:** `requireOrgRole` **silently skips every check** when the route has no `:orgId` (`middleware/rbac.ts` — "No org context — skip RBAC"), so bolting it onto a bare `/api/traces` would have produced a gate that *looks* like one and enforces nothing. Fixed by giving it an org to be scoped to: **`GET /api/orgs/:orgId/traces`**, owner-gated on a real `:orgId`, spans **filtered to the caller's org**, and an **unattributed span is served to nobody**. The bare `/api/traces` is **removed** (no caller anywhere in the monorepo). It also closes a structural blind spot: the MCA-85 net only inspects `:orgId|:agentId` routes, so a bare `/api/traces` serving cross-tenant data was **invisible to the guard by construction** — under `:orgId` it is inside the net for good.
- **Rulings on the rest:** the depth cap's `'[TRUNCATED]'` is **safe containment, not a redaction hole** (the whole subtree is dropped, and strings are scrubbed *before* the depth check) · `redactTokensInText`'s four prefixes are a **belt for our own credentials, not a secret detector** — that is the right contract, keep it, and **ONB3 must not add a free-text field to the join body** · the guard's no-op audit sink is **route-tagging coverage only and claims nothing about persistence** — correct as built.
- **Known follow-ups:** `llm.call` spans carry no org id, so the org-scoped traces route **under-reports** (the correct direction to fail — isolation first, utility second; fixing it means threading an org id through `LLMStreamOpts`). And `requireOrgRole`'s skip-when-no-`:orgId` is a **live footgun** left flagged, not fixed: making it fail-closed could 403 live routes and needs its own PR.

**Invariant green: 1167 backend tests (+7) · 11/11 evals · web build.** **H-1 and `allowShell` still untouched — both remain operator/product calls. ONB3 is CLEAR TO START, and not started.**

**(2026-07-14) — Epic ONB / pre-ONB3 SECURITY HARDENING: the audit trail is now safe to enable — but it is NOT enabled (#248):**

The three ONB2-audit findings (`docs/AUDIT-ONB2.md`) that had to close **before** ONB3 wires the join route. All three were dormant today and would have gone live together the moment anyone "just fixed the audit hook":

- **H-2 — the audit surface is no longer public.** `GET /api/orgs/:orgId/audit-log` and `GET /api/traces` were registered *inside* the audit/telemetry hook plugins, and `src/index.ts` registers those plugins in the **public** block — so the audit-log route inherited **no Clerk hook and no RBAC**: any caller who knew an `orgId` could read that org's audit log, and `/api/traces` served real spans (path, provider, model, org id, user id) to anyone. Only the H-1 no-op (an empty table) kept it from being a live cross-tenant leak — luck, not design. Both query routes are **split out of their plugins** (`auditLogQueryRoutes`, `telemetryQueryRoutes`) and registered in the **secured** scope; the audit log is additionally **`requireOrgRole('owner')`** (an audit log is an owner artefact).
- **…and the guard gap that let it through is closed.** The MCA-85 public-leak net (`bootLikeIndex()`) **never registered the audit-log or telemetry plugins** — which is exactly why a `:orgId`-scoped public route sat there unnoticed. Both plugins are now booted in the guard (with a no-op sink), and the guard asserts both routes are Clerk-tagged. A plugin-registered route can no longer hide from the leak guard.
- **H-3 — `sanitizeBody` recurses.** It redacted only **top-level** secret-shaped keys and copied object values in **whole, un-walked** — so a secret nested inside `agentDefaultsPayload` (the key ONB2's own onboarding document **instructs joining agents to put their `apiKey` / `x-openclaw-token` into**) would have been persisted to `audit_logs.metadata` **in plaintext** the day ONB3 lands. It now walks objects **and arrays**, depth-capped at 8, applies the same key test at **every depth**, and runs every surviving **string** through `redactTokensInText` — so a token echoed inside an error message or a URL is scrubbed too, and NIT-1's exported-but-never-called helper is finally wired into production.
- **M-1 — the telemetry span URL is redacted.** The span carried the **raw** `req.url` into `http.url` / `http.route` / the span name, and those spans were served by the (then public) `/api/traces`. Same `redactPath` helper as the audit row and the request logger: **one helper, every sink**, no drift.
- ⚠️ **H-1 stays OPEN, deliberately — it is an OPERATOR DECISION.** Both hooks are still encapsulated siblings in `src/index.ts`, so they still fire for **no route**, and `audit_logs` still records **nothing**. This PR makes the trail **safe-to-enable-by-construction**; it does not enable it. **Enabling it** means hoisting the hooks (`fastify-plugin`, or add-at-root before any `register()`), which switches on **one Turso `INSERT` per HTTP request, forever**. Pair it with (a) a **retention policy** and (b) a **`SENSITIVE_METHODS`-only filter** (skipping the read-only `GET` flood — the expensive, low-value half). A tripwire test now **fails** if the hooks are quietly hoisted without that decision.

**Invariant green: 1160 backend tests (+7) · 11/11 evals.** **ONB3 not started.**

**(2026-07-14) — Epic ONB / ONB2: the onboarding document — the self-describing contract an agent can drive itself from (#246):**

- **Pure `services/onboarding-doc.ts`.** `buildOnboardingDoc(invite, registry, posture, baseUrlCandidates)` is a **pure function** (injectable `now`, zero I/O) that renders the whole agent-readable contract: the **reachable-base-URL candidates** + the probe rule (`GET <candidate>/api/health`, take the first 200, put it in `agentDefaultsPayload.mcApiUrl` — *if none answer, escalate to the human; never guess a URL*) → **which `adapterType`s this invite allows**, each with its fields, secret fields, defaults, runtime note and a worked example → the **join request** → the **wait for a human approval** → the **one-time claim**. Every adapter shape is read **FROM the ONB1 registry** and nothing is re-described here — a test asserts each note/example/field-list is the registry's, so a second source of truth cannot quietly appear. Declared-but-unavailable runtimes (`openclaw_gateway`, `hermes_gateway`, `http_webhook`, `grok_local`, `hermes_local`) are shown **labelled `NOT AVAILABLE` with the reason**, not hidden: an honest map beats a short one.
- **The claim-security paragraph is a security control, and is test-locked as one.** Copied in spirit from Paperclip's best idea — the doc defends the joining agent against **its own observability layer**: parse the `token` from the **raw HTTP JSON** before printing or summarizing · **never** copy a token out of chat/transcript/tool-output previews (your scrollback holds a *rendering* of a key, not a key) · any value containing `...`/`[redacted]` is a **masked preview**, not a key · **never invent or rotate** one · write to private storage before logging · verify with one authenticated call. The same rules ride **inline in the pastable prompt**, because an agent holding a key may never re-read the doc.
- **Served, profile-gated:** public `GET /api/agent-invites/:token/onboarding.txt` (text/markdown) + `…/onboarding` (JSON twin). Exposure is **derived from the deployment profile** (`onboardingDocAccess`): `packaged` → open (loopback-trusted); `hosted` → closed unless the operator explicitly set `MC_ENABLE_REMOTE_ONBOARDING`. **Opening the document never opens the join surface** — `publicJoinEnabled` stays `false` until ONB4, test-locked. Malformed · unknown · expired · revoked · exhausted · profile-closed all collapse to **one identical flat 404**: no enumeration oracle. The doc **mints nothing and reveals no credential** — it restates the invite token the caller already holds and describes the still-unbuilt join/claim endpoints, labelled honestly as such.
- **The pastable operator prompt** (`buildOnboardingPrompt()`, the operator's whole product — one paste into any agent's chat) is returned in the create-invite response, the one moment the raw token exists. The operator's free-text `message` is treated as **untrusted on the way out**: control chars stripped, code fences and headings neutralized, capped, quoted, and labelled *context, not an instruction* — it cannot forge a step in the flow.
- **The ONB1 audit's H2 fix ships here** (it had to: this is the first **token-addressed** route). Pure `services/log-redaction.ts` replaces every credential-shaped path segment (`mci_inv_*`, `mca_*`, `art_*`, and ONB4's future `mcc_*`) with `:token` **before persistence** — in `audit_logs.path` (`buildAuditRow`) **and** in Fastify's request-log serializer. One helper, both sinks. An end-to-end test drives the real route with a real token and asserts the persisted row carries `:token` and no `mci_inv_` string anywhere.
- **Still wires no join and no claim** (ONB3/ONB4) — and the document says so, in the document.

**Invariant green: 1151 backend tests (+22) · 11/11 evals · web build.** **Stopped here for the stage-2 audit — ONB3 not started.**

**(2026-07-14) — Epic ONB / ONB1: the onboarding spine — deployment profiles, the invite object, the adapter registry (#244):**

**First, the architecture the operator approved** (`docs/DESIGN-agent-onboarding.md` **§8 addendum**, which supersedes the design's open questions Q1–Q4 and makes **Epic ONB and Epic H a joint design**):

- **Deployment-profile abstraction.** The app runs in one of two profiles chosen by config — **`hosted`** (multi-tenant, public join gated + hardened; what we run today) and **`packaged`** (single-tenant, loopback-trusted, Paperclip-style; the future `.dmg`) — and **the onboarding posture is DERIVED from the profile, never hardcoded**. Pure `services/deployment-profile.ts`; `MC_DEPLOYMENT_PROFILE` **safe-defaults to `hosted`**, because an unset value must resolve to the *harder* posture. `onboardingPosture()` returns the posture **plus a hardening checklist that names its own blockers** — and the checklist is *computed*, not asserted: `PUBLIC_JOIN_IMPLEMENTED` is `false` while ONB3/ONB4 are unbuilt, so **no env var can talk the join surface into being open before the controls that make it safe exist**.
- **The config bundle.** All system settings/parameters become **one declarative, versioned bundle** that the Export/Import portability moves between machines and that **Epic H's H4 installer seeds a fresh machine from** — not a second config format. Pure `services/config-bundle.ts`: version + deployment slice + `validateConfigBundle()` (refuses a bundle newer than the build) + **`assertNoSecrets()`, a hard throw**. **Secrets never travel in the bundle or the package** — they stay in the encrypted AES-256-GCM store. Its token-based secret detector (catches `apiKey`, `x-openclaw-token`, `webhookAuthHeader`; correctly ignores `sessionKeyStrategy`, `paperclipApiUrl`) is the *same* one the adapter registry uses to route payload secrets to the encrypted store — **one detector, so the bundle rule and the payload rule cannot drift**. (It earned its keep immediately: it rejected our own first name for an invariant field, `revealClaimedTokenInUi`, because it contains "token". It is now `operatorCanSeeClaimedKey`.)
- **Four posture defaults, baked in as INVARIANTS rather than switches**, each test-locked: (1) **public join endpoint OFF by default**, gated by profile; (2) **invites single-use by default** (multi-use = an explicit, bounded opt-in — we invert Paperclip's default); (3) **every invite-created agent lands in `low_trust_review` regardless of runtime** (extends CC3 beyond `claude_code`); (4) the **raw claimed token never reaches an operator UI, clipboard or log** — only the claiming agent reads it, once, from the raw HTTP response.

**Then ONB1 itself — the invite object + the server-side adapter registry:**

- **`agent_invites`** (idempotent `CREATE TABLE IF NOT EXISTS` + a **unique `token_hash` index**, nothing backfilled, no existing table touched). The invite is a **door, not a credential**: **hash-only** storage — the raw `mci_inv_*` token is shown to the operator **exactly once** at create and is unrecoverable (a lost invite is re-created, not recovered). Same hash + TTL + single-use pattern as `arturita_bindings` (A1). The token is **128-bit**, deliberately stronger than the design's sketched 12 chars, because on a backend that will eventually expose a public join endpoint an invite token *is* a bearer credential.
- **Pure `services/agent-invites.ts`** — the state machine with **`revoked > expired > accepted > active`** precedence (an operator who shuts a door always sees it read *revoked*), and the fail-closed rule that **expired / revoked / exhausted / unknown all collapse to one identical `not_found`**, so the lookup can't be used as an oracle to enumerate valid invite tokens. Out-of-range TTL or use-count is **refused, not silently clamped** — "I asked for 30 days and got 7" is exactly the surprise a security control must not spring.
- **Pure `services/adapter-registry.ts`** — the load-bearing shared schema ONB2–ONB4 build on. Ten adapterTypes (`openclaw_local` · `openclaw_gateway` · `claude_code` · `cursor` · `openai_generic` · `http_webhook` · `hermes_gateway` · `hermes_local` · `grok_local` · `internal`), each with capability flags, its `agentDefaultsPayload` contract, its **secret fields**, and an **`available`** flag — **declared ≠ available**: an unavailable adapter is describable (so the onboarding doc stays an honest map) but a join naming it is refused *with the reason*. `internal` is **never invitable**. Every entry maps onto the legacy `agents.runtime` column, so **nothing about the existing schema changes**. `validateDefaultsPayload()` is an **allowlist**, not a sanitizer: unknown keys, `__proto__`/`constructor`/`prototype`, size/key-count/string caps, enums and types are all refused; declared secrets are **split OUT of config** into the encrypted store; and an **undeclared secret-shaped key is refused loudly rather than dropped quietly** (a dropped credential is a credential the joining agent thinks it configured). No secret field carries a default — **we never invent a credential**. **`claude_code` defaults to `plan`** (propose-and-approve) and its enum *cannot express* autonomy, with a tripwire test that fails if anyone ever adds one; `openclaw_local.allowShell` defaults off.
- **Routes.** Owner-gated `POST/GET /api/orgs/:orgId/agent-invites`, `POST …/:inviteId/revoke` (idempotent), `GET …/onboarding-posture` (reports *why* the public surface is closed). The **only** public route is `GET /api/adapters` — a static, org-agnostic, secret-free description of the runtimes we speak, which a joining agent must be able to read before it holds any credential. **Nothing of the join/claim flow is wired**: no public join endpoint, no join request, no claim, no token minting. Those land in ONB3/ONB4 behind the board-approval gate + the per-IP rate limit.

**Invariant green: 1129 backend tests (+56) · 11/11 evals · web build.** **Stopped here for the stage-1 audit — ONB2 not started.**

**Prior (2026-07-14) — Epic P / P1: sidebar restructure — a shorter rail, nothing lost (#241):**

P0b promoted every Cockpit section to a first-class nav area, which worked but left the rail **long**. P1 shortens it. The rule throughout: **remove from the rail, never from the app** — every surface stays routable (⌘K palette + any deep link to its id still renders it), and `web/lib/navModel.ts` remains the single IA source that `Sidebar.tsx` and the dashboard render from.

**Six surfaces off the rail** (`HIDDEN_ITEMS`): **Search · Issues · Goals · Pipelines · Workspaces · Artifacts**. Search/Pipelines/Artifacts were "coming soon" placeholders; Issues/Goals/Workspaces are real surfaces whose components, routes and data are **untouched** — they simply aren't in the sidebar. All six still resolve through `findNavItem`, still appear in the command palette, and still render.

**Five surfaces folded into a parent's tab bar** — new `PageTabs.tsx`, driven by `navPageTabs()`: **Costs → `Costs | Budgets`**, **Connectors → `Connectors | Plugins`**, **Inbox (renamed **Inbox / Comms**) → `Inbox | Comms`**, **Settings → `Settings | Adapters | Secrets`**. Opening a hosted tab keeps its **parent** lit in the rail (`navSelectedId`), so the rail can't lose the user inside a tabbed page. No panel was rebuilt: each tab renders the exact component it always did (a promoted Cockpit section, a dashboard tab, or the honest Adapters placeholder).

The model now partitions every surface into exactly one of **rail · hosted tab · off-rail**, and the tests assert that partition — plus each removal, each fold target, the full rail order, and the carried-over **"every prior surface still reachable (nothing lost)"** invariant, which now spans all three homes. **17 nav tests** (was 10); the removals and folds can't silently regress.

**Invariant green: web build · 161 web tests (+9) · backend untouched (1073 tests).**

**Prior (2026-07-14) — Epic AG follow-up 2: the avatar-Remove 400 + three UI fixes (#239):**

**BUG — avatar Remove returned `HTTP 400: Bad Request` (the layer under the CORS fix).** Once CORS stopped blocking the DELETE preflight, the request finally reached the backend — and was refused **before any handler ran**. `web/lib/api.ts` set **`Content-Type: application/json` on every request**, body or not. A DELETE has no body, and Fastify's stock JSON parser rejects exactly that pair: **`FST_ERR_CTP_EMPTY_JSON_BODY`** — *"Body cannot be empty when content-type is set to 'application/json'"* → 400, which the client surfaced as a bare `HTTP 400`. The route, the Zod validators and the owner gate were all innocent; `avatar_url` was never nullable-constrained and the DELETE handler was correct all along. **Why the existing route test passed:** it injected the DELETE **without a Content-Type**, so it never reproduced the browser's request. Fixed at both layers — the client no longer claims a JSON body it isn't sending (`apiHeaders`), and the API now installs a **tolerant JSON parser** (`middleware/body-parser.ts`, mirroring the `cors.ts` precedent: a transport default that breaks a healthy route belongs in a tested module) that parses an **empty body to `{}`** while still refusing genuinely broken JSON with a message that names the problem. This fixes **every** bodiless write across the dashboard, not just the avatar. Regression-guarded by a test that drives the real handler with the **browser's exact headers**, plus one that asserts the raw Fastify default still 400s — so the bug can't come back unnoticed.

**FIX — Dashboard avatars now match Agent Configuration.** The Dashboard's Agent Squad, the Agents table and the cockpit **Agent fleet** each rendered a bare `avatarEmoji`, so an agent with an uploaded picture showed its picture in the Staff grid and its emoji on the Dashboard. All three now render the **same `<AgentAvatar>`** the Staff grid and the agent header use (uploaded picture → else icon/emoji), and the cockpit payload carries `avatarUrl` so it has the data to do it. One component, one source — the surfaces cannot disagree again.

**FIX — clicking an agent lands on Configuration.** `DEFAULT_AGENT_TAB` is now `configuration` (was `dashboard`). Every entry point routes through it — the cockpit fleet card (now clickable), the Staff card, the Agents table row, the command palette, and a tabless deep link (`#agents/<id>`) — so the default moved in one place. All six tabs stay reachable from the tab bar; explicit deep links (`#agents/<id>/runs`) are unchanged.

**FIX — the "Arturita" nav item is now "Command Center".** Label only: the nav id stays **`assistant`** (it is the dashboard Tab value and is deep-linked), the panel heading matches the nav, and the **agent named Arturita and the assistant persona are untouched** — she is still the one who answers, and the subtitle still says so.

**Invariant green: 1073 backend tests · 11/11 evals · typecheck · web build · 152 web tests.**

**Prior (2026-07-14) — Epic AG follow-up: the two operator-reported bugs + two features (#235–#237):**

**BUG — one root cause, three symptoms (#235).** `@fastify/cors` v11 defaults `methods` to **`GET,HEAD,POST`** and we never set it, so the deployed backend answered every preflight with an `Access-Control-Allow-Methods` that omitted PUT/PATCH/DELETE. The browser therefore **refused to send** those requests at all. `fetch` rejects on a blocked preflight exactly as it does on a dead host, and `web/lib/api.ts` collapsed that to *"Network error — backend unreachable"* — which pointed the investigation at an undeployed route. The routes were deployed and healthy the whole time (an unauthenticated `PUT …/files` returned 401, not 404). This one default broke the **Instructions save** (PUT), the **avatar Remove** (DELETE — while upload, a POST, kept working, which is exactly why it looked like an avatar-specific bug), and made the **Skills checkboxes** look read-only. Fixed with an explicit method list in `middleware/cors.ts`, guarded by a test that injects a real preflight per verb **and** walks the route table so a future dependency default cannot silently re-break it. Error surfacing now names the method, the path and CORS as a cause, and the Instructions tab names the failing file and says the draft is still in the editor.

**Skills — editable (#236).** Tick to install, untick to uninstall, saved as you go, optimistic with rollback. A second, independent bug was found and fixed here: `resolveSelection` refused **any** name absent from the library, and the checkbox list resends the whole selection — so on any agent carrying an **orphaned** skill (its library row deleted underneath it), **every** toggle returned `400 Unknown skill(s)`. An orphan the agent already has is now kept; unticking it is the only way to drop one; a genuinely unknown name is still refused and never partially applies.

**Custom adapter (#237).** Agent → Configuration → **Custom models**: display name, base URL, model id, optional API key, and a **Test connection** probe. OpenAI-compatible is the general case, so it covers **NVIDIA NIM**, Together, vLLM, a local server, or any OpenAI-standard provider. Reuses the **#207** custom-model service rather than forking it (`probeEndpoint` was lifted out of the Arturita route into the service — one probe, not two). Agent models register into `custom_models`, **not** `arturita_llm_chain` (that chain is Arturita's ordered *failover* list; defining a model for another agent must not silently change what Arturita falls back to); `available-models` merges both. The key is stored **AES-256-GCM encrypted**, never returned or logged, and scrubbed from audit rows. **The interop gap this closes:** `agent-executor` read only the *plaintext* `<slug>_api_key` while the save path only ever writes the *encrypted* `<slug>_api_key_enc` — a custom model with a key could not have authenticated on a single run. It now resolves credentials through `resolveLlmCreds` for every provider, on the primary call and every fallback hop.

**Net-new test coverage:** the AG write paths are now driven **end-to-end against a real SQLite file through the real owner gate** (`agent-detail-routes.test.ts`, `custom-model-agent.test.ts`) — a save that does not survive a re-read now fails a test. Invariant green: **1065 backend tests · 11/11 evals · web build · 146 web tests**.

**Prior (2026-07-14) — Epic AG: the Agents experience — Staff grid + a six-tab agent detail page — EPIC AG COMPLETE (AG1–AG7, #227–#233):** the Agents area now opens on a **Staff card grid** matching the operator's mockup — a card per agent with a large **avatar** (uploaded picture, falling back to the icon/emoji), name, email/handle, a **colorblind-safe status dot** (shape + text label + colour: ⬡ RUNNING / ⚠ NEEDS ATTENTION / ✓ IDLE, with **attention outranking running** so a busy agent holding a blocked task never reads as healthy), and the three metric chips (**Tasks Active · Token Cost today · Last active**). Clicking a card opens a **six-tab agent detail page** (deep-linkable `#agents/<id>/<tab>`): **Dashboard** (Latest Run · four 14-day charts · Recent Tasks · Costs strip) · **Instructions** (the managed markdown bundle) · **Skills** (Installed + Other, with a real uninstall) · **Configuration** (avatar upload · identity · Reports-to · Adapter + Model) · **Runs** (history + per-run log) · **Budget** (cap + spend). Built by **reuse first** — runs, tasks, costs, skills, adapter/model, budget/preflight, heartbeat and the org chart were all already there. Only **three** things were genuinely missing and are net-new: the **managed instructions bundle** (`agent_files` — the gap doc's #1 config gap: per-agent AGENTS.md/HEARTBEAT.md/SOUL.md/TOOLS.md + operator-added `.md`, owner-gated, path-traversal-proof), **avatar upload/storage** (`avatar_url`, a capped 256KB data URI — PNG/JPEG/WebP/GIF, **SVG refused**; the browser downscales to 256px/WebP first), and the **per-task token split** (`input_tokens`/`output_tokens`/`cached_tokens`) that makes the Costs strip real rather than decorative. **Safety of the migration:** every change is an idempotent nullable ALTER (or `CREATE TABLE IF NOT EXISTS`) with **nothing backfilled**, so existing agents are byte-for-byte unchanged — most importantly, the instructions bundle **only feeds the system prompt for files the operator actually saved**, so an agent whose bundle was never edited gets exactly the prompt it got before. Write paths are **owner-gated, field-allowlisted and snapshotted** to `configRevisions` (the legacy unvalidated `PATCH /api/agents/:id` was deliberately *not* extended), and a Reports-to change that would create a **reporting loop is refused** so the org chart stays a tree. Tokens only, no raw hex; Glassmorphism preserved. Invariant green: **1029 backend tests · 11/11 evals · web build · 135 web tests**. PLAN §0 **AG1–AG7** `done` · REQUIREMENTS **FR-AG1..AG8**.

**Prior (2026-07-13) — Epic CC / CC6: autonomous-exec mode (OFF by default) + go-live — EPIC CC COMPLETE:** the final story wires the (optional) autonomous posture and ships it **disabled by default, fail-closed behind three preconditions**: guard #1 `CC_AUTONOMOUS=1`, guard #2 `CC_AUTONOMOUS_CONFIRM=1`, **and** the CC5 command denylist being importable on the host — `resolve_permission_mode` collapses to `plan` if any one is missing (mirrors the wallet-mainnet / machine_exec two-guard pattern; the denylist is now a **hard precondition** for autonomy). Even in the autonomous posture, every command still passes the `cc_guard.py` hook → CC5 denylist: catastrophic/privilege/exfil commands are **refused**, unknown commands are **proposed** (never auto-run), only fully-allowlisted read-only commands run un-attended. New **`cc_adapter.py --doctor`** prints the exact resolved posture + why autonomy is blocked; **`setup.sh`** installs a launchd keep-alive (chmod-600 `mc.env`, `--doctor` + `--once` verify, propose-and-approve). **Docs:** README "Autonomous execution" section (exact enable/disable steps), `adapters/CLAUDE.md`, **GO-LIVE.md item 6** (host prereqs: `claude` CLI logged in, `mca_` token, workboundary), vault milestone. **Autonomous host execution is never on by default; nothing runs un-approved on the machine.** Invariant green: **934 backend tests · 11/11 evals · web build · 48 adapter tests**. PLAN §0 **CC6** `done` → **Epic CC complete (CC1–CC6)**.

**Prior (2026-07-13) — Epic CC / CC5: semantic command allowlist/denylist (the new safety gate):**

**Prior (2026-07-13) — Epic CC / CC5: semantic command allowlist/denylist (the new safety gate):** the one genuinely-new safety control the design flagged (R4) — before CC5 there was **no** semantic shell-command denylist; command control rested only on the A2 approval + the host not exposing an exec endpoint. New pure, **exhaustive** `services/cc-denylist.ts` (`evaluateCommand()`): classifies a shell command **deny** (fs-catastrophe `rm -rf /` · raw-disk `dd/mkfs/diskutil` · fork-bomb · privilege `sudo/doas/su` · system/SIP/Gatekeeper/firewall · pipe-to-shell `curl|sh` · reverse-shell `nc -e` / `/dev/tcp` · secret-exfil `.ssh`/`.aws`/keychain · anti-forensics `history -c`) / **allow** (opt-in read-only allowlist — `git status`, `npm test`, …) / **gate** (default — unknown ⇒ A2 approval, never auto-run). Precedence **deny > allow > gate**, per-segment chain analysis (`a && sudo rm -rf /` → deny), fail-closed. **Integrated into `prepareApprovalRecord`**: a denylisted `machine_exec` proposal is **refused pre-approval** (400, no row written) and the `allowlisted` flag is stamped honestly on the card. A **Python twin** `adapters/claude-code/cc_denylist.py` gives the host guard hook the same rules — the autonomous path (CC6, off by default) now enforces the denylist, allowing only fully-allowlisted commands and refusing denylisted ones. 36 backend + 13 adapter tests (incl. guard autonomous-path e2e). Invariant green: **934 backend tests · 11/11 evals · web build · 48 adapter tests**. PLAN §0 **CC5** `done`. **Next: CC6** — wire autonomous-exec behind the two operator guards (OFF by default) + go-live docs.

**Prior (2026-07-13) — Epic CC / CC4: UI/CLI adapter wiring:**

**Prior (2026-07-13) — Epic CC / CC4: UI/CLI adapter wiring:** hiring/adding a Claude Code agent now prints the **correct** adapter run-command + env (before CC4 a `claude_code` hire fell through to the **OpenClaw** run-command). New pure **`web/lib/adapterProfile.ts`** is the single source of truth for per-runtime adapter path + `mc.env` + copy-paste run block; `claude_code` prints `python3 adapters/claude-code/cc_adapter.py` with `CC_PERMISSION_MODE=plan` (propose-and-approve — no OpenClaw shell flags, and never an autonomous flag from the wizard). Both **`HireDialog`** and **`AddAgentWizard`** refactored onto it (openclaw/cursor blocks unchanged). Added an optional **externalEndpoint** field to the wizard (sent only when set). CLI already accepts `--runtime claude_code`. 8 web tests. Invariant green: **web build · 112 web tests · 898 backend tests · 11/11 evals**. PLAN §0 **CC4** `done`. **Next: CC5** (command denylist) → **CC6** (autonomous-exec, off by default).

**Prior (2026-07-13) — Epic CC / CC3: secure-by-default registration for code executors:**

**Prior (2026-07-13) — Epic CC / CC3: secure-by-default registration for code executors:** closes the allow-all-when-empty footgun for the Claude Code runtime. New pure **`services/code-executor.ts`** (`secureRegistration()`): a `claude_code` agent now registers **contained** — `trust_mode:'low_trust_review'` + an **explicit, non-empty** capability list (`memory:write`, `attachment:write`, and the new **`machine_exec`** capability — an empty list means allow-all, the footgun) + a `trust_boundary` seeded from the target workspace/project (empty = most restrictive, since the low-trust boundary is fail-closed). Non-code runtimes (openclaw/cursor/custom) keep their **legacy allow-all/standard** defaults — no regression — and explicit caller values always win. Wired into **both** create paths: `POST …/agents/external` (the wizard) and the goal-driven **hire-confirm** path. 16 tests in the CC suite. Invariant green: **898 backend tests · 11/11 evals · web build**. PLAN §0 **CC3** `done`. **Next: CC4** (UI/CLI adapter wiring) + **CC5** (command denylist).

**Prior (2026-07-13) — Epic CC / CC2: propose-and-approve `machine_exec` bridge:** closes a real safety hole and gives the Claude Code agent a correct propose channel. **Backend:** the agent-facing `POST /api/agent/approvals` previously stored the agent's own `summary`/`payload` verbatim for a **dangerous** approval — model prose on a `machine_exec` card. Now it routes through a shared pure **`prepareApprovalRecord`** (in `dangerous-approvals.ts`): for the four A2 danger types the summary is **machine-rendered from the structured `action`** (verbatim `argv` for `machine_exec`), the payload is tagged `requiresStepUp`, and a malformed payload is **fail-closed 400** (no row written). The human A2 route (`tasks.ts`) was refactored onto the same helper — one source of truth for the verbatim-render rule. **Adapter:** new **`cc_guard.py`** PreToolUse guard hook — when Claude could attempt a command, it turns each `Bash` call into a `machine_exec` approval (`argv=['sh','-lc',<command>]`, raw command + cwd for audit) and **denies** the call, so **nothing runs on the host**; installed via `--settings` whenever a non-`plan` posture is active (or `CC_GUARD=1`). Pure hook helpers in `cc_headless.py` + a subprocess e2e test. Invariant green: **891 backend tests · 11/11 evals · web build · 35 adapter tests**. PLAN §0 **CC2** `done`. **Next: CC3** (secure-by-default registration) + **CC4** (UI/CLI wiring).

**Prior (2026-07-13) — Epic CC / CC1: Claude Code as an office engineering agent — host adapter (poll loop):** Claude Code now joins the fleet as a first-class `runtime:'claude_code'` external executor (design: `docs/DESIGN-claude-code-agent.md`). New **`adapters/claude-code/`** (stdlib-only, its own thing — the live OpenClaw install is untouched): a pure, unit-tested core **`cc_headless.py`** (argv builder, `--output-format stream-json` parser, result/cost/session extractor, secret redaction, worktree planning, and the **fail-closed permission-mode gate** — 25 unittest) + the daemon **`cc_adapter.py`** (claim assigned task → run a **headless `claude -p`** in the workspace/`cc/`-worktree → stream stream-json to `/runs/:id/log` → post `/result` + `/heartbeat`; resumes a prior `claude` session via `sessionState`). The flagged **headless contract is nailed** against the installed CLI: `claude -p --output-format stream-json --verbose --permission-mode plan`. **Safety — propose-and-approve only:** `plan` mode means Claude reads/analyses and **proposes**, never edits/executes on the host; any non-`plan` (autonomous) mode collapses back to `plan` unless **both** operator guards are set (CC6, off by default). New `npm run smoke:claude-code` drives the real adapter with a **fake `claude`** (no network). Invariant green: **882 backend tests · 11/11 evals · web build · 25 adapter unittest**. PLAN §0 **CC1** `done`. **Next: CC2** (machine_exec approval bridge) + **CC3** (secure-by-default registration).

**Prior (2026-07-13) — Arturita voice input: graceful STT + FREE local Whisper (2 PRs):** the operator got **"Speech Error: network"** on push-to-talk even after the LLM went local. Root cause = the **STT / mic-capture** leg (not the TTS `speak()` path fixed earlier): both voice panels capture via the Web Speech `webkitSpeechRecognition`, whose Chromium backend is **Google's cloud STT — which Brave disables**, so the constructor feature-detects as present (mic enables) but every `.start()` fires `onerror` with `error:'network'`, dead-ending voice even online, printed verbatim. **PR #217 (graceful):** new pure `classifySttError()` in `talkDiagnostics.ts` maps `network` → a specific, **non-fatal** "voice input isn't available" status (names **Brave** when detected) and steers to the text box + local Whisper; `permission`/`no-mic`/`unknown` get actionable lines; `no-speech`/`aborted` stay benign. Both panels stop the recognizer and show a **colorblind-safe** notice (icon+label, never a raw code); typing is the always-available fallback. Shared `lib/browserEnv.ts` (`detectBrave`/`hasWebSpeechStt`). **PR (Whisper):** a **zero-dep** Node bridge **`adapters/arturita-stt`** wraps the already-installed `whisper` CLI (+ffmpeg) — the **browser** POSTs captured mic audio to `127.0.0.1:8790` (same trust model as local Ollama; CORS-scoped via `ARTURITA_STT_ORIGINS`, mirroring `OLLAMA_ORIGINS`) → `{ text }` → the converse flow. Works in **Brave**. Web `lib/whisper.ts` (client) + `lib/sttEngine.ts` (pure engine resolver: whisper → web_speech → typing, honouring the ⚙ Pipeline STT chain); `AssistantPanel` push-to-talk uses **MediaRecorder → Whisper** when the bridge is reachable, else Web Speech, else typing; the self-test STT leg now probes Whisper + reports the ONE start command. **Verified end-to-end** (macOS `say` → webm/opus → bridge → correct transcript + CORS header). **The one command:** `cd adapters/arturita-stt && ARTURITA_STT_ORIGINS=https://app.7ei.ai npm run stt`. Invariant green: **web build · 103 web tests · 7 arturita-stt tests**.

**Prior (2026-07-08) — Epic P / P0b: promote Cockpit sections to first-class Paperclip areas:** the Cockpit "Operations" mega-stack is now also reachable as **discrete nav areas** matching Paperclip. `CockpitPanel` gained an `only` **section filter** — the *same* composition root (one data load, all the existing section components) renders either the **full Operations stack** or a **single focused area**, with a lighter focused header (title + Refresh; no StatsRow/Hire/Add). Eight sections are promoted into the right groups: **Inbox · Activity** (Overview), **Goals · Org** (Workspace), **Workspaces** (Operate), **Budgets** (Delivery), **Plugins · Secrets** (Company). Reuse, **no rebuild** — every section is the existing component. Approvals stay reachable via **Inbox** (inline tri-state) + **Governance**, so no duplicate queue. Nav model gained a `section` kind + `isSection`/`navSectionKey` helpers (**9 web tests**). Invariant green: **web build · 80 web tests**. PLAN §0 **P0** `done` · REQUIREMENTS **FR-P0c** · `docs/IA-paperclip-mapping.md`.

**Prior (2026-07-08) — Epic P / P0a: Paperclip IA re-fold — nav shell + mapping:** the web dashboard nav now **mirrors Paperclip's information architecture** (`docs/TRD-paperclip.md` §3). The old flat 14-item sidebar is replaced by a **folded, grouped, collapsible rail** in Paperclip's group order (**Overview · Workspace · Operate · Delivery · Company · General**), driven by a **pure, unit-tested nav model** (`web/lib/navModel.ts`). Every one of the 14 existing surfaces is **re-homed** under the right group (Tasks→**Issues**, agent fleet→**Agents**, Preflight/Budgets→**Costs**, Governance→**Operate**, Memory→**Learnings** slot, Arturita→**Board Chat** slot, …) — **nothing rebuilt, everything still reachable**. Paperclip areas we don't have yet (**Routines, Pipelines, Review Queue, Artifacts, Adapters, Members & Access, Search**) appear as honest **"coming soon" placeholders** (`PlaceholderView.tsx`) that name the area and link to the Epic-P gap plan — the IA reads complete without faking features. Per-group **collapse** and whole-rail **icon-fold** both **persist** to `localStorage`. Colorblind-safe (active = accent + bold + indicator bar, never color alone), **tokens only**, Glassmorphism preserved. **P0b** (promote the Cockpit sections — Inbox/Activity/Goals/Org/Approvals/Budgets/Secrets/Workspaces/Plugins — to first-class areas, reusing the section components) is the next slice. Web: `lib/navModel.ts` (**8 tests**) + `Sidebar.tsx` + `PlaceholderView.tsx`; mapping in `docs/IA-paperclip-mapping.md`. Invariant green: **web build · 79 web tests**. PLAN §0 **P0** · REQUIREMENTS **FR-P0a..c**.

**Prior (2026-07-08) — Epic P / P2: model profiles (per-agent primary + cheap model + reasoning effort — the near-term cost lever):** each agent now carries a **model profile** — an optional explicit **primary** model, an optional **cheap** model, and a **reasoning-effort** setting (low/medium/high). A pure router (`services/model-profile.ts`) decides the tier per turn: **cheap** for ask-mode / low-stakes turns, **primary** for execute / heavier-reasoning / orchestration turns, with an **explicit override** (`modelTierOverride[:agentId]`) that wins. It's **wired into the executor** (`planWakeModel` selects the model + provider on both the execute and ask paths), and **interoperates with every shipped cost lever**: the routed model is the **F1 fallback-chain head** when no explicit chain is configured (an explicit chain stays the operator's override), the **preflight per-wake cap** now prices the *routed* model, and a cheap tier genuinely lowers **scoped-budget** spend (cost-rates / unbounded flags unchanged). **Reasoning effort** flows to the live LLM call, mapped per provider (Anthropic extended-thinking budget · OpenAI `reasoning_effort` · Gemini `thinkingBudget`) — additive and guarded, so a null-effort agent's request is byte-identical to before. Defaults keep **every existing agent unchanged** (null primary → `llm_model` stays primary; cheap off). Backend: pure `services/model-profile.ts` (**30 tests**) + idempotent `primary_model`/`cheap_model`/`cheap_model_enabled`/`reasoning_effort` ALTERs + owner-gated `GET/PUT /orgs/:orgId/agents/:agentId/model-profile` + `GET …/available-models` (catalogue + S8 custom entries). Web: Governance panel **Model profiles** section (per-agent primary/cheap/effort selects + cheap-routing toggle + routing summary + colorblind-safe tier/effort badges, owner-only); pure `lib/modelProfile.ts` (**6 tests**). **No new dangerous surface.** Invariant green: **867 backend tests · 11/11 evals · web build · 71 web tests**. PLAN §0 **P2** · REQUIREMENTS **FR-P5..P8** · gap doc `docs/GAP-paperclip-config.md`.

**Prior (2026-07-08) — Epic P / P1: low-trust review mode (the marquee Paperclip-gap safety item):** agents now have a **trust level** (`standard` default — nothing changes for existing agents — or `low_trust_review`). A **contained** agent may only touch the resources in its **boundary set** (project / task / agent ids); any escape is **refused + logged**, and every **gated action** — the A2 taxonomy (file_destructive / wallet_tx / email_send / machine_exec) **plus** create-agents / create-skills / assign-tasks — is **quarantined**: held in a review queue for explicit human approve/reject before it can take effect. The queue **reuses the existing tri-state approvals / inbox machinery** (a `low_trust_review` approval row — no parallel store) and stacks **in front of** the A2 gate, never bypassing it: a quarantined dangerous action still needs **step-up** to approve. Backend: pure `services/review.ts` (trust/boundary/taxonomy/decision/row-builder — **24 tests**), idempotent `trust_mode`/`trust_boundary` ALTERs, owner-gated `GET/PUT /orgs/:orgId/agents/:agentId/trust` + `POST …/review-evaluate` (fail-closed: 202-held on quarantine, 403 on escape) + `GET …/review-queue`, decide-route step-up inheritance, orchestrator delegation chokepoint enforced. Web: Governance panel **Trust & containment** section (per-agent toggle + boundary editor, owner-only, colorblind-safe icon+label+tone) + a **🛡 quarantine chip + warnings** on review cases in the inbox; pure `lib/trust.ts` (**5 tests**). **No new dangerous surface** — the gate only ever withholds an action. Invariant green: **836 backend tests · 11/11 evals · web build · 65 web tests**. PLAN §0 **P1** · REQUIREMENTS **FR-P1..P4 · NFR-9** · gap doc `docs/GAP-paperclip-config.md`.

**Prior (2026-07-08) — Arturita custom LLM insertion (S8):** the operator can now add an **arbitrary model** from the Config panel instead of only presets. A **"Custom model"** form (display name · base URL · model id · type · optional key) POSTs `…/arturita/custom-model`; the entry slots into `arturita_llm_chain` and rides the **F1 breaker/failover** like any built-in. General case = **OpenAI-compatible** (`/chat/completions`); plus the **keyless local base-URL / Ollama-style** case. Backend: pure `services/custom-model.ts` (namespaced slug, http(s) validation, `applyCustomModel`/`removeCustomModel` deployConfig mutation, `resolveLlmCreds`/`hasStoredKey`) + `routes/arturita-custom-model.ts` (owner-gated add/**test**/delete; the test button probes GET `/models`→1-token `/chat/completions`). **Key is stored AES-256-GCM encrypted** (`<slug>_api_key_enc`) — never returned, never logged (masked tail only); converse `resolveCreds` decrypts custom hops; `llm-router` keyless guard relaxed for base-URL endpoints. Config schema documented in **DECISIONS S8 · REQUIREMENTS FR-49**. Invariant green: **812 backend tests · 11/11 evals · web build · 60 web tests**.

**Prior (2026-07-08) — Arturita talk-path bugfix (graceful degrade + self-test):** the tab surfaced a raw "network error" and voice silently died. Root cause — two legs failed opaquely: (1) **browser TTS** used `SpeechSynthesisUtterance` without selecting an on-device voice, so Chrome's network-backed voices threw `SpeechSynthesisErrorEvent.error === 'network'` and playback died with the error swallowed; (2) every backend/local-Ollama transport failure mapped to the generic `web/lib/api.ts` "Network error — backend unreachable" with no leg/fix hint. Fix — new pure `web/lib/talkDiagnostics.ts` (voice selection, TTS-error classification, per-leg error descriptions with fix hints, self-test shaping — 16 tests): `speak()` now prefers a `localService` voice + auto-retries local on a network error + shows a specific non-fatal status; converse/local-Ollama errors surface which leg failed + the fix (e.g. **set `OLLAMA_ORIGINS`**); added a **🩺 Talk-path self-test** to the Pipeline panel (backend/Ollama/TTS/STT, colorblind-safe icon+label rows). Text reply is always shown; the tab never dead-ends. Invariant green: **57 web tests · web build**.

**Prior (2026-07-08) — Arturita Epic J to production (4 PRs):** the greenlit build of the Jarvis "Arturita" tab. **#201** the real **7Ei mark** (honeycomb) wired as the orb centerpiece (transparent vector of the operator's raster; `web/public/7ei-logo.jpg` source). **#202** the **free-first pipeline backend** — pure `arturita-pipeline` resolvers (LLM/STT/TTS chains, free-first defaults, S1 privacy filter, `usableLlmChain` prune+guarantee) + `GET/PUT /arturita/pipeline`; converse now runs the configured LLM chain. **#203** the **Config panel** — a per-layer chain editor (reorder/mode/add/remove/reset) in the tab. **+ local-Ollama PR** — **browser-direct local Ollama streaming** for the answer path (real token streaming, on-device, $0; graceful cloud fallback; `via` provenance chip). Brainstorm-first + A2 gating unchanged. **Go-live to fully local:** set `OLLAMA_ORIGINS=https://app.7ei.ai` (+ restart Ollama) so the browser can reach it; optionally add Groq/Gemini keys (free-tier LLM fallbacks) and the NVIDIA key (hosted Chatterbox TTS). Invariant green: **794 backend tests · 11/11 evals · web build · 41 web tests**.

**Prior (2026-07-08) — Arturita Epic J research doc (`docs/PRD-jarvis-tab.md`, research-first rewrite):** the operator refined the direction to **research-and-document first, hold the build**. The spec is now built around two principles: (1) the tab is a **brainstorming/conversation partner FIRST** — Arturita answers directly and commands the builder/executor agents **only when explicitly told to** (interaction model in PRD §1); (2) **free-first / self-hosted-first with configurable fallbacks on every layer**. It carries three grounded comparison tables + a config schema — **LLM** (local Ollama `llama3.2:3b`→`qwen3:8b`→`qwen2.5:14b` → free-tier Groq/Gemini), **STT** (whisper.cpp Metal → browser Web Speech), **TTS** (Piper/local-Chatterbox → browser SpeechSynthesis; **Chatterbox resolved** = Resemble AI open-source MIT, runs local *or* NVIDIA-hosted). Logged as **DECISIONS S7**; PLAN §0 J2–J6 re-phased; REQUIREMENTS **FR-47/48 · NFR-32**. Docs-only PR. **J1 scaffold (below) already shipped; deeper build held pending review.**

**Prior (2026-07-08) — Arturita J1, the Jarvis tab (Epic J):** a new Cockpit **"Arturita"** tab (🌸) — a Glassmorphism conversational surface with a **reactive HUD orb** (idle/listening/thinking/speaking, colorblind-safe). Default behaviour: **Arturita answers directly**; she only routes into the task/agent flow when the operator **explicitly** asks to build/do/delegate (or the intent is destructive) — the routing decision is shown on every turn. Backend: pure `arturita-converse.decideConverseMode` + `POST /arturita/converse` (answer via the **F1 fallback chain** `streamLLMWithFallback`; delegate via B3 routing → a `pending` task at the **A2 gate**). Web: `assistant.logic` + `AssistantOrb`/`AssistantPanel`, push-to-talk + wake-word + typed, streamed replies, glass hero (tokens only, light+dark). **No dangerous surface** — chat turns take no actions. Placeholder logo at `web/public/arturita-logo.svg` (one-file swap; see `ARTURITA_LOGO_SRC`). Spec: `docs/PRD-jarvis-tab.md`. **+24 tests (12 backend · 12 web).**

**Live:** backend on Fly (`7ei-backend`, v1.3.0), web on Vercel (`app.7ei.ai`), Turso DB. All PRs merged to `main`.
**Zero-auth vault-graph preview:** https://app.7ei.ai/vault-graph.html — self-contained render of the enriched graph (785 nodes / 108 named communities), no login, no `GITHUB_VAULT_TOKEN` needed (#196).
Health `GET /api/health` → 200, `db: connected`, `scheduler: running`. Invariant green: **781 backend tests · 11/11 evals · web build green · 27 web tests**.
Full write-up in the shared vault: `07-Agents/STATUS-Mission-Control-2026-07-02.md` · plan: `01-Projects/Paperclip-Gap-Analysis-v2-2026-07-06.md`.

## Paperclip gap-bridge v2 — 4 / 4 epics complete
| Epic | Stories (all ✅) | PR trail |
|---|---|---|
| **MCA-82** · Theme (design system v2) | T1 foundation · T2 glass chrome · T3 hex sweep + WCAG AA audit | #155 · #156 · #159 |
| **MCA-83** · Work surface / failure UX | W1 recovery cards · W2 blocker chips + next-up + cost rollups · W3 thread wake-on-comment · W4 task watchdogs · W5 ask-mode | #157 · #158 · #161 · #168 · #169 |
| **MCA-84** · Visibility | V1 heartbeat 24h timeline · V2 tri-state approvals + inbox retry + read receipts · V3 per-wake preflight cap + model validation | #160 · #162 · #163 |
| **MCA-85** · DX | D1 self-describing API (`/api/openapi.json` + CLI) · D2 `llms.txt` + `7ei-mc onboard` | #164 · #167 |
| _Unplanned security_ | Auth-scoping hardening (org/agent groups → Clerk) · per-org webhook shared-secret verification | #165 · #166 |

**All four v2 epics (MCA-82/83/84/85) complete.** Per-story detail below. Standing items (Jira transitions, webhook-secret rollout, apex DNS, vault mirror) tracked in **Standing items** at the foot of this file.

## Next: major update — Arturita (voice-first personal AI agent)

> **DECISION LOCK 2026-07-08:** S1–S6 all **CONFIRMED**; wallet model **CHANGED** (S4 — bounded autonomous signing from a dedicated capped burner: autonomous **< $100** per-tx, ≥ $100 → A2 approval; local encrypted keystore / delegated session key; **testnet only this wave, mainnet behind an explicit go flag** — this **overrides** the old never-sign/no-custody invariant). Also: **S1** voice `local|provider` config (interim `provider` = Chatterbox TTS via NVIDIA API, key → encrypted store only, **verified not committed**, `.env.example` placeholder); **S2** Telegram-only v1; **S3** whole-machine access + minimal self-protection denylist; **S5** push-to-talk default; **S6** broad exec (destructive A2-gated). New **Epic H — Packaging & Distribution**. Detail: `docs/DECISIONS-arturita.md`, cascaded into PRD/PLAN/REQUIREMENTS.

Full planning/tracking layer filed: **PRD** `docs/PRD-arturita.md` (intent) · **PLAN** `docs/PLAN-arturita.md` (Epics A–G → stories A1…G2 + §0 living status tracker) · **DECISIONS** `docs/DECISIONS-arturita.md` (S1–S6, provisional/confirmed) · **REQUIREMENTS** `docs/REQUIREMENTS-arturita.md` (FR/NFR master acceptance checklist) · **ONBOARDING** `docs/ONBOARDING-arturita.md` (cold-start). Vault mirror: `07-Agents/Arturita.md`. A voice-first personal-agent persona layered on the existing surface: desk voice (Cockpit) + remote iPhone control via Telegram (voice/text/files/one-tap approvals), hardened local machine control, Google email/calendar, and **read+prepare-only** crypto wallet (human-in-the-loop signing, **no key custody, never auto-sign**). Reuses the shipped primitives — tri-state approvals gate all dangerous actions, preflight/budget bound LLM failover, plus watchdogs/wake-on-comment/ask-mode/heartbeat/HMAC-Telegram/secrets/mac-mini-adapter. Epics A–G (PRD §10), **Epic A safety spine ships first**. See `HANDOFF.md` → "Incoming major update" and the §0 tracker in `docs/PLAN-arturita.md`.

### Arturita build — story tracker (live)
| Story | What | Status | PR |
|---|---|---|---|
| **A1** · Persona, sessions & `/panic` | `services/arturita-session.ts` (pure: session mint/verify/revoke + TTL/step-up freshness, single-operator binding, nonce guard, panic plan); `arturita_sessions`/`arturita_bindings`/`arturita_nonces` tables; owner-scoped Arturita persona seed; session/bind routes + **`POST panic`** (owner-authed via session token). | ✅ done | #174 |
| **A2** · Dangerous approval types + step-up | `services/dangerous-approvals.ts` (pure: `file_destructive`/`wallet_tx`/`email_send`/`machine_exec` classification, **machine-rendered verbatim** action summaries + scam/danger warnings, step-up evaluation); `decideApproval` gains a step-up gate (approve a dangerous type ⇒ needs a fresh command session, else 403); approvals POST regenerates the summary from a structured `action` (ignores model prose). | ✅ done | #175 |

| **A3** · Intent classifier + two-phase confirm | `services/intent.ts` (pure: `classifyIntent` tiers safe/destructive/critical + maps to A2 approval types; `confirmationPhraseFor`; `isConfirmed` two-phase — bare "yes" rejected for the top tier, action-verb restatement or tap required; sub-threshold STT re-prompts, never guesses). Table-driven tests over a destructive/safe/ambiguous corpus. | ✅ done | #176 |

| **F1** · LLM fallback chain + circuit breaker | `services/llm-fallback.ts` (pure: `parseFallbackChain` from deployConfig; `classifyLlmError` → 6 failure classes + failover/breaker guidance; circuit breaker `recordFailure`/`isProviderHealthy` with window + cooldown/re-probe; `planFallback` walks the chain, skips open breakers, drops hops over the preflight per-wake cap, parks with a plain-language reason when exhausted). **Pure decision layer only** — live wiring into the `agent-executor`/`streamLLM` retry loop is a deliberate follow-up (kept off the LLM hot path in the overnight session). | ✅ done | #177 |

| **E1** · Wallet read + prepare + simulate | `services/wallet.ts` (pure: `buildUnsignedTx` key-free assembly; `decodeCalldata` ABI-layout decode of a curated selector table; `summarizeSimulation` gas/revert; `checkCaps` per-tx/per-day/allowlist; `detectScamSignals` new-address/setApprovalForAll/unlimited/drain/unknown-contract; `weiToEth`/`weiToGwei`; **`assertNoKeyMaterial` no-key-custody invariant**). `wallet_intents` table (no key material). Routes `POST …/wallet/prepare` + `…/:id/simulate` + `GET …/wallet/intents` — **no signing endpoint by design**. | ✅ done | #178 |

| **C1** (pure planner) · Host safety logic | `services/host-planner.ts` (pure, **fail-closed**: `HOST_EXECUTION_ENABLED=false` + `assertExecutionEnabled()` throw until S3; `canonicalizePath`/`isWithinRoot` no-escape prefix check; `hitsDenylist` catastrophic-target hard-deny; `classifyBlastRadius` auto-safe/needs-approval/refuse with destructive-never-auto-safe; `decideAccess`; `planHostOp` combined plan → `file_destructive` approval; undo-journal `buildUndoEntry`/`isReversible`). **No daemon, no routes, NO filesystem execution** — the write/destructive path stays blocked on **S3**. | ✅ done | #179 |

| **B1** (pure helpers) · Voice gateway core | `services/voice.ts` (pure: `normalizeTranscript`; `hasWakeWord`/`stripWakeWord`/`shouldProcessCapture` — push-to-talk default, wake-word opt-in; `gateTranscript` empty/reprompt/accept confidence gate; `orderVoiceProviders` cloud→alt→local→text-only with **local-only for sensitive/wallet contexts** (S1 privacy default); `nextVoiceProvider`; `AUDIO_RETENTION` discard-after-transcription marker). Provider adapters + the `/voice` endpoint land when an **S1** STT/TTS provider is configured. | ✅ done | #180 |

| **B3** (pure routing) · Ask-vs-execute from voice | `services/voice-routing.ts` (pure: `isQuestion`; `routeVoiceCommand` → question→`ask` single-turn, work order→`execute`, **destructive→execute-always** even if phrased as a question; `isFollowUp` re-enters the thread via wake-on-comment). Reuses `intent.ts` + `askmode.ts` + `thread.ts` — no new loop. Endpoint wiring pends the B1 voice endpoint (S1). | ✅ done | #181 |

| **F2** (pure helpers) · Degraded/offline + watchdogs | `services/arturita-resilience.ts` (pure: `actionNeed` local/cloud/host; `routeForConnectivity` — local runs offline, cloud queues offline with a spoken notice, host fails closed when the daemon is down; `planReplay` idempotent nonce-guarded exactly-once replay on reconnect; `defaultArturitaWatchdogs` runtime/cost/no_activity via the shipped `watchdogs.ts`). Queue store + host-health wiring pends B1/C1 execution. | ✅ done | #182 |

| **C1** (daemon scaffold) · Local Host | `adapters/arturita-host/` — a hardened **localhost+token** Node daemon (real `/list`+`/read`+`/preview`+`/apply`+`/undo`). **S3 whole-machine root + minimal self-protection denylist** (own config/token, `.arturita-keystore` burner, SSH/keychains/`.env`/wallet vaults, OS system-integrity `/System`,`/usr`≠`/usr/local`,…); symlink-safe realpath resolution; blast-radius caps. **Reads/previews safe; destructive `/apply` FAILS CLOSED without `approved:true`** (A2 gate) + stages originals to a 10-min undo journal. Refuses to start without a token; binds 127.0.0.1 only. `setup.sh` + launchd + README + 7 action-layer tests; verified end-to-end over HTTP. Backend `host-planner.ts` hardened (system-integrity denylist + `buildPreviewManifest`). `machine_exec` not exposed yet (C3). | ✅ done | #189 |

| **B1** (config + provider + endpoint) · Voice gateway | `services/voice-config.ts` (pure: `resolveVoiceMode` `local\|provider` per-context — **sensitive → forced local**; `selectVoiceProvider` → Chatterbox/NVIDIA \| local \| text-only, never cloud in local mode; `parseVoiceModeSetting` from deployConfig). `services/voice-provider.ts` (`chatterboxNvidiaSynthesize` Bearer-auth NVIDIA TTS; `synthesizeSpeech` never throws — degrades to text on outage; audio not persisted). `POST …/arturita/voice` (Clerk-secured): gate STT confidence → resolve mode → route ask/execute (B3) → create Arturita task → spoken reply. Key read from the encrypted secret store at call time (never logged/persisted). Live raw-audio STT + local engine = go-live. | ✅ done | #188 |

| **F1** (executor wiring) · LLM fallback on the live path | `services/llm-fallback-runtime.ts` (`streamLLMWithFallback` wraps the executor's `streamLLM` call — walks `arturita_fallback_chain` from deployConfig on failure via the shipped pure `planFallback`/`classifyLlmError`; module-level circuit-breaker registry skips unhealthy providers across wakes; cost-bounded — over-cap hops dropped; **behaviour identical to a bare call when no chain configured**). `/health` gains `llm.providers`/`llm.unhealthy` breaker health. Wired the primary executor call + task `llmModel`/result provider reflect the model that answered. | ✅ done | #187 |

| **E2** (policy engine) · Wallet bounded-signing policy *(S4 model change)* | `services/wallet-policy.ts` (pure: `evaluateWalletPolicy` → `autonomous_sign\|require_approval\|refuse` with per-tx threshold (default **$100**)/per-day cap/allowlist/scam/simulate-before-sign; fail-closed `checkSigningGate`/`assertSigningAllowed` keeping **mainnet signing off** unless both switches on; `classifyNetwork`; `SIGNER_MODELS` — WalletConnect can't do unattended signing). `wallet_policy` table (both switches default 0). Routes `GET/PUT …/wallet/policy` + `POST …/wallet/:id/evaluate` (no signing — real testnet signer is go-live). Keystore design: `docs/WALLET-KEYSTORE-arturita.md`. **No key material anywhere; no mainnet signing.** | ✅ done | #186 |

Safety gate: **A2 (on `main`) is the mechanical approval-type gate** every dangerous surface depends on. **S1–S6 CONFIRMED (2026-07-08).** Wallet mainnet autonomous signing stays behind `WALLET_MAINNET_ENABLED`/`WALLET_AUTONOMOUS_SIGNING_ENABLED` (both default off) + a final operator go; real destructive host ops route through A2 with two-phase confirm. **No mainnet wallet signing; no irreversible action without the operator.**

### Epic M — Memory & Vault Graph (operator request 2026-07-08)
| Story | What | Status | PR |
|---|---|---|---|
| **M1/M2** (backend) · Vault graph + Graphify | `services/vault-graph.ts` (pure: `buildNativeGraph` from markdown — [[wikilinks]]/#tags/frontmatter, folder clusters, degree, unresolved-count, file cap; `parseGraphifyGraph` normalizes a Graphify `graph.json` into the same node/edge model, scoped to the vault root, dropping `.obsidian/`+out-of-root leaks). `GET …/memory/graph` (Graphify-first: prefers `<root>/graphify-out/graph.json`, native fallback, TTL-cached, `?rebuild=1`/`?tags=0`, `hasGraphify`+`rebuildCommand`). `vaultTree` (1-call recursive listing). Graph built from the TARCO vault + **semantic pass via local Ollama `qwen2.5:14b`** (S-M1 resolved) — **110 communities / 109 named concepts / god-node hubs / GRAPH_REPORT.md**, 786 nodes/747 edges, **$0 / no key / nothing external**; committed to `vault/graphify-out/`. `parseGraphifyGraph` surfaces `community`/`communityName`; tab shows concepts on hover/search/meta. 19 unit tests. | ✅ done | #192, #194 |
| **M3** (web) · Vault picker + d3-force graph map | MemoryPanel upgrade — vault picker at top + `VaultGraph.tsx` interactive force-directed map (folder-clustered Okabe–Ito palette, zoom/pan/search/hover-highlight/click-to-open, d3-force). | ✅ done | #192 |
| **M+** (web) · Zero-auth vault-graph preview | `web/public/vault-graph.html` — a self-contained, dependency-free render of the enriched Graphify graph (785 in-vault nodes / 108 named communities), reachable at `app.7ei.ai/vault-graph.html` **without login or `GITHUB_VAULT_TOKEN`** (the Memory-tab graph stays token-gated). `build-vault-graph-preview.mjs` precomputes the d3-force layout at build time and bakes positioned nodes/edges inline (no CDN/d3 at runtime → survives any CSP, works offline). Normalization mirrors `parseGraphifyGraph`; Okabe–Ito colorblind-safe, label-first; served public because `middleware.ts` excludes `.html`. | ✅ done | #196 |
| **B2** (web) · Cockpit voice panel | `web/app/dashboard/cockpit/VoiceSection.tsx` — push-to-talk default + "Arturita" wake-word opt-in (S5); browser Web Speech capture with an always-present typed fallback; routes the recognized command through `POST …/arturita/voice` (B1 endpoint + B3 ask/execute); spoken reply via the configured mode (provider TTS bytes → else local `SpeechSynthesis`); approval-aware action feed (destructive execute flagged ⛔ needs-approval) + live approvals rendered inline with the tri-state controls; colorblind-safe (icon+text+shape, red never lone CTA), design tokens only. Pure decision logic (`voicePanel.logic.ts`) + **15 web tests** via the Node 22 built-in runner (zero new deps). Live raw-audio STT engine = go-live. | ✅ done | #197 |

## Paperclip gap-bridge — 5 / 5 phases shipped
| Epic | Phase | Status |
|---|---|---|
| MCA-47 | 1 · Execution core (atomic checkout, run telemetry, deps, overspend) | ✅ Done |
| MCA-52 | 2 · Adapters (http executor, presets) + `7ei-mc` CLI | ✅ Done |
| MCA-56 | 3 · Attachments/work-products, ticket timeline, workspace preview URLs | ✅ Done |
| MCA-60 | 4 · Execution policies + rollback, permissions, run-tokens, plugin jobs | ✅ Done |
| MCA-65 | 5 · Orchestration evals, PWA, self-host Docker | ✅ Done |

## UI (epic MCA-69)
| Story | Status |
|---|---|
| MCA-71 · Task detail drawer (timeline/attachments/runs/comments) | ✅ Done |
| MCA-72 · Governance panel (policies, permissions, revisions/rollback) | ✅ Done |
| MCA-70 · Design tokens + shared primitives | ✅ Done |
| MCA-73 · Accessibility + responsive/mobile | ✅ Done |

**MCA-69 (UI epic) — complete.**

## Go-live (runbook: `GO-LIVE.md`, PR #147)
Engineering done: adapter now pulls `MC_LLM_API_KEY` from the encrypted secret store at boot (no plaintext key on disk);
`adapters/mac-mini/setup.sh` one-command installer + launchd keep-alive; `GO-LIVE.md` documents each step.

Remaining **user-only** console actions (assistant can't create accounts / enter credentials / rotate tokens):
1. Clerk **production** instance + `pk_live`/`sk_live` on Vercel.
2. Google consent-screen sensitive scopes (Gmail/Calendar) + add test user.
3. Rotate NVIDIA key → set as `MC_LLM_API_KEY` secret; rotate vault GitHub PAT.
4. Run `adapters/mac-mini/setup.sh` on the Mac mini; unload the laptop's launchd service.

## Shared-memory bus (vault proposal R1 + R3)
- **MCA-76** (2026-07-02): weekly consolidation routine (Sun ≥04:00 UTC) — losslessly archives >7-day session blocks from each agent's `recent.md` to `archive-recent.md` (prune-then-archive with failure guard), then creates a review task for the org's orchestrator with a consolidation report (promotion per Memory-Protocol via gated memory writes). New `services/consolidation.ts` + 14 tests (448 total).
- **MCA-75** (2026-07-02): `POST /api/agent/memory/session-summary` (namespaced append to `Memory/agents/<slug>/recent.md`, same capability/policy gates as memory writes); `buildSystemPrompt()` now injects org + agent long-term vault memory (TTL-cached, truncated, non-critical on failure); nightly scheduler job exports each agent's DB memory KVs to `Memory/agents/<slug>/kv.md`. New `services/agent-memory.ts` + 19 tests (434 total).

## UI
- **P2 org chart canvas** (2026-07-14): the Org surface is now an interactive pan-and-zoom org chart instead of the MCA-80 indented text list. Studied the live Paperclip org page (`/EIA/org`) by pulling its SPA bundle: it uses **no charting library at all** — a hand-rolled viewport (`data-testid="org-chart-viewport"`) with its own pointer/wheel/pinch handlers, two stacked layers sharing one `translate(pan) scale(zoom)` transform (an `<svg>` of orthogonal elbow paths under an absolutely-positioned card layer), fixed 200×100 cards, and a drag threshold that suppresses the click after a pan. We reproduced that approach rather than adding React Flow (~500 kB for a static tree we already model) — **zero new dependencies**. New pure helper `web/lib/orgLayout.ts`: `buildOrgTree` (flat roster → tree; the web mirror of `services/orgchart.ts`, same loop-guard — a `reports_to` cycle, a self-reference, or a manager outside the set promotes the agent to a root so no agent is lost and no input can hang the renderer), `layoutOrgTree` (tidy top-down: leaves packed left-to-right, each manager centred over its subtree span, one elbow edge per reporting line), `fitToView` (centres + scales the tree, never magnifies past 1×) and `zoomAbout` (the canvas point under the cursor/centre stays put), zoom clamped 0.2–2. `OrgChart.tsx` is now just wiring: cards render the shared `AgentAvatar` (uploaded picture → emoji fallback), name, title/role, the runtime/model line and a 2-line job description, with a status dot that carries **both** colour and glyph plus an SR-only status label (colorblind rule 3 — never colour alone). Drag to pan, wheel/pinch or the +/−/⤢ controls to zoom, click a card to open that agent's Configuration page; glass/token styling only, responsive, empty-state when the org has no agents. **Import/Export company** (top-left) wire to the existing MCA-PC D3 portability endpoints — Export downloads the secret-scrubbed bundle as JSON; Import is **fully wired** (not stubbed) and, per those endpoints' existing semantics, remaps the bundle into a **new** organisation rather than overwriting the current one. `GET /api/orgs/:orgId/orgchart` now also returns the flat `agents` roster (plus `avatarUrl`/`llmModel`/`jobDescription`) so the canvas derives and lays out its own tree through the tested helper; the pre-nested `tree` stays for existing callers. Backend **1,073 tests**, web **181 tests** (+19 layout/geometry), 11/11 evals, web build green.
- **W5 ask-mode** (2026-07-07, epic MCA-83 story W5 — Work surface, **closes epic MCA-83 W**): a task now carries a `work_mode` — `execute` (the full loop: workspace checkout, RAG/Drive/memory context, delegation + synthesis, tool side-effects) or `ask`, a question rather than a work order. An ask is a **single LLM turn**: no workspace/checkout, no RAG/Drive/goal context, no `[REMEMBER]`/`[WEBHOOK]`/`[DELEGATE]` machinery — the answer is posted straight to the ticket thread (agent-authored `answer` comment) and mirrored to `task.output`. New pure helper `services/askmode.ts` (`normalizeWorkMode`/`isAskMode` — anything unknown = execute; `buildAskSystemPrompt` — a lean identity + org + memory prompt that deliberately omits the execute-loop directives so it never advertises tools an ask can't run, and is honest about the one-reply constraint; `ASK_ANSWER_KIND`). Executor: `executeAgentTask` routes an ask task to the new **`answerAskTask`** lean path (reuses the governance/scoped-budget/preflight guards above it + the daily-budget/concurrency slot, skips all the workspace setup); **W3 wake-on-comment re-enters through the same path**, so an answered ask becomes a thread you can keep asking (each follow-up = another single-turn answer with the prior thread as context). Idempotent `ALTER TABLE tasks ADD COLUMN work_mode` (default `execute`); task-create accepts `workMode`; new ergonomic **`POST /api/agents/:agentId/ask`** (Clerk-secured) creates an ask task + fires the run, returns `{ taskId }`. Web: Cockpit **Agent fleet** cards gain a 💬 Ask affordance (inline composer, Enter/Escape, opens the answer in the task drawer); TaskDrawer badges an ask ticket (💬 Ask, accent pill) so the operator reads the thread, not "output". Backend **588 tests** (+8 askmode helpers), 11/11 evals, web build green. **Epic MCA-83 (Work surface / failure UX) complete.**
- **W4 task watchdogs** (2026-07-07, epic MCA-83 story W4 — Work surface, Paperclip gap G2): declarative checks an operator attaches to a task; the scheduler evaluates them each tick and posts an in-thread notice when one flips state — "stop babysitting long runs." New `services/watchdogs.ts` pure helpers (`parseWatchdogSpec` validate/normalize · `describeWatchdog` label · `evaluateWatchdog` check→{triggered,message} · `watchdogTransition` edge-trigger decision) + a DB sweep `runWatchdogSweep()` (mirrors `runHeartbeatSweep`), wired into the 1-min scheduler tick after the heartbeat sweep. Four **kinds**: `runtime` (a *running* run older than N min — the flagship), `cost` (task cost over $X), `no_activity` (task idle over N min, never on a `done` task), `status` (task reaches a watched status: blocked/failed/done/in_progress). **Edge-triggered** by design: `task_watchdogs.state` (ok|triggered) flips and a `system_notice` comment fires only on the transition (⚠ triggered / ✓ cleared) — a task sitting over-threshold for an hour posts *once*, not sixty times (the deliberate correction to the competitor teardown's activity-log spam). The notice is an ordinary `system_notice`, so it renders in the thread and feeds the W1 recovery card's evidence for free. The `no_activity` activity signal excludes `system_notice` comments (incl. the watchdog's own) so it can't flap triggered→cleared on its own post. New `task_watchdogs` table (idempotent CREATE + task/enabled indexes); routes on the Clerk-secured task surface: `GET/POST /api/tasks/:taskId/watchdogs`, `PATCH/DELETE /api/watchdogs/:id`. Web: TaskDrawer gains a **Watchdogs** section (after Runs) — existing checks with ✓/⚠ state icons (triggered = amber + text, colorblind-safe, never color-only) + remove, and an add row (kind Select → threshold input with unit hint, or a status Select). Backend **580 tests** (+10 watchdog helpers), 11/11 evals, web build green. **NEXT: W5 ask-mode (closes epic MCA-83 W).**
- **D2 llms.txt + `7ei-mc onboard`** (2026-07-07, epic MCA-85 story D2 — DX, closes epic D): the two agent-facing on-ramps. **`llms.txt`** (per [llmstxt.org](https://llmstxt.org)) — a concise install/context doc for AI agents/tools: H1 title + blockquote summary, then quick-start (install `@7ei/mc`, `onboard`, drive the agent), the API section pointing at D1's `/api/openapi.json` as the machine-readable source of truth, the token-authed agent-API verbs, and doc links. New pure builder `services/llms-txt.ts` `buildLlmsTxt({ apiUrl, appUrl })` (interpolates the live `PUBLIC_URL` into absolute links, trailing-slash-safe); served public as `text/markdown` at **`GET /llms.txt`** on the API host, with a static mirror at `web/public/llms.txt` so it also resolves at the web/apex domain (7ei.ai/llms.txt on Vercel). **`npx @7ei/mc onboard`** — wraps the org + external-agent + token mint into one guided flow: `--org-name` creates a new org (auto-hires Arturito) or `--org <id>` targets an existing one, then mints an external agent (`--name/--role/--runtime/--provider/--model`) and prints its `MC_AGENT_TOKEN` **once** plus the `export` lines. Both mint endpoints are Clerk-secured, so onboard authenticates with the operator's Clerk session JWT (`MC_CLERK_TOKEN`), not an agent token; `--dry-run` prints the planned requests without calling the backend. New pure planner `cli/onboard.mjs` (`parseFlags`/`parseOnboard`/`orgRequest`/`agentRequest`) + runner in `mc.mjs`. Backend **570 tests** (+4 llms.txt), CLI **7 tests** (+5 onboard), 11/11 evals, web build green. **Epic MCA-85 (DX) complete.** NEXT: W4 task watchdogs, W5 ask-mode.
- **Per-org webhook receivers — shared-secret verification** (2026-07-07, follow-up to the auth-scoping hardening / MCA-85 D1): the two public per-org inbound receivers — `POST /api/telegram/webhook/:orgId` (`comms.ts`) and `POST /api/jira/webhook/:orgId` (`jira-webhook.ts`) — had **no signature check**, so anyone who guessed the URL could POST forged Telegram/Jira events for any org (fake messages, spoofed issue-status syncs that flip local task state). The auth-scoping pass had correctly kept these public (external services carry no Clerk session) but left the residual open. New pure helper `services/webhook-auth.ts`: `deriveWebhookSecret(serverSecret, channel, orgId)` = `HMAC-SHA256(serverSecret, "channel:orgId")` — deterministic (no per-org storage) and bound to both org **and** channel (no cross-tenant/cross-channel replay); `verifyWebhookSecret()` constant-time compares (never throws on length/format mismatch); `checkWebhook()` returns authorized/enforced. **Telegram**: `register` now passes the derived token as Telegram's `secret_token`, which Telegram echoes back in `x-telegram-bot-api-secret-token` on every update; the receiver verifies it. **Jira** (Cloud can't set a custom header): `webhook-url` mints the URL with `?secret=<derived>`; the receiver checks the query param (or an `x-webhook-secret` header). Gated on `WEBHOOK_SIGNING_SECRET` (falls back to legacy `TELEGRAM_WEBHOOK_SECRET`/`JIRA_WEBHOOK_SECRET`): set → enforced (403 on missing/forged/cross-org token, before any DB work); unset → receivers stay open (dev parity with the global Telegram webhook). **Re-register both integrations after setting the secret.** New `webhook-auth.test.ts` (11 tests: pure-helper determinism/replay + receiver 403/200 integration inject for both channels). Backend **566 tests** (+11), 11/11 evals, web build green.
- **Auth-scoping hardening — org/agent route groups moved behind Clerk** (2026-07-07, follow-up to MCA-85 D1): D1's `/api/openapi.json` made every route's auth scope visible and surfaced that several tenant-scoped groups had been registered in the **public** scope with no Clerk session — a real data-exposure/abuse hole. Audited the full route table and moved the affected groups into the Clerk-secured scope in `index.ts`: **jira** (`/orgs/:orgId/jira/*`), **gmail/comms** (inbox, gmail threads/send, telegram + meet send), **notifications** (`/orgs/:orgId/notifications` + push register — closes a push-token-hijack vector), **memory** (`/agents/:agentId/memory` read/write), **outbound webhooks** (`/orgs/:orgId/webhooks` — was an unauthenticated SSRF/exfil surface), **usage** (`/orgs/:orgId/usage`,`/limits`), and **skills** (read + write/sync — anon skill injection, which agents execute; the web app already sent a token and `docs/API.md` already marked it auth-required). Two files that mixed an external **webhook receiver** with org-scoped reads were split so only the receiver stays public: `comms.ts` → `commsWebhookRoutes` (Telegram receiver); `jira-webhook.ts` → `jiraEventRoutes` (secured events/live/webhook-url) vs. the public Jira receiver. Genuinely-public surfaces unchanged: inbound webhook receivers, the Google OAuth handshake, the static model catalogue, `agent-api` (its own token), routine URL-token trigger, `openapi`/`health`. **No client breaks** — every web callsite already passed a Clerk token (verified). New **lock-down test** (`auth-scoping.test.ts`): boots the app like `index.ts` and fails if any `:orgId`/`:agentId` route is public outside a short, self-verifying allowlist (webhook receivers + OAuth), and asserts the secured scope actually returns 401; the openapi integration test now asserts the moved routes carry `clerkAuth` and the receivers stay open. Backend **557 tests** (+2), 11/11 evals, web build green.
- **D1 self-describing API — `/api/openapi.json` + `7ei-mc openapi`** (2026-07-07, epic MCA-85 story D1 — DX, Paperclip gap G6): the API now describes itself, so agents/tools self-configure instead of drifting against a hand-written doc. New `services/openapi.ts` pure helpers — `buildOpenApiSpec()` assembles a valid, deterministic **OpenAPI 3.1** document; `zodToOpenApiSchema()` converts the constructs our validators use (object/string+min/max/email, number/int, boolean, enum, literal, array, record, nullable/optional/default, union-of-literals→enum; unknown → accept-anything, never throws); plus `openApiPath`/`pathParameters`/`tagForPath`/`defaultSummary`. **The route table is collected live from Fastify's `onRoute` hook** (in `index.ts`), so every registered path is in the spec — it *cannot* drift the way `docs/API.md` does. Auth is tagged by three composable hooks: a baseline root hook (`none`) that fires for all routes, plus scoped hooks in the secured scope (`clerk`) and a new wrapper scope around the agent API (`agentToken`); rank makes it order-independent, auto HEAD/OPTIONS filtered. Request bodies + summaries come from the **actual Zod validators** via `documentEndpoint()` — routes push their schema into the service (routes→services is the allowed import direction; a service must not import routes), wired across the whole agent-facing surface (`documentAgentApi()` in `agent-api.ts`). New public `GET /api/openapi.json` (served from the live route table + docs; version + `PUBLIC_URL` server). CLI: `7ei-mc openapi` fetches the spec token-free (`mc.mjs` now only requires `MC_AGENT_TOKEN` for non-public commands). Live spec: **158 paths, 26 tag groups**. `docs/API.md` header now points to the endpoint as the machine-readable source of truth. Backend **555 tests** (+15: 14 helper/wiring + 1 end-to-end inject), CLI test +1, web build green. **NEXT per plan order: D2** (llms.txt + `npx 7ei-mc onboard`); then W4 watchdogs, W5 ask-mode.
- **V3 per-wake preflight cap + cheap-model config validation** (2026-07-07, epic MCA-84 story V3 — Visibility, closes epic V's cheap-wins tier / Paperclip gap G9): two small levers on the existing budget/cost code. **Per-wake preflight cap** — before an internal wake runs the LLM, bound its *worst-case* cost (full input context at the input rate + a full `max_tokens` completion at the output rate) and skip the wake if it would exceed a configured per-wake ceiling; the task is parked `blocked`/`needs_attention` with a plain-language reason and a durable `system_notice` comment (feeds the W1 recovery card). Distinct from the cumulative scoped budgets in `budget.ts` (those stop an agent once *total* spend crosses a limit) — this stops a *single* wake from being surprisingly expensive (runaway context, an accidental Opus swap). Runs before any expensive setup (RAG/Drive/memory fetches) so a capped wake costs nothing. Opt-in via `deployConfig.maxCostPerWakeUsd` (org-wide) with an optional `maxCostPerWakeUsd:<agentId>` override; null/≤0 = no cap. **Cheap-model config validation** — new `services/preflight.ts` pure helpers (`estimateWakeCost`/`preflightWake`/`parseCapUsd`/`validateModelConfig`/`validateRoster`) audit each agent's model: unpriced models (no `COST_RATES` entry — spend can't be tracked or capped) and models whose output rate exceeds the "cheap" threshold ($5/M output — passes Haiku/GPT-4o-mini/Flash/DeepSeek/Qwen-turbo/local, flags Sonnet/Opus/GPT-4o) are surfaced with reasons. Unpriced models under a cap are *allowed but flagged* `unbounded` (never silently block a working custom/local runtime). New `GET /api/orgs/:orgId/preflight` (cap + per-agent validation rows + warn count) and `PUT` (set/clear the cap in deployConfig). Web: `cockpit/PreflightSection.tsx` after Budgets — editable per-wake cap (accent CTA, never red) + per-agent audit rows (✓/⚠ iconed + amber, colorblind-safe) with est-max-wake-cost and flagged issues. Backend **540 tests** (+19), web build green.
- **V2 tri-state approvals + inbox retry rows + read receipts** (2026-07-07, epic MCA-84 story V2 — Visibility): closes the three Paperclip failure-UX gaps that live in the inbox and board. **Tri-state approvals** — approvals were binary (approve|reject); added `revision_requested` + a reviewer note (the revision loop). New `services/approvals.ts` pure helper `decideApproval()` validates the tri-state and computes the record patch (`revision_requested` *requires* a note — the loop is pointless without telling the requester what to change; approve/reject take an optional trimmed note). New `approval_requests.decision_note` column (idempotent ALTER). `/api/approvals/:id/decide` now accepts `{ decision, note }` for all three states. InboxSection gains an inline "↩ Request changes" composer (Enter to send, Esc to cancel) between ✓ Approve and ✕ Reject — every action iconed, red never the lone CTA (colorblind rule). **Inbox retry rows** — `buildInbox` marks failed tasks `retryable` and carries their `output` as a truncated inline `error`; the executor catch now persists the failure text as `output` (external result path already did) so the row shows *why* it failed with no extra fetch; InboxSection failed rows render the red error line + a "↻ Retry" button that re-executes via the existing `POST /api/tasks/:id/execute`. **Board read receipts** — new `services/receipts.ts` pure helpers (`taskActivityAt` = max(createdAt, completedAt); `unreadTaskIds` — unread when activity is newer than the operator's receipt, or never opened); new `task_reads` table + `POST /api/tasks/:id/read` (one row per user/task, bumped on open); `/cockpit` tags each task `unread` for the caller; cockpit TaskBoard cards are now click-to-open (wired to the page drawer) and unread cards carry a "● new" chip (accent + text, never color-only), cleared optimistically + durably on open. Backend **521 tests** (+15: approvals 6, receipts 8→ inbox 3), web build green.
- **W3 task thread + wake-on-comment** (2026-07-07, epic MCA-83 story W3 — Work surface): commenting on an idle ticket picks the work back up (Paperclip parity — the async office loop, closed). New `services/thread.ts` pure helpers (`decideWake` — a *user* comment on a task that has an agent and no in-flight run wakes it; `isWakeableStatus` — pending/assigned/blocked/failed/done, `done` reopens the ticket, `in_progress` deliberately excluded since an internal agent mid-run writes no run row so `activeRun` can't prove it and a wake would double-fire; a stalled in_progress is normalized to pending by the heartbeat orphan sweep; `hasActiveRun`; `threadHistory` — projects the prior thread into the executor's `conversationHistory`, agent→assistant / user+system_notice→user, oldest-first, capped+truncated; `buildWakeInput` — frames the comment as a follow-up). No new store: the thread IS `task_comments`, the wake reuses `executeAgentTask` (which owns every task/run transition — internal re-runs the LLM loop, external re-notifies). `POST /api/tasks/:id/comments` now decides + fires the wake (posts a durable "Agent woken" system-notice, replays the thread), returns `{ comment, woke, wakeReason }` (optional `wake` body flag forces/suppresses); `GET …/comments` enriches agent authors with name+emoji so the thread reads as a conversation. Web TaskDrawer: composer promises the wake up front (`Comment & wake` button + hint) when the task is wakeable, shows an accent-tinted "agent woken — re-running…" confirmation (colorblind-safe: ↩ glyph + accent, never color-only) and reloads to surface the new run, renders agent comments as `emoji name`. Backend **506 tests** (+10), web build green.
- **V1 heartbeat 24h timeline** (2026-07-07, epic MCA-84 story V1 — Visibility): per-agent lanes of activity blocks over the last day — the office's pulse (who worked, when, on what). New `services/timeline.ts` pure helpers (`runActivity`/`taskActivity`/`mergeActivity` normalize the two activity sources, `buildHeartbeatTimeline` projects them onto a 24h window as start/width percentages). No new store: external agents write `agent_runs` (authoritative start/end per session), internal agents record `durationMs`/`completedAt` on the task itself — `mergeActivity()` reconciles so each wake shows once (runs win; a task with runs contributes no task-derived block). New `GET /api/orgs/:orgId/timeline` (last-24h runs ∪ ongoing runs, recent tasks → merged → lanes; idle non-terminated agents included so you see who is *not* working). Web: `cockpit/TimelineSection.tsx` — one lane per agent (emoji/name + heartbeat status icon + run-count/active-time/spend), a track with axis ticks (24h→now) + gridlines, blocks coloured on the status table, ongoing runs striped (a shape cue, not colour), a persistent colour→status legend; wired into CockpitPanel after Agent fleet, loads with the cockpit payload + skeleton. Colorblind-safe: every block carries a text tooltip/aria-label and the legend keys colour→status; `orphaned` aliased → failed in `status.ts`. Backend **496 tests** (+14), web build green.
- **T3 hex sweep + contrast audit** (2026-07-06, epic MCA-82 story T3): completes the theme migration. Verified no raw hex remains in any dashboard component (T1 already re-pointed cockpit `shared.tsx`/`status.ts` domain colors at the semantic/purple CSS vars); dropped the dead `--7ei-*` raw-hex brand aliases from `globals.css` to hold the "raw hex only in `tokens.ts`" invariant. **WCAG AA contrast audit both modes** (scripted): all text/surface pairs pass AA (≥4.5:1) on page `--s0` and card `--s1`; fixed `--muted` which was failing for hints/labels/placeholders/idle status — light `#8a8a88`→`#6b6b6b` (3.5→5.3:1), dark `#555555`→`#7e7e7e` (2.6→4.7:1); documented `--accent` on dark as a kept AA-large brand item (3:1 non-text rule, never body copy). **PWA `theme-color` now theme-aware**: the pre-paint script + ThemeProvider point the browser `theme-color` meta at the resolved page surface (`--s0`: light `#f5f5f3` / dark `#070707`) so the address/title bar matches the app in both modes incl. explicit toggle override (no flash); new `themeSurface()` single-source helper; manifest `theme_color`/`background_color` corrected `#0a0a0a`→`#070707` (matched no token). Web-only; build green, backend 482 tests untouched. **Epic MCA-82 (theme) complete: T1→T2→T3.**
- **W2 work surface** (2026-07-06, epic MCA-83 story W2): reasoned blocker chips + next-up + sub-task cost rollups. New `services/worksurface.ts` pure helpers (`nextUp`/`readyQueue` — the single task the office should pick up next: unstarted, every upstream blocker `done`, highest priority then oldest; a blocker id absent from the set counts as still blocking, so readiness is never over-claimed; `rollupCost` — parent's own spend + sub-tasks' spend/tokens). `buildRecovery()` gains a `blockerTasks` input and emits reasoned `blockers` chips (upstream title + status, open-first) so the recovery card shows *why* a task is blocked, not just a count. Routes: `/recovery` resolves `blockedBy` ids → chips; `/subtasks` now returns `{ subtasks, rollup }`; `/cockpit` returns `nextUp`. Web: RecoveryCard renders blocker chips (iconed + status-coloured, colorblind-safe); TaskDrawer shows a sub-task cost roll-up row + header "incl. subtasks" total; TaskBoard gains a "Next up" banner (accent-tinted, ▶ glyph — never color-only) and flags the head card in To-do. Backend **482 tests** (+12), web build green.
- **W1 recovery cards** (2026-07-06, epic MCA-83 story W1): failure/recovery UX (the Paperclip gap). New `services/recovery.ts` pure helper `buildRecovery()` derives a structured recovery card from existing state (task status + run history + system-notice comments) — no new failure store. `GET /api/tasks/:taskId/recovery` returns `{ owner, sourceRun, evidence, nextAction, since, reason }` (or null), enriched with the owner agent's name. Failures now post a durable `system_notice` comment to the ticket thread from both execution paths (in-process executor catch + external-adapter result endpoint); new idempotent `task_comments.kind` column (user|agent|system_notice). Web: `RecoveryCard.tsx` — red LEFT border + ⚠ heading (prominence by placement, retry button is purple not red per colorblind rule 4), structured owner/source-run/evidence/next-action rows, "open · Nm ago" until decision; rendered at the top of `TaskDrawer`, with a Retry-run action (re-executes then reloads so the card self-clears) and Add-a-note. System-notice comments render iconed/tinted in the thread; TaskBoard flags failed/blocked cards with ⚠. `TextInput` primitive now forwards refs. Backend **470 tests** (+10), web build green.
- **T2 glass chrome** (2026-07-06, epic MCA-82 story T2): controlled glassmorphism on chrome only (DESIGN_SYSTEM v2) — sidebar, TaskDrawer, all cockpit modals (shared `Modal`), and a new ⌘K command palette all frosted via a single `.mc-glass` utility (translucent `--glass` fill + 16px blur, solid `--s1` `@supports` fallback; never on content cards/rows). New `CommandPalette.tsx` shell: keyboard-first (⌘/Ctrl-K toggle, ↑↓/↵/esc), grouped Navigate + Theme commands, colorblind-safe selection (accent fill + ring, not color-only); designed for Epic V to extend by concatenating Commands. Sidebar gains a `Search… ⌘K` launcher (desktop-only). Tokenized `--shadow-modal`/`--shadow-drawer` replace the baked-in dark drop-shadow so chrome reads right in both themes. Web-only; build green, backend 460 tests untouched.
- **MCA-86** (2026-07-06, epic MCA-82 story T1): 7Ei theme foundation — light+dark via CSS variables (`tokens.ts` v2 theme map, single source of raw hex), ThemeProvider + three-state toggle (system/light/dark, pre-paint script, no flash), colorblind-safe status helpers (⬡ active purple, ✓ done, ⏸ paused, ⛔/✕ red always iconed) applied to KPI strip/fleet/task statuses, primary buttons now purple / danger = red outline, official 7-hexagon mark top-left (currentColor, both modes). Glass chrome = T2.
- **MCA-81** (2026-07-06, epic MCA-78 story 3 — EPIC COMPLETE): KPI strip (dense variant A: agents health/tasks/spend/approvals), loading skeletons across panels, consolidated Google card (one grant, per-service rows + toggles), gear settings on connector cards (Google: switch-account via `?switch=1` re-consent, calendarId, Drive scope; Vault: repo/root/branch + replace-token). Backend: `GOOGLE_CONNECTOR_CONFIG` config-as-secret + GET/PUT config endpoints; config honored in gmail/calendar/drive routes (default-open). 460 tests. Note: bound-account email shows '—' until callback stores userinfo (follow-up); agent-executor drive search doesn't honor driveScope yet (flagged).
- **MCA-80** (2026-07-03, epic MCA-78 story 2): CockpitPanel 808→129-line composition root + 13 section components under `dashboard/cockpit/`; Cockpit + TaskDrawer + page.tsx migrated to the shared api client, primitives, and 28px density; a11y additions (aria on icon actions/toggles, progressbar semantics); fixed the undefined `s.tag` style bug.
- **MCA-79** (2026-07-03, epic MCA-78 story 1): UI foundation — tokens gain density/text/space scales (28px rows/controls), shared primitives `dashboard/ui.tsx` (Button/Card/Pill/TextInput/DenseTable/SectionLabel), shared API client `web/lib/api.ts` (Clerk-token aware + MCA-77 error mapping); Connectors/Governance/Memory panels migrated. Cockpit split = story 2.
- **MCA-77** (2026-07-03): Connectors UX from design critique — inline Replace-token (GitHub/HF/vault; no disconnect needed for rotation), honest error mapping (network vs HTTP status + 401/403 hint), status pills (Connected/Failing/Untested) + last-tested time, truthful header count, design-token adoption in the panel.

## Security fixes
- **SEC-ORG** (2026-07-16): the org row stopped shipping its credentials. `GET /api/orgs` was `db.select().from(organisations)` — the whole row — so `telegramBotToken` and `deployConfig` (LLM API keys, some **plaintext**) reached every authenticated client (web, desktop, and the phone since MOB-1) and sat in client JS memory. Five surfaces fixed via one shared projection `toPublicOrg()` (`backend/src/services/org-public.ts`): `GET /api/orgs`, `GET /api/orgs/:orgId`, `POST /api/orgs` (it echoed back the plaintext `llmApiKey` the caller had just sent), `GET /api/users/:userId/orgs`, `GET /api/orgs/switch/list`. Allow-list, not deny-list — a new column is invisible to clients until classified, and `org-public.test.ts` fails until someone does. No client read either secret (mobile's `settings.ts` had flagged this as a backend follow-up), so nothing was dropped that anyone used; access control is unchanged (response shape only). 1370 tests + 11/11 evals green.

- **MOB-7d** (2026-07-17): **The agent detail becomes EDITABLE — owner-gated agent settings from the phone.** MOB-6b named this deferral (Config/Instructions/Skills = desk work); this is the write layer, additive to `apps/mobile/**`, **no backend route added, no gate weakened, no backend/web/desktop source touched.** The phone edits an agent over the **same owner-gated, validated routes the web's edit tabs use** — never the legacy unvalidated `PATCH /api/agents/:id`: **Identity & adapter** (`PUT …/config` — name/role/title/description/icon/email/reports-to/runtime), **Model profile** (`PUT …/model-profile` — primary + cheap model + reasoning effort; **confirm** before save, spend+capability), **Trust tier** (`PUT …/trust` — standard ⇄ low_trust_review; **confirm**, containment), **Skills** (`GET`/`PUT …/skills` — the whole selection, optimistic with rollback). **Owner-only is the backend's rule, and the phone mirrors it, never invents it:** every route is `requireOrgRole('owner')`, so the phone learns the caller's role from `/api/orgs` (`memberRole`, now threaded through the session/auth as `orgRole`) — a known **member** gets the read-only screen the desk shows them, an **owner** gets Edit, and a caller whose role is genuinely **unknown** (a pasted token) is offered Edit *with a caution* while the backend 403 stays the real enforcer. No gate is loosened: the phone at most declines to offer what the server would refuse. **Client validation mirrors the server message-for-message** (`agentEdit.ts` re-implements `validateConfigPatch`'s caps/wording + `wouldCycle`), so a caught error reads identically and the round-trip is saved — but the server is the final validator. **A failed save never loses edits:** the form stays, the 400/403/409/network error is named, and the server's response (not an optimistic guess) replaces on-screen state. **⚠️ Flagged for the auditor (finding, not shipped):** per-agent **permissions** editing is deferred *because* its route (`PATCH /api/agents/:id/permissions`, and the legacy `PATCH /api/agents/:id`) is **member-gated and unvalidated** server-side — shipping an owner-gate there from the phone would be a **client-only** gate over a server that lets any member write. The enforcement hole is pre-existing and backend-side. Also deferred (named in-code): the instructions markdown bundle, avatar *photo* upload (the icon emoji **is** editable), and the trust *boundary* multiselect. **Tripwires** (`agentEdit.test.ts`, +26): import the dep-free real sources and assert agreement — `backend/src/services/agent-config.ts` (`CONFIG_FIELDS`/`RUNTIMES`/`wouldCycle`/`validateConfigPatch` verdicts+wording), `web/lib/agentSkills.ts` (toggle logic), `web/lib/trust.ts` (`TRUST_MODES`); `REASONING_EFFORTS` isn't import-pinnable (its source imports the model catalogue) so it's asserted against the literal + documented as a copy. Verified: mobile **254/254** (+26) · typecheck clean · `expo export` bundles (1290 modules) · **react/react-dom still exactly 19.1.0**, **SDK 54 untouched**, **boot-safe** (`bootSafety.test.ts` green — only RN-core `Alert`/`Modal`/`Switch`/`TextInput`, no native module). `mob-7d-agent-settings-edit`.
- **MOB-7c** (2026-07-17): **Agent pictures on the phone + the Activity feed reads as a log.** Two display mirrors, **zero backend change**, additive to `apps/mobile/**`. **(1) Avatars.** The web shows an agent's uploaded picture via one `AgentAvatar` (`web/app/dashboard/agent/shared.tsx`): the field is `avatarUrl` — **not an asset URL but a base64 data URI** the backend stores in `agents.avatar_url` (`services/agent-avatar.ts`, 256 KB cap, PNG/JPEG/WebP/GIF), so it rides the existing JSON with **no image endpoint and no auth header**; absent → `avatarEmoji || '🤖'`. The phone now mirrors it in the three places the web renders a real picture **and** the endpoint carries the field: the **roster** (`…/agents`, `SELECT *`), the **agent detail** header (`/api/agents/:id`, whole row), and the **org tree** (`…/orgchart`, selects it). New `AgentAvatar.tsx` renders `<Image>` (RN core — no new dep; already used for the photo-attach thumbnail) with an **`onError` fallback to the emoji** (the web's `<img>` would show a broken box; the phone shows a face), plus a **safety gate** (`avatar.ts` `isSafeAvatarValue`, a byte-for-byte mirror of the backend's read-path guard: only a base64 data URI of an allowed type reaches `<Image>`, so `javascript:` / remote-tracker / `data:image/svg+xml` can't render). **Kept emoji where the web keeps emoji** — Costs bars, Governance rows, and the Activity actor are `avatarEmoji`-only on the web (and the `…/timeline` lanes carry no `avatarUrl`), so mirroring the decision means *not* inventing a picture there. **(2) Activity.** MOB-6d already shipped the flattened log over `…/timeline` — which **is** what the web's "Activity" renders (its swimlane); `audit_logs` is **not** used, because the web doesn't render it (no `…/audit-log` consumer anywhere in `web/`, owner-gated, no-op writer). The MOB-7c delta: a pure `actionVerb(status, ongoing)` so each row reads as a log line — *"Dev **completed** …"*, *"Maya **is running** …"* — plus the shared-avatar actor frame. No `Timeline | Log` control: the phone deliberately has no swimlane (a 24h chart at 390 pt is a smudge), so a clean scrollable log list is the whole surface. **Tripwire:** `avatar.test.ts` imports the backend's real `isSafeAvatarValue`/`ALLOWED_AVATAR_TYPES` and asserts the phone's mirror agrees value-for-value (backend module is pure `Buffer`-only, so Mobile CI runs it). Verified: mobile **228/228** (+12) · typecheck clean · `expo export` bundles (1289 modules) · **react/react-dom still exactly 19.1.0**, **SDK 54 untouched**, boot-safe (`bootSafety.test.ts` green — `<Image>` is core, no native module added). `mob-7c-avatars-activity-log`.
- **MOB-7b** (2026-07-17): **Arturita can see photos.** The operator attaches a photo to a Command Center turn — 🖼 beside 📎 on **both** the phone (action sheet → photo library *or* camera, each with its own permission ask) and the web — and it reaches the model as a real **image content block**, not text. `POST …/arturita/converse` gains an optional `image: { name, mediaType, data }` (raw base64, no `data:` prefix); no extract round-trip, because pixels have no text to extract. `llm-router.ts`'s `LLMMessage.content` widened to `string | LLMContentPart[]` with a mapper per provider (Anthropic `source` · OpenAI `image_url` · Gemini `inline_data`), **each short-circuiting on a string** so every existing text turn builds a byte-identical request. **The investigation mattered more than the build:** `DEFAULT_LLM_CHAIN` is free-first — local `llama3.2:3b`, `qwen3:8b`, `groq/llama-3.3-70b`, all **text-only** — ahead of the vision-capable `gemini-2.5-flash` and the guaranteed `claude-sonnet-4`. On Fly it works only *by accident* (the Ollama hops fail, so the turn falls through to Claude); on a self-hosted backend with Ollama up, hop 1 would have answered *about an image it never received*. So an image turn is pruned to sighted hops (`visionChain`), `supportsVision` is an **allowlist that fails closed**, and a fully-blind chain **tells the operator** (`code: 'no_vision_model'`, naming the fixes) instead of guessing. Cap is 3.75 MB — not round: base64 inflates 4/3 and the provider caps the *encoded* image at 5 MB. HEIC refused by name with the fix (the phone's picker re-encodes to JPEG at `quality: 0.7`, so it never produces one). **Nothing persisted, nothing logged — tested against the real captured log stream, not asserted in prose.** Mutation-proven: dropping the image, removing the prune, and a module-scope `expo-image-picker` import each fail the test written for them. Deferred: multiple images, vision via a `custom` provider, server-side HEIC transcode. 1405 tests + 11/11 evals + web 222 + mobile 221 green.

## Refactors
- **MCA-74** (2026-07-02): `routes/all.ts` (1,383 lines) split into domain modules (orgs/agents/tasks/projects/costs/skills/auth/credentials), `all.ts` kept as barrel; `sendPushNotification` + token map extracted to `services/push.ts` (fixes the routes→services inversion). No behavior change; 415 tests + 11/11 evals green.

## Standing items (carry forward)
Not blockers for the code — deferred console/ops actions and follow-through owed after the v2 epics:
- **Jira transitions pending** — MCA-82/83/84/85 stories are shipped in code but not transitioned in Jira. Atlassian Rovo OAuth is **unavailable in these build sessions**, so the assistant cannot move them; transition MCA + OS issues to Done in the next interactive/authorized session (cloudId `5dadc567-085a-4cd8-99a3-c0bd9886fee9`).
- **`WEBHOOK_SIGNING_SECRET` rollout** (PR #166) — set the secret via Fly/Cockpit to enforce per-org Telegram/Jira receiver verification. It's **open in dev until set**. After setting it, **re-register both integrations** (Telegram `secret_token`, Jira `?secret=` URL) so they carry the derived token — see the W-side note in `services/webhook-auth.ts`.
- **7ei.ai apex DNS for `llms.txt`** (PR #167) — `web/public/llms.txt` mirror resolves at the web/apex domain only once apex DNS points at Vercel; API host already serves `GET /llms.txt`.
- **Vault milestone mirror** — mirror the v2-epics-complete milestone into the Obsidian vault `07-Agents/` (repo `Arturito7ei/7Ei-MC_TARCO`, content under `vault/`), alongside `STATUS-Mission-Control-2026-07-02.md`.
- **Go-live user-only console actions** — unchanged, see `GO-LIVE.md` §1–4 (Clerk prod keys, Google sensitive scopes, NVIDIA/vault key rotation, Mac-mini adapter move).

## Verify
`cd backend && npm test` · `npm run evals` · `cd web && npm run build` · self-host: `docker compose up -d --build`.
