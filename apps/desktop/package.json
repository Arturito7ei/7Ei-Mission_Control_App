{
  "name": "@7ei/mission-control-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Epic H — Electron shell that boots the packaged/loopback Mission Control mesh (compiled Fastify backend + Next.js UI) on a local file DB as one app.",
  "main": "src/main.cjs",
  "author": "7Ei",
  "scripts": {
    "desktop": "node scripts/build-desktop.mjs --stage-only && electron .",
    "dist:mac": "node scripts/build-desktop.mjs && CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --mac --config electron-builder.yml",
    "pack:mac": "node scripts/build-desktop.mjs && CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --mac dir --config electron-builder.yml"
  },
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8",
    "esbuild": "^0.25.0"
  }
}
